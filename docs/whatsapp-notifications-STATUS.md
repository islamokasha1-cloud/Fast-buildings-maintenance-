# حالة مشروع إشعارات واتساب — اقرأني أولاً (Handoff)

> **الغرض:** ملف ذاكرة دائم عبر المحادثات. أي محادثة جديدة تقرأ هذا الملف أولاً لتعرف
> أين توقّفنا دون إعادة أي شيء. آخر تحديث: **2026-07-24**.
> التفاصيل الكاملة في `docs/whatsapp-notifications-plan.md` (خصوصاً القسمان §11 و§12).

---

## 1) الهدف
إرسال إشعارات واتساب في نظام صيانة/مشتريات المباني السريعة (SPA + Firebase):
- **إشعار الفني** بالبلاغ المُسنَد إليه (عند تغيّر حقل `tech` في مستند البلاغ).
- **إشعار المعنيين بطلبات الشراء**: طلب جديد، أو طلب ينتظر **اعتمادهم حسب صلاحياتهم**
  (توجيه حسب حالة `status` في `global_purchases` → الدور المطلوب).

## 2) قرارات معمارية ثابتة
- لا يوجد backend حالياً (كل شيء client-side + Firebase). واتساب يحتاج **طبقة خادمية**.
- التصميم: مشغّلات Firestore → طابور **`wa_outbox`** (منع تكرار + إعادة محاولة) → مُرسِل
  ينادي WhatsApp API. **محايد تجاه المزوّد** (تبديل المزوّد = تغيير نقطة الإرسال فقط).
- يجب إضافة حقول `phone` + `waOptIn` إلى `technicians` و`USERS` (غير موجودة حالياً).
- الأسرار في مكان آمن (Secret Manager/متغيّرات الخادم)، **لا في المتصفح**.

## 3) ✅ ما أُنجز
- خطة تصميم كاملة موثّقة في `docs/whatsapp-notifications-plan.md`.
- **توثيق حساب Meta for Developers** تم بنجاح.
- بيانات حافظة الأعمال جاهزة: **Fast Buildings** / إيميل `Projects@fast-buildings.com`.

## 4) ⛔ العائق الحاسم (سبب التوقّف)
- حساب Meta الشخصي **Islam Okasha** عليه **حظر مدفوعات دائم** (Ad account `667647593252622`).
- عند إنشاء Business Portfolio تظهر **«Payment is disabled»**. الرصيد **0.00** (ليست مشكلة سداد،
  بل **قيد سياسة** منذ 2018؛ رسالة دعم رسمية تقول لا يمكن إعادة تفعيله).
- **الدعم الرسمي أكّد نهائياً** (2026-07-24): لا مختصّ بشري، والقيد تديره **أنظمة آلية** ولا يُرفع.
- **كل الأصول تحت نفس البروفايل المحظور**، و**لا يتوفّر حساب فيسبوك بديل سليم**.
- ⇒ **المسار المباشر عبر Meta مغلق نهائياً. لا تُعِد محاولاته.**

## 5) ✅ القرار المعتمد: مسار BSP المُدار
اعتماد **BSP مُدار / Fully-Managed** يستضيف WhatsApp Business Account **تحت أعماله**،
فيتجاوز حظر حساب Meta. **تجنّب** مزوّدي **Embedded Signup** (يطلبون تسجيل دخولك بحسابك
المحظور فيفشلون).

**رسالة السؤال للمزوّدين (تُرسل كما هي):**
> «حساب Meta Business الخاص بي محظور مالياً نهائياً (مخالفة شروط قديمة، رفضت Meta إعادة
> تفعيله). هل يمكنكم استضافة WhatsApp Business Account **تحت أعمالكم** وتسجيل رقمي **دون
> الحاجة إلى Business Manager سليم من طرفي**؟ وما التكلفة؟»

**مزوّدون للمقارنة:** Gupshup · AiSensy · Interakt · DoubleTick · أو مزوّد محلي سعودي/مصري.
**التكلفة المرجعية:** رسوم Meta للرسالة (Utility ~$0.02–0.04) + رسوم المزوّد (تفصيل §5 بالخطة).

## 6) ما يجهّزه المستخدم
- [ ] خط جوال جديد مخصّص (نظيف، غير مربوط بأي واتساب).
- [ ] السجل التجاري للشركة (لتوثيق الأعمال لدى المزوّد).

## 7) ▶️ نقطة الاستئناف (ابدأ من هنا)
1. راسل ٢–٣ مزوّدي BSP بالرسالة في §5، وتأكّد أنهم **استضافة مُدارة** (لا Embedded Signup).
2. قارن الردود (استضافة تحت أعمالهم؟ + السعر) واختر الأنسب.
3. حين يتأكّد المزوّد + الرقم + السجل التجاري → **يبدأ التنفيذ التقني** حسب مراحل الخطة
   (نموذج البيانات → مشغّلات Firestore/طبقة الإرسال → حدث «إسناد الفني» أولاً → توجيه اعتماد الشراء).
