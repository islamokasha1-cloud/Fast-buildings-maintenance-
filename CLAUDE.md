# إرشادات المشروع لـ Claude

## أين يُكتب الكود الجديد (قاعدةٌ قبل أيّ تعديل)

**السؤالُ الحاسم: أهذا عملٌ كبيرٌ جديد أم إصلاحُ ما هو قائم؟** الملفُّ الجديد للأوّل
وحدَه. `index.html` بلغ ~٢٫٨ ميغابايت (أكثر من ٤٠ ألف سطر)، فما يُبطئ التحريرَ
والمراجعةَ ويضاعف تعارضاتِ الدمج هو **نموُّه بميزاتٍ جديدة** لا إصلاحُ سطرٍ فيه.

- **الكبيرُ الجديد ⇐ ملفُّ `.js` مستقل:** ميزةٌ أو شاشةٌ أو مجالُ عملٍ جديد — له اسمُه
  وحالتُه وشاشتُه. ولا يُكتب في `index.html` ولا في `tech-app.html` إطلاقاً.
- **الإصلاحُ والتعديلُ على منطقٍ قائم ⇐ في مكانه حيث هو،** ولو كان في `index.html`.
  **لا تنقل منطقاً إلى ملفٍّ جديدٍ بحجّة إصلاحه** — النقلُ يُخفي التغييرَ الحقيقيَّ داخل
  كتلةٍ منقولةٍ فتعجز المراجعةُ عن تمييزهما، ويُبطل حفظياتِ الكاش بلا داعٍ. الإصلاحُ
  في موضعه أوضحُ وأقلُّ خطراً. (وإن استحقّ منطقٌ قائمٌ النقلَ فعلاً، فهو **تغييرٌ
  مستقلٌّ بذاته** لا يُخلط بإصلاحٍ ولا بميزة.)
- **الحدُّ الفاصلُ عملياً:** كتلةٌ جديدةٌ قائمةٌ بنفسها (شاشةٌ · دورةُ اعتمادٍ · محرّكُ
  حسابٍ · تقريرٌ) ⇐ ملفُّ وحدة. أسطرٌ تُصلح سلوكاً قائماً أو تُضيف حقلاً أو زرّاً إلى
  شيءٍ موجود ⇐ مكانُها حيث ذلك الشيء. وعند الشكّ: **اسأل المالك**، لا تُنشئ ملفاً.
- **إن كان العملُ الكبيرُ في مجالِ وحدةٍ قائمة** (مشتريات · تعاقدات · مشاريع · نظافة ·
  موارد بشرية · جرد · أداء…) فمكانُه **تلك الوحدة** لا ملفٌّ جديد. الملفُّ الجديد
  لمجالٍ جديد وحدَه.
- **قالبُ الوحدة الجديدة** (كما في `photo-queue.js` · `hr-payments.js`):
  1. ترويسةٌ تشرح المشكلةَ والمبدأَ والقرار — لا وصفَ الكود.
  2. نمطُ IIFE يعرّض **كائناً واحداً** على `window`، ويقرأ خدماتِ النواة بالاسم
     المجرّد (`db` · `firebase` · `esc` · `toast` · فحوص الأدوار…).
  3. سطرُ `const MODULE_BUILD = "v18.9.0";` ثم `npm run stamp` يكتب الرقمَ الصحيح.
  4. وسمٌ في `index.html`: `<script src="اسم-الملف.js?v=18.9.0"></script>` — و`stamp`
     يختمه. **لا تكتب رقمَ إصدارٍ بيدك في أيٍّ من الموضعين.**
  5. **الدوالُّ الحسابية نقيّةٌ ومعروضةٌ على الكائن** ليفحصها `hail-tests.js` بلا متصفّح.
  6. سطرٌ للملفّ في جدول `NOTES.md §2` — ملفٌّ بلا سطرٍ فيه غيرُ موجود.

## سير العمل (Git / PR)

- **افتح Pull Request دائماً** بعد إتمام أي تعديل ودفعه — حتى لو لم يُطلب ذلك صراحةً.
  التسلسل الثابت: تطوير على فرع العمل ← commit ← push ← **فتح PR إلى `main`**.
- الفرع الأساسي: `main`. لا تدمج في `main` مباشرةً دون طلبٍ صريح — الدمج قرار المالك من الـ PR.
- عنوان الـ PR ووصفه بالعربية، مع تلخيص التغييرات والتحقق (نتائج الاختبارات) وخطوات ما بعد الدمج إن وُجدت.

## الاختبارات (إلزامية قبل الدفع)

- شغّل `node hail-tests.js` — يجب أن تنجح كلها.
- عند تعديل منطق المخزون/الحالة/الحسابات: أضِف حارس اختبار في `hail-tests.js` يمنع الارتداد.
- عند تعديل الواجهة/الوحدات: شغّل `node ui-walkthrough.mjs` و`node browser-scenarios.mjs` (يحتاجان Chromium).
- عند تعديل حسابات طلب الشراء أو مراحله أو عرضِ أرقامه: شغّل `node po-lifecycle-check.mjs`
  (يشغّل دوالّ التطبيق في متصفّحٍ حقيقيّ ويتحقّق أن الرقم المرسوم = الرقم المحسوب).
- **عند استخراج كتلةٍ من `index.html` إلى وحدة — إلزاميّ:**
  `node global-surface-check.mjs snapshot /tmp/before.json` قبل النقل، ومثلُها بعده، ثم
  `diff` بينهما بـ`--expect-added <اسم الواجهة>`. **مفقود = صفر، وإلا فالنقلُ كسر شيئاً.**
  السببُ أن سماتِ `onclick` تُقيَّم في **النطاق العام**، فاسمٌ يسقط منه = **زرٌّ ميتٌ
  بصمت** بلا مترجمٍ ولا خطأِ جافاسكربتٍ يُنذر. و**النقلُ لا يُخلط بالتعديل**: النقلُ
  حرفيٌّ بايتاً ببايت في commit، والتحسينُ في آخر — وإلا لم يُعرف أيُّهما كسر.
- **وعند نقل أنماط (CSS):** `node css-surface-check.mjs` بالطريقة نفسِها — وضابطُ السطح
  العام **لا يُغني عنه**: نقلُ CSS لا يُسقط اسماً فيمرّ بلا ملاحظة. الخطرُ هنا **ترتيبُ
  التتالي** (خمسُ وحداتٍ تحقن `<style>` وقتَ التشغيل)، وانزلاقُ ورقةٍ فيه يبدّل عشراتِ
  القواعد بلا خطأٍ واحدٍ في وحدة التحكّم.
- **وضابطُ «المنقولُ = المستخرَج» مقارنةٌ دائريةٌ لا تُثبت شيئاً.** الضابطُ الصحيح:
  **إعادةُ تركيب الملفّين يجب أن تُنتج الأصلَ حرفياً** — هو وحده يمسك خطأَ الحدود.
- عند تعديل `firestore.rules`: شغّل `npm run rules:check` (محاكي Firestore حقيقيّ — يحتاج Java).
  **لا تحكم على قاعدةٍ أمنيةٍ بقراءة سطرها**: القواعد تُقيَّم بـ«أو»، وقاعدةٌ عامةٌ واحدةٌ
  تُبطل عشرَ قواعدَ صارمةٍ بلا أن يظهر ذلك في أيّ سطر.
- قائمةُ فحوص المتصفّح كاملةً وسطورُ تشغيلها في `NOTES.md §5`.

## الإصدار (عند أي تعديل)

- **لا تُحرّر رقم الإصدار يدوياً في أي ملف.** اكتب قيد `NOTES.md §6` بعلامة `vNEXT`
  ثم شغّل `npm run stamp` — يشتقّ `v18.9.<عدد الـcommits>` من git ويكتبه في كل مواضعه
  (`APP_VERSION` · تذييل الدخول · كل `?v=` · كل `MODULE_BUILD`) دفعةً واحدة.
- `npm run stamp:check` يفحص الاتّساق ويُشغّله CI — والدفعُ بختمٍ ناقصٍ يُسقط الفحص.
- عند تعارض دمجٍ على سطر الإصدار: خذ أيَّ جانبٍ ثم أعِد `npm run stamp`. بلا اجتهاد.

## المرجع

- `NOTES.md` = المرجع الدائم لمنطق العمل والقرارات. **حدّثه مع كل تغيير — بلا استثناء**:
  كل رفعِ إصدارٍ (`APP_VERSION`) يرافقه قيدٌ موجز في «§6 سجل موجز بأهم التعديلات»
  يشرح التغيير وجذره ونتائج الاختبارات. **يحرسه فحص في `hail-tests.js`** يطابق قيد
  الإصدار الحالي حرفياً (`**vX.Y — `) — فالدفع بلا توثيق يُسقط الاختبارات.
- `docs/deep-review-2026-08.md` = تقرير الفحص العميق وقائمة البنود المؤجّلة (المرحلة ٣: قواعد Firestore/الأمان).
