# NOTES — مرجع مشروع نظام المباني السريعة

مرجع دائم مختصر وعملي. **يُحدَّث مع كل تغيير — بلا استثناء**: كل رفع إصدار يرافقه
قيد في §6، ويحرسه فحص في `hail-tests.js` يطابق قيد الإصدار الحالي (الدفع بلا توثيق
يُسقط الاختبارات).

## 1) نظرة عامة

نظام إدارة **صيانة ومشتريات ومستودعات** لشركة المباني السريعة للمقاولات.
تطبيق صفحة واحدة (SPA) يعمل في المتصفح، وكل البيانات تُخزَّن وتُزامَن عبر **Firebase**
(Firestore للبيانات + Auth للدخول + Storage للمرفقات). لا خادم خلفي خاص.

## 2) دليل الملفات

| الملف | الوظيفة (سطر واحد) |
|---|---|
| `index.html` | التطبيق الكامل: الواجهة + كل منطق العمل + الربط بـ Firebase (المصدر الرئيسي). |
| `purchase-kpi.js` | وحدة مؤشرات أداء المشتريات — تحسب أزمنة المراحل من timeline وتركّب صفحتها ذاتياً (IIFE، `window`). |
| `project-management.js` | وحدة إدارة المشاريع (مرحلة ١) — قائمة المشاريع وبطاقة المشروع (نظرة عامة + موازنة بالبنود العامة الهجينة). تركّب صفحتها وقائمتها الجانبية ذاتياً وتلفّ `showPage`. تعرض `window.projectMgmt`، وتسحب المصروف/المرتبط من `purchases` عبر `poIsClosed`/`poActualCost`/`poStageIsWip`. الموازنة في `meta/{id}_budget`. **التصميم الكامل:** `docs/project-management-plan.md`. |
| `price-analysis.js` | محرك تحليل الأسعار (Rate Build-up) — يعرّض `window.priceAnalysis` ويكتب على مجموعة `global_price_analysis`. |
| `stocktake.js` | وحدة الجرد الشهري للمستودعات — تُحقَن في `#page-stocktake` وتعرّض `window.stocktake`. |
| `finance-audit.js` | وحدة الرقابة المالية على المشتريات — عينة شهرية عشوائية حتمية + استثناءات آلية، بدورة ردّ للمشتريات ومرفقات أدلة و**فحص وقائي** لحظة إنشاء الطلب. تُحقَن في `#page-finance-audit` وتعرّض `window.financeAudit`، وتكتب على `global_finance_audits` (وثيقة لكل شهر) وأدلتها في Storage تحت `finance_audits/`. **التفصيل المرجعي في §3-أ.** |
| `labor-catalog.js` | كتالوج بنود المصنعيات/العمالة — وحدة خارجية تغذّي محرك تحليل الأسعار. |
| `photo-queue-check.mjs` | فحصُ متصفّحٍ للطابور المؤجَّل: يُحاكي فشلَ الرفع فعلاً ويتحقّق من بقاء الصورة في IndexedDB بعد إعادة التحميل، ثم رفعِها وإلحاقِها بـ`arrayUnion` عند عودة الخدمة، ومن سلوك `imgBroken`. |
| `photo-queue.js` | طابورُ رفعٍ مؤجَّلٌ للصور — يحفظ جسم الصورة في IndexedDB عند فشل الرفع ويعيد المحاولة تلقائياً، ثم يُلحق الرابط الدائم بالوثيقة بـ`arrayUnion`. يعرّض `window.photoQueue` ويشاركه `index.html` و`tech-app.html`. **لا يُسقط عنصراً تلقائياً أبداً.** |
| `HailNotify.js` | طبقة عرض الإشعارات (toast + Notification API) — تعرّض `window.HailNotify` فقط، بلا معرفة بـ Firestore. أنواعُها الأربعة `ticket`/`po`/`hr`/`urgent`، لكلٍّ أيقونةٌ وكِنيةٌ ولون. **مَن يستدعيها هو من يقرّر الاستحقاق** (الوحدةُ تُنبّه صاحبَ الدور وحدَه) — الملفُّ عرضٌ محض. **القنواتُ الثلاث ومصفوفةُ تغطيتها في §3-ج.** |
| `sync-version.mjs` | ختمُ الإصدار الآليّ — يشتقّ `v18.9.<عدد الـcommits>` من git ويكتبه في كل مواضعه (`APP_VERSION` · التذييل · الـcache-busters · `MODULE_BUILD`) ويستبدل علامة `vNEXT` في §6. `npm run stamp` للكتابة، و`npm run stamp:check` للفحص. |
| `firestore.rules` | **قواعدُ الأمان على الخادم.** القاعدةُ العامة تسمح لكلّ ذي دورٍ غيرِ الزائر بالكتابة، و**مجموعاتُ التعاقدات الأربعُ مستثناةٌ منها** (`ctrLocked`) فيحكمها بلوكُها وحدَه: أدوارُ البوّابات، وجدولُ انتقالات العقد، وحقولٌ لا تُمَسّ (القيمةُ والطرفُ والبنود)، والآيبانُ للمالية والأدمن وحدَهما. والحذفُ ممنوعٌ من العميل **إلا بابين ضيّقين**: طلبٌ **ملغى** (`crqDeleteOk`) وعقدٌ **لم يُوقَّع** (`ctrDeleteOk`) — للأدمن وحدَه، وما عداهما أثرٌ ماليٌّ يُقرأ ولا يُمحى. وبابٌ ثالثٌ للتعديل: البنودُ والقيمةُ للأدمن، و**إلغاءُ التحويل** (`crqUnconvertOk`) بأربعة شروطٍ معاً — وهو ما يجعل معاملةَ حذف العقد تمرّ بكتابتيها. **تنبيهٌ منهجيّ:** قاعدةٌ تمسّ وثيقتين تُفحَص بشكل **العملية** (`writeBatch`) لا قاعدةً قاعدة — فحصُ كلٍّ على حدة مرّ بينما رفض الإنتاجُ المعاملةَ كلَّها. تنبيهٌ دائم: القواعدُ تُقيَّم بـ«أو» — فقاعدةٌ صارمةٌ لا تقيّد شيئاً ما لم تُستثنَ المجموعةُ من العامة أوّلاً. |
| `rules-check.mjs` | فحصُ القواعد على **محاكي Firestore حقيقيّ** بأدوارٍ حقيقيةٍ في التوكن: يكتب بدورٍ ويتوقّع القبولَ أو الردّ. خمسةُ أقسام، آخرُها **المساراتُ الحقيقيةُ للتطبيق** — فقاعدةٌ تمنع المهاجمَ والمستخدمَ الشرعيَّ معاً تعطيلٌ لا حراسة. `npm run rules:check`، ويشغّله CI في وظيفةٍ مستقلّة. |
| `wa-routing-check.mjs` | فحصُ **طبقة التوجيه الخادمية تنفيذاً** — يستدعي `findByRoleAnywhere`/`routeContractDoc`/`routePurchase` على Firestore مُحاكاةً ويتحقّق من الناتج: المستلمُ وعددُ الخانات ومحتواها. يغطّي الدورَ الاحتياطيَّ ومنعَ تسرّب النصّ الحرّ وشكلَ القالبين ومفتاحَ القتل، **ومرحلتَي البتّ في البند الإضافي** (مدير المشاريع ثم التنفيذي بحالةِ طلبٍ واحدة). بلا شبكةٍ ولا متصفّح ⇒ في CI. `node wa-routing-check.mjs` |
| `hail-tests.js` | سلسلة فحوص Node للمنطق الحرج (توزيع الاستلام، حركة المخزون، توازن HTML، تطابق الإصدار/الـ cache-busters). |
| `tvwall-check.mjs` | فحصُ متصفّحٍ لمركز العمليات: أربعةُ مشاريعَ مزروعة (منها مشروعُ نظافة بسبعةَ عشرَ مبنًى) ⇐ الأرقام والترتيب «الأسوأ أولاً»، والتدويرُ التلقائي ولوحةُ المشروع داخله، ومشروعُ النظافة بمهامّه، وحارسٌ **هندسيٌّ** أن لا بطاقةَ مبنًى تُقصّ خارج شبكتها، وأيقوناتُ المركز svg لا إيموجي، وبقاءُ المستمعين وفتحُ مشروعٍ من بطاقته وعزلُ المركز عن المشروع المفتوح. |
| `po-lifecycle-check.mjs` | فحصُ متصفّحٍ لدورة حياة طلب الشراء وحساباته: يشغّل دوالَّ التطبيق نفسها (`_poRepriceItem`/`getPOTotal`/`_poItemLine`/`poActualCost`/`poReceivedQty`/`_poHealItems`/`_poAlignRows`/`poStageOf`) ⇐ اصطلاحُ ض.ق.م **على الوحدة** لا على السطر، والتكلفةُ الفعلية من **المستلَم** لا المطلوب، وسندٌ مكرَّرٌ لا يُضخّم، والبندُ غير المستلَم بلا تكلفة، ودورةٌ كاملةٌ عبر ست مراحل ⇐ إغلاق؛ ثم **دخولٌ ونقرٌ فعليّان** يتحقّقان أن الرقم المرسوم في البطاقة هو الرقم المحسوب. |
| `cleaning-report-check.mjs` | فحصُ تشغيلٍ حقيقيٍّ في Chromium للتقرير المصوّر **المجمّع** لأعمال النظافة: يبني تنفيذاتٍ وهمية، يجمّعها بالوحدة، يرسم التقرير بنواة `index.html`، ويتحقّق من الـDOM الناتج (عدد البطاقات والصور، حذفُ صورةٍ من بطاقةٍ مجمّعة، العدُّ بالتنفيذات، عدّاد الحجم، وبقاءُ الوضع المفصّل). |
| `docs/performance-contract-assessment-2026-08.md` | تقييم توافق المنصة مع كراسة **العقد القائم على الأداء** (أمانة حائل — المرحلة الثانية): مصفوفة التوافق بند بند، الفجوات الحرجة (أمر العمل الكامل، بطاقة الأداء المتوازن والغرامات، ربط الاستهلاك بالعمل، السلامة، التوطين)، خطة تطويرٍ بثلاث موجات، واستفساراتٌ على الكراسة. |
| `docs/performance-project-type-plan.md` | **اقرأني أولاً** لملف العقد القائم على الأداء — الخطة بلغةٍ بسيطة: الفرق بين المشروع التقليدي والقائم على الأداء، كيف يتم التقييم شهرياً (خمس مواد بأوزان + نوعا الخصم)، ودورة الشهر، و**خمس مراحل تنفيذ تبدأ بعَلَم «عقد قائم على الأداء» على المشروع** — فالمشاريع القائمة لا تتأثر إطلاقاً. |
| `docs/consultant-kpi-mapping.md` | **البطاقة الفعلية** من الاستشاري (V3): البنية (٥ مجموعات × ١٨ مؤشراً موزوناً + ٦ حسميات، ٤ أبعاد OKR)، الأوزان والأهداف السنوية، آلة الحساب المستخرَجة (الدرجة ← الانحراف ← الغرامة ← **الحسميات التلقائية المتصاعدة**)، **٥١٫٥٪ من الدرجة تقارير عدم مطابقة يكتبها الطرف الآخر**، مصفوفة مطابقة المنصة لكل مؤشر، ست قدراتٍ مطلوبة، وثمانية تعارضاتٍ في ملف الاستشاري. **يَنسخ أوزان §٤ من `performance-kpi-model.md`.** |
| `docs/performance-kpi-model.md` | **جوهر القياس**: تشخيص محرّك المؤشرات الحالي (٧ مؤشرات — ما يُسمّى مقابل ما يُحسب فعلاً)، والعيوب البنيوية الخمسة (تراكميٌّ لا شهريّ · مقاييس مخزونٍ لا تدفّق · **زمن الاستجابة غير مخزَّن** · الوقائي يُعلَّم «منفَّذاً» عند إنشاء البلاغ لا إنجازه · لا مفهوم للتوقف/الجاهزية)، والحقول السبعة الناقصة، ومجموعة مؤشراتٍ شهريةً بأوزانٍ على ركائز بند ٦٧ الخمس، ومعادلة الدرجة/الانحراف/الغرامة، وقواعد إيقاف الساعة. |
| `contracts.js` | **وحدة التعاقدات مع مقاولي الباطن والموردين** (المرحلة ١) — IIFE تعرّض `window.contracts` وتركّب صفحتها ومجموعتها الجانبية ذاتياً وتلفّ `showPage`. تحمل **الدوالَّ النقيةَ الحسابية** (مصدرُ الحقيقة الوحيد، ممنوعٌ حسابُ أرقامها في الترميز): `vatSplit` بثلاثة أوضاع (`excl`/`incl`/**`none` بلا ضريبة للطرف غير المسجَّل**) · `lineTotal` بتقريب الضريبة **على الوحدة** · `payOrderAllowed` (عتبةُ أمر الدفع — تسمح دونها وتمنع فوقها) · `crqNextStage` (**دالةٌ واحدةٌ للعقد ولأمر الدفع**) · `contractValue` · `extNet` (سُلَّم المستخلص الثمانيّ) · `docExpiryState`. و**صفةُ الطرف** (`entityType`): منشأةٌ بسجلٍّ تجاريّ أو **شخصٌ نتعاقد معه بهويته** — تتبعها الهويةُ (`identityOf`) ووضعُ الضريبة المقترَح (`suggestVatMode`) والوثائقُ المتاحة ومفتاحُ منع التكرار (`duplicateOf` — بالرقم لا بالاسم). و**المرحلةُ ٢**: طلبُ التعاقد (`global_contract_requests`) يُولَد من مقايسة المشروع، ودورةُ اعتمادٍ بأربع بوّابات ببصمتَي اعتمادٍ (`crqProcKey`/`crqFinanceKey`) تُسقط توقيعَ مَن تغيّر ما وقّع عليه وحدَه، ومسارُ **أمر الدفع** المختصر دون العتبة (مديرُ المشاريع ← المشتريات ← سدادُ المالية — بلا بوّابةِ اعتمادٍ ماليٍّ لأنّ لا شروطَ تجاريةً تُراجَع)، **وحذفُ الطلب الملغى للأدمن وحدَه** (وما أنتج أثراً ماليّاً لا يُحذف). والمرحلةُ ١ تعرض **سجلَّ الأطراف** (`global_vendors`) بوثائقه وتنبيهات سريانها، و**أرقامَ جواله** (`phone` وجدولُ `contacts` القابلُ للتحرير — **تخزينٌ مطبَّعٌ** بصيغةٍ دوليةٍ واحدة عبر `normPhone` **مطابقةٍ حرفياً** لتطبيع `functions/lib/whatsapp.js`، وعرضٌ محلّيٌّ بروابط `tel:` وواتساب، و`phoneOk` تمنع حفظَ ثابتٍ أو ناقصٍ في حقل جوال، و`vendorMatchesPhone` تبحث بأيّ صيغةٍ كتبها الباحث، و`phoneOwner` تنبّه على رقمٍ يملكه طرفٌ آخر بلا منع)، و**نوعَ أعماله** (`trades` — قائمةٌ من ٣٨ تخصّصاً **أو** نصٌّ يُكتب، يلتقيان في `normTrade` فما يُكتب مطابقاً لاسمٍ في القائمة يصير مفتاحَها) بمرشّحٍ يعطي «مقاولٌ أو مورّدٌ لتخصّصٍ معيّن» عبر الدالّة النقيّة `vendorsByTrade`، و`kindMatches` تجعل «مقاول ومورّد» يظهر في القائمتين. و**المراحلُ ٣–٤**: تحويلُ الطلب المعتمَد إلى **عقدٍ** (`global_contracts`) بمعاملةٍ ذرّية يبدأ **بانتظار التوقيع** ثم يسري بالتوقيع، ثم **المستخلصاتُ التراكمية** (`global_contract_extracts`) بسُلَّم خصماتٍ ثمانيّ ومستخلصٍ مفتوحٍ واحدٍ في المرّة وحارسٍ يمنع تجاوزَ قيمة العقد أو التراجعَ عن المنجَز، و**شروطٌ نصّيةٌ** قوالبُها للأدمن ونسختُها لكل عقدٍ مع مخرَجٍ ورقيٍّ للتوقيع. و**المرحلةُ ٧**: **أوامرُ التغيير** (`global_contract_changes`) — مستندٌ بأربع بوّاباتٍ يُضاف إلى العقد ولا يستبدله (`value` مجمَّدٌ و`changeOrders` تنمو)، بحارسٍ يمنع الخفضَ تحت المنفَّذ أو تحت المسدَّد، وأمرٍ مفتوحٍ واحدٍ في المرة. و**المرحلةُ ٥**: `contractRollup` تُظهر أثرَ التعاقدات في موازنة المشروع بثلاثة أرقام (قيدَ الاعتماد · متعاقَدٌ عليه · المصروف)، و`poIsUnderContract` **حارسُ منع الازدواج** الذي تقرؤه `project-management` فتستبعد طلبَ الشراء المرتبطَ بعقدٍ من مصروف الشراء. و**المرحلةُ ٨**: ربطُ طلب الشراء بعقدٍ (تبويبٌ في بطاقة العقد يكتب `contractId`، وشارةٌ في تفاصيل الطلب)، وبطاقةُ **«بانتظار إجراءك»** التي تلفّ بطاقةَ المشتريات، و**توجيهُ واتساب** لمجموعاتها الثلاث في `functions/lib/contracts.js`. و**المخرَجان الورقيّان**: `printContract` (وثيقةُ العقد للتوقيع) و`printPayOrder` (**سندُ صرفِ أمر الدفع** — أرقامُه من الدوالِّ النقية، ووسمُ صلاحيته للصرف من `payOrderPrintState`، وخاناتُ توقيعه من `payOrderSignoffs`، والمبلغُ كتابةً من `amountWords`، والآيبانُ مقنَّعٌ بقاعدة `canBank` نفسِها)، وكلاهما يفتح نافذتَه بـ`_emitPrint` الواحدة، و**كلاهما يخرج على الورقة الرسمية للشركة** (`letterheadCSS`/`letterheadWrap`/`docHeadHTML` — ترويسةٌ وتذييلٌ وعلامةٌ مائيةٌ **على كل صفحة**، بهندسةٍ مقيسةٍ من قالب الشركة الورقيّ، وصورُها الثلاث `letterhead-*` تُقرأ عناوينُها المطلقةُ من `img#_lh_*` في `index.html` لأن نافذةَ iOS مستندُ `blob:` لا مسارَ نسبيَّ فيه؛ وعند غيابها تعود الترويسةُ النصّية). **معزولةٌ عن `global_purchases` والمخزون إلا حقلَ الربط وحدَه** — يحرسه فحصٌ يفحص أسماءَ الحقول المكتوبة. **وطبقةُ الحوكمة** (تفصيلُها في §3-ب — تُقرأ قبل أيّ تعديل): **فصلُ المهام** (`gateActMode` — من وقّع بوّابةً لا يوقّع التاليةَ، بمهربٍ عند غياب صاحبٍ آخر) · **الإرجاعُ إلى بوّابةٍ محدّدة** للأدمن (`docRewind` — مسحُ اعتماداتٍ ثمّ دالّةُ التوجيه تقرّر، ووجهاتٌ مشتقّةٌ بالتجربة) · **حذفُ ما لم يُنتج أثراً** (طلبٌ ملغى · عقدٌ لم يُوقَّع — ويُحرِّر طلبَه) · **تعديلُ البنود للأدمن** (تُسقط بصمةَ المالية فيعود الطلبُ لبوّابتها) · **صياغةُ البنود بالذكاء الاصطناعي** (الوصفُ والوحدةُ فقط — لا كميةَ ولا سعر) · و`_errMsg` تترجم رفضَ الخادم إلى عربيةٍ تدلّ على العلاج. **التصميم:** `docs/contract-lifecycle-design.md`. |
| `contracts-check.mjs` | فحصُ متصفّحٍ لوحدة التعاقدات: حقنُ القائمة، ودخولٌ ونقرٌ فعليّان، و**الرقمُ المرسوم = الرقمُ المحسوب**، والتطبيعُ العربيُّ في البحث، و**أرقامُ الجوال** (تُقرأ من صيغةٍ خام وتُعرَض روابطَ اتصالٍ وتُبحَث بأربع صيغٍ وتُحفَظ مطبَّعة)، وشرائطُ الحالة من توكنز SLA، والثيمُ الداكن، والجوّالُ بلا قصّ. |
| `docs/contract-lifecycle-design.md` | **الهيكلةُ القابلةُ للتنفيذ (مقترحٌ للاعتماد، لا كودَ بعد)** لمسار تعاقدات مقاولي الباطن: **ثلاثُ مجموعاتٍ مستقلّة** (`global_contract_requests` CRQ · `global_contracts` CTR · `global_contract_extracts` EXT) لأن الطلبَ قد يُرفَض والمستخلصَ يتكرّر شهرياً. طلبُ التعاقد **يُولَد من مقايسة المشروع** لا من فراغ، ويمرّ **أربعَ بوّاباتٍ** آخرُها شرطيٌّ بـ`CEO_APPROVAL_THRESHOLD` عبر دالةٍ نقيةٍ واحدة `crqNextStage` (نمط `hrPayments._nextStage`)؛ ثم **تحويلٌ بمعاملةٍ واحدة** إلى عقدٍ ساري (نمط `rfq_converted`) بحارس idempotency؛ ثم مستخلصاتٌ **تراكميةٌ** بسُلَّم حسابٍ من ثماني خطواتٍ في دالةٍ نقيةٍ واحدة `extNet`. **أمرُ التغيير طلبُ تعاقدٍ من نوعٍ آخر** لا محرّكٌ ثانٍ. الموازنةُ تصير **خمسَ خانات** (+ «قيدَ الاعتماد»). يفصّل ما أجمله `docs/subcontractor-contracts-plan.md`. |
| `docs/subcontractor-contracts-plan.md` | **مقترحٌ للنقاش (لا كودَ بعد)** — إدارة التعاقدات مع مقاولي الباطن والموردين: القرارُ المحوريُّ أن **أمر الإسناد ليس طلب شراء**، وخمسُ طبقات (سجلُّ الأطراف `global_vendors` · بطاقةُ أداءٍ مشتقّةٌ من البيانات القائمة · العقدُ بأنواعه الأربعة `global_contracts` · المستخلصُ التراكميُّ بخصوماته الخمسة — منها **خصمُ المواد المصروفة من `global_issue_orders`** · الربطُ بالأداء والضمان)، والتجميعُ الرباعيُّ في الموازنة مع **حارس منع الازدواج المحاسبي**، وثلاثُ موجاتِ تنفيذٍ وسبعةُ أسئلةٍ للمالك. يكمّل `docs/project-management-plan.md` من الجهة المقابلة (ما ندفعه لا ما نقبضه). |
| `hr-payments.js` | **وحدة سداد أعمال الموارد البشرية** (إقامات · رخص عمل · تأشيرات · نقل كفالة · تأمين…) — IIFE تعرّض `window.hrPayments` وتُحقَن في `#page-hr-payments`/`#page-new-hr-payment`. مجموعتها `global_hr_payments` **معزولةٌ تماماً عن `global_purchases`** (مصروفٌ إداريٌّ لا شراء). التوجيهُ كلُّه في دالةٍ نقيةٍ واحدة `_nextStage` (مدير المشاريع إن طُلب ← التنفيذي إن بلغت التكلفة `CEO_APPROVAL_THRESHOLD` ← المالية ← إغلاق)، واعتمادُ التنفيذي يسقط إن رُفعت التكلفة فوق ما اعتمده. العرضُ لأدوارها وحدها (`hr_officer`/`project_manager`/`ceo`/`finance`/`admin`). و`_liveToast` يُظهر تنبيهَ `HailNotify` **لمن دورُه الآن وحده**. |
| `functions/` | **طبقةُ إشعارات واتساب الخادمية** (Cloud Functions 2nd gen) — مشغّلاتُ Firestore تكتب في طابور `wa_outbox` ثم يرسل `waSender` عبر WhatsApp Cloud API (و`waRetry` كلَّ دقيقتين). ثلاثةُ مصادرِ أحداث: البلاغاتُ (`ticketAssign*` — إسنادُ فني) · الشراءُ (`poRoute*` على `global_purchases`) · **سدادُ الموارد البشرية** (`hrpRoute*` على `global_hr_payments`). المستلمون بالدور من `meta/users` (`lib/recipients.js`)، ومنعُ التكرار بمعرّفٍ حتميّ يحمل `occurrence` (`lib/outbox.js`)، والتوكن سرٌّ في Secret Manager. **الكود لا يعمل حتى يُنشَر:** `firebase deploy --only functions`. |
| `functions/wa-health.js` | **فحصُ صحّة منظومة الإشعارات** — تشغيلٌ يدويٌّ من Cloud Shell (`node wa-health.js` داخل `functions`). يراجع الحلقةَ كاملةً في تقريرٍ واحد: مفاتيحُ `.env` التي لا افتراضَ لها · وجودُ **كلِّ قالبٍ يستعمله النظام فعلاً** (يقرؤه من `.env` لا من قائمةٍ مكتوبةٍ فيه) معتمَداً بلغة `ar` في WABA الذي يملك الرقمَ المُرسِل · المشغّلاتُ منشورةٌ ونشطة وبيئةُ `waSender` تطابق الملفّ · مفتاحُ القتل وإخفاقاتُ الطابور في آخر ٢٤س · و**أيُّ دورٍ بلا رقمٍ مُفعَّل** (أي إشعارٌ يُسقَط بصمت — وهو الجذرُ الأشيع لبلاغ «الإشعارات لا تصل»). **قراءةٌ محضة** لا يكتب شيئاً. يُنصح بتشغيله بعد كل نشر. |
| `scripts/` | **سبعةُ سكربتاتِ تشغيلٍ لمنظومة النسخ الاحتياطي** — تُشغَّل من Cloud Shell لا من الدمج (تفصيلُها وقراراتُها في §5 و`docs/storage-backup-plan-2026-08.md`). **الإعداد:** `storage-backup-setup.sh` (مرحلتان: `--layer1-only` داخلَ الحاوية · `--vault-only` الخزنةُ في مشروعٍ منفصل) و`firestore-export-setup.sh` (تصديرٌ يوميٌّ إلى حاويةٍ خارج المشروع). **الإثبات:** `storage-restore-drill.sh` (تكسر الرابطَ بثلاث طرقٍ وتشترط عودتَه `200` — أي **نجاةَ توكِن التنزيل**) و`firestore-export-drill.sh --cross-project` (تستورد في **مشروعٍ آخر** إلى قاعدةٍ مؤقّتةٍ ثمّ تحذفها — ولا تلمس `(default)`). **الرقابة:** `backup-health.sh` **شهرياً** — يجمع `storage-backup-check.sh` و`firestore-export-check.sh` **ويضيف عمرَ آخر بروفة** (⚠ عند ٩٠ يوماً · ❌ عند ١٢٠)، إذ تختم كلُّ بروفةٍ ناجحةٍ تاريخَها في `_drills/` بالخزنة. **وكلُّها عديمةُ الأثر عند التكرار.** ⚠ **والمبدأُ الحاكم: تسأل «هل هو قائمٌ الآن؟» لا «هل الآليةُ تعمل؟»** — وهو ما كشف خزنةً فارغةً بعد أن قُيّد إقفالُ البند. |
| `docs/whatsapp-notifications-STATUS.md` | **اقرأني أولاً** لمشروع إشعارات واتساب — حالة العمل ونقطة الاستئناف. المراحل ١–٤ حيّةٌ على الإنتاج، و**المرحلة ٥ (سداد الموارد البشرية) كودُها جاهزٌ وينتظر النشر** وإدخالَ أرقام أدوار السداد. |
| `docs/whatsapp-notifications-plan.md` | خطة تصميم إشعارات واتساب الكاملة (المعمارية، القوالب، قائمة الإعداد، سجل العوائق §12). |

كل الوحدات الخارجية أنماط IIFE مستقلة تعرّض كائناً واحداً على `window`،
وتقرأ خدمات النواة من `index.html` بالاسم المجرّد (db / firebase / esc / toast / فحوص الأدوار...).

## 3) قرارات منطق العمل المهمة

- **الملخصات المالية من المغلق فعلياً فقط:** ملخص الموردين وملخص المشاريع (والمبنى)
  و«إجمالي التكاليف» في تقرير المشتريات ولوحة المعلومات تُحسب **فقط من طلبات الشراء
  المغلقة فعلياً** (`closed` / `closed_after_receipt`)، وتُستبعد الطلبات الجارية أو
  تحت مراجعة المستودع من قيمة التكلفة.
- **التكلفة الفعلية لا المقدّرة:** يُستخدَم لكل طلب مغلق **التكلفة الفعلية بعد التوريد**
  (`actualCost` = فاتورة المورد شامل الضريبة)، لا التكلفة المقدّرة وقت إنشاء الطلب.
  الرجوع للمقدّرة يحدث فقط لطلب مغلق قديم لا `actualCost` مسجّلاً له.
- **حالة موحّدة (مصدر واحد للحقيقة):** تصنيف حالة طلب الشراء يمرّ عبر دوال مركزية واحدة
  تقرأها **كل الشاشات** (لوحة المعلومات، بطاقة الملخص، تقرير الشاشة، تقرير PDF) — فلا
  يوجد تصنيف مختلف بين اللوحة والتقرير. ممنوع كتابة قوائم حالات يدوية محلية في أي شاشة.
- **عدّ مقابل تكلفة:** أعمدة «عدد الطلبات» تبقى عدّاً لكل الطلبات (حجم النشاط)، أمّا
  «منجز» والتكلفة فمن المغلقة فعلياً فقط.
- **هوية بند الطلب: المرساةُ تسبق النصّ.** `itemId` (مرساةُ الكتالوج) هي الهوية،
  و`itemName`/`itemCode` **بياناتٌ وصفيةٌ تُشتقّ منها** — لا نصٌّ مجمَّدٌ لحظةَ الإنشاء.
  فالبندُ الحرُّ الذي يربطه أمينُ المستودع عند التدقيق (أو يضيفه للكتالوج باسم الفاتورة)
  يعرض اسمَ الكتالوج وكودَه في تفاصيل الطلب وفي الطباعتين — كما يفعل المخزونُ والسجلُّ
  منذ v18.9nr/v18.9nw. **وما كتبه الطالب لا يضيع**: يُحفَظ في `requestedItemName` ويظهر
  «طُلب باسم: …» تحت الاسم. **وبلا مرساةٍ لا تخمين**: البندُ يبقى على نصّه حرفياً — لا
  مطابقةَ بالاسم، فالآلةُ لا تميّز «لمبة» من «كشاف». (v18.9xb)
- **البتُّ في البند الإضافي: مرحلتان بحالةِ طلبٍ واحدة.** البندُ الذي يصل ولم يطلبه أحد
  يُنشأ `pending_pm` — **مديرُ المشاريع أولاً دائماً** — ثم `pending_ceo` إن كان
  **إجمالي الطلب** فوق العتبة. وحالةُ الطلب تبقى `pending_extra` عبر المرحلتين، فلا
  تصلح وحدَها إشارةَ توجيه: `p.extraStage` هي التي تتحرّك مع الدور، ويقرؤها
  `routingKey` في الطبقة الخادمية. **من يضيف مرحلةً أو يغيّر ترتيبَ البتّ فليكتبها،
  وإلّا صمت التنبيه بلا أثرٍ ظاهر.** (v18.9xb)
- **عتبةُ المدير التنفيذي تُقاس على الطلب لا على البند.** في البند الإضافي وفي غيره:
  المقياسُ `getPOTotal(p)` — إجمالي الطلب بعد الإضافة. والسببُ صريحٌ ومقصود: القياسُ
  على البند بابٌ خلفيّ (اطلب دائماً تحت العتبة وأضف الباقي عند الاستلام). ولذلك
  **يُقال للمستخدم صراحةً** عبر `_poExtraCeoWhy(p)` إن العتبة على الطلب كلِّه لا على
  البند الموقوف — وقد يكون البندُ بأربعة ريالات. **واعتمادُ الطلب ككل** مضمونٌ عند
  بوّابة «إرسال للمالية» بـ`poCEOCovers`: ختمُ التنفيذيّ مقرونٌ بمبلغه، فما تجاوزه
  يعود إليه. (v18.9xa/v18.9xb)

### 3-أ) منظومة الرقابة المالية على المشتريات (`finance-audit.js`) — مرجع دائم

بُنيت عبر v18.9wk→ws (أغسطس 2026) بطلب المالك وقراراته. طرفان يقرآن محرك مقارنة واحداً:

- **العينة الشهرية الهجينة:** المرشّحون = الطلبات **المغلقة فعلياً** (`poIsClosed`)
  في الشهر المُدقَّق. طبقتان: (١) عشوائية **حتمية** — البذرة = مفتاح الشهر + معرّفات
  كل المرشّحين (FNV-1a + mulberry32)، فنفس المدخلات ⇒ نفس العينة دائماً: **لا يمكن
  «إعادة السحب»** حتى تخرج عينة مريحة، وبصمة البذرة تُخزَّن مع الدورة للتحقق؛
  مرجّحة بالقيمة (الأغلى احتماله أعلى)؛ الحجم 10% ضمن [3–10]. (٢) **إدراج آلي**
  (بعلامة وأسباب ظاهرة نصاً) لأي طلب فيه إشارة خطر: فعليّه > أرخص عرض كامل مسجّل
  ×1.15 ضريبة ×1.05 تسامح، أو > التقدير ×1.25، أو ≥ 20000 بلا أي مقارنة أسعار.
  الثوابت كلها أعلى الوحدة (`FA_*`) وتُخزَّن نسخة منها مع كل دورة.
- **محرك المقارنة (استرشادي):** لكل بند، صافي سعر وحدتنا (`(itemCost−vat)÷rcvQty`)
  مقابل أفضل بديل من: التاريخ الشرائي (الطلبات المغلقة الأخرى ±6 أشهر — مطابقة
  أسماء بالتطبيع العربي: همزة/تاء مربوطة/ياء/تشكيل/مسافات)، وكتالوج الأسعار،
  وأرخص عرض كامل التغطية مسجّل على الطلب نفسه، وعروض يدوية يضيفها المدقّق
  (بمرفق دليل اختياري صورة/PDF إلى Storage — فشل الرفع لا يضيف العرض).
- **دورة الحالة للعينة:** `pending` ← مراجعة المالية بحكم: مطابق/فرق مقبول
  (⇒ `closed` مباشرة) أو ملاحظة/مخالفة (⇒ `awaiting_procurement` — **إلزامية
  الملاحظات**) ← رد المشتريات بمبرر (⇒ `responded`) ← إغلاق المالية: قبول الرد أو
  **تصعيد للتنفيذي** (`escalated`). الدورة تُغلق فقط بعد إغلاق كل بنودها.
  حذف عينة من الدورة = **المسؤول فقط** وعلى الدورة المفتوحة، ويُقيَّد بالسجل.
- **الفحص الوقائي:** نفس المحرك يعمل لحظة إدخال بنود «طلب شراء جديد»
  (`renderPrecheck` تستدعيها النواة بعد كل تعديل بند): تنبيه فقط عند بديل أرخص
  يتجاوز التسامح — **صمت تام عند السلامة، واسترشادي لا يمنع الإرسال**.
- **التخزين والتزامن:** `global_finance_audits` وثيقة لكل شهر (المعرّف = `YYYY-MM`)؛
  الإنشاء create-if-absent داخل معاملة، وكل التعديلات معاملات على الوثيقة الطازجة
  (`_txAudit`) **تحدّث الذاكرة المحلية فوراً بنتيجتها** (`_applyLocalDoc` — درس
  رحلة E2E: بدونها يُرفض إغلاق الدورة ظلماً بانتظار اللقطة). التحميل الأولي:
  `get()` موازٍ لـ`onSnapshot` + مهلة 8 ثوانٍ + رسالة اتصال صادقة مع إعادة محاولة.
- **الصلاحيات:** الإنشاء/المراجعة/الإغلاق: المالية + الأدمن · الرد: المشتريات +
  الأدمن · العرض: هم + التنفيذي (زر القائمة مخفي عن غيرهم) · حذف دورة/عينة: الأدمن.
- **أمان:** كل معاملات `onclick` النصية عبر `_jq` (جسر `_jsq`) لا `esc` — درس
  v18.9vu-H5. **مؤجَّل للمرحلة ٣:** قواعد Firestore/Storage للمجموعة والمسار.
- **أفكار معتمدة تنتظر التنفيذ** (ناقشها المالك): سجل أداء الموردين (شارة الملاحظات
  في شاشات المقارنة)، الوفر المحقَّق من التوصيات، الدورة التلقائية أول الشهر
  وأعمار البنود، ترجيح المخاطر في العينة، تغذية الكتالوج من التوصيات، عتبات من
  الواجهة. **معايرة العتبات** (20000/25%/5%) تُراجع بعد أول دورة ببيانات حقيقية.

### 3-ب) حوكمةُ التعاقدات — مرجع دائم

> §6 سجلٌّ زمنيّ يقول **متى** تغيّر الشيء ولماذا. هذا القسمُ يقول **ما هو الحكمُ
> اليوم** — يُقرأ وحدَه قبل أيّ تعديلٍ على `contracts.js` أو `firestore.rules`.

**١) البوّابات — مصدرُ الحقيقة دالّةٌ واحدةٌ لكل مستند.** لا تُكتب قائمةُ حالاتٍ في
شاشة: `crqNextStage` (الطلب) · `extNextStage` (المستخلص) · `chgNextStage` (أمر التغيير)
· `CTR_TRANSITIONS` (العقد). ومسارُ الطلب اليوم:

| | المسار |
|---|---|
| **العقد / أمر الإسناد** | مدير المشاريع ← المشتريات ← المالية ← (التنفيذي فوق السقف) ← معتمَد |
| **أمر الدفع** | مدير المشاريع ← **المشتريات** ← (التنفيذي فوق السقف) ← سدادُ المالية |

أمرُ الدفع بلا بوّابةِ **اعتمادٍ ماليّ** (لا شروطَ تجاريةً تُراجَع)، والماليةُ فيه
**مُسدِّدةٌ لا مُعتمِدة**.

**٢) فصلُ المهام — من وقّع بوّابةً لا يوقّع التاليةَ على المستند نفسِه.** يسري على
الطلب والمستخلص وأمر التغيير معاً عبر `gateActMode` ⇒ `none`/`act`/`delegate`/`blocked`.
- **المهربُ شرطٌ لا تفصيل:** المنعُ مشروطٌ بوجود **مستخدمٍ آخرَ يملك البوّابة** (يُقرأ
  من `USERS`)؛ وإلا بقي الزرُّ وسُجِّل الاعتمادُ **«نيابةً»** مع علَم `delegatedApproval`.
  فقاعدةٌ بلا مهربٍ تُجمّد العملَ في فريقٍ لا ثانيَ فيه.
- **الرفضُ/الإعادة لا يُمنع أبداً** (لا يراكم سلطةً، ومنعُه يحبس المستند).
- **والسدادُ تحت القاعدة:** من اعتمد لا يصرف.
- **المطابقةُ باسم الدخول** (`*ApprovedByUser`) مع ارتدادٍ للاسم المعروض في الوثائق
  القديمة. و«بانتظار إجراءك» وعدّادُ «بانتظار دورك» يحترمان القاعدةَ — عدّادٌ بلا زرٍّ
  يبعث المستخدمَ يبحث عن عملٍ ليس له.
- **حدُّها الصريح:** ضبطٌ إجرائيٌّ لا حاجزٌ أمنيّ (الأدمن يملك القاعدةَ نفسَها).

**٣) الإرجاعُ إلى بوّابةٍ محدّدة (أدمن).** ليس حالةً جديدة: يُمسح اعتمادُ البوّابة
**وما بعدها** ثمّ **دالّةُ التوجيه وحدَها** تقرّر أين يقف المستند (`docRewind`).
والوجهاتُ **تُشتقّ بالتجربة** لا تُكتب: تُقبَل الوجهةُ إن كانت الآلةُ ستقف عندها فعلاً
— فأمرُ الدفع لا يعرض «المالية»، وما دون السقف لا يعرض «التنفيذي»، ولا وجهةَ بلا أثر.
والسببُ إلزاميٌّ ويُحفَظ مع الحالة التي أُرجع منها.

**٤) ما يُحذف وما لا يُحذف — القاعدةُ الجامعة: يُحذف ما لم يُنتج أثراً.**

| المستند | يُحذف؟ |
|---|---|
| طلبُ تعاقدٍ **ملغى** | ✅ للأدمن (`crqDeleteOk`) |
| عقدٌ **لم يُوقَّع بعد** | ✅ للأدمن (`ctrDeleteOk`) — ويُحرِّر طلبَه في المعاملة نفسِها |
| طلبٌ حيٌّ · معتمَدٌ · محوَّلٌ · مسدَّد | ❌ مهما كان الدور |
| عقدٌ سارٍ · مفسوخٌ · مقفل | ❌ — **الساري يُفسَخ ولا يُمحى** |
| مستخلصٌ · أمرُ تغيير | ❌ **إطلاقاً** (٤ مواضع `allow delete: if false`) |

والعقدُ غيرُ الموقَّع **لا أبناءَ له بالبناء** (المستخلصُ وأمرُ التغيير لا يُنشآن إلا
على سارٍ) — فحالتُه وحدَها ضمانةٌ كافيةٌ على الخادم. وحذفُه يُعيد طلبَه إلى «معتمَد»
ويمسح `contractId` — وهو **الاستثناءُ الوحيد** من «المحوَّلُ لا يُفتح» (`crqUnconvertOk`،
بأربعة شروطٍ معاً). وكلُّ حذفٍ بسببٍ إلزاميٍّ وقيدِ تدقيق.

**٥) ما يُعدَّل بعد الإرسال.** الطرفُ وشكلُ الارتباط والمشروعُ **مجمَّدةٌ للجميع بلا
استثناء**. والبنودُ والقيمةُ **للأدمن وحدَه** — والعهدُ الأصليُّ («وقّع المعتمِدُ على
رقمٍ وسُدِّد غيرُه») يبقى قائماً لأن `crqRevalidate` تُسقط بصمةَ المالية ويسقط اعتمادُ
التنفيذيِّ بقيمته، فيعود الطلبُ إلى بوّابتهما: **ما وُقِّع على رقمٍ قديمٍ يُبطَل لا يُمرَّر**.

**٦) الذكاءُ الاصطناعيُّ في البنود — يصوغ الكلماتِ لا الأرقام.** الكميةُ وسعرُ الوحدة
لا تُرسَلان ولا تُقرآن في الرد (`aiParseLines`)؛ المقترَحُ الوصفُ ووحدةُ القياس فقط،
ويُعرَض «قبل/بعد» ويُطبَّق بقرار المستخدم، ولا يزيد بنداً ولا ينقص.

**٧) غرامةُ التأخير بالريال** (مبلغٌ يوميٌّ وسقف). والوثائقُ القديمةُ المخزَّنةُ بالنسبة
**تُقرأ وتُطبَع نسبةً كما وُقِّع عليها** — الوسمُ (`penalty.mode`) هو الفيصل، وكلُّ
حسابٍ يمرّ بـ`penaltyPerDay`/`penaltyCap`.

**٨) رسائلُ الرفض.** رفضُ الخادم يُترجَم بـ`_errMsg` إلى عربيةٍ تدلّ على العلاج —
«قواعدُ Firestore لم تُنشَر بآخر نسخة» — لا سطرٍ إنجليزيٍّ يبدو عطلاً في النظام.

**٩) ما يراه المعتمِد = ما سيوقّعه الطرف.** بطاقةُ طلب التعاقد تعرض **مسودةَ العقد**
كاملةً (شروطاً قانونيةً ومالية) **في كلّ مرحلةٍ ولكلّ دور** — القراءةُ ليست بوّابة.
وثلاثةُ قيودٍ تحكمها:
- **لا وثيقةَ ثانية:** المسودةُ هي `contractFromRequest` (الدالّةُ الناقلةُ نفسُها)
  مطبوعةً بورقة `contractPaperHTML` نفسِها التي تطبع العقدَ النافذ. نسخةٌ ثانيةٌ
  «للعرض» كانت ستفترق عند أوّل تعديل، فيوقّع المعتمِدُ على ورقةٍ ليست التي ستُوقَّع.
- **تُعلن عن نفسها** (`crqDraftState`): «مسودةٌ لا تُوقَّع» ما لم تكتمل البوّابات —
  ومنعُ الطباعة كان سيُنتج صورةَ شاشةٍ بلا وسمٍ أصلاً (قرارُ سند صرف أمر الدفع نفسُه).
  وحين يُنشأ العقدُ فعلاً تُحوَّل الطباعةُ إليه: النافذُ أولى بالورق من صورةٍ عنه.
- **ولا رقمَ مخترَعاً:** رقمُ العقد يُولد مع العقد، والمسودةُ تحمل رقمَ طلبها.
- وخاناتُ الاعتماد على الورقتين من **مصدرٍ واحد** (`crqSignoffs`) ومسمّياتُها من
  `GATE_ROLES` — فلا تُسمّى بوّابةٌ على الورق باسمٍ غيرِ اسمها على الشاشة.
### 3-ج) منظومة الإشعارات — القنوات الثلاث ومصادر الأحداث (مرجع دائم)

> **لماذا هذا القسم موجود.** بلاغُ المالك «إشعارات سداد الموارد البشرية لا تصل
> للمسؤولين» (v18.9.2518) لم يكن عطلاً في سطر: كان **وحدةً كاملةً بُنيت خارج المنظومة
> بلا أن يلاحظ أحد**. والسببُ أن المنظومة نفسَها لم تكن موصوفةً في موضعٍ واحد — ثلاثُ
> قنواتٍ في ثلاثة ملفّاتٍ لا يجمعها مخطّط. فمن يضيف وحدةً جديدة لا يعرف أنّ عليه
> وصلَها بثلاثة أماكن. هذا القسم هو ذلك المخطّط، و**قائمةُ الوصل** في آخره هي خلاصتُه.

#### القنوات الثلاث — لكلٍّ غرضٌ لا يؤدّيه غيرُه

| # | القناة | أين | تصل من؟ | تقاطع المستخدم؟ |
|---|---|---|---|---|
| ١ | **جرس التطبيق** | `addNotification` · وثيقة `meta/notifications` المشتركة | متصفّح صاحب الإجراء | ❌ سجلٌّ يُراجَع |
| ٢ | **التنبيه اللحظي** | `HailNotify.push` · توست في الصفحة | مستمع `onSnapshot` عند كلِّ متصفّح | ✅ داخل التطبيق فقط |
| ٣ | **واتساب** | `functions/` → `wa_outbox` → `waSender` | **الخادم** (Admin SDK) | ✅✅ **على الجوّال ولو كان خارج التطبيق** |

**القناةُ ٣ وحدَها تصل من ليس أمام الشاشة** — وهي الوحيدةُ الخادمية، فلا تعتمد على نجاح
متصفّحٍ ولا على كون أحدٍ مسجّلاً دخوله. القناتان ١ و٢ عميلٌ محض.

#### مصفوفة التغطية

| مصدر الحدث | جرس | توست | واتساب |
|---|---|---|---|
| البلاغات (`<projectId>_tickets`) | ✅ | ✅ `startHailNotifications` | ✅ `ticketAssign*` |
| طلبات الشراء (`global_purchases`) | ✅ | ✅ `startHailNotifications` | ✅ `poRoute*` |
| سداد الموارد البشرية (`global_hr_payments`) | ✅ `_notifyStage` | ✅ `_liveToast` | ✅ `hrpRoute*` |
| التعاقدات (طلب · مستخلص · أمر تغيير · عقد) | ✅ | — | ✅ `crq/ext/chg/ctrRoute*` |

#### أربعةُ مزالقَ وقعت فعلاً — لا تتكرّر

1. **مزامنةُ الجرس مربوطةٌ بمسار الدخول لا بالمستخدم.** `startNotifSync()` تُستدعى في
   `enterApp` **و**`openGlobalPurchases` — لأن أدوار المشتريات المركزية
   (`_isGlobalOnlyRole`: المالية · الموارد البشرية · المشتريات · المستودع) **لا تمرّ
   بـ`enterApp` إطلاقاً**. غيابُها من المسار الثاني جعل جرسَهم يعرض ما ولّده متصفّحُهم
   هم فقط — طوال أشهر، بلا أن يظهر خطأٌ في أيّ مكان.
2. **رقمُ الواتساب يُخزَّن في موضعين.** «إدارة مستخدمي المشتريات» تُزامن المركزيَّ
   `meta/users`، ولوحةُ الأدمن **داخل المشروع** تكتب في `meta/{projectId}_users`.
   الحدثُ المرتبطُ بمشروعٍ يقرأ الاثنين؛ والحدثُ العامُّ (السداد) كان يقرأ المركزيَّ
   وحده ⇒ `findByRoleAnywhere`/`findRequesterAnywhere` (المركزيُّ أوّلاً، ثم مستنداتُ
   المشاريع بسقف ٢٥). والشاشتان الآن تُزامنان مركزياً كلتاهما.
3. **نصُّ القالب ثابتٌ لا تبلغه المتغيّرات.** استعارةُ قالبِ الشراء أوصلت رسالةً تقول
   «**طلب شراء** بانتظار سدادك» عن طلبِ سداد. ⇒ قالبان مخصّصان، والكودُ يفرز شكلَ
   الخانات **بمطابقة اسم القالب المضبوط** (`usingPoApprovalTemplate`) لا بعَلَمٍ يُنسى.
4. **القالبُ المعتمَد قد يكون في حسابِ أعمالٍ آخر.** اعتُمد قالبا السداد، وبُدِّل `.env`،
   ونُشر — ففشل كلُّ إرسالٍ بخطأ Meta **`#132001 Template name does not exist in the
   translation`**. السببُ أنهما أُنشئا تحت **WABA غيرِ الذي يملك الرقمَ المُرسِل** (الحسابُ
   الإنتاجيُّ لم يكن فيه إلا قوالبُ البلاغات والشراء). **والقوالبُ لا تُنقَل بين
   الحسابات** — تُعاد كتابتُها في الحساب الصحيح. ⇒ **قبل تبديل `.env`: اسأل Meta**
   (`GET /{WABA}/message_templates?fields=name,language,status`) وتأكّد أن الاسمَ واللغةَ
   والحالةَ كما تتوقّع. الأمرُ الجاهزُ والتراجعُ الفوريّ في
   `docs/whatsapp-notifications-STATUS.md`.

#### قواعدُ الطبقة الخادمية (تسري على كل مصدر)

- **مصدرُ المستلمين** الدورُ لا الاسم: `recipients.js` ⇒ من له `phone` **و**`waOptIn===true`.
  من لا رقمَ له لا تصله رسالة — **بالتصميم**، لا عطلاً.
- **منعُ التكرار** بمعرّفٍ حتميّ = `sha1(النوع|المعرّف|الانتقال|المستلم|occurrence)`.
  و`occurrence` (طابعُ الكتابة `updatedAt`) هو ما يفرّق بين **إعادةِ إطلاقٍ** لنفس
  الكتابة (تُدمج) و**حدثٍ جديدٍ بنفس الانتقال** (يُرسَل) — درس M14.
- **مفتاحُ القتل** `meta/wa_settings.enabled=false` يوقف كلَّ الإرسال فوراً.
- **بلا مبالغَ ولا أسماءِ موظفين** في نصّ الرسالة: بياناتُ السداد حسّاسة، والتفاصيلُ
  تُقرأ داخل الوحدة التي لا يفتحها إلا صاحبُ الصلاحية (زرّ «فتح الطلب» يوصله).
- **الرابطُ العميق يوجَّه بالبادئة لا باسم المتغيّر:** `?po=HRP-…` يفتح وحدة السداد،
  لأن العميلَ يفحص `_isHrpId`. فقالبٌ واحدٌ يخدم وحدتين.
- **الكودُ الخادميُّ لا يعمل حتى يُنشَر:** `firebase deploy --only functions`. الدمجُ في
  `main` يحدّث الموقعَ (GitHub Pages) **لا** الخادم.
- **`functions/.env` هو مصدرُ الحقيقة للإعدادات، لا الدوالُّ المنشورة.** النشرُ يكتب بيئةَ
  الدالّة من الملفّ **كاملةً**: ما ليس فيه **يُحذَف** من الدالّة. وأخطرُ ما يُحذَف
  `WHATSAPP_PHONE_NUMBER_ID` — بلا افتراضٍ في الكود (لا يُخمَّن رقمُ مرسِل)، فغيابُه يوقف
  **كلَّ** إرسال. **وقع فعلاً (2026-08-11):** وُجد `.env` بلا هذا السطر بينما الخادمُ يحمل
  القيمة — لأن `waSender` (الدالّةُ الوحيدةُ التي تقرؤه) كان **فشل تحديثُها** في نشرةٍ
  سابقة فبقيت على نسختها القديمة ومعها القيمة. أيْ أن **فشلَ النشر ستر العطلَ**، وأوّلُ
  نشرةٍ تنجح عليها كانت ستطفئ الإرسال. ⇒ **قبل أيّ نشر:** `cat functions/.env` وتأكّد أن
  `WHATSAPP_PHONE_NUMBER_ID` فيه. وبعد نشرٍ يمسّ `waSender`، اقرأ القيمةَ من الدالّة
  المنشورة (`gcloud functions describe waSender --gen2 …`) — فراغٌ يعني أن الملفَّ ناقص.
  (بقيةُ الإعدادات — `WA_TEMPLATE` · `WA_PO_*` · `WA_TICKET_COLLECTIONS` — افتراضاتُها في
  `config.js` مطابقةٌ للإنتاج، فغيابُها لا يضرّ. هذا السطرُ وحدَه بلا شبكةِ أمان.)
  **والفحصُ الآليُّ لكلّ ما سبق:** `node functions/wa-health.js` — يراجع الطبقاتِ الخمس
  (الإعداد · القوالب · النشر · التشغيل · المستلِمون) في تقريرٍ واحد. شغّله بعد كل نشر.

#### قائمةُ الوصل — لأي وحدةٍ جديدة فيها اعتماداتٌ أو مراحل

1. **جرس:** استدعِ `addNotification(t,b,id,"<type>")` عند كل انتقال حالة.
2. **رؤية الجرس:** أضِف نوعَك إلى `_notifVisible` إن كانت الوحدة محدودةَ الصلاحية.
3. **توست:** نوعٌ في `HailNotify.js` + استدعاءٌ من لقطة الوحدة، **لمن دورُه الآن وحده**،
   بخطِّ أساسٍ صامتٍ عند أول لقطة، ولا تُنبِّه من نقل الحالة بنفسه.
4. **واتساب:** `functions/lib/<وحدتك>.js` + خريطةُ توجيهٍ في `config.js` + مشغّلان
   (`onDocumentCreated`/`onDocumentUpdated`) في `index.js`.
5. **إن كانت المجموعةُ عامّةً بلا `projectId`:** استعمل `*Anywhere` لا `findByRole`.
6. **حارسُ اختبارٍ ينفّذ التوجيه** على محاكي Firestore — لا فحصَ نصٍّ وحده.

## 4) الدوال والثوابت المركزية

موضعها في `index.html` بجوار `PO_FINAL_STATUSES`:

- `PO_CLOSED_STATUSES` — قائمة حالات «المغلق فعلياً» = `["closed","closed_after_receipt"]`.
- `poIsClosed(p)` — تُرجع true إن كان الطلب مغلقاً فعلياً (تمرّ عبر `normalizePOStatus`). هي المرجع الوحيد لتحديد الإغلاق في كل الشاشات.
- `poActualCost(p)` — التكلفة الفعلية بعد التوريد للطلب: `actualCost` إن وُجد، وإلا الرجوع للتقديرية `getPOTotal(p)` (لطلب مغلق قديم فقط).

دوال مساندة ذات صلة: `poStageIsWip(p)` (الطلب جارٍ/غير منتهٍ)، `getPOTotal(p)` (التكلفة التقديرية من البنود أو `estCost`)، `normalizePOStatus(s)` (توحيد رموز الحالة).

دوالُّ بند الطلب والبتّ في الإضافي (v18.9xb — **مصدرٌ واحد**، ممنوع قراءةُ الحقل الخام في أي شاشة):

- `_poShownName(it)` · `_poShownCode(it)` — الاسمُ والكودُ المعروضان لبند الطلب: يُشتقّان من مرساة الكتالوج (`_resolveItemName`/`_resolveItemCode`) ويرتدّان للنصّ الخام بلا مرساة. تقرؤهما تفاصيلُ الطلب و`printPurchaseDetail` و`printPurchaseDetailNoPrices`. **ممنوع** كتابة `it.itemName`/`it.itemCode` مباشرةً في خلية اسم/كود البند.
- `_poReqName(it)` — ما كتبه الطالب إن خالف المعروض («طُلب باسم: …»)؛ يُرجع فارغاً عند التطابق أو عند الاستبدال (شارةُ «⇄ استُبدل» تكفي).
- `poExtraStage(p)` — مرحلةُ البتّ الحالية في البنود الإضافية: `pending_pm` إن بقي بندٌ عند مدير المشاريع (**الأقدمُ دوراً أولاً**)، وإلا `pending_ceo`، وإلا `""`. تُكتب في `p.extraStage` عند التدقيق ومع كل بتّ — **وهي وحدَها ما يلتقطه التوجيهُ الخادميّ**، فحالةُ الطلب ثابتةٌ عبر المرحلتين.
- `_poExtraCeoWhy(p)` — جملةُ سبب استدعاء التنفيذيّ برقمها، وتنسبه للطلب لا للبند. تقرؤها نافذةُ الاعتماد وبطاقةُ البنود الإضافية والقيدُ والإشعار.
- `routingKey(po)` (في `functions/lib/purchases.js`) — مفتاحُ التوجيه الخادميّ: الحالةُ وحدَها في كل المسار، ومركَّبٌ `pending_extra:<extraStage>` عند البتّ في البند الإضافي. المقارنةُ عليه لا على الحالة.

- `poIsCustomProject(p)` — **مصدر واحد** لتحديد «المشروع المُدخَل يدوياً» (المخصّص): `p.isCustomProject===true || p.projectId==="__OTHER__"`. تقرؤها كل شاشات التقرير/التصدير/الـ KPI فلا يختلف التصنيف بينها. **ممنوع** فحص `projectId==="__OTHER__"` وحده في أي شاشة.
- `poProjectDisplayName(p)` — اسم مشروع الطلب المعروض: يدوياً من `projectName`، ورسمياً من `_getProjName(projectId)`.

دوالُّ شاشات العرض والمشاريع (v18.9ag — **مصدرٌ واحد**، ممنوع نسخُ قواعدها في أي شاشة):

- `tvHealth(overdue)` — تصنيفُ صحّة اللوحة من عدد المتأخّرات: `0` مستقر · `1-2` متابعة · `≥3` حرِج. تقرؤها لوحةُ المشروع (`renderTVDashboard`) وبطاقاتُ مركز العمليات ولوحاتُه.
- `_visibleProjectsFor(user)` — مشاريعُ المستخدم المرئية: الأدمن يرى الكل، و`user.projects` غير الفارغة تحصر الرؤية. تقرؤها بوّابةُ المشاريع (`renderProjectGrid`) ومركزُ العمليات.
- `_canSeeTVWall(user)` — من يفتح مركز العمليات: يُستثنى المراقب وأدوارُ المشتريات المركزية، ويُشترط مشروعٌ مرئيٌّ واحدٌ فأكثر. تفحصها `openTVWall` بنفسها لا الزرّ وحده.
- `_tvwallSyncScreens(order)` (v18.9ai) — شاشاتُ التدوير: «الكل» ثم مشروعٌ لكل بطاقة بترتيبها؛ وتغيّرُ الترتيب يُبقي المعروضَ على **مشروعه** لا على رقم شاشته.
- `cleaningOps.isCleaningProjectRec(p)` · `statsForTasks(list)` · `coverageForTasks(list)` · `splitTasks(list)` · `tasksColOf(id)` (v18.9aj) — **جسرُ النظافة لمركز العمليات**: كشفُ نوع مشروعٍ ليس المفتوح، وحسابُ لوحة يومه من قائمة مهامّه بقواعد الوحدة نفسها (داخل `_inCleanCtx` المتزامن). ممنوع نسخُ قواعد الاستحقاق/التأخّر/الإجازة خارج الوحدة.
- `_PERM_GRANT_KEYS` + `_permOn(perms,k)` (v18.9.2590) — **نوعا مفاتيح الصلاحيات**: المفتاحُ **الحاجب** (كلُّ القديمة) افتراضُه مسموحٌ ويُقرأ `!== false`؛ والمفتاحُ **المانح** (`cleaningEdit` أوّلُها) افتراضُه ممنوعٌ ولا يُقرأ إلا `=== true` — وإلا انفتح ما يمنحه لكلّ مستخدمٍ قائمٍ بأثرٍ رجعيّ. شاراتُ المستخدمين ونافذةُ تعديل الصلاحيات تقرآن `_permOn` وحدَها، **وممنوع** كتابةُ المقارنة في الترميز.
- `_ICON.radar` + `applyNavIcons` (v18.9.128) — **علامةُ مركز العمليات**: أيقونةٌ واحدةٌ في مجموعة المنصة يقرؤها زرُّ القائمة الجانبية (يُضبَط صراحةً لأنه بلا `data-page`) وزرُّ بوّابة المشاريع (`#tvwall-btn-ico`). **ممنوع** نسخُ مسار الأيقونة في الترميز — نسختان تنحرفان.

## 5) التشغيل والاختبارات

- **ختمُ الإصدار (قبل كل commit):** اكتب قيدَ §6 بعلامة `vNEXT` ثم `npm run stamp` —
  يُشتقّ `v18.9.<عدد الـcommits>` ويُكتب في الثمانيةَ عشرَ موضعاً دفعةً واحدة.
  و`npm run stamp:check` يفحص الاتّساق (يُشغّله CI أيضاً). **لا تُحرّر رقم الإصدار يدوياً.**
  وعند تعارضِ دمجٍ على سطر الإصدار: خذ أيَّ جانبٍ ثم أعِد `npm run stamp` — بلا اجتهاد.

- **تشغيل الاختبارات محلياً:**
  ```bash
  npm install jsdom --no-save
  node hail-tests.js
  ```
  (نفس ما يفعله سير عمل CI: `.github/workflows/hail-tests.yml`.)
- **اختبارات المتصفّح (Chromium حقيقي + Firestore وهميّ في الذاكرة — لا تلمس الإنتاج):**
  ```bash
  npm install playwright-core --no-save
  node browser-scenarios.mjs   # سيناريوهات منطقية (توزيع الاستلام، المخزون، الأدوار…)
  node ui-walkthrough.mjs      # رحلة مستخدم: دخول ← بوّابة المشاريع ← كل الصفحات ← تفاعلات
  node perf-contract-check.mjs # العقد القائم على الأداء: عزلُ المشروع التقليدي + الجدولة والطوابع والتجربة
  node tvwall-check.mjs        # مركز العمليات: الأرقام والترتيب والتدوير · النظافة بمهامّها · لا بطاقةَ مبنًى مقصوصة
  node cleaning-report-check.mjs # التقرير المصوّر المجمّع للنظافة: قصُّ الصور والعدُّ بالتنفيذات وعدّاد الحجم
  node photo-queue-check.mjs   # طابور الرفع المؤجَّل: فشلٌ فبقاءٌ في IndexedDB ⇐ رفعٌ وإلحاقٌ بـarrayUnion + المربّع الصريح
  node contracts-check.mjs     # وحدة التعاقدات: سجلّ الأطراف ونوعُ أعماله (القائمةُ والكتابةُ تلتقيان · المرشّحُ = الدالّةُ النقيّة) · **أرقامُ الجوال** (قراءةٌ من صيغةٍ خام · روابطُ الاتصال · البحثُ بأربع صيغ · رفضُ الثابت · الحفظُ مطبَّعاً من أرقامٍ عربية) · الطلب والبوّابات وأمرُ الدفع · **سندُ صرفِ أمر الدفع** (الوسمُ والتفقيطُ وقناعُ الآيبان) · التحويلُ لعقدٍ ساري · المستخلصُ وسُلَّمُ خصوماته · الوثيقةُ التعاقدية وحالةُ التوقيع والمخرَجُ الورقيّ · **الورقةُ الرسمية** (تُطبَع إلى PDF فعلاً ⇐ الترويسةُ والتذييلُ والعلامةُ على كل صفحة) · الرقم المرسوم = المحسوب · وطبقةُ الحوكمة (فصلُ المهام · الإرجاعُ لمرحلة · الحذفُ والتحرير · صياغةُ الذكاء بجذعٍ في الصفحة بلا شبكة)
  node po-lifecycle-check.mjs  # دورة حياة طلب الشراء وحساباته: ض.ق.م على الوحدة · الفعليُّ من المستلَم · الرقمُ المرسوم = المحسوب
  node users-guard-check.mjs   # شاشةُ إدارة المستخدمين (C1) **بدورَين**: المستودعاتُ ترى قائمةً بلا أزرارٍ ولا نموذجِ إضافة والكتّاب يرفضون النداءَ المباشر · والأدمن يرى كلَّ شيءٍ ويكتب. **يسدّ فجوةً بنيوية**: رحلةُ المتصفّح تدخل أدمن دائماً، فمسارُ غيرِ الأدمن — وهو ما نغيّره حين نقفل — لا يُرسَم في متصفّحٍ إلا هنا. وبذرتُه في `USERS_DOC()` لا في `window.USERS` وحدَه (الشاشةُ تُعيد القراءةَ قبل الرسم فتمحو المصفوفةَ المحلّية).
  node auth-guard-check.mjs   # حرّاسُ المصادقة: الدورُ من التوكِن لا من الجلسة (H2 — تُزوَّر الجلسةُ فعلاً ثمّ يُسأل النظام: بأيّ دورٍ دخل؟) · ومُرحِّلُ الذكاء يحمل هويّةً ويسقط سقوطاً آمناً إن لم يُحدَّث الـWorker (H3)
  ```
  كلاهما يحقن مُحاكي Firebase نفسه (`MOCK_FIREBASE` معرَّف في `browser-scenarios.mjs`
  ويقرؤه `ui-walkthrough.mjs` و`contracts-check.mjs` — مصدرٌ واحد).
  **ومستمعوه أحياء** (v18.9.2548): كلُّ كتابةٍ تُبثّ فوراً لمشتركي المجموعة كما في
  Firestore الحقيقيّ — وهو ما يجعل صنفَ **تعويض الكمون** قابلاً للرصد (اللقطةُ المحلّيةُ
  تسبق حلَّ وعد `set()`، فمن يضيف الوثيقةَ يدوياً بعد الوعد يكرّرها). **ولذلك تُزرَع
  البذرةُ في `window.__store` لا في مصفوفةٍ محلّيةٍ فحسب**: أوّلُ كتابةٍ في المجموعة
  تُعيد بناء المصفوفة من المخزن، فتختفي البذرةُ المحلّيةُ وحدَها. `ui-walkthrough.mjs` يُصادِق باعتراض
  `AUTH_LOGIN_URL` (لا كلمات مرور حقيقية)، ويلتقط لقطات في `/tmp/ui-shots`،
  ويرصد أخطاء الجافاسكربت لكل صفحة على حدة. **غير مُدرَجَين في CI** (يحتاجان متصفّحاً).
  ملاحظة: تشغيلهما عبر `file://` يُظهر خطأ Service Worker غير ضارّ (أثر بروتوكول لا خلل).
- **فحصُ قواعد Firestore (محاكٍ حقيقيّ — يحتاج Java):**
  ```bash
  npm install firebase-tools @firebase/rules-unit-testing firebase --no-save
  npm run rules:check          # يشغّل rules-check.mjs داخل محاكي Firestore
  ```
  **قاعدةٌ أمنيةٌ لا تُختبَر بقراءة سطرها**: القواعدُ تُقيَّم بـ«أو» عبر كلّ البلوكات
  المتطابقة، وقاعدةٌ عامةٌ واحدةٌ تُبطل عشرَ قواعدَ صارمةٍ بلا أن يظهر ذلك في أيّ سطر.
  فالفحصُ الوحيدُ المعتبَر: اكتب بدورٍ معيّن — أقُبِلت الكتابةُ أم رُدَّت؟
  **وأبعدُ من ذلك (درسُ v18.9.2588): افحص العمليةَ لا القاعدة.** إن كانت العمليةُ في
  التطبيق **معاملةً تكتب في وثيقتين**، فافحصها بشكلها (`writeBatch`) لا بفحص كلّ قاعدةٍ
  على انفراد: Firestore يردّ المعاملةَ **كلَّها** إن رُفض جزءٌ منها. وقع فعلاً — حذفُ
  العقد يحذفه **ويُعيد طلبَه**، فمرّ فحصُ `deleteDoc` وحدَه ورفض الإنتاجُ العمليةَ
  بـ`permission-denied` لأنّ قاعدةً أخرى تمنع الكتابةَ الثانية. **مُدرَجٌ في
  CI** في وظيفةٍ مستقلّةٍ (`rules`) تُشغَّل عند تغيّر `firestore.rules`.

  **وثالثةٌ لا تقلّ عنهما (درسُ C1): اجعل للفحص ضبطاً سالباً.** فحصُ منعٍ ينجح قد
  ينجح **لسببٍ آخر** — دورٌ لا يملك الوصولَ أصلاً، أو مسارٌ خاطئ، أو مصادقةٌ منقطعة.
  فقبل أن تصدّق ✅ على قاعدةٍ جديدة: **أعِد القواعدَ القديمةَ وشغّل الفحصَ** — إن لم
  يسقط، فهو لا يرصد شيئاً. (`git stash push firestore.rules` ثمّ `npm run rules:check`
  ثمّ `git stash pop`.) فحصُ C1 يسقط **١٢** من فحوصه بالقواعد القديمة — وهذا وحدَه
  ما يُثبت أنه يقيس القفلَ لا يصفه.

  **⛔⛔ والأغلى ثمناً (v18.9.2635 — انقطاعُ إنتاجٍ حقيقيّ): `read` عمليتان لا
  واحدة، ولا شرطَ مسارٍ في قاعدة القراءة العامة.**
  كتبتُ `allow read: if hasRole() && !srvOnly(document[0])` لأُخرج طابورَ واتساب من
  القراءة — **فسقط استعلامُ كلّ مجموعةٍ في النظام**: كلُّ الشاشات «Missing or
  insufficient permissions» والمالكُ داخلٌ بدور admin سليم.

  | العملية | ما هي | مع شرطِ مسار |
  |---|---|---|
  | `get` | قراءةُ مستندٍ بعينه | ✅ تعمل |
  | `list` | استعلامُ مجموعة | ❌ **تسقط** |

  في الـ`list` **لا مستندَ بعدُ** — الاستعلامُ يسأل عن مجموعةٍ كاملة، فمقاطعُ المسار
  غيرُ محسومة وأيُّ شرطٍ عليها يُسقط الطلب. **والكتابةُ لا تُصاب** (لا `list` فيها)،
  ولذلك عملت `ctrLocked(document[0])` سنواتٍ بلا أثر — فبدا النمطُ مُجرَّباً وهو
  ليس كذلك في القراءة.
  **القاعدة: القراءةُ العامة `if hasRole();` وحدَها بلا أيّ شرطٍ على `document[…]`.**
  ولإخراج مجموعةٍ من القراءة: **بلوكٌ مستقلٌّ لها يفصل `get` عن `list`**، لا شرطٌ في
  العامة. يحرسه حارسٌ صريحٌ في `hail-tests` يرفض أيَّ `&&` في سطر القراءة العامة.

  **وأمرُّ ما فيه أنّ ١٣٣ فحصاً مرّت وهي عمياءُ عن الصنف**: كلُّها `getDoc`/`setDoc`
  على **مستندٍ واحد** — أي `get` لا `list`. **فأيُّ فحصٍ لقاعدة قراءةٍ يجب أن يشمل
  `getDocs(collection(...))`** لكلّ مجموعةٍ يستعلمها التطبيق وبكلّ دورٍ يستعلمها به
  (القسم ١٠ في `rules-check.mjs`). وأسطرُ التحقّق بعد النشر كانت على مجموعاتٍ لها
  بلوكاتُها الخاصّة، فلم تلمس القاعدةَ العامةَ أصلاً — **اختبارٌ يفحص الاستثناءاتِ
  ويغفل الأصل**. فليكن في كلّ تحقّقٍ بعد نشرٍ **استعلامُ مجموعةٍ عادية**:
  ```js
  db.collection("global_purchases").limit(1).get()
    .then(s=>console.log("✅ الاستعلامُ يعمل:", s.size))
    .catch(e=>console.log("⛔ النظامُ مقطوع:", e.code));
  ```

  **وخامسةٌ (درسُ M11) — البلوكُ المتساهلُ الباقي في أسفل الملفّ يُبطل التشديدَ في
  أعلاه.** أضفتُ بلوكاً مشدَّداً لـ`audit_log` وتركتُ القديمَ `allow read, write: if
  hasRole()` في موضعه — فسقطت خمسةُ فحوصٍ والقاعدةُ الجديدةُ مكتوبةٌ صحيحةً تماماً.
  **فتشديدُ قاعدةٍ ليس إضافةً بل استبدال**: احذف القديمَ، ثمّ ابحث في الملفّ كلِّه عن
  بلوكٍ آخرَ يطابق المسارَ نفسَه. `grep -n "match /<المسار>" firestore.rules`.

  **وسادسةٌ (درسُ M11 أيضاً) — على التوكِن استعمل `get(key, default)` لا الوصولَ
  المباشر.** `request.auth.token.role` على توكِنٍ **لا يحمل الحقلَ أصلاً** (الفنّيُّ
  المُصادَقُ مجهولاً) ليس «فارغاً» بل **خطأُ تقييم**، والخطأُ يسري في التعبير كلِّه
  فيُردّ الطلب. فـ`!hasRole()` بدت صحيحةً ومنعت الفنّيَّ من الكتابة. الصحيح:
  `request.auth.token.get('role', '')` — تماماً كما نفعل مع `resource.data.get(...)`.
  ولا يظهر هذا في أيّ سطرٍ يُقرأ: يظهر في فحصٍ **بسياق المجهول** وحدَه.

  **ورابعةٌ خاصّةٌ بتطبيق الفنيين: سياقُ «المجهول» ليس `unauthenticatedContext`.**
  الفنّيُّ يدخل **مُصادَقاً مجهولاً** — `request.auth != null` بلا ادّعاء `role`.
  ففحصُه بسياقٍ غيرِ مُصادَقٍ يُظهر «مُنِع» فتظنّ القفلَ سليماً وقد عطّلتَ الميدانَ
  كلَّه (أو العكس). السياقُ الصحيح: `env.authenticatedContext("tech_anon", {})`.
- **نشرُ دوالّ الإشعارات (خطوةٌ يدويةٌ أخرى لا يفعلها الدمج):**
  ```bash
  # من Google Cloud Shell (متصفّحٌ فقط — لا يلزم جهاز)
  gcloud config set project fast-buildings
  git clone https://github.com/islamokasha1-cloud/Fast-buildings-maintenance-.git
  cd Fast-buildings-maintenance-/functions && npm install && cd ..
  firebase login --reauth --no-localhost      # الرابطُ يُفتح في المتصفّح، والشفرةُ تُلصق في الطرفية
  firebase deploy --only functions --project fast-buildings
  ```
  **ثلاثةُ مزالقَ وقعنا فيها فعلاً:**
  (أ) **مجلّدٌ قديمٌ في `~` يجعل `git clone` يتخطّى الجلبَ صامتاً** («already exists») —
  فكِدنا ننشر كوداً أقدمَ بأربعة PRs بلا ملفّ التوجيه أصلاً. **لا تنشر قبل التحقّق:**
  `git log --oneline -1 && ls functions/lib/contracts.js` — القيدُ الصحيح والملفُّ موجود.
  (ب) `--no-localhost` إلزاميّ (لا متصفّحَ في السحابة يستقبل الرجوع)، و**الرابطُ يُفتح
  ولا يُلصق في خانة الشفرة** — الشفرةُ تأتي بعد الموافقة وتبدأ بـ`4/0A`.
  (ج) `npm install` في `functions/` ليس زائداً رغم أنّ البناء يجري عند Google: أداةُ
  Firebase **تُشغّل `index.js` محلّياً** لتكتشف الدوالَّ المُصدَّرة، وبلا `node_modules`
  تفشل بـ`Failed to load function definition from source` قبل أن يبدأ النشر.

  **والتحقّقُ بعده من خارج المنصّة** (يفصل الخادمَ عن الواجهة كلَّها): اكتب مستنداً في
  `global_contract_changes` بـ`firebase-admin` من Cloud Shell، ثمّ
  `firebase functions:log --only chgRouteCreate`. و**لاختبار طرف الإرسال وحدَه** احقن
  مستنداً في `wa_outbox` بـ`status:"queued"` ورقمِك بالصيغة الدولية بلا `+` — يلتقطه
  `waSender` ويرسله. النصفان معاً يغطّيان السلسلة: التوجيهُ يُثبت اختيارَ الدور،
  والحقنُ يُثبت القالبَ والتوكنَ والتسليم.

- **⏳ نشرٌ معلَّقٌ للوسيط (منذ v18.9.2663):** `MAX_TOKENS_CAP` رُفع إلى **8192** في
  `worker/hail-ai-proxy.js` ليتّسع لجدول المقارنة الذكية (§6). **حتى يُنشَر الوسيط
  يبقى القصُّ عند 4096** — فالمقارنةُ تعمل لكنها قد تُبتَر عند ~٢٥ بنداً وتظهر لافتةُ
  «تحليلٌ ناقص» (رأبُ الناتج ينقذ البنودَ المكتملة، فلا سقوطَ في الحالتين).
  انشرِ الوسيطَ من `worker/README.md` ثمّ احذف هذا البند.

- **تأمينُ مُرحِّل الذكاء — H3 ✅ نُفِّذ ونُشِر (Worker v8 — ١٤ أغسطس ٢٠٢٦):**
  **البندُ مُغلقٌ الآن بنصفيه.** يبقى هذا القسمُ مرجعاً للطريقة وللتحقّق عند أيّ
  تعديلٍ لاحقٍ على الـWorker — فالنشرُ يدويٌّ لا يفعله الدمج، وإسقاطُ الحارس
  سهوٌ لا يُنبّه عليه شيء.
  **المستودعُ يُنجز النصفَ العميل:** كلُّ نداءٍ يحمل `Authorization: Bearer <توكِن هويّة Firebase>`
  (موقَّعٌ ومحدودُ الأجل — **لا سرٌّ مشتركٌ يُقرأ من المتصفّح، فذلك لا يحمي شيئاً**).
  **والنصفُ الحاسمُ في الـWorker**: ما لم يرفض الـWorker من لا يحمل توكِناً صحيحاً،
  يبقى الرابطُ مفتوحاً لمن يعرفه — والرابطُ مقروءٌ من `meta/ai_settings`.

  **ما نُشِر فعلاً (Worker v8):** التحقّقُ من توكِن Firebase بمفاتيح Google العامة
  (توقيعٌ + `iss` + `aud` + انتهاءُ الصلاحية)، و`Authorization` في ترويسات CORS،
  و**مفتاحٌ واحدٌ يحكم السلوك**: `const AI_AUTH_ENFORCE` — `false` يسجّل ولا يرفض،
  `true` يرفض. و`/login` و`/health` **خارج الحارس عمداً** (الأوّل يُصدر التوكِن أصلاً).

  **ومصدرُ الـWorker صار في المستودع: `worker/hail-ai-proxy.js`** (ودليلُه
  `worker/README.md`). كان يعيش في لوحة Cloudflare **وحدَها** — بلا نسخةٍ ولا تاريخٍ
  ولا مراجعة، وهو يحمل **بوّابةَ الدخول** لا وسيطَ الذكاء فحسب. **والقاعدةُ من الآن:
  عدّل الملفَّ هنا أوّلاً ثمّ انشره** — لا العكس؛ فتعديلُ اللوحة يضيع عند النشر التالي
  (نفسُ مصيدة `firestore.rules` أعلاه). ويحرسه في `hail-tests.js`: أنّ الحارسَ مُفعَّلٌ
  في النسخة المحفوظة، وأنّ `/login` خارجه، و**أن لا سرَّ يُلصق فيه** يوماً.

  **ونتيجةُ التحقّق الحيّ:** الغريبُ بلا توكِن ⟵ **401**، والموظّفُ الداخلُ ⟵ **يعمل**.
  (وقبل النشر كان الردُّ **400** — أي أنّ الطلبَ يمرّ إلى Anthropic. الفرقُ بين
  ٤٠٠ و٤٠١ هو **كلُّ** الدليل: الأولُ بابٌ مفتوح، والثاني بابٌ مُغلق.)

  **انشر على مرحلتين — لا دفعةً واحدة** (وإلّا تعطّلت خصائصُ الذكاء عند الجميع):
  **(أ)** أضِف `Authorization` إلى ترويسات CORS المسموحة **وتحقّق بلا رفض** (سجِّل فقط)،
  **(ب)** وبعد أن تُظهر السجلاتُ وصولَ التوكِنات، حوِّل التحقّقَ إلى **رفض**.

  ```js
  // ── التحقّق من توكِن هويّة Firebase داخل Cloudflare Worker (بلا مكتبات) ──
  const PROJECT_ID = "fast-buildings";   // مُعرّف مشروع Firebase
  const JWK_URL = "https://www.googleapis.com/service_accounts/v1/jwk/securetoken@system.gserviceaccount.com";
  let _jwks = null, _jwksExp = 0;
  async function jwks(){
    const now = Date.now();
    if(_jwks && now < _jwksExp) return _jwks;
    const r  = await fetch(JWK_URL);
    const m  = /max-age=(\d+)/.exec(r.headers.get("cache-control") || "");
    _jwks    = await r.json();
    _jwksExp = now + ((m ? +m[1] : 3600) * 1000);
    return _jwks;
  }
  function b64u(x){
    x = x.replace(/-/g,"+").replace(/_/g,"/"); while(x.length % 4) x += "=";
    const b = atob(x), u = new Uint8Array(b.length);
    for(let i=0;i<b.length;i++) u[i] = b.charCodeAt(i);
    return u;
  }
  async function verifyIdToken(jwt){
    const p = String(jwt||"").split("."); if(p.length !== 3) return null;
    const dec = t => JSON.parse(new TextDecoder().decode(b64u(t)));
    let head, claims;
    try{ head = dec(p[0]); claims = dec(p[1]); }catch(e){ return null; }
    if(head.alg !== "RS256") return null;
    if(claims.aud !== PROJECT_ID) return null;
    if(claims.iss !== "https://securetoken.google.com/" + PROJECT_ID) return null;
    if(!claims.exp || claims.exp * 1000 <= Date.now()) return null;
    const jwk = ((await jwks()).keys || []).find(k => k.kid === head.kid);
    if(!jwk) return null;
    const key = await crypto.subtle.importKey("jwk", jwk,
      { name:"RSASSA-PKCS1-v1_5", hash:"SHA-256" }, false, ["verify"]);
    const ok = await crypto.subtle.verify("RSASSA-PKCS1-v1_5", key, b64u(p[2]),
      new TextEncoder().encode(p[0] + "." + p[1]));
    return ok ? claims : null;
  }

  // ── في معالج مسار الذكاء، قبل النداء إلى Anthropic ──
  const auth   = request.headers.get("Authorization") || "";
  const tok    = auth.startsWith("Bearer ") ? auth.slice(7) : "";
  const claims = tok ? await verifyIdToken(tok) : null;
  // المرحلة (أ): سجِّل ولا ترفض ──> console.log("ai-auth", !!claims, claims && claims.role);
  // المرحلة (ب): ارفض
  if(!claims || !claims.role){
    return new Response(JSON.stringify({ error:{ message:"unauthorized" } }),
      { status:401, headers:{ ...CORS, "Content-Type":"application/json" } });
  }

  // ── وترويساتُ CORS يجب أن تُدرج Authorization وإلّا ردَّ المتصفّحُ الطلبَ قبل وصوله ──
  const CORS = {
    "Access-Control-Allow-Origin":  "*",
    "Access-Control-Allow-Methods": "POST, OPTIONS",
    "Access-Control-Allow-Headers": "Content-Type, Authorization"
  };
  // وعالِج OPTIONS بردٍّ فارغٍ يحمل CORS — الطلبُ المبدئيُّ (preflight) يسبق كلَّ نداء.
  ```

  **والتحقُّقُ بعده — لا تكتفِ بأن «الذكاء ما زال يعمل»** (فذلك يثبت المسارَ الشرعيَّ وحدَه):
  ```bash
  # يجب أن يُردّ 401 — هذا هو الاختبارُ الحقيقيّ: بلا توكِنٍ لا خدمة
  curl -s -o /dev/null -w '%{http_code}\n' -X POST "$PROXY_URL" \
    -H 'Content-Type: application/json' --data '{"model":"claude-sonnet-4-6","max_tokens":8,"messages":[]}'
  ```
  فإن ردَّ `200` فالبابُ ما زال مفتوحاً مهما بدا التطبيقُ سليماً.
  **ومصيدةٌ متوقَّعة:** مسارُ `/login` في الـWorker نفسِه **يجب أن يبقى بلا تفويض** —
  فهو الذي يُصدر التوكِنَ أصلاً، وقفلُه يُغلق الدخولَ على الجميع.

- **النسخُ الاحتياطي والتعافي — مفحوصٌ باستعادةٍ حقيقيةٍ لا بافتراض (١٤ أغسطس ٢٠٢٦):**
  **الحالةُ القائمة** (تحقَّقنا منها بالأوامر لا بالذاكرة):
  · **PITR مُفعَّل** — الرجوعُ لأيّ **دقيقة** خلال آخر ٧ أيام.
  · **جدولةٌ يوميةٌ** قائمةٌ منذ ٣ يوليو ٢٠٢٦، احتفاظٌ **٩٨ يوماً**، بلا فجوةٍ بين الأيام.
  · موقعُ القاعدة `nam5` · النوع `FIRESTORE_NATIVE`.
  ```bash
  gcloud firestore databases describe --database='(default)' --format="value(pointInTimeRecoveryEnablement)"
  gcloud firestore backups schedules list --database='(default)'
  # أحدثُ نسخةٍ وعمرُها — حكمٌ فوريٌّ على حياة الجدولة
  LAST=$(gcloud firestore backups list --location='-' --format="value(snapshotTime)" | sort -r | head -1)
  echo "$LAST — منذ $(( ( $(date -u +%s) - $(date -u -d "$LAST" +%s) ) / 3600 )) ساعة"
  ```
  **⚠ ومصيدةٌ في القائمة:** `backups list` يُرتِّب **بالاسم (UUID) لا بالتاريخ**، فأحدثُ ما
  يظهر لك في أوّل الصفحة قد يكون أقدمَ نسخةٍ عندك. **افرزها دائماً** كما في السطر أعلاه،
  وإلّا حكمتَ بأنّ الجدولةَ متوقّفةٌ وهي تعمل — أو العكس.

  **والاختبارُ الوحيدُ المعتبَر: استعادةٌ فعلية.** نسخةٌ لم تُستعَد ليست نسخةً بل افتراضٌ
  أنّ الملفَّ سليم. **والاستعادةُ آمنةٌ تماماً**: Firestore يُنشئ **قاعدةً جديدةً منفصلة**
  ولا يكتب فوق `(default)` بأيّ حال — فلا خطرَ على الإنتاج مهما جرى.
  ```bash
  BK=$(gcloud firestore backups list --location='-' \
        --format="value(snapshotTime,name)" | sort -r | head -1 | awk '{print $2}')
  gcloud firestore databases restore --source-backup="$BK" --destination-database=restore-test
  # والحكمُ ليس رسالة «تمّت» بل وجودُ البيانات فعلاً:
  TOKEN=$(gcloud auth print-access-token)
  BASE="https://firestore.googleapis.com/v1/projects/fast-buildings/databases/restore-test/documents"
  for C in global_purchases global_contracts global_vendors global_inventory audit_log; do
    N=$(curl -s -H "Authorization: Bearer $TOKEN" "$BASE/$C?pageSize=5" | grep -c '"name"')
    printf "%-22s → %s\n" "$C" "$([ "$N" -gt 0 ] && echo "✅" || echo "❌ فارغة")"
  done
  gcloud firestore databases delete --database=restore-test   # **لا تنسَ** — تُحاسَب كتخزين
  ```
  **نتيجةُ التشغيل الفعليّ (١٤ أغسطس):** المجموعاتُ الخمسُ كلُّها فيها بيانات، و**زمنُ
  الاستعادة ≈ ١٥ دقيقة** لقاعدةٍ كاملة. فهذا هو **جوابُ «كم أحتاج لأعود لو ضاع كلُّ شيء»**:
  ربعُ ساعةٍ — مقيسٌ لا مُقدَّر. أعِد الاختبارَ كلَّ بضعة أشهرٍ وبعد أيّ تغييرٍ بنيويّ.
  **ومصيدتان صغيرتان:** جلسةُ Cloud Shell جديدةٌ تفقد `core/project` (أضِف `--project` أو
  أعِد `gcloud config set project`)، و`NOT_FOUND` عند الحذف تعني أنها **حُذفت سلفاً** لا أنّ
  الأمرَ فشل — تأكّد بـ`gcloud firestore databases list` لا بقراءة سجلّ التمرير.

- **نسخُ `Cloud Storage` — الفجوةُ وعلاجُها (أغسطس ٢٠٢٦):** نسخُ Firestore تغطّي
  **البيانات** وحدَها. أمّا **صورُ الفواتير وسنداتُ الاستلام والعقودُ الموقَّعة وصورُ
  البلاغات** فتعيش في `gs://fast-buildings.firebasestorage.app` (٧٠٨م.ب · US-EAST1)
  ولم تكن تحميها إلّا `Soft Delete` سبعةَ أيام. **والفجوةُ ليست «٧ أيامٍ قليلة» بل عدمُ
  التناظر:** المستنداتُ تُستعاد إلى يومٍ عمرُه ٩٨ يوماً وصورُها لا تعود إلّا سبعةً —
  فاستعادةٌ لِما قبل أسبوعٍ تُنتج نظاماً كاملَ البيانات **كلُّ روابطِ مرفقاتِه ميتة**.

  **⚠ والمعيارُ الحاكمُ ليس البايتات بل الـtoken.** التطبيقُ يخزّن في Firestore ناتجَ
  `getDownloadURL()` **لا المسار** (`index.html:27314` فواتير · `:21660` عهدة ·
  `stocktake.js:903` · `cleaning-operations.js:491` · `hr-payments.js:391`)، والرابطُ
  يحمل `token` يعيش في **ميتاداتا الكائن** (`firebaseStorageDownloadTokens`) لا في
  المسار ولا في المحتوى. **فاستعادةٌ تُعيد البايتاتِ وتُسقط الميتاداتا تقول «تمّت»
  وكلُّ رابطٍ في النظام يردّ `403`** — وهي أسوأُ من فشلٍ صريحٍ لأنها تُطمئن.
  ولذلك: **الاستعادةُ داخلَ الحاوية (نسخةٌ غيرُ حالية / حذفٌ ليّن) أقوى تشغيلياً من
  الخزنة** — تُعيد الكائنَ بميتاداتاه فيعمل الرابطُ فوراً بلا لمسِ مستندٍ واحد.

  **التصميمُ المعتمَد — ثلاثُ طبقات** (التفصيلُ والمقارنةُ في
  `docs/storage-backup-plan-2026-08.md`):
  · **داخلَ الحاوية:** `Versioning` + `lifecycle` يحذف غيرَ الحالية بعد **١٢٠ يوماً**
  (> ٩٨ بهامش) · `Soft Delete` **٩٠ يوماً**. **وSoft Delete وحدَه لا يسدّ الفجوةَ
  أبداً**: سقفُه ٩٠ < ٩٨ مهما رفعتَ الرقم — وVersioning بلا سقفٍ هو ما يغطّي الباقي.
  · **الخزنة:** حاويةٌ في **مشروعٍ منفصل** وإقليمٍ آخر بـSTS يومياً، **تراكميةٌ بلا
  حذفٍ في الوجهة** (التطبيقُ يحذف ملفّاتٍ فعلاً — `stocktake.js:914` ·
  `index.html:21693` — ومرآةٌ حقيقيةٌ كانت ستنشر الحذفَ فتُبطل الغرض)، وصلاحيةُ
  الوظيفة على المصدر **قراءةٌ فقط**، وسياسةُ احتجاز ١٠٠ يومٍ **غيرُ مقفلة**.
  · **المرفوض:** الفئةُ الباردة — تحمي من **لا شيء**، وتوفيرُها ≈ سنتٌ شهرياً مقابل
  حدِّ تخزينٍ أدنى ورسومِ استرجاعٍ واحتكاكٍ وقتَ الكارثة. الكلفةُ كلُّها < عُشر دولارٍ شهرياً.

  **والتنفيذُ مرحلتان لا واحدة** — وهذا قرارٌ عمليٌّ لا تجميل: المرحلةُ ١ أمران على
  الحاوية القائمة (بلا مشروعٍ ولا فوترةٍ ولا صلاحيات) تنقل الحمايةَ من ٧ أيامٍ إلى
  ١٢٠ في دقيقتين، والمرحلةُ ٢ تحتاج إنشاءَ مشروعٍ وربطَ فوترة — قرارٌ إداريٌّ قد
  يتأجّل أسابيع. **وربطُهما في أمرٍ واحدٍ كان يُبقي ٩٠٪ من الحماية رهينةَ ذاك القرار.**
  ولذلك **فحصُ وجودِ مشروع الخزنة يجري بعد تطبيق المرحلة ١ لا قبلها**، وحارسٌ في
  `hail-tests` يقارن موضعَي الأمرين في الملفّ فيمنع ارتدادَ الترتيب.
  ```bash
  bash scripts/storage-backup-setup.sh --layer1-only   # المرحلة ١ — دقيقتان، ابدأ بها
  bash scripts/storage-restore-drill.sh --layer1-only  # البروفة — قسمُها الأوّل يعمل بلا خزنة
  bash scripts/storage-backup-setup.sh --vault-only    # المرحلة ٢ — بعد إنشاء المشروع والفوترة
  bash scripts/storage-restore-drill.sh                # البروفةُ الكاملة
  bash scripts/storage-backup-check.sh                 # الصحّةُ الدورية — شهرياً
  ```
  **ومصيدةُ bash وقعتُ فيها في الإرسال:** `[ cond ] && func` مع `set -e` **تُنهي
  السكربتَ عند الشرط الكاذب** — فكان `--vault-only` يخرج بلا أن يفعل شيئاً **ويبدو
  ناجحاً**. الإرسالُ بـ`if` صراحةً، وحارسٌ يمنع العودة.

  **✅ الحالةُ القائمةُ — مُطبَّقةٌ ومقيسة (١٥ أغسطس ٢٠٢٦).** `backup-health.sh`
  يردّ **«المنظومةُ سليمة»** بكلّ فحوصه، والأرقامُ أدناه **مقيسةٌ لا منقولة**:
  · **داخلَ الحاوية:** `Versioning` مُفعَّل · `Soft Delete = 90d` **قُبلت** (فسقفُ
  الـ٩٠ صار مقيساً لا موثوقاً عن وثيقة) · `lifecycle` يحذف غيرَ الحالية بعد **١٢٠
  يوماً** — أي تجاوزَ أفقِ Firestore (٩٨) بهامش ٢٢ يوماً، وانتهاءَ عدمِ التناظر الذي
  كان يُنتج نظاماً كاملَ البيانات ميّتَ الروابط.
  · **الخزنة:** وظيفةُ النقل قائمةٌ ومفعَّلة · النقلةُ الأولى تمّت · **وعددُ كائنات
  الخزنة ≥ المصدر** (وهو الفحصُ الوحيدُ الذي يرى **خزنةً فارغة**).
  · **والبروفةُ الكاملة: ١١ ناجحة · ٠ ساقطة · ٠ متجاوَزة.** ومنها الفحصُ الحاكم:
  كُسر الرابطُ **بالاستبدال ثمّ بالحذف الكامل**، وبعد الاستعادة — من النسخة غير
  الحالية ومن الخزنة — **ردّ الرابطُ الأصليُّ نفسُه `200`** والمحتوى هو الأصلُ لا
  التالف. أي أنّ **التوكِنَ ينجو في المسلكين**، فالتعافي **لا يحتاج لمسَ مستندٍ
  واحدٍ في Firestore** — وهو ما رجّح ترتيبَ الطبقات، وقد صار مُثبَتاً لا مُرجَّحاً.

  **⚠ والمبدأُ الحاكمُ لكلّ ما سبق — كُتب بثمنٍ لا باستنتاج:** **الفحصُ يسأل «هل هو
  قائمٌ الآن؟» لا «هل الآليةُ تعمل؟».** فقد أُقفل هذا البندُ مرّةً وخزنةُ الملفّات
  **فارغةٌ فعلاً** (٧ كائناتٍ مقابل ٣٧١٠، والوظيفةُ لم تُنشأ قط) — **والبروفةُ كانت
  خضراء**، لأنها تنسخ كائنَ اختبارها **بنفسها** فتُثبت الآليةَ ولا تسأل عن التشغيل.
  ولم يكشفه تدقيقٌ ولا مراجعة، بل **الفحصُ الدوريّ**. (التفصيلُ والجذرُ في §6.)
  **فلا يُقفَل بندٌ إلا بفحصٍ يقول «قائمٌ الآن».**

  **والمرحلةُ ٢ — التصميم (١٤ أغسطس ٢٠٢٦).**
  · مشروعٌ منفصل `fast-buildings-vault` · حاوية `gs://fast-buildings-storage-vault`
  في **us-central1** (آيوا — والمصدرُ us-east1 ساوث كارولاينا، ١٥٠٠ كم ومركزا
  بياناتٍ مستقلّان كهرباءً وشبكةً ومناخاً) · `Versioning` · احتجاز **١٠٠ يوم غيرُ
  مقفل** · **لا lifecycle** فتحتفظ بكلّ شيءٍ بلا مدّة · وظيفةُ نقلٍ يوميةٌ ٠٢:٠٠ UTC
  **تراكميةٌ بلا حذفٍ في الوجهة**، صلاحيتُها على المصدر **قراءةٌ فقط**.

  **نتيجةُ البروفة الكاملة: ١١ ناجحة · ٠ ساقطة · ٠ متجاوَزة.** ومنها الفحصُ الذي
  كان المجهولَ الأخير: **نجاةُ التوكِن عبر النسخ بين حاويتين** — كُسر الرابطُ بحذفٍ
  كاملٍ من المصدر، واستُعيد الكائنُ **من الخزنة** إلى المسار نفسِه، **فردّ الرابطُ
  الأصليُّ `200`**. أي أنّ استعادةً من الخزنة لا تحتاج لمسَ مستندٍ واحدٍ في Firestore.

  **فالفجوةُ التي فتحت هذا البند مغلقةٌ بالقياس لا بالافتراض:** ٩٨ يوماً للبيانات
  و١٢٠ لصورِها، ونسختان في إقليمين ومشروعين. **والتكلفةُ سنتاتٌ شهرياً** (٧٠٨ م.ب
  في حاويتين — دون حدّ الطبقة المجانية ٥ غ.ب)، مع حدِّ تنبيهٍ مضبوطٍ على الفوترة.

  **⚠ وما يبقى واجباً دورياً — فالنسخُ يتعفّن بصمت** (تتغيّر صلاحيةٌ أو ينكسر مسارٌ
  فيبقى كلُّ شيءٍ أخضرَ حتى تحتاجَه فعلاً): **أمرٌ واحدٌ شهرياً** يجمع كلَّ شيء —
  ```bash
  bash scripts/backup-health.sh   # الفحصان + عمرُ آخر بروفة
  ```
  والبروفتان **كلَّ ٣ أشهر** في اليوم نفسِه الذي تُعاد فيه بروفةُ استعادة Firestore
  (موعدٌ واحدٌ للثلاث أسهلُ في التذكّر من ثلاثة مواعيد).

  **وكلُّ بروفةٍ ناجحةٍ تختم تاريخَها** في `_drills/` بالخزنة، و`backup-health.sh`
  يقرؤه: ⚠ عند ٩٠ يوماً و❌ عند ١٢٠. **فتحوّل «أعِدها كلَّ ٣ أشهر» من تذكيرٍ يُنسى
  إلى رقابةٍ تسقط.** وبلا هذا الأثر لا سبيلَ لأحدٍ — بعد سنةٍ أو بعد تبدّل مسؤول —
  أن يعرف متى أُثبتت النسخةُ آخرَ مرّة. **ويُختَم وضعُ العبور وحدَه** في بروفة
  Firestore: ختمُ البروفة المحلّية كان سيوثّق «إثباتاً» لشيءٍ لم يُثبَت — وذاك أسوأُ
  من غياب السجلّ، وحارسٌ يمنعه.

  **✅ وحفظُ المسار مع الرابط — مُوحَّدٌ في كلّ مواضع الرفع.** كان التطبيقُ يخزّن
  **الرابطَ وحدَه** في أكثرها بينما بعضُها يحفظ المسارَ معه. والفرقُ ليس ترتيباً:
  الرابطُ يحمل توكِناً في **ميتاداتا الكائن**، فلو ضاع في استعادةٍ يوماً صار الرابطُ
  ميتاً بلا سبيلٍ لإحيائه. **وبالمسار يصير فقدانُه إزعاجاً — نُعيد توليدَ الرابط —
  لا كارثة.** فهو **يُلغي هذا الصنفَ من الخطر لا يعالجه**، وهو آخرُ ما يجعل المنظومةَ
  لا تعتمد على نجاةِ شيءٍ لا نملكه.
  الحقولُ بتسميةٍ موازيةٍ للرابط: `invoicePhotoPath` · `photoPath` · `filePath` ·
  `storagePath`. **وصورُ قبل/بعد بمصفوفةٍ موازية** (`photosBeforePaths`) لا بتغيير
  شكل `photosBefore` — فهي مصفوفةُ روابطَ تقرؤها الواجهةُ والمطبوعات، وتحويلُها إلى
  كائناتٍ يمسّ كلَّ قارئ. **والإضافةُ لا تكسر أحداً؛ تغييرُ الشكل كان سيكسر.**
  **ويحرسه ١٤ فحصاً** فيها **حارسُ تغطية** يقارن عددَ مواضع المسار بعدد مواضع الرابط
  في كلّ ملفّ — فأيُّ رفعٍ جديدٍ يُضاف بلا مسارٍ يُسقط الفحص، ولا يعود النمطُ يُنسى
  مع أوّل شاشةٍ تُضاف.

- **تصديرُ Firestore إلى الخزنة — آخرُ طريقٍ يضيع فيه كلُّ شيءٍ دفعةً واحدة:**
  **النسخُ المُدارةُ (`firestore backups`) موردٌ داخلَ المشروع نفسِه.** ممتازةٌ ضدّ
  الخطأ البشريّ وعطلِ الإقليم — **ولا تنجو من فقدِ المشروع**. فبعد أن صارت **الصورُ**
  في مشروعين، بقيت **البياناتُ** — وهي الأثمن — في مشروعٍ واحد. **مفارقةٌ يجب أن تُغلق.**
  **وفرقٌ عمليٌّ حاسم:** النسخةُ المُدارة تُستعاد إلى **مشروعها وحدَه**؛ أمّا التصديرُ
  فملفّاتٌ محمولةٌ **تُستورَد إلى أيّ مشروعٍ وأيّ قاعدة**.

  **التصميم:** حاويةٌ **مستقلّةٌ** `gs://fast-buildings-firestore-vault` في مشروع
  الخزنة و`us-central1` · احتجاز ١٠٠ يوم · بلا lifecycle · وتصديرٌ يوميٌّ ٠٣:٠٠ UTC
  بـCloud Scheduler ينادي `:exportDocuments` بحساب خدمةٍ صلاحيتُه على المصدر
  **`importExportAdmin` فقط** (لا قراءةَ مستنداتٍ ولا كتابة).
  **ولماذا حاويةٌ مستقلّةٌ لا مجلّدٌ في خزنة الملفّات:** Firestore يولّد بادئةً زمنيةً
  تلقائيةً **فقط** حين يكون الهدفُ حاويةً بلا مسار — فمجلّدٌ داخلَ خزنةٍ كان سيكتب
  كلَّ يومٍ فوق سابقه في المسار نفسِه.
  ```bash
  bash scripts/firestore-export-setup.sh                  # الإعداد
  bash scripts/firestore-export-check.sh                  # الصحّة — شهرياً
  bash scripts/firestore-export-drill.sh --cross-project  # **البروفةُ الجوهرية**
  ```
  **⚠ ووضعُ العبور ليس ترفاً:** البندُ نشأ لحماية البيانات من **فقد المشروع**،
  واستيرادٌ ناجحٌ **داخلَ** المشروع لا يُثبت العبورَ إلى مشروعٍ آخر — مسلكان مختلفان.
  **وهو الخطأُ نفسُه الذي وقعنا فيه مع الملفّات** (نجاةُ التوكِن داخلَ الحاوية لم تكن
  تُثبت نجاتَه عبر الحاويتين). شغّله مرّةً على الأقلّ.
  **ومصيدتان يحرسهما الفحص:** (١) **حاويةُ التصدير خارجَ المشروع المصدر** — لو
  أُنشئت فيه يوماً عاد العطلُ كلُّه **بلا أن يتغيّر شيءٌ ظاهر**: التصديرُ يعمل والفحصُ
  أخضرُ والحمايةُ صفر (ولذلك يسأل الفحصُ: هل يظهر اسمُ الحاوية في **قائمة حاويات
  المشروع المصدر**؟ — سؤالٌ عن انتماءٍ لا عن حقلٍ في الوصف؛ وقائمةٌ فارغةٌ تُعامَل
  **سقوطاً** لا نجاحاً، فلا حكمَ بلا معرفة). (٢) **تصديرٌ
  انقطع في منتصفه يترك مجلّداً بلا `overall_export_metadata`** — ويبدو في القائمة
  تصديراً سليماً حتى تحاول استيرادَه.

  **⚠ ومصيدةٌ ثالثةٌ وقعنا فيها فعلاً — الهويّةُ التي تنفّذ غيرُ التي تطلب:**
  `firestore export` **لا يعمل بحساب الخدمة المنادي** بل **بوكيل خدمة Firestore**
  الخاصّ بالمشروع (`service-<NUM>@gcp-sa-firestore.iam.gserviceaccount.com`).
  فمنحُ الطالبِ وحدَه يردّ `PERMISSION_DENIED: Service account does not have access
  to ... file` — **ورسالةٌ تقول «حساب خدمة» تجعلك تُراجع حسابَك الذي منحتَه للتوّ**،
  وهي تعني وكيلاً آخرَ لم تسمع به. والاستيرادُ مثلُه بوكيل **مشروع الهدف** — فبروفةُ
  العبور تمنحه قبل أن تستورد. **والقاعدةُ العامة: في GCP اسأل «من ينفّذ؟» لا «من أنا؟».**

  **✅ مُطبَّقٌ ومُثبَتٌ باستيرادٍ في مشروعٍ آخر (١٥ أغسطس ٢٠٢٦).** التصديرُ الأوّل
  اكتمل (`overall_export_metadata` موجود)، **وبروفةُ العبور ٥ ناجحة · ٠ ساقطة**:
  أُنشئت `projects/fast-buildings-vault/databases/export-drill` — **قاعدةٌ في مشروع
  الخزنة لا في المصدر** — واستُورد إليها التصديرُ، **والمجموعاتُ الخمسُ كلُّها فيها
  بيانات** (`global_purchases` · `global_contracts` · `global_vendors` ·
  `global_inventory` · `audit_log`)، ثمّ حُذفت قاعدةُ البروفة.

  **فلم يعد شيءٌ في المنصّة رهينةَ مشروعٍ واحد:** الصورُ في مشروعين، والبياناتُ في
  مشروعين، **وكلاهما مُثبَتٌ باستعادةٍ فعليةٍ عابرةٍ للمشروع لا بافتراض.**

- **نشرُ القواعد والتحقّقُ الحيُّ منها (خطوةٌ يدويةٌ لا يفعلها الدمج):**
  الملفُّ في المستودع **نصٌّ لا أثرَ له** — قاعدةُ البيانات تحمل نسختَها الخاصةَ على
  خوادم Google. فأيُّ تعديلٍ على `firestore.rules` لا يسري حتى يُنشَر:
  ```bash
  firebase deploy --only firestore:rules --project fast-buildings
  ```
  أو نسخُ الملفّ في **لوحة Firebase ← Firestore ← Rules ← Publish**. والنشرُ **يستبدل
  المجموعةَ كاملةً**، فقارِن نسخةَ اللوحة بالمستودع أوّلاً: تعديلٌ أُجري في اللوحة ولم
  يُنقَل إلى المستودع يُمحى بالنشر. والتراجعُ نقرتان من **History** في التبويب نفسِه.

  **والتحقّقُ بعد النشر يجري من Console التطبيق لا من اللوحة** — زرُّ «Develop & Test»
  في اللوحة يفتح **Emulator Suite** لا الـPlayground. ثلاثةُ أسطرٍ تُثبت الطرفين:
  ```js
  // (١) هل التوكن يحمل الدور أصلاً؟ بلا هذا يُردّ كلُّ شيءٍ لسببٍ آخر فتُخدَع بـ✅ كاذبة
  firebase.auth().currentUser.getIdTokenResult().then(t => console.log(t.claims.role));
  // (٢) يجب أن يُردّ — عقدٌ يولد سارياً، ممنوعٌ حتى على الأدمن
  db.collection('global_contracts').doc('RULES-TEST-1').set({status:'ctr_active'})
    .catch(e => console.log(e.code));           // permission-denied
  // (٣) ويجب أن يُقبل — وإلا فالقاعدةُ تعطيلٌ لا حراسة
  db.collection('global_contracts').doc('RULES-TEST-1').set({status:'ctr_pending_signature'});
  ```
  **مصيدتان وقعنا فيهما:** (أ) معرّفُ المستند المطابقُ لـ`__…__` **محجوزٌ في Firestore**،
  فيُردّ بـ`invalid-argument` قبل أن يصل إلى القواعد أصلاً — فبدا الاختبارُ ناجحاً وهو
  لم يُجرَ. (ب) اختبارُ المنع وحدَه لا يكفي: لو رُدّ لانقطاع مصادقةٍ لَبدا كأنّ القاعدةَ
  تعمل. **الاختبارُ الثالث هو الحَكَم**: نفسُ المسار ونفسُ الدور، والفرقُ كلمةٌ واحدةٌ
  في الحالة — فأن يُردَّ أحدُهما ويُقبل الآخرُ يعني أن القاعدةَ تقرأ المحتوى وتحكم عليه.
  ومستندُ الاختبار الذي يُنشئه السطرُ الثالث **يلزم حذفُه من اللوحة**: القواعدُ تمنع
  الحذفَ من العميل نهائياً، واللوحةُ وحدَها تتجاوزها.
- **الحالة الحالية:** ✅ **2327/2327** فحصاً ناجحة على `v18.9.2688` (+ توجيه واتساب 29/29، ورحلة المتصفّح 61/61، والسيناريوهات المنطقية 58/58، والعقد القائم على الأداء 48/48، وحرّاسُ المصادقة 12/12، وشاشةُ المستخدمين 13/13، والتقرير المصوّر المجمّع 13/13، وطابور الرفع المؤجَّل 15/15، ودورة حياة طلب الشراء 57/57، ووحدة التعاقدات 274/274، وقواعد Firestore على المحاكي **166/166**). **ومركزُ العمليات 72/72 في أيام العمل و70/72 يومَي الجمعة والسبت** — قنبلةٌ زمنيةٌ في الفحص نفسِه (بذرةٌ بالأيام التقويمية وشرائحُ بأيام العمل)، مُثبَتةٌ على `main` ومقيَّدةٌ في §6 بانتظار تغييرٍ مستقلّ. **شُغِّلت كلُّها فعلاً** — لا نقلاً عن قيدٍ سابق.
- **⚠ ولا تُحفَر تواريخُ في توقُّعٍ زمنيّ:** حالةُ الوثيقة (سارية · توشك · منتهية)
  واستحقاقُ الخطة دوالُّ في «اليوم» — ففحصٌ يقارن تاريخاً محفوراً بتوقُّعٍ محفورٍ يمرّ
  اليومَ ويسقط بعد أسابيع **لأن الزمنَ تقدّم لا لأن الكودَ ارتدّ**، وسقط اثنان بهذا فعلاً
  (قيدُ §6). القاعدة: المرساةُ إزاحةٌ مسمّاةٌ عن `Date.now()`، والتوقُّعُ **يُحسَب** بنفس
  قاعدة الدالّة لا يُحفَر رقماً. يحرسه `browserCheckTimeBombs` في `hail-tests`.
- **وضع التشخيص (`?diag=1` / `?diag=2`) — لعزل انهيار عرضٍ لا يلتقطه JS:** انهيار الـ
  renderer/GPU («Aw, Snap») يقتل التبويب قبل أي معالج أخطاء، فلا يُسجَّل. لذلك يسجّل
  `_diagMark` مراحلَ الدخول (`loadData` → `enterApp` → `renderCurrentPage` →
  `renderDashboard`) في `localStorage` مع الذاكرة المستهلَكة عند كلٍّ — **يبقى بعد موت
  التبويب**. اقرأه من الـ console: **`hailDiagLog()`** (وسجلّ الإقلاع: `hailBootLog()`).
  `?diag=1` يتخطّى لوحة المعلومات كاملةً (عزلٌ سريع + وصولٌ مؤقّت للنظام)، و`?diag=2`
  يُشغّلها مع علاماتٍ دقيقة عند كل جزء لتحديد الجزء المنهار. كلاهما **مسوَّر خلف علامة
  الرابط** — صفر أثرٍ على المستخدم العادي.
- **تدقيق يوليو ٢٠٢٦ (العمليات الحسابية):** ✅ **مُكتمل — ١١/١١ بنداً مُغلقاً** (لا بنود مفتوحة).
  التقرير الكامل ونطاق كل بند في `docs/calc-audit-2026-07.md`. غطّى الحرج (C1) والعالي
  (H2/H3/H4/H5/H6) والمنخفض (L7–L11)، ودُمج كلٌّ في `main` عبر PRs #107–#113، مع حرّاس
  اختبار تمنع تكرار كل صنف من أخطاء التجميع.
- **ثابت الإصدار `APP_VERSION`:** في `index.html` (بحث عن `const APP_VERSION`). يُعرَض في
  تذييل تسجيل الدخول تلقائياً. **عند أي تعديل يُرفع الإصدار** (لاحقة حرفية: ...qf → qg)،
  وتُرفع معه cache-busters كل الوحدات في نهاية `index.html` (`?v=...`) — يفحص hail-tests
  تطابقها جميعاً مع `APP_VERSION`، ووجود سطر إصدار مطابق في موضعين.

## 6) سجل موجز بأهم التعديلات

- **v18.9.2688 — تحديثُ NOTES مرجعاً: ما استقرّ من v18.9xa/xb يصعد من §6 إلى الأقسام الدائمة.**
  **الطلب (المالك): «حدّث notes».** و§6 يؤرّخ التغييرَ تباعاً، لكنّ NOTES **مرجعٌ** —
  فمن يقرأ غداً لا يفتش في سجلٍّ زمنيّ عن قاعدةٍ سارية. رُفع إلى مواضعها الدائمة:
  (١) **§3** ثلاثُ قواعدَ قائمة: هويةُ بند الطلب (المرساةُ تسبق النصّ، و`requestedItemName`
  يحفظ ما طُلب، وبلا مرساةٍ لا تخمين) · البتُّ في البند الإضافي مرحلتان بحالةِ طلبٍ
  واحدة (**من يضيف مرحلةً فليكتب `extraStage`، وإلّا صمت التنبيه بلا أثرٍ ظاهر**) ·
  وعتبةُ التنفيذيّ تُقاس على الطلب لا على البند (والقياسُ على البند بابٌ خلفيّ).
  (٢) **§4** الدوالُّ المركزية الستّ مع نهيها الصريح: `_poShownName`/`_poShownCode`/
  `_poReqName` · `poExtraStage` · `_poExtraCeoWhy` · `routingKey`.
  (٣) **§2** وصفُ `wa-routing-check.mjs` صار يذكر `routePurchase` ومرحلتَي البتّ.
  **و«الحالة الحالية» صُحّحت — وشُغِّلت كلُّها فعلاً لا نقلاً:** كانت مجمَّدةً على
  `v18.9.2637` بـ2202 فحصاً وتوجيهِ واتساب 19/19، وهما رقمان ماتا منذ إصدارات.
  الآن `v18.9.2688`: `hail-tests` **2327/2327** · توجيه واتساب **29/29** · التعاقدات
  **274/274** · قواعد Firestore على محاكٍ حقيقيّ **166/166** · رحلة المتصفّح **61/61** ·
  السيناريوهات **58/58** · دورة حياة الطلب **57/57** · الأداء **48/48** · المستخدمون
  **13/13** · المصادقة **12/12** · التقرير المصوّر **13/13** · طابور الرفع **15/15**.
  **ومركزُ العمليات 70/72** — شُغِّل يومَ سبت، وهو العددُ الموثَّق لعطلة الأسبوع (القنبلةُ
  الزمنيةُ في الفحص نفسِه لا في الكود، ومقيَّدةٌ أصلاً).

- **v18.9.2686 — البتُّ في البند الإضافي مرحلتان وحالةُ الطلب واحدة: التنبيه يصل الخطأ ولا يصل الصواب.**
  **البلاغ (المالك) على `PO-202608-0160`:** «لم يصل تنبيه للمدير التنفيذي بأن طلب شراء
  تخطّى العتبة» — «والمفترض أن يكون اعتماد التنفيذي **لتخطّي الطلب ككل** لا لبندٍ محدّد،
  فليس شرطاً أن يكون بند المحبس هو ما جعل الطلب يتخطّى العتبة».
  **الجذر — حالةٌ واحدةٌ تغطّي دورين:** البتُّ في البند الإضافي مرحلتان (`pending_pm`
  لمدير المشاريع ثم `pending_ceo` للتنفيذي فوق العتبة)، لكنّ **حالةَ الطلب تبقى
  `pending_extra` في كلتيهما**. والتوجيهُ الخادميّ (`functions/lib/purchases.js`) لا
  يرسل إلا حين تتغيّر الحالة — `if (prevStatus === newStatus) return`. فالنتيجة ثلاثيّة:
  (١) `PO_ROUTING.pending_extra` كان يقصد **التنفيذيّ** — فتصله رسالةٌ لحظةَ الدخول وهو
  **لا يملك زرّ البتّ أصلاً** (`canDecideExtra` يشترط مدير المشاريع عند `pending_pm`)؛
  (٢) **مديرُ المشاريع — أولُ من يبتّ — لا يصله شيء**؛
  (٣) وحين يصير الدورُ للتنفيذيّ **حقاً**: صمتٌ تامّ، لأن الحالة لم تتغيّر.
  فالتنفيذيُّ عرف من الشاشة لا من تنبيه.
  **العلاج — التوجيهُ على «الدور» لا على «الحالة»:**
  (١) `poExtraStage(p)` تُشتقّ مرحلةَ البتّ من البنود المعلّقة (**الأقدمُ دوراً أولاً**:
  بندٌ واحدٌ عند مدير المشاريع يمنع استدعاء التنفيذيّ)، وتُكتب في `p.extraStage` عند
  التدقيق ومع كل بتّ، وتُصفَّر بعد آخر قرار.
  (٢) `routingKey(po)` يُلحق المرحلةَ بالحالة عند `pending_extra` وحدها، والمقارنةُ
  صارت على المفتاح لا على الحالة — فيتحرّك التوجيهُ مع الدور ولو ثبتت الحالة، وبقيةُ
  المسار بلا حرفٍ واحدٍ جديد (المفتاح = الحالة). والمجرَّدُ يبقى ارتداداً للطلبات
  الأقدم بلا الحقل — **ولمدير المشاريع**، لأنه أولُ من يبتّ دائماً.
  (٣) **والنصُّ يقول الحقيقة:** `_poExtraCeoWhy(p)` تُصرّح بالرقم وبمن يخصّه — «إجمالي
  الطلب 3,483.71 ر.س تخطّى عتبة 2,000 — **العتبة على الطلب كلِّه لا على هذا البند
  وحده**». والبندُ هنا **بأربعة ريالات**؛ النصُّ القديم كان يقرأ كأنّ الاعتراض عليه.
  تظهر في نافذة الاعتماد وفي بطاقة البنود الإضافية وفي القيد والإشعار.
  **وما لم يتغيّر — لأنه كان سليماً:** العتبةُ كانت **تُقاس على إجمالي الطلب** أصلاً
  (`poExtraNextStatus` ⇐ `getPOTotal`)، واعتمادُ **الطلب ككل** مضمونٌ عند بوّابة «إرسال
  للمالية» عبر `poCEOCovers` (ختمُ التنفيذيّ مقرونٌ بمبلغه، فالطلبُ الذي صار أعلى ممّا
  اعتمده يعود إليه إلزاماً). فالخللُ كان في **التوقيت والتوجيه والوضوح** لا في الرقابة.
  **التحقق:** `wa-routing-check` **29/29** (قسمٌ جديد ٥ بتسعة فحوصٍ **تنفيذاً**: أولُ
  البتّ ⇒ مدير المشاريع · انتقالُ الدور يُرسِل رغم ثبات الحالة · كتابةٌ بلا انتقالٍ
  لا تُرسِل · التوافقُ الرجعيّ) · `hail-tests` **2327/2327** (قسمٌ جديد 20ب بـ21 حارساً)
  · `po-lifecycle-check` **57/57** · `ui-walkthrough` **61/61** · `browser-scenarios`
  **58/58** — شُغِّلت كلُّها فعلاً.
  **بعد الدمج — إجراءٌ مطلوب:** التعديلان (١) و(٣) يعملان بمجرّد التحديث، أمّا التوجيهُ
  الخادميّ فلا يعمل **حتى تُنشَر دوالُّ Firebase** (`functions/`). قبل النشر يبقى
  السلوكُ القديم في الواتساب (الجرسُ الداخليُّ يعمل على أي حال).

- **v18.9.2684 — بندُ الطلب يُربط بالكتالوج عند التدقيق ويظلّ يُعرض باسمه القديم بلا كود.**
  **البلاغ (المالك) على `PO-202608-0160`:** بنودٌ لم تكن في كتالوج الأسعار، فأدخل
  مسؤولُ المستودعات أسماءها **كما في الفاتورة** وأضافها للكتالوج فأخذت أكواداً —
  «والمفروض ياخد كود، لكن يظل يظهر في طلب الشراء باسمه القديم المُدخَل عند إنشاء
  الطلب لا الاسم كما هو في الفاتورة». وفعلاً: في وثيقة الطلب ١٢ بنداً بعمود كودٍ «—».
  **الجذر:** الربط كان يُثبّت **المرساة وحدها**. `waPickCat`/`waOpenNewCatItem`
  تكتبان `itemId` على صفّ الاستلام، ويكتبها الحفظ على بند الطلب (`itemId: _anchor`)
  — بينما `itemName`/`itemCode` تمرّان في `...it` بلا لمس. فانقسم النظام نصفين:
  **المخزونُ والسجلّ** يشتقّان الاسم والكود من المرساة (`_resolveItemName`
  و`_resolveItemCode` منذ v18.9nr/v18.9nw)، و**طلبُ الشراء وطباعتُه** يقرآن الحقل
  الخام المجمَّد لحظةَ الإنشاء. الاسم الصحيح كان موجوداً في الوثيقة — ولا أحد يعرضه.
  **العلاج — من المرساة يُشتقّ المعروض، وعندها يُثبَّت المخزَّن:**
  (١) `_poShownName`/`_poShownCode` تُشتقّان من `itemId` — تقرؤهما تفاصيلُ الطلب
  ونسختا الطباعة (بأسعار وبلا أسعار)، **فتُشفى الطلباتُ المحفوظة فوراً بلا كتابة**.
  (٢) عند التدقيق يُثبَّت الاسمُ والكود على `p.items[i]` وعلى صفّ السند معاً — فيتّسق
  المخزَّن مع سجلّ المخزون المكتوب من الصفّ نفسه.
  (٣) **وما طلبه المشرف لا يضيع:** يُحفَظ في `requestedItemName` عند أول دهس ويظهر
  سطراً تحت الاسم («طُلب باسم: …») في الشاشة والطباعتين. وعند الاستبدال لا يُكرَّر —
  شارةُ «⇄ استُبدل: كذا ← كذا» تقول القصة كاملةً.
  (٤) نافذةُ تعديل المسؤول تفتح البند باسمه المعياريّ، وإلا أعاد أوّلُ حفظٍ منها
  تجميدَ الاسم القديم فوق المرساة.
  **ولا تخمين:** البند بلا مرساة يبقى على نصّه حرفياً — لا مطابقةَ بالاسم (الآلة لا
  تميّز «لمبة» من «كشاف»)، وبابُ «🔗 اربط» قائمٌ في شاشة التدقيق لمن يريد ربطه.
  **والهوية لا تنكسر:** `_poItemKey` يقدّم `itemId` على الاسم، والاسمُ لا يُلمس إلا
  مع وجود مرساة — فاصطفافُ `auditItems`/`grnDocs` و`_waPrevRcv` سليمٌ كما هو.
  **التحقق:** `hail-tests` **2306/2306** (منها قسمٌ جديد 21ج بـ17 فحصاً: الاشتقاق من
  المرساة · «طُلب باسم» بلا تكرار · الاستبدال · البند بلا مرساة · حرّاسُ المسار في
  العرض والطباعة والتخزين) · `po-lifecycle-check` **57/57** · `ui-walkthrough`
  **61/61** · `browser-scenarios` **58/58** — شُغِّلت كلُّها فعلاً.
  **بعد الدمج:** الطلباتُ القديمة تُعرَض صحيحةً فوراً؛ ويُثبَّت المخزَّن عند أوّل
  تدقيقٍ أو حفظٍ من نافذة تعديل المسؤول. وأيُّ بندٍ ما زال بلا كود = **لم يُربط**
  بالكتالوج أصلاً (لا خللَ عرض) — يُربط من شاشة التدقيق أو «📚 كتالوج» في تعديل الطلب.

- **v18.9.2682 — تحديثُ NOTES مرجعاً لا سجلّاً: §5 يقول «ما هو قائمٌ الآن» لا «كيف وصلنا».**
  **الطلب (المالك): «حدث notes».** و§6 يؤرّخ التغييراتِ تباعاً، لكنّ NOTES **مرجعٌ
  دائم** — ومن يقرأ §5 غداً ليسأل «ما وضعُ نسخنا؟» كان عليه أن يمرّ بسرديةٍ كاملة:
  تصميمٌ ← «مكتوبٌ غيرُ مُطبَّق» ← «مُطبَّقٌ ومُثبَت» ← **تصحيحٌ: لم يكن مقفلاً** ←
  «أُغلق فعلياً». **أركيولوجيا في موضعِ مرجع.**
  **العلاج:** جُمِّعت في §5 **«الحالةُ القائمة»** بأرقامها المقيسة (المددُ الثلاث ·
  الخزنةُ ووظيفتُها · البروفةُ ١١/١١ · نجاةُ التوكِن في المسلكين)، **وأُبقيت المصائدُ
  لأنها تشغيليةٌ** لا تاريخية، **ونُقلت قصّةُ التصحيح إلى §6 حيث موضعُها**.
  **ولم يُحذف الدرسُ بل رُفع:** بقي في §5 **المبدأُ الحاكم** — «الفحصُ يسأل هل هو
  قائمٌ الآن؟ لا هل الآليةُ تعمل؟» — بجملةِ سببه (خزنةٌ فارغةٌ وبروفةٌ خضراء)، فمن
  يقرأ يعرف **لِمَ** القاعدةُ لا أنّها قاعدة.
  **وأُضيف `scripts/` إلى دليل الملفّات §2** — سبعةُ سكربتاتٍ لم يكن لها سطرٌ فيه
  رغم أنها الآن جزءٌ من تشغيل المنصّة. **وصُحّح وصفٌ متقادم**: فحصُ انتماء حاوية
  التصدير صار يسأل عن **قائمة حاويات المشروع** لا عن رقمٍ في الوصف.
  **لا تغييرَ في أيّ كود — توثيقٌ فقط** (ورفعُ الإصدار التزاماً بقاعدة «أيّ تعديل»).
  hail-tests **2289/2289**.

- **v18.9.2680 — ✅ منظومةُ النسخ مكتملةٌ ومُثبَتة: «المنظومةُ سليمة» من الفحص الشامل.**
  **النتيجةُ المقيسة (١٥ أغسطس ٢٠٢٦).** بعد إنشاء وظيفة النقل وإتمام النقلة الأولى،
  عاد `backup-health.sh` **«✅ المنظومةُ سليمة»**: فحصُ الملفّات كاملاً (ومنه **عددُ
  كائنات الخزنة ≥ المصدر** ووجودُ الوظيفة وعمرُ آخر نقلة) · وفحصُ البيانات **٥/٥** ·
  وعمرا البروفتين **٠ يوماً**.
  **وبهذا اكتمل ما بدأ أمس:** الصورُ والبياناتُ لكلٍّ نسختان في مشروعين وإقليمين،
  كلاهما مُثبَتٌ باستعادةٍ فعليةٍ عابرةٍ للمشروع، وفوقهما **رقابةٌ شهريةٌ تسقط** إن
  تعفّن شيء، **وتطبيقٌ لم يعد يعتمد على نجاةِ ما لا يملكه** (المسارُ مع الرابط).
  **والخلاصةُ الأثمنُ من الإعداد نفسِه:** أُحصيت في هذا البند **عشرةُ أعطالٍ** كلُّها
  في **أدوات القياس والإعداد** لا في المنظومة، وصنفُها واحد: **مخرَجٌ لا يطابق
  الواقع.** ثلاثةٌ قرأت اسمَ حقلٍ أو دلالةَ نمطٍ فحكمت بما لا تعرف · واثنان أعلنا
  «تمّ» على إجراءٍ لم يقع أو توقّف صامتاً · وواحدٌ كسرته ميزةٌ أُضيفت في مكانٍ آخر ·
  وثلاثةٌ كانت رسائلُها تصف عَرَضاً لا سبباً. **ونجونا في أكثرها بالمصادفة: كلُّها
  خرجت سقوطاً كاذباً لا نجاحاً كاذباً.** ولذلك صار المبدأُ المكتوب: **الفحصُ يسأل
  «هل هو قائمٌ الآن؟» لا «هل الآليةُ تعمل؟»** — وهو الذي كشف أنّ الخزنةَ كانت فارغةً
  بعد أن قُيّد إقفالُ البند. hail-tests **2289/2289**.

- **v18.9.2673 — صلاحياتٌ مؤشَّرةٌ لا تفعل شيئاً: نافذةٌ تَعِد بما لا تملك، ومفتاحٌ واحدٌ يعمل.**
  **البلاغ (المالك).** نافذةُ «صلاحيات ومشاريع» لمسؤول الموارد البشرية تعرض عشرَ
  خاناتٍ **كلُّها مؤشَّرة** (البلاغات · الأصول · المشتريات · التقارير …) — «لكن
  فعلياً لا يظهر عنده غير صفحة الموارد البشرية». وطلبٌ معه: أن يُمنح **طلبَ شراء**.
  **الجذر.** تلك المفاتيحُ تحكم صفحاتِ **وضع المشروع**، ودورُه من أدوار الوضع
  المركزيّ (`_isGlobalOnlyRole`) التي **لا ترى شاشةَ مشروعٍ أصلاً** — وسايدبارُه
  تحكمه قاعدةُ CSS بدورِه لا بصلاحياته. فالخاناتُ زينةٌ بلا أثر. **وخانةٌ لا أثرَ
  لها أسوأُ من غيابها**: تُوهم المسؤولَ أنه منح ما لم يمنح، فيبحث عن العطل في
  المستخدم أو المتصفّح بينما لا عطلَ أصلاً — النافذةُ تصف عالَماً غيرَ الواقع.
  **العلاج شقّان:**
  (١) **لا تُعرض خانةٌ لا تعمل** — `_permKeysForUser` تشتقّ الخاناتِ من الدور:
  الموارد البشرية ترى «طلب شراء» وحدَه، وبقيةُ أدوار الوضع المركزيّ لا ترى خانةً،
  **مع لافتةٍ تشرح السبب** (لا حذفٌ صامتٌ يُربك). والحفظُ **لا يمسّ مفتاحاً لم
  تُعرض خانتُه** — ولولا ذلك لمحا قيمةً سابقةً لم يرَها المسؤولُ ولم يقصد تغييرَها.
  (٢) **مفتاحٌ مانحٌ جديد: `poRequest` «طلب شراء»** (الافتراض: ممنوع، فلا فتحَ
  بأثرٍ رجعيّ لأحد). يفتح بابين لا مساراً: «طلب شراء جديد» و«طلبات الشراء»
  **مقصورةً على ما أنشأه هو** — لا تقاريرَ ولا سجلَّ تدقيقٍ ولا رقابةً ماليةً ولا
  بنداً مستعاضاً ولا مخزوناً. ويسلك طلبُه مسارَ الاعتماد المعتاد بلا استثناء.
  **وقيدان يستحقّان البقاء:**
  • **الحجبُ في `_blockedPagesForUser` لا في CSS وحدَه** — إخفاءُ الزرِّ بابٌ يبقى
  مفتوحاً بالعنوان. فالقاعدةُ الجديدة تحجب مسارَ الشراء كلَّه عن الدور، والمفتاحُ
  يرفع الحجبَ عن صفحتين اثنتين لا غير.
  • **القائمةُ واللوحةُ والعدّادُ من `_poVisibleList` وحدَه** — ولولا توحيدُه لعرضت
  القائمةُ طلبَه وحدَه بينما تُصرّح البطاقاتُ فوقها بإجمالي مشتريات الشركة:
  **تضييقٌ في مكانٍ وتسريبٌ في آخر**. (والتضييقُ عرضيٌّ؛ حدُّ البيانات في القواعد.)
  التحقّق: hail-tests **2289/2289** (١٩ حارساً جديداً يُنفّذ البوّابةَ على مستخدمين
  حقيقيين لا يقرأ سطورَها) · وفحصٌ في متصفّحٍ حقيقيّ: النافذةُ تعرض خانةً واحدةً
  للموارد البشرية وعشراً للمشرف، والسايدبار يفتح الزرّين بالمفتاح ويقفل الأربعةَ
  الأخرى ويقفل المجموعةَ كلَّها بلا مفتاح.

- **v18.9.2675 — خروجٌ صامتٌ بعد منحٍ ناجح: `pipefail` في إسنادِ متغيّرٍ لا يطبع شيئاً.**
  **البلاغ (المالك).** بعد `--add-missing` صارت الصلاحياتُ كاملةً (`Missing roles: []`
  للمستخدم ولحساب الخدمة)، **ثمّ خرج السكربتُ بلا رسالةٍ ولا خطأ** قبل إنشاء الوظيفة.
  **الجذر:** بريدُ حساب النقل كان يُستخرج بأنبوب
  `curl … | grep -o … | cut`. ومع `set -euo pipefail` **فشلُ أيّ حلقةٍ يُنهي
  السكربتَ فوراً** — و`die` التي بعده **لا تُنفَّذ أصلاً**، لأن الإخفاقَ يقع في
  **إسنادِ المتغيّر** لا في أمرٍ مستقلّ. فلا رسالةَ ولا شفرةَ خطأٍ مفهومة.
  **وهو الوجهُ المقابل لعطل `authorize`:** ذاك إجراءٌ لم يقع ومخرَجُه يقول «تمّ»،
  وهذا إجراءٌ توقّف **ولم يقل شيئاً**. **وكلاهما أخطرُ من فشلٍ صريح.**
  **العلاج:** البريدُ **يُشتقّ من رقم المشروع** بصيغةٍ ثابتة
  (`project-<NUM>@storage-transfer-service…`) — بلا شبكةٍ ولا تحليلِ JSON ولا أنبوب.
  وحارسٌ يمنع العودةَ إلى الأنبوب. **والقاعدة: في سكربتٍ بـ`pipefail`، لا تُسنِد من
  أنبوبٍ قد تفشل إحدى حلقاته — الصمتُ هناك ليس نجاحاً.** hail-tests **2269/2269**.

- **v18.9.2674 — ★★ `transfer authorize` يُبلّغ ولا يُصرِّح: أمرٌ اسمُه «فوِّض» لا يفوّض.**
  **الاكتشاف.** بعد تفعيل `cloudresourcemanager`، مضى الأمرُ وطبع:
  `Google-managed transfer account … has roles: []` ·
  `Missing roles: ['roles/storage.admin','roles/storagetransfer.serviceAgent']` ·
  `Rerun with --add-missing to add missing roles.` — **وانتهى بنجاح.**
  **أي أنّ الأمرَ تقريرٌ لا إجراء.** واسمُه `authorize` يوحي بالعكس تماماً، وقد
  استعملتُه في السكربت بلا الراية فبقي **حسابُ خدمة النقل بلا صلاحيةٍ واحدة** —
  والسكربتُ يمضي كأنّ شيئاً لم يكن. **هذا هو الصنفُ الأخطر في هذا الملفّ كلِّه:
  إجراءٌ لم يقع ومخرَجٌ يقول «تمّ».**
  **ونقصٌ ثانٍ كشفه التقرير:** `roles/storagetransfer.serviceAgent` على **مستوى
  المشروع** — ومنحُ الحاويتين وحدَه (كما كان السكربت) **يترك الوظيفةَ عاجزةً**.
  **العلاج:** `--add-missing`، وحارسٌ يشترطها. **والقاعدة: لا تحكم على أمرٍ باسمِه
  ولا بشفرة خروجه — اقرأ ما طبعه.** hail-tests **2268/2268**.

- **v18.9.2673 — إصلاح: `transfer authorize` يحتاج `cloudresourcemanager`، ورسالتُه تدلّ على الصلاحيات.**
  **البلاغ (المالك).** إعادةُ `--vault-only` بعد كشفِ الخزنةِ الفارغة سقطت عند
  الخطوة (٤): `Cloud Resource Manager API has not been used in project
  fast-buildings-vault ... SERVICE_DISABLED`.
  **الجذر:** السكربتُ يُفعّل `storage` و`storagetransfer` في مشروع الخزنة **ولا
  يُفعّل `cloudresourcemanager`** — و`transfer authorize` يقرأ **سياسةَ صلاحيات
  المشروع** (`getIamPolicy`) ليمنح وكيلَ النقل.
  **ومصيدةُ الرسالة:** تبدأ بـ`does not have permission to access projects
  instance` — **فتُرسل التشخيصَ إلى الصلاحيات**، والسببُ الحقيقيُّ في ذيلها:
  واجهةٌ معطَّلة. **وهي رابعُ رسالةٍ اليومَ تصف عَرَضاً لا سبباً** بعد «Service
  account» الغامضة و«لا صلاحية أو قد لا يكون موجوداً».
  **العلاج:** الواجهاتُ الثلاثُ تُفعَّل **معاً** في أوّل خطوة، فلا يُكتشَف النقصُ عند
  رابعِ خطوةٍ بعد إنشاء الحاوية والاحتجاز. وحارسٌ يشترطها. hail-tests **2267/2267**.

- **v18.9.2672 — ⛔ تصحيحُ سجلّ: خزنةُ الملفّات كانت فارغةً وقد قُيّد أنها مُثبَتة.**
  **الاكتشاف.** `backup-health.sh` — في أوّل تشغيلٍ حقيقيٍّ له — ردّ ثلاثةَ سقوطاتٍ
  سببُها واحد: **وظيفةُ النقل غيرُ موجودة**، ولا نقلةَ منتهية، **وعددُ كائنات
  الخزنة ٧ مقابل ٣٧١٠ في المصدر** (والسبعةُ آثارُ بروفات). أي أنّ **نسخةَ الملفّات
  خارجَ الحاوية لم تكن قائمةً أصلاً**، وقد قُيّد في §5 أنها «مُطبَّقةٌ ومُثبَتة».
  **الجذر:** `--vault-only` **علّق عند الخطوة (٤)** بسؤالٍ تفاعليٍّ مكتوم، فأُوقف
  بعد إنشاء الحاوية والاحتجاز وقبل إنشاء الوظيفة. **ثمّ أُصلح سببُ التعليق ولم
  يُعَد تشغيلُ الخطوة** — انتُقل إلى البروفة مباشرةً، وهي خضراء، فقُيّد الإقفال.
  **ولماذا لم تكشفه البروفة — وهو الدرسُ الأهمّ:** البروفةُ **تنسخ كائنَ اختبارها
  بنفسها**، فتُثبت آليةَ النسخ والاستعادة ونجاةَ التوكِن — **ولا تسأل هل الجدولةُ
  موجودةٌ أصلاً**. **إثباتُ الآلية ليس إثباتَ التشغيل**، وبروفةٌ خضراءُ على مسارٍ
  يدويٍّ لا تقول شيئاً عن وظيفةٍ لم تُنشأ.
  **وما أمسكها هو الفحصُ الدوريّ** — الذي أُضيف بوصفه «رقابةَ إيقاع» فإذا به يكشف
  فجوةً في التطبيق نفسِه. **وهذا الفرقُ العمليُّ بين توثيقٍ يقول «تمّ» وأداةٍ تسأل
  «هل هو قائمٌ الآن؟»** — ولذلك أُضيف حارسان يشترطان بقاءَ فحصِ وجودِ الوظيفة وعدِّ
  كائنات الخزنة، فهما وحدَهما ما يرى **خزنةً فارغة**. hail-tests **2266/2266**.

- **v18.9.2670 — ميزةُ الرقابة كسرت كاشفَ التصدير: `_` يسبق الأرقام في الفرز.**
  **البلاغ (المالك).** فحصُ التصدير ما زال ٣ ناجحة · ٢ ساقطة بعد إصلاح نمط `**`.
  **الجذر — ومن صنعنا:** ختمُ البروفات (المضاف قبل ساعة) ينشئ `_drills/` **في حاوية
  التصدير نفسِها**. وكاشفُ «أحدث تصدير» يسرد المجلّدات ويفرزها تنازلياً — و`_`
  (0x5F) **يسبق الأرقام** (0x32)، فصار الكاشفُ يشير إلى **مجلّد الختم**: لا ملفَّ
  فهرسَ فيه (❌) ولا تاريخَ يُفكَّك (❌ الثاني). **ميزةٌ في مكانٍ أسقطت قارئاً في
  مكانٍ آخر بلا صلةٍ ظاهرة** — وهو أخطرُ من عطلٍ في موضعه.
  **والأخطرُ ما لم يظهر:** **البروفةُ تختار المصدرَ بالطريقة نفسِها** — فكانت
  ستستورد من مجلّد الختم لا من تصدير. ولأنها تسقط عند «المجموعاتُ فيها بيانات»،
  كان الظنُّ سيتّجه إلى **التصدير** لا إلى الكاشف.
  **العلاج:** ترشيحٌ **بشكلِ مجلّد التصدير** (`YYYY-MM-DDTHH:MM:SS_NNN/`) قبل الفرز
  في الاثنين — فلا يبالي الكاشفُ بما يُضاف حولَه من مجلّدات. وحارسٌ يشترطه.
  **والقاعدةُ المستخلَصة:** حين تكتب في حاويةٍ يقرؤها كاشفٌ، اسأل **ما الذي سيلتقطه
  الكاشفُ الآن؟** — فالإضافةُ لا تكون بريئةً لمجرّد أنها إضافة. hail-tests **2264/2264**.

- **v18.9.2669 — إصلاح: نمطُ `**` أعلن نقصاً في تصديرٍ مكتمل — ثالثُ «فحصٍ يحكم بما لا يعرف».**
  **البلاغ (المالك).** `backup-health.sh` ردّ ❌ على «التصديرُ مكتمل (فيه ملفُّ
  الفهرس)» — **وقد تحقّقنا يدوياً قبلها أنّ الملفَّ موجود** (`grep -c` ردّ `1`).
  **الجذر:** الفحصُ بحث بنمط `${LATEST}**overall_export_metadata`. و`**` في أنماط
  gcloud **تعبر المجلّدات ولا تُكمل جزءَ اسمِ ملفّ** — والملفُّ اسمُه
  `<البادئة>.overall_export_metadata`. فرجع فارغاً.
  **وهو ثالثُ تكرارٍ للنمط نفسِه في يومين** بعد `metadata`/`custom_fields` و
  `project_number`: **الاتّكاءُ على دلالةٍ خاصّةٍ بالأداة — اسمِ حقلٍ أو معنى نمط —
  يجعل الفحصَ يحكم بما لا يعرف.** والعلاجُ في الثلاثة واحد: **اسأل بوضوحٍ عمّا تريد
  معرفتَه** — سردٌ صريحٌ و`grep` هنا، وقائمةُ حاويات المشروع هناك.
  **ولاحظ أنّ الثلاثة كانت سقوطاً كاذباً لا نجاحاً كاذباً** — وهذا حظٌّ لا تصميم:
  النمطُ نفسُه كان يمكن أن يُخرج «أخضرَ» على تصديرٍ ناقص. hail-tests **2247/2247**.

- **v18.9.2664 — المقارنةُ الذكيةُ لم تكن تفشل، بل كانت تُبتَر: سقفُ الناتج يقطع الجدولَ فيُلقى كلُّه.**
  **العطلُ كما ظهر:** «⚠ فشل التحليل: تعذّر تفسير ناتج التحليل — أعِد المحاولة» على
  مجموعةٍ من ١٦ بنداً بعرضَي مورّدين — **وإعادةُ المحاولة تُعيد الفشلَ نفسَه**، لأنّ
  العلّة ليست عارضاً بل حسابٌ ثابت.
  **الجذر.** جدولُ المقارنة البنديّ بالعربية يكلّف ~١٥٠ رمزاً للبند الواحد بمورّدين
  (اسمٌ موحّد · `rawName` لكلّ مورّد · `unifyNote`)، و`max_tokens` كانت **٤٠٠٠**
  والوسيطُ يقصّ عند **٤٠٩٦** — فعند ~٢٥ بنداً ينقطع الناتجُ في منتصف بندٍ أو سلسلةٍ
  نصّية. عندها يفشل `JSON.parse` مرّتين في `_pcParseAIJson` فتُرمى رسالةٌ عمياءُ
  **وتضيع كلُّ البنود التي اكتملت فعلاً**. الرسالةُ كانت تصف العَرَض («تعذّر التفسير»)
  وتقترح علاجاً لا يعمل («أعِد المحاولة») — **وهذا أسوأُ من الصمت** لأنه يُرسل المستخدم
  في دورةٍ لا تنتهي.
  **العلاج ثلاثُ طبقات، لا واحدة:**
  (١) **رأبُ الناتج المبتور** — `_pcRepairTruncatedJson` يمشي على النصّ محترماً
  السلاسلَ وهروبَها (فالقوسُ داخل نصٍّ عربيٍّ ليس حاويةً)، يقصّ عند آخر عنصرٍ مكتمل
  ويُغلق ما بقي مفتوحاً بالترتيب العكسيّ — فتنجو البنودُ المكتملةُ بدل خسارة العروض
  كاملةً. **وهذه الطبقةُ وحدَها تعمل بلا نشرِ الوسيط.**
  (٢) **السقفُ يُرفَع في الطرفين معاً** — `PCAI_EXTRACT_MAX_TOKENS = 8000` في الواجهة
  و`MAX_TOKENS_CAP = 8192` في `worker/hail-ai-proxy.js`. **رفعُ أحدهما وحدَه عبث**:
  الوسيطُ يقصّ صامتاً ما يطلبه العميل، فيبقى العطلُ قائماً بلا أثر. ⚠ **يتطلّب نشرَ
  الوسيط** (§5) ليُثمر الرقمُ كاملاً. وحارسٌ في `hail-tests` يشترط `سقف الوسيط ≥ سقف
  الطلب` فلا ينفصلان ثانيةً.
  (٣) **لا نتيجةَ ناقصةٌ تمرّ بصمت** — `stop_reason==="max_tokens"` يُقرأ ويُميَّز:
  إن لم ينجُ بندٌ فرسالةٌ تقول السببَ والعلاج («قسّم المجموعة أو ارفع عروضاً أقلّ»)
  لا «أعِد المحاولة»؛ وإن نجت بنودٌ فالجدولُ يحمل لافتةَ **«⚠ تحليلٌ ناقص»** بعدد ما
  اكتمل. **فجدولُ مقارنةٍ ناقصٌ يُقرأ كاملاً أخطرُ من فشلٍ ظاهر** — قد يُرسي على
  مورّدٍ لأن بنودَ منافسِه سقطت في البتر لا لأنه الأرخص.
  **وتقليلُ الناتج من منبعه:** القاعدةُ ٦ في التوجيه تحذف `unifyNote` حين
  `unified=false` وتقصر `notes` — فالرموزُ لا تُهدر على حقولٍ لا تُعرض.
  hail-tests **2263/2263** (منها ١٨ حارساً جديداً يُنفّذ الرأبَ فعلاً على ناتجٍ مبتور).
- **v18.9.2662 — حفظُ المسار مع الرابط في كلّ رفع: إلغاءُ صنفِ الخطر لا معالجتُه.**
  **الجذر الذي لاحقناه يومين:** التطبيقُ يخزّن ناتجَ `getDownloadURL()` — رابطاً
  يحمل `token` في **ميتاداتا الكائن** لا في المسار ولا في المحتوى. فكلُّ منظومة
  النسخ التي بنيناها كانت **تعتمد على نجاةِ شيءٍ لا نملكه**؛ وقد نجا (مقيسٌ مرّتين)،
  لكنّ الاعتمادَ نفسَه هشاشة. **وبالمسار يصير فقدانُ التوكِن إزعاجاً — نُعيد توليدَ
  الرابط — لا كارثةً بلا سبيلِ إحياء.**
  **التنفيذ:** ١٤ موضعَ رفعٍ في `index.html` و`tech-app.html` و`cleaning-operations`
  و`hr-payments` و`contracts` صارت تحفظ المسارَ بموازاة الرابط، بتسميةٍ موازيةٍ له
  (`invoicePhotoPath` · `photoPath` · `filePath` · `storagePath`).
  **وقرارٌ يستحقّ البقاء — الإضافةُ لا تكسر، وتغييرُ الشكل يكسر:** صورُ قبل/بعد
  تُحفظ في `photosBefore` **مصفوفةَ روابطَ** تقرؤها الواجهةُ والمطبوعات. فأُضيفت
  `photosBeforePaths` **مصفوفةً موازية** بدل تحويل العناصر إلى كائنات — لأن تغييرَ
  الشكل كان يمسّ كلَّ قارئ، والإضافةُ لا تمسّ أحداً. وحارسٌ يمنع «التنظيم» لاحقاً.
  **وحارسُ تغطيةٍ يقارن عددَ مواضع المسار بعدد مواضع الرابط في كلّ ملفّ** — فأيُّ
  رفعٍ جديدٍ بلا مسارٍ يُسقط الفحص، ولا يعود النمطُ يُنسى مع أوّل شاشةٍ تُضاف.
  **فحوصُ المتصفّح كلُّها شُغِّلت فعلاً** (تعديلُ واجهةٍ ووحدات): الرحلة **61/61** ·
  السيناريوهات **58/58** · دورةُ طلب الشراء **57/57** · التعاقدات **274/274** ·
  طابورُ الرفع **15/15**. hail-tests **2247/2247**.

- **v18.9.2661 — إصلاح: فحصٌ يقرأ حقلاً لا وجودَ له أعلن سقوطاً كاذباً على إعدادٍ سليم.**
  **البلاغ (المالك).** `backup-health.sh` ردّ ❌ على «حاويةُ التصدير خارجَ المشروع
  المصدر» — **والحاويةُ خارجَه فعلاً**، والدليلُ قاطعٌ في سياسة صلاحياتها
  (`projectOwner:fast-buildings-vault`) وفي أنّ بروفةَ العبور استوردت منها بنجاح.
  **الجذر:** الفحصُ سأل `buckets describe` عن `project_number` — **حقلٌ لا تعرضه
  الأداة** — فرجع فارغاً فسقط الشرط. **وهو ثالثُ مرّةٍ يتكرّر فيها الصنفُ نفسُه**
  (`metadata` مقابل `custom_fields` · رسالةُ «Service account» الغامضة): **فحصٌ يقرأ
  اسمَ حقلٍ فيحكم على الواقع بما لا يعرفه**، ونتيجتُه إمّا سقوطٌ كاذبٌ يُفزع أو —
  والأخطر — نجاحٌ كاذبٌ يُطمئن.
  **والعلاجُ ليس تصحيحَ الاسم بل تغييرَ السؤال:** صار يسأل عن **انتماء** — هل يظهر
  اسمُ الحاوية في **قائمة حاويات المشروع المصدر**؟ — سؤالٌ لا يعتمد على تسميةٍ
  داخليةٍ قد تتبدّل. **والقاعدة: اسأل عمّا تريد معرفتَه، لا عن حقلٍ تظنّه يحمله.**
  **وقيدٌ ثانٍ:** قائمةٌ فارغةٌ **ليست دليلَ براءة** (قد تكون صلاحيةً ناقصة) — فتُعامَل
  **سقوطاً** لا نجاحاً. لا حكمَ بلا معرفة. hail-tests **2233/2233**.

- **v18.9.2659 — فحصُ صحّةٍ شامل، والبروفاتُ تختم تاريخَها: من تذكيرٍ يُنسى إلى رقابةٍ تسقط.**
  **الحاجة.** بعد إقفال ملفَّي النسخ، بقي ما يحدّد إن كان العملُ سيبقى حيّاً أم
  يتعفّن: **الإيقاعُ الدوريّ**. وكان يعتمد على أمرين يُتذكَّران وبروفتين تُعادان من
  الذاكرة — **والنسخُ يتعفّن بصمت**: تتغيّر صلاحيةٌ أو ينكسر مسارٌ فيبقى كلُّ شيءٍ
  أخضرَ حتى تحتاجَه فعلاً.
  **والفجوةُ الحقيقيةُ لم تكن في الفحص بل في الذاكرة:** البروفاتُ **لا تترك أثراً**،
  فلا سبيلَ لأحدٍ — بعد سنةٍ أو بعد تبدّل مسؤول — أن يعرف **متى أُثبتت النسخةُ آخرَ
  مرّة**. و«نسخةٌ لم تُستعَد ليست نسخةً» تفقد معناها إن لم يُعرَف تاريخُ آخر استعادة.
  **العلاج:** `backup-health.sh` — أمرٌ واحدٌ شهريٌّ يجمع الفحصين **ويضيف عمرَ آخر
  بروفة**؛ وكلُّ بروفةٍ ناجحةٍ **تختم تاريخَها** في `_drills/` بالخزنة. ⚠ عند ٩٠
  يوماً و**❌ عند ١٢٠ — انقطاعٌ لا تأخّر**. فصار الإيقاعُ **رقابةً تسقط** لا تذكيراً.
  **وقيدٌ على النفس:** **يُختَم وضعُ العبور وحدَه** في بروفة Firestore — ختمُ البروفة
  المحلّية كان سيوثّق «إثباتاً» لشيءٍ لم يُثبَت، **وهو أسوأُ من غياب السجلّ** لأنه
  يُطمئن كاذباً. حارسٌ يقرأ فرعَ الوضع المحلّيّ ويشترط خلوَّه من الختم.
  hail-tests **2232/2232**.

- **v18.9.2657 — ✅ تصديرُ Firestore مُطبَّقٌ ومُثبَتٌ باستيرادٍ في مشروعٍ آخر: لا شيءَ رهينةَ مشروعٍ واحد.**
  **بروفةُ العبور ٥ ناجحة · ٠ ساقطة.** أُنشئت `fast-buildings-vault/databases/
  export-drill` — **قاعدةٌ في مشروع الخزنة** — واستُورد إليها التصديرُ، والمجموعاتُ
  الخمسُ كلُّها فيها بيانات، ثمّ حُذفت. **فالنسخةُ ليست ملفّاتٍ نأمل أنها سليمة، بل
  قاعدةٌ عملت فعلاً في مشروعٍ لا يملك المصدرُ عليه شيئاً.**
  **وبهذا اكتمل ما بدأ أمس:** الصورُ في مشروعين والبياناتُ في مشروعين، **وكلاهما
  مُثبَتٌ باستعادةٍ عابرةٍ للمشروع.** وحذفُ `fast-buildings` أو وقفُ فوترته لم يعد
  يعني ضياعَ شيء.
  **والعطلُ الأخيرُ قبل الإقفال درسٌ يستحقّ البقاء:** `PERMISSION_DENIED` عند أوّل
  تصدير — **لأن الهويّةَ التي تنفّذ غيرُ التي تطلب**. `firestore export` ينفّذه وكيلُ
  خدمة Firestore لا حسابُ الخدمة المنادي، **ورسالتُه تقول «Service account» فتُرسل
  التشخيصَ إلى الحساب الذي منحتَه للتوّ**. والاستيرادُ مثلُه بوكيل مشروع الهدف —
  فسقطت بروفةُ العبور حتى مُنح. **القاعدة: في GCP اسأل «من ينفّذ؟» لا «من أنا؟».**
  hail-tests **2228/2228**.

- **v18.9.2654 — تصديرُ Firestore إلى الخزنة: سدُّ آخرِ طريقٍ يضيع فيه كلُّ شيءٍ دفعةً واحدة.**
  **السؤال (المالك).** «والنسخةُ الدائمةُ للبيانات تُحفظ في نفس المكان؟» — وكشف
  **مفارقةً أحدثَها إنجازُ اليوم نفسُه**: بعد أن صارت الصورُ في مشروعين، بقيت
  البياناتُ — وهي الأثمن — في مشروعٍ واحد. **فصارت البياناتُ الحلقةَ الأضعف.**
  **الجذر:** النسخُ المُدارةُ موردٌ **داخلَ المشروع**؛ تنجو من الخطأ البشريّ وعطلِ
  الإقليم **لا من فقدِ المشروع**. وتُستعاد إلى **مشروعها وحدَه** — فلو ضاع الوصولُ
  إليه، لا تملك ما تستعيده إلى مكانٍ آخر. **والتصديرُ ملفّاتٌ محمولةٌ تعبر المشاريع.**
  **التصميم:** حاويةٌ **مستقلّةٌ** في مشروع الخزنة (لأن Firestore يولّد بادئةً زمنيةً
  تلقائيةً **فقط** مع حاويةٍ بلا مسار — ومجلّدٌ داخل خزنة الملفّات كان سيكتب كلَّ
  يومٍ فوق سابقه) · احتجاز ١٠٠ يوم · تصديرٌ يوميٌّ بـScheduler · وصلاحيةُ حساب
  الخدمة على المصدر `importExportAdmin` **فقط** — لا قراءةَ مستنداتٍ ولا كتابة.
  **والبروفةُ لها وضعُ عبورٍ بين المشاريع، وهو الجوهر:** استيرادٌ ناجحٌ **داخلَ**
  المشروع لا يُثبت العبورَ — **وهو الخطأُ نفسُه الذي وقعنا فيه اليومَ مع الملفّات**
  (نجاةُ التوكِن داخلَ الحاوية لم تكن تُثبت نجاتَه عبر الحاويتين). فبُني الوضعان.
  **ومصيدتان يحرسهما الفحص:** حاويةُ التصدير **خارجَ المشروع المصدر** — ولو أُنشئت
  فيه عاد العطلُ **بلا أن يتغيّر شيءٌ ظاهر** (التصديرُ يعمل والفحصُ أخضرُ والحمايةُ
  صفر)، فيقارن الفحصُ **رقمَ المشروع** لا الاسم · و**تصديرٌ منقطعٌ يترك مجلّداً بلا
  فهرس** ويبدو في القائمة سليماً حتى تحاول استيرادَه.
  **وعشرةُ حرّاسٍ جديدة**، ودروسُ اليوم مطبَّقةٌ من أوّل سطر (بلا أسئلةٍ تفاعلية ·
  أسماءُ عرضٍ لاتينية · فرزٌ صريحٌ للقوائم).
  **ومصيدةٌ ثالثةٌ ظهرت عند أوّل تشغيلٍ حقيقيّ — الهويّةُ التي تنفّذ غيرُ التي تطلب:**
  `firestore export` ينفّذه **وكيلُ خدمة Firestore** لا حسابُ الخدمة المنادي، فمنحتُ
  الطالبَ ونسيتُ المنفِّذ فسقط بـ`PERMISSION_DENIED`. **ورسالتُه تقول «حساب خدمة»**
  فتُرسل التشخيصَ إلى الحساب الذي منحتَه للتوّ، وهي تعني وكيلاً آخرَ لم تسمع به.
  والاستيرادُ مثلُه بوكيل **مشروع الهدف** — فبروفةُ العبور تمنحه قبل أن تستورد.
  **مكتوبٌ غيرُ مُطبَّق.** hail-tests **2228/2228**.

- **v18.9.2651 — ✅ إقفالُ فجوة نسخِ Cloud Storage: الطبقتان مُطبَّقتان والبروفةُ الكاملة ١١/١١.**
  **البندُ المفتوحُ الأخيرُ في منظومة النسخ صار مغلقاً — بالقياس لا بالافتراض.**
  المرحلةُ ٢ نُفِّذت: مشروعٌ منفصل `fast-buildings-vault` · حاويةٌ في **us-central1**
  (والمصدرُ us-east1 — إقليمان مستقلّان كهرباءً وشبكةً ومناخاً) · احتجاز ١٠٠ يوم ·
  نقلٌ يوميٌّ تراكميٌّ بلا حذفٍ في الوجهة · صلاحيةٌ على المصدر **قراءةٌ فقط**.
  **والفحصُ الذي كان المجهولَ الأخير نجح: نجاةُ التوكِن عبر النسخ بين حاويتين.**
  حُذف الكائنُ حذفاً كاملاً من المصدر، واستُعيد **من الخزنة** إلى المسار نفسِه،
  **فردّ الرابطُ الأصليُّ `200`** — أي أنّ استعادةً من الخزنة لا تحتاج لمسَ مستندٍ
  واحدٍ في Firestore. **١١ ناجحة · ٠ ساقطة · ٠ متجاوَزة.**
  **وحصادُ اليوم أربعةُ أعطالٍ أمسكناها في الأدوات لا في المنظومة** — كلُّها من صنفٍ
  واحد: **عطلٌ يبدو نجاحاً أو يبدو عطلاً في الموضع الخطأ.** (١) `[ ] && func` مع
  `set -e` كانت تُخرج `--vault-only` بلا عملٍ **وتبدو ناجحة**. (٢) شرطةٌ طويلة في
  `--name` أسقطت الإنشاءَ برسالةٍ **لا تذكر المحرفَ الجاني**، ورسالةُ `describe`
  الغامضةُ وجّهت التشخيصَ إلى حصّةٍ ومعرّفٍ محجوز. (٣) سؤالٌ تفاعليٌّ مكتومُ المخرَج
  صار **تعليقاً صامتاً** بلا رسالةٍ ولا خطأ. (٤) فحصُ التوكِن سأل عن `metadata`
  و`gcloud` يعرضها تحت `custom_fields` — **فأعلن سقوطاً كاذباً بينما `★★` في الجولة
  نفسِها ردّ `200`**. والتناقضُ بين فحصَين هو ما كشفه: **الحقيقةُ مع الذي يقيس
  السلوكَ لا الذي يقرأ حقلاً.** ولذلك صار فحصُ التوكِن **يتحقّق من نفسِه** (يشترط
  قراءةَ التوكِن من المصدر أوّلاً) — فقراءةٌ فارغةٌ من الطرفين كانت ستمرّ «نجاحاً».
  hail-tests **2217/2217**.

- **v18.9.2649 — عطلان في أدوات المرحلة ٢: سؤالٌ مكتومٌ علّقها، وفحصٌ يسأل عن حقلٍ باسمٍ خطأ أعلن سقوطاً كاذباً.**
  **(ب) الفحصُ الذي كذب.** بعد نجاح الإعداد، أعلنت البروفةُ ســقوطَ
  `★ نسخةُ الخزنة تحمل firebaseStorageDownloadTokens` — **بينما `★★` في الجولة
  نفسِها ردّ `200`**: أي أنّ الاستعادةَ من الخزنة أعادت الرابطَ للعمل فعلاً.
  تناقضٌ صريحٌ بين فحصَين، والحقيقةُ مع الذي يقيس السلوكَ لا الحقل.
  **الجذر:** `gcloud storage objects describe` يعرض الميتاداتا المخصَّصة تحت
  `custom_fields` لا `metadata` — فردّ الفحصُ **فارغاً** وقرأ الفراغَ فقداً.
  **وأُثبت بمِسبارٍ مباشر:** رفعُ كائنٍ بتوكِنٍ ونسخُه إلى الخزنة ثمّ `describe` —
  فظهر `custom_fields: firebaseStorageDownloadTokens: 14afcd93…` في الوجهة. **فالنسخُ
  بين الحاويتين يحفظ الميتاداتا، والعطلُ كان في الأداة لا في المنظومة.**
  **والعلاجُ ليس تصحيحَ الاسم فحسب:** الفحصُ صار **يتحقّق من نفسِه** — يقرأ
  الاسمَين، **ويشترط أوّلاً أن يقرأ التوكِنَ من المصدر**. فقراءةٌ فارغةٌ من الطرفين
  كانت ستمرّ «نجاحاً» وهي عمًى تامّ. **فحصٌ لا يميّز «لا بيانات» من «كلُّ شيءٍ سليم»
  لا يفحص شيئاً** — وهو الدرسُ الذي كاد يُخفي نجاحاً لا فشلاً هذه المرّة.

- **(أ) السؤالُ المكتوم — سؤالٌ تفاعليٌّ في أمرٍ مكتومِ المخرَج علّق المرحلة ٢ بلا رسالةٍ ولا خطأ.**
  **البلاغ (المالك).** المرحلةُ ٢ توقّفت عند «(٤) حسابُ خدمةِ النقل» ولم تتقدّم،
  ووظيفةُ النقل `NOT_FOUND`. **ولا سطرَ خطأٍ واحد** — الشاشةُ ساكنةٌ والمؤشّرُ ينتظر.
  **الجذر:** `gcloud transfer authorize` **تسأل** «هل تتابع؟» — وأنا كتبتُها
  `>/dev/null 2>&1`. فكتمُ المخرَج أخفى السؤالَ لا الجواب: صار الأمرُ **تعليقاً
  صامتاً** بلا رسالةٍ ولا خطأٍ ولا تقدُّم. **وأخطرُ من الفشل الصريح** لأن الفشلَ
  يُشخَّص والصمتَ يُنتظَر.
  **ومصيدةٌ ثانيةٌ في التشخيص:** المستخدمُ كتب أمرَين أثناء التعليق، **وكانا سيُقرآن
  إجابةً للسؤال المخفيّ** لا أمرَين — فلا يكفي إنهاءُ الانتظار بل يلزم `Ctrl+C`.
  **العلاج طبقتان لا واحدة:** `export CLOUDSDK_CORE_DISABLE_PROMPTS=1` يجعل gcloud
  كلَّها غيرَ تفاعلية (فتُجيب بالافتراض أو تسقط بخطأٍ صريح)، **وإلغاءُ كتمِ مخرَج
  ذاك الأمر** — فالكتمُ هو ما حوّل السؤالَ إلى صمت. وحارسان يمنعان عودةَ الاثنين.
  **والقاعدةُ المستخلَصة:** في سكربتٍ يُشغَّل بلا مراقبة، `>/dev/null` على أمرٍ قد
  يسأل **ليست تنظيفَ مخرَجٍ بل إخفاءَ سببِ التوقّف**. hail-tests **2216/2216**.

- **v18.9.2648 — إصلاح: شرطةٌ طويلة في اسم عرض المشروع أسقطت المرحلة ٢، ورسالةُ الخطأ لا تدلّ عليها.**
  **البلاغ (المالك).** المرحلةُ ٢ لم تُنشئ شيئاً، والبروفةُ قالت «الخزنةُ لم تُنشأ».
  **والتشخيصُ ضلّ أوّلاً** لأن `projects describe` يردّ **رسالةً موحَّدةً غامضة**
  («لا صلاحية… أو قد لا يكون موجوداً») سواءٌ أكان المشروعُ غيرَ موجودٍ أم مملوكاً
  لغيرك — فبدا الأمرُ حصّةً ممتلئةً أو معرّفاً محجوزاً، وطاردنا وهماً.
  **والجذرُ حين ظهر الخطأُ صريحاً:** `--name="Fast Buildings — Storage Vault"` —
  **شرطةٌ طويلة**. و`display_name` في Google يقبل اللاتينيَّ والأرقامَ والمسافةَ
  والشرطةَ العاديةَ فقط، فردَّ `INVALID_ARGUMENT` **بلا أن يذكر أيَّ محرفٍ الجاني**.
  **ومصيدةٌ داخلَ المصيدة:** `--project` (المعرّف) و`--name` (اسمُ العرض) حقلان
  بقيدَين مختلفَين — والخلطُ بينهما يجعلك تصحّح الحقلَ السليم.
  **العلاج:** اسمٌ لاتينيٌّ بسيطٌ في الرسالة المقترَحة + شرحُ القيد فيها،
  **وحارسٌ يفحص محارفَ كلّ `--name=` في السكربت** (مستثنياً `$JOB_NAME`) فلا يعود
  محرفٌ عربيٌّ أو طباعيٌّ يتسلّل إلى أمرٍ يُنفَّذ. hail-tests **2214/2214**.

- **v18.9.2646 — المرحلةُ ١ من نسخِ Storage مُطبَّقةٌ على الإنتاج: ٧ أيامٍ ← ١٢٠، والتوكِنُ نجا (مقيسٌ لا مُفترَض).**
  **التشغيل (المالك، ١٤ أغسطس ٢٠٢٦).** شُغِّل `--layer1-only` على الحاوية الحيّة ثمّ
  البروفة. **والنتيجةُ مقيسةٌ لا منقولة:** `Versioning` مُفعَّل · `Soft Delete = 90d`
  **قُبلت** · `lifecycle` ١٢٠ يوماً. **والبروفة ٥ ناجحة · ٠ ساقطة · ١ متجاوَزة.**
  **وأهمُّ ما ثبت — وكان المجهولَ الذي قد يُبطل التصميم كلَّه:** بعد كسرِ الرابط
  بالاستبدال ثمّ بالحذف، **ردّ الرابطُ الأصليُّ نفسُه `200`** بعد الاستعادة. أي أنّ
  ميتاداتا `firebaseStorageDownloadTokens` تنجو، فالتعافي لا يحتاج لمسَ مستندٍ واحدٍ
  في Firestore. **ورقمُ الـ٩٠ سقفاً للحذف الليّن صار مقيساً** بعد أن كان منقولاً.
  **وقيدان على النفس:** (١) نجاةُ التوكِن **داخلَ الحاوية** لا تُثبت نجاتَه **عبر
  النسخ بين حاويتين** — مسلكان مختلفان، والثاني ما زال غيرَ مفحوص. (٢) البروفةُ
  أعلنت الخزنةَ **متجاوَزةً** بعدّادٍ مستقلّ ولم تحسبها نجاحاً — ولذلك **لم يُقفل
  البند**: المرحلةُ ٢ (الخزنة في مشروعٍ منفصل) ما زالت مفتوحةً وسابقتُها فوترةٌ يدوية.
  hail-tests **2213/2213**.

- **v18.9.2644 — تقسيمُ سكربت النسخ مرحلتين: ٩٠٪ من الحماية لا تنتظر قراراً إدارياً.**
  **الطلب (المالك).** «قسّم السكربت» — بعد أن رصدتُ في التوصيات أنّ تصميمي نفسَه
  فيه خطأٌ عمليّ: سكربتٌ واحدٌ يفعل كلَّ شيءٍ دفعةً واحدة، **وأوّلُ ما يفعله التوقُّفُ
  إن لم يجد مشروعَ الخزنة**. والخزنةُ تحتاج إنشاءَ مشروعٍ وربطَ فوترة — قرارٌ إداريٌّ
  قد يتأجّل أسابيع، تبقى الحمايةُ خلالها **٧ أيام**. بينما المرحلةُ ١ أمران على
  الحاوية القائمة يرفعانها إلى **١٢٠ يوماً في دقيقتين**.
  **العلاج:** `--layer1-only` و`--vault-only` في الثلاثة، و**نقلُ فحصِ مشروع الخزنة
  إلى ما بعد تطبيق المرحلة ١**. وحارسٌ يقارن **موضعَي الأمرين في نصّ الملفّ**
  (`--lifecycle-file` قبل `projects describe`) — فارتدادُ الترتيب يُسقط الفحص.
  **ومصيدةُ bash وقعتُ فيها وأنا أكتب الإرسالَ نفسَه:** `[ cond ] && func` مع `set -e`
  **تُنهي السكربتَ عند الشرط الكاذب** — فكان `--vault-only` يخرج بلا أن يفعل شيئاً
  **ويبدو ناجحاً**. أخطرُ من عطلٍ صريح. الإرسالُ بـ`if`، وحارسٌ يمنع العودة.
  **والبروفةُ لا تعدّ ما لم تفحصه نجاحاً:** بلا خزنةٍ تُعلن القسمَ الثاني **متجاوَزاً**
  وتُخرج عدّاد «متجاوَزة» مستقلّاً — فبروفةٌ تحسب المتجاوَزَ ناجحاً أخطرُ من بروفةٍ
  لم تُشغَّل. hail-tests **2213/2213**.

- **v18.9.2643 — سدُّ فجوةِ نسخِ Cloud Storage: تصميمٌ وسكربتاتٌ وبروفةُ استعادةٍ تقيس الرابطَ لا البايتات.**
  **الطلب (المالك).** «قارِن الخياراتِ واعرض الخطّة» — البندُ المفتوحُ الأخيرُ في
  منظومة النسخ: البياناتُ محفوظةٌ ٩٨ يوماً وصورُها ٧ أيامٍ فقط.
  **والفجوةُ ليست «٧ أيامٍ قليلة» بل عدمُ التناظر:** استعادةٌ لِما قبل أسبوعٍ تُنتج
  نظاماً كاملَ البيانات **كلُّ روابطِ مرفقاتِه ميتة**.
  **والمكتشَفُ الذي غيّر شكلَ الحلّ — قبل مقارنةِ أيّ خيار:** التطبيقُ يخزّن ناتجَ
  `getDownloadURL()` **لا المسار** في ٢٠+ موضعاً، والـ`token` يعيش في **ميتاداتا
  الكائن**. فاستعادةٌ تُعيد البايتاتِ وتُسقط الميتاداتا تقول «تمّت» **وكلُّ رابطٍ يردّ
  `403`** — أسوأُ من فشلٍ صريحٍ لأنها تُطمئن. فصار المعيارُ الحاكمُ نجاةَ الـtoken لا
  الحجمَ ولا السعر، **ومنه انقلب الترتيب**: الاستعادةُ داخلَ الحاوية أقوى تشغيلياً من
  الخزنة (تُعيد الميتاداتا فيعمل الرابطُ فوراً)، فصارت الخزنةُ ملاذاً أخيراً لا أداةً أولى.
  **وحسمٌ عدديٌّ في المقارنة:** `Soft Delete` سقفُه ٩٠ يوماً < ٩٨ — **فلا يسدّ الفجوةَ
  أبداً** مهما رُفع، وVersioning بلا سقفٍ هو ما يغطّي الباقي.
  **والمرفوضُ بقرار:** الفئةُ الباردة — تحمي من **لا شيء** في جدول التهديدات، وتوفيرُها
  ≈ سنتٌ شهرياً مقابل حدِّ تخزينٍ أدنى ورسومِ استرجاعٍ واحتكاكٍ وقتَ الكارثة؛ والكلفةُ
  ليست قيداً فتحسينُها تحسينُ المتغيّر الخطأ. ومرآةٌ بحذفٍ في الوجهة (التطبيقُ يحذف
  ملفّاتٍ فعلاً فكانت ستنشر الحذف) · وسياسةُ احتجازٍ مقفلة (بابٌ ذو اتجاهٍ واحد).
  **التسليم:** `docs/storage-backup-plan-2026-08.md` + ثلاثةُ سكربتات (إعدادٌ عديمُ
  الأثر عند التكرار · بروفةٌ تكسر الرابطَ بثلاث طرقٍ وتشترط عودتَه `200` · فحصُ صحّةٍ
  يقارن المدى بأفق الـ٩٨ رقمياً فيمسك انحرافَ الطرفين) + §5 و**ثمانيةُ حرّاسٍ** يمنعون
  ارتدادَ القيود الثلاثة (بلا حذفٍ في الوجهة · Versioning شرطُ الاحتجاز · `isLive:false`).
  **وأوّلُ صياغةٍ للحارس سقطت على تعليقٍ يشرح القيدَ نفسَه** — فصار يقرأ الشفرةَ بعد
  إسقاط أسطر التعليق: حارسٌ يكسره توثيقُ سببه حارسٌ يُغري بحذف التوثيق.
  **وحالتُه: مُصمَّمٌ غيرُ مُطبَّق** — لا gcloud في بيئة التطوير، والحمايةُ الفعليةُ ما
  زالت ٧ أيامٍ حتى تُشغَّل السكربتاتُ وتعود البروفةُ خضراء. **ولم يُقيَّد منجَزاً** لأن
  إعداداً سليماً ليس استعادةً مُثبَتة — درسُ §5 حرفياً. hail-tests **2210/2210**.

- **v18.9.2637 — سدُّ فجوةٍ بنيويةٍ في الفحص: شاشةُ المستخدمين تُفحص بدورَين في متصفّحٍ حقيقيّ.**
  **السؤال (المالك).** «هل تحتاج فحصاً لما قمنا به؟» — فراجعتُ ما نُفِّذ اليومَ ووجدتُ
  مساراً **غُيِّر ولم يُرَ**: شاشةُ «إدارة مستخدمي المشتريات» بدورٍ غيرِ أدمن.
  **الفجوةُ بنيويةٌ لا سهو:** `ui-walkthrough.mjs` يدخل **أدمن دائماً**، وحرّاسُ
  `hail-tests` يطابقون **نصَّ الكود** لا سلوكَه — يُثبتون أنّ السطرَ مكتوب، لا أنّ
  الزرَّ اختفى ولا أنّ الدالّةَ ترفض. فكلُّ قفلٍ نضعه على الواجهة يقع في المنطقة
  العمياء نفسِها.
  **العلاج:** `users-guard-check.mjs` — يدخل **بدورَين** ويسأل الشاشةَ نفسَها.
  بدور المستودعات: القائمةُ ظاهرةٌ (لم نُخفِ الشاشةَ بل الأزرار) · لا زرَّ صلاحياتٍ
  ولا كلمةِ مرورٍ ولا حذفٍ ولا حقلَ واتساب · النموذجُ مُخفًى · وشريطُ «للاطّلاع فقط».
  **و★★★ نداءُ الكتّاب الخمسةِ مباشرةً من الـConsole لا يغيّر شيئاً** — فالحارسُ في
  الدالّة لا في الشاشة (إخفاءُ زرٍّ ليس حراسة). وبدور الأدمن: كلُّ شيءٍ حاضرٌ ويعمل —
  فالقفلُ حراسةٌ لا تعطيل. **13/13**.
  **ومصيدةٌ وقعتُ فيها فيه:** بذرتُ المستخدمين في `window.USERS` فحسب، والشاشةُ
  **تُعيد قراءتَهم من Firestore قبل الرسم** فمُحيت البذرةُ وظهرت القائمةُ فارغةً —
  وسقط أربعةُ فحوصٍ **بالدورَين معاً**، وهو ما كشف أنّ العلّةَ في البذرة لا في القفل.
  البذرةُ الصحيحةُ في `USERS_DOC()` (نفسُ درس `window.__store` في §5).
  hail-tests **2202/2202** بحارسٍ يربط الملفَّ بالمشروع فلا يُنسى.

- **v18.9.2636 — ⛔ إصلاحٌ عاجل: شرطُ المسار في قاعدة القراءة أسقط استعلامَ كلّ مجموعة (انقطاعُ إنتاج).**
  **البلاغ (المالك).** «لم أقدر على الدخول» — ومعه لقطةٌ فيها كلُّ الوحدات تردّ
  `Missing or insufficient permissions`، والمالكُ داخلٌ بدور admin سليم.
  **الجذر — سطرٌ واحدٌ من دفعة M16.** كتبتُ في القاعدة العامة
  `allow read: if hasRole() && !srvOnly(document[0])` لأُخرج طابورَ واتساب من القراءة.
  **و`read` عمليتان لا واحدة**: `get` لمستندٍ بعينه — تعمل — و`list` لاستعلام مجموعة.
  وفي الـ`list` **لا مستندَ بعدُ** فمقاطعُ المسار غيرُ محسومة، وأيُّ شرطٍ عليها يُسقط
  الطلب. والتطبيقُ كلُّه يقرأ باستعلامات، فسقطت الشاشاتُ جميعاً. **والكتابةُ لم تُصَب**
  (لا `list` فيها) — ولذلك عملت `ctrLocked(document[0])` سنواتٍ فظننتُ النمطَ مُجرَّباً.
  **التشخيصُ بمقارنةٍ ثلاثيةٍ على المحاكي** قبل لمس شيء: ما قبل C1 ✅ · C1 وحدَه ✅ ·
  دفعتي ❌ — فتحدَّد الجاني في دقائق بلا تخمين.
  **العلاج:** القراءةُ العامة `if hasRole();` بلا أيّ شرطِ مسار. **والمقفولُ في M16
  صار الكتابةَ وحدَها — وهي جوهرُ البند** (الحقنُ لا التلصّص). قفلُ القراءة يحتاج
  بلوكاً مستقلّاً يفصل `get` عن `list` — بندٌ مستقلٌّ لاحق.
  **ولماذا لم يمسكه فحصٌ من ١٣٣:** كلُّها `getDoc`/`setDoc` على **مستندٍ واحد** — أي
  `get` لا `list`. **الصنفُ كلُّه كان أعمى.** وأسطرُ التحقّق بعد النشر كانت على
  `wa_outbox` و`audit_log` — ولهما بلوكاهما الخاصّان — فلم تلمس القاعدةَ العامة أصلاً:
  **اختبارٌ يفحص الاستثناءاتِ ويغفل الأصل.**
  **الأثر:** التطبيقُ الرئيسيُّ مقطوعٌ لكلّ الأدوار · **وتطبيقُ الفنيين لم يتأثّر**
  (بلوكاتُه مستقلّةٌ بلا شرطِ مسار) · ودوالُّ السحابة لم تتأثّر (Admin SDK) · **ولا
  بياناتٍ فُقدت** — العطلُ منعُ قراءةٍ لا فساد.
  **الحراسة الآن:** قسمٌ كاملٌ في `rules-check.mjs` (١٠) يستعلم **٢٢ مجموعةً بالأدمن**
  و**الأساسيةَ بستّة أدوار** وتطبيقَ الفنيين — `getDocs` لا `getDoc`. وحارسٌ في
  `hail-tests` **يرفض أيَّ `&&` في سطر القراءة العامة**، وآخرُ يشترط أن يبقى فحصُ
  الاستعلام مغطّياً ١٥ مجموعةً فأكثر. rules-check **166/166** · hail-tests **2201/2201**.
  **والدرسُ كاملاً في §5** — ومعه سطرُ التحقّق الذي كان يجب أن يرافق كلَّ نشر:
  استعلامُ مجموعةٍ عادية، لا قراءةُ مستندٍ في مجموعةٍ لها بلوكُها.

- **v18.9.2635 — المجموعة ٤ (قفلُ القواعد): M16 طابورُ واتساب · M11 سجلُّ التدقيق. وM10 خرج منها بقرارٍ مُعلَّل.**
  **الطلب (المالك).** «أكمل عائلة القواعد» — بعد أن صارت الآلةُ جاهزةً من C1
  (محاكٍ · نمطُ الاستثناء · ضبطٌ سالب · وظيفةُ CI)، فتكلفةُ البند التالي جزءٌ يسير.
  **(١) M16 — `wa_outbox` قناةُ إرسالٍ لا مجموعةُ بيانات.** ما يُكتب فيه يلتقطه
  `waSender` ويرسله **رسالةً رسميةً من رقم الشركة**: أيُّ نصٍّ إلى أيّ جوّالٍ بصوتها.
  والقاعدةُ العامة كانت تمنح ذلك لكلّ ذي دور. **والكلفةُ صفر**: الكاتبُ الشرعيُّ
  دوالُّ السحابة بـAdmin SDK — وهو يتجاوز القواعدَ أصلاً — و**لا موضعَ واحدٌ** في
  `index.html` ولا الوحدات يقرأ `wa_outbox`/`wa_log` أو يكتب فيهما (تُحقِّق بالبحث).
  **والقراءةُ مقفولةٌ هنا خلافاً لبقية النظام** — قرارٌ واعٍ لا سهو: لا شاشةَ تقرؤهما
  فلا صفرَ صامتاً يظهر، ومحتواهما أرقامُ الناس ونصوصُ رسائلهم. ولم يُقفل معهما
  `meta/wa_settings` (مفتاحُ التشغيل) — ويحرسه فحص.
  **⚠ والعلاجُ المكتوبُ في `deep-review` كان ناقصاً**: `allow read,write: if false`
  وحدَه **لا يقيّد حرفاً** بلا الاستثناء من العامة. صُحِّحت الوثيقة.
  **(٢) M11 — السجلُّ الذي يشهد على الفعل كان يمحوه فاعلُه.** البلوكُ كان
  `allow read, write: if hasRole()` — و`write` تشمل **التعديلَ والحذف** — والعامةُ
  تمنح المثلَ. **والوثيقةُ كانت تقول «لا يمكن المحو — إنشاء فقط»، وهذا خطأٌ أثبته
  المحاكي**: الحذفُ نجح فعلاً قبل الإصلاح. الآن `allow update, delete: if false`
  في النسختين + استثناءٌ من العامة، **والدورُ المكتوبُ مقيَّدٌ بدور التوكِن**
  (`auditRoleOk`) فلا يوقّع أحدٌ قيداً باسم غيرِه. **والإضافةُ والقراءةُ كما كانتا**
  — تطبيقُ الفنيين يكتب في السجلّ ويقرؤه، وقفلُهما كان سيُسقط تدقيقَ الميدان كلَّه.
  **وما بقي من M11:** الحقنُ من المجهول — لا يُغلق قبل C2.
  **(٣) M10 أُخرج من الدفعة — والسببُ حاسم:** القواعدُ لا تقرأ إلا
  `request.auth.token.*`، و**التوكِنُ لا يحمل `projects` أصلاً** (الـWorker يضع
  `role`·`name`·`u`·`perm` فقط، و`projects` في ملفّ الدخول لا في التوكِن). فأيُّ
  قاعدةِ عزلٍ اليومَ **لا تملك ما تحكم به**. ترتيبُه: تعديلُ الـWorker (مع حدّ حجم
  الـclaims ~1000 بايت) ← نشرٌ يدويّ ← ثمّ القاعدة. فهو **من عائلة الـWorker**.
  **الفحص:** `rules:check` **133/133** (كانت 114) — يجرّب حقنَ رسالةٍ بكلّ دورٍ
  **وبالأدمن نفسِه**، وطمسَ قيدٍ في السجلّ وحذفَه والكتابةَ فوقه، والتوقيعَ باسم
  الغير؛ ويُثبت أنّ الفنّيَّ ما زال يكتب ويقرأ وأنّ إعداداتِ واتساب ما زالت تُكتب.
  **وضبطٌ سالب: ١٢ فحصاً تسقط** بالقواعد السابقة (٧ لـM16 · ٥ لـM11).
  وhail-tests **2199/2199** بتسعة حرّاسٍ تشتقّ قائمةَ المقفول من `functions/lib/config.js`
  وحاجةَ الفنّيّ من `tech-app.html` — لا من نصٍّ مكتوبٍ هنا.
  **بعد الدمج:** انشر القواعدَ يدوياً (§5).

- **v18.9.2633 — البند C1: مستنداتُ الحسابات مقفولةٌ على الأدمن في `firestore.rules`.**
  **الطلب (المالك).** «أغلق C1» — أثقلُ ما بقي من فحص أغسطس، والبندُ الذي قال عنه
  التقريرُ إنّ إغلاقَ H2 وحدَه لا يُغني عنه.
  **الجذر: الحسابُ ليس بياناً بل إصدارُ صلاحية.** `meta/users` و`meta/{proj}_users`
  (ونسختاهما `_dev`) تحمل `hash` و`role` و`permissions`، **ومنها يُصادِق الـWorker
  ويُصدر التوكِن**. والقاعدةُ العامة `match /{document=**}` كانت تمنح الكتابةَ فيها
  **لكلّ دورٍ غيرِ الزائر**: مراقبٌ يفتح الـConsole، يضيف `{user:"evil",hash:…,role:"admin"}`
  إلى المصفوفة، ثمّ يدخل بـevil فيُصدر له الـWorker توكِنَ أدمن. سلسلةٌ من سطرٍ واحد
  إلى الصلاحية الكاملة، لا تمرّ على أحد.
  **العلاج شقّان — وأحدُهما بلا الآخر لا يعني شيئاً** (القواعدُ تُقيَّم بـ«أو»):
  `isUsersDoc(document[0], document[1])` **تستثني** هذه المستنداتِ من القاعدة العامة،
  وبلوكٌ مستقلٌّ `match /meta/{doc}` يقصر الكتابةَ على `isAdmin()`. والنمطُ
  `(.*_)?users(_dev)?` يغطّي المسارات الأربعةَ التي تكتبها الواجهةُ فعلاً ولا يبتلع
  `_counter` ولا `_settings` ولا `_notifications`.
  **والقراءةُ لم تُمَسّ** (شرطُ المالك): تبقى لكلّ ذي دورٍ من القاعدة العامة — التضييقُ
  على مَن **يغيّر** لا مَن يقرأ. **وتطبيقُ الفنيين لم يُمَسّ**: يدخل مُصادَقاً مجهولاً
  ولا يقترب من مستندات الحسابات أصلاً، وفحصٌ صريحٌ يُثبت أنّ إعداداتِ مشروعه وقوائمَ
  الصيانة والحضورَ والبلاغاتِ ما زالت تعمل بسياقه هو.
  **والشاشةُ تبعت الخادم.** «إدارةُ مستخدمي المشتريات» يراها **المستودعاتُ والمشتريات**
  لا الأدمن وحدَه — وهي بالضبط أخطرُ ما في البند (مسؤولُ مستودعٍ يُنشئ أدمن). فبلا
  حارسٍ في الواجهة كان النقرُ يعدّل النسخةَ المحلّيةَ ثمّ يُردّ بـ`permission-denied`
  بلا تفسير: قفلٌ صحيحٌ بواجهةٍ كاذبة. الآن لغير الأدمن **قائمةٌ للاطّلاع** بلا أزرارٍ
  ولا نموذجِ إضافة، وحارسُ أدمنٍ على الكتّاب الستّة (`puAddUser` · `puDeleteUser` ·
  `puChangePassword` · `puSavePassword` · `puSaveUserWa` · **و`saveUserPerms`** —
  نافذةُ الصلاحيات تُفتح من الشاشتين معاً، وهي أُنسى الأبواب).
  **الفحص — والعمليةُ لا القاعدة:** `rules-check.mjs` **114/114** (كانت 87): يجرّب
  استغلالَ C1 حرفياً بكلّ دورٍ على المسارات الأربعة، وبالـ`update` والـ`delete` لا
  بالـ`set` وحدَه، ويجرّب **«إضافةَ مستخدم» كعمليةٍ كاملةً** (مستندُ المشروع ثمّ
  المركزيّ — كتابتان متتاليتان كما تفعل الواجهة) و**«حذفَ المشروع»** الذي يمحو مستندَ
  حساباته مع بقيةِ مستنداته. **وضبطُ الفحص أُثبت بضبطٍ سالب**: بالقواعد القديمة تسقط
  **١٢** من فحوصه — فهو يرصد الثغرةَ لا يصف القفل. وhail-tests **2190/2190** بحرّاسٍ
  تُثبّت الشقّين معاً وتشتقّ مساراتِ الحسابات من `index.html` نفسِه (فمسارٌ خامسٌ
  يُضاف لاحقاً بلا تغطيةٍ يُسقط الفحص). ورحلةُ المتصفّح 61/61 والسيناريوهات 58/58
  وحرّاسُ المصادقة 12/12.
  **بعد الدمج — خطوةٌ يدويةٌ لا يفعلها الدمج:** انشر القواعدَ
  (`firebase deploy --only firestore:rules --project fast-buildings`) — الملفُّ في
  المستودع نصٌّ لا أثرَ له حتى يُنشَر. وتحقّق من Console التطبيق بدورٍ غيرِ أدمن:
  `db.doc("meta/users").set({users:[]})` يجب أن يردّ `permission-denied`،
  و`db.doc("meta/users").get()` يجب أن **ينجح** (القراءةُ لم تُمَسّ) — والاختباران
  معاً هما الدليل، فالمنعُ وحدَه قد يكون انقطاعَ مصادقة.

- **v18.9.2628 — المجموعة ٣ من خطّة الأمان: الدورُ من التوكِن لا من الجلسة (H2) · وهويّةٌ على مُرحِّل الذكاء (H3).**
  **الطلب (المالك).** «ابدأ بالمجموعة ٣» — بعد أن رُتِّبت البنودُ المؤجَّلةُ بحسب
  خطرِ التعطيل، وهي أقلُّها خطراً وتُغلق بابَ استنزافِ رصيد الذكاء.
  **(١) H2 — الدورُ كان يُقرأ من `sessionStorage`.** `_ensureRoleClaim` كانت تتحقّق من
  **وجود** `claims.role` لا من **قيمته**. فمن يملك أيَّ دورٍ صحيح — مراقبٌ مثلاً —
  يكتب `role:"admin"` في `hail_session` من الـConsole ويُحدّث الصفحة: يمرّ الفحصُ
  (دورُه موجودٌ في التوكن فعلاً) وتُفتح له لوحةُ الأدمن. وتحت المصادقة الخادمية
  **لا تُحمَّل `USERS` أصلاً**، فـ`USERS.find(...)||saved` تُرجع الجلسةَ المحرَّرةَ نفسَها.
  **العلاج:** `_authClaims` تُعيد الحمولةَ لا صحيحاً/خطأ، والاستعادةُ تعتمد `claims.role`
  وتُطبّقه داخل `_proceedRestore` **على الكائن المستعاد أيّاً كان مصدرُه** (لا على
  `saved` وحدَه — فمطابقةُ `USERS` قد تُرجع كائناً آخرَ فيضيع الإسناد). والتباينُ
  يُسجَّل في التدقيق. **والدخولُ كذلك يعتمد دورَ التوكِن** — فما تراه الواجهةُ وما
  يقبله الخادمُ صارا مصدراً واحداً، وهو ما ستبني عليه المرحلةُ التالية (قواعد Firestore).
  **(٢) H3 — المُرحِّلُ مفتوحٌ بلا تفويض.** المفتاحُ خادميٌّ منذ قبل (سرٌّ في الـWorker)،
  لكنّ **الرابطَ نفسَه بابٌ مفتوح**: من يعرفه يستنزف الرصيدَ ويُرسل باسمنا، وهو مقروءٌ
  من `meta/ai_settings`. الآن يُرفَق **توكِنُ هويّة Firebase** — موقَّعٌ ومحدودُ الأجل،
  **لا سرٌّ مشتركٌ يُقرأ من المتصفّح**. **والفرضُ على الـWorker لا على المتصفّح**:
  النصفُ الخادميُّ خطوةٌ يدويةٌ موثَّقةٌ في §5 بشفرتها وبتحقُّقٍ يردّ `401`.
  **وترتيبُ النشر لا يُعطّل أحداً:** لو ردَّ المتصفّحُ الطلبَ لأنّ الـWorker لا يُدرج
  `Authorization` في CORS، تُعاد المحاولةُ **مرّةً** بلا ترويسةٍ ويُسجَّل تحذير — مرّةً
  واحدةً للجلسة لا مع كلّ نداء. والمهلةُ الحقيقيةُ (`AbortError`) **لا تُعاد** فلا
  يتضاعف الاستهلاك، ولكلّ محاولةٍ مهلتُها (وإلّا ورثت الثانيةُ ما تبقّى من الأولى).
  **حزمةُ فحصٍ جديدة: `auth-guard-check.mjs`** — تفحص السلوك **تنفيذاً في متصفّحٍ
  حقيقيّ** لا بمطابقة نصوص: تُزوَّر الجلسةُ فعلاً (`admin` في الجلسة و`observer` في
  التوكِن) ثمّ يُسأل النظامُ بأيّ دورٍ دخل. وأُضيف إلى المحاكي دعمُ `getIdTokenResult`
  بحمولةٍ قابلةٍ للضبط — فصار حارسُ الدور قابلاً للفحص أصلاً.
  **وجُرِّب معكوساً:** بإرجاع سطرٍ واحدٍ عاد الدورُ `admin` و`isAdmin()` صادقة —
  أي أنّ الفحصَ يُعيد إنتاجَ التصعيد فعلاً لا يفترضه.
  **ولم يتغيّر شيءٌ للمستخدم الشرعيّ:** دورُ الجلسة ودورُ التوكِن واحدٌ دائماً
  (كلاهما من ردّ الدخول نفسِه) — ويحرسه فحصان صريحان (أدمنٌ وأمينُ مستودع).
  **الاختبارات.** `hail-tests.js` **2174/2174** (+12)، و`auth-guard-check.mjs` **12/12**،
  وبقيةُ الحزم على حالها الأخضر.
  **خطوةٌ يدويةٌ بعد الدمج — ✅ نُفِّذت في اليوم نفسِه (Worker v8):** نُشِر تحقّقُ
  الـWorker (§5 «تأمين مُرحِّل الذكاء»)، **فصار H3 مُغلقاً بنصفيه**. التحقّقُ الحيّ:
  طلبٌ بلا توكِن يُردّ بـ**401** (وكان **400** قبل النشر — أي يمرّ)، والموظّفُ
  المُصادَقُ يعمل. **ودرسٌ من التنفيذ:** خُطِّط لمرحلتين (`false` يسجّل ثمّ `true` يرفض)
  ليُفصَل سؤالُ «هل نزل الكودُ سليماً؟» عن سؤال «هل المنعُ يعمل؟» — وقد أدّت المرحلةُ
  الأولى غرضَها الأوّل (`/health` أعاد `v:8` بلا خطرٍ على أحد)، **لكنّ غرضَها الثاني
  فات**: مراقبةُ السجلّ لتأكيد قبول توكِنٍ حقيقيٍّ **قبل** المنع لم تجرِ (تعثّرت
  خطواتُ الـConsole)، فوقع التأكيدُ **بعد** المنع بتجربةِ خاصيةٍ حقيقية. النتيجةُ
  سليمةٌ والترتيبُ أخشنُ ممّا خُطِّط — ولو تكرّر الموقفُ فالأصوبُ لمالكٍ غيرِ مطوّرٍ
  أن يُنشَر بـ`true` مباشرةً ويُجرَّب، فالتراجعُ (Deployments ← Rollback) نقرتان.
  **⚠ وبندٌ مفتوحٌ اكتُشف أثناء التحقّق (ليس من هذا التغيير):** `tvwall-check` يسقط
  **٧٠/٧٢ يومَي الجمعة والسبت**. أُثبت أنّه **قنبلةٌ زمنيةٌ في الفحص لا ارتدادٌ في
  الكود**: شُغِّل على شجرة `main` بلا أيٍّ من تغييرات هذا القيد فسقط السقوطَ نفسَه.
  الجذر: تُبذَر مهمّتا النظافة بإزاحةٍ **بالأيام التقويمية** (`daysAgo(3)`/`daysAgo(1)`)
  بينما الشرائحُ تُحسَب **بأيام العمل** — ففي عطلة نهاية الأسبوع تصير المهمّتان
  «متأخّرةً صفرَ أيامِ عمل» فلا تُرسم أعمدةٌ أصلاً ويسقط الفحصان. وهو نفسُ الصنف
  الذي تنهى عنه §5 («لا تُحفَر تواريخُ في توقُّعٍ زمنيّ») — والعلاجُ أن تُبذَر الإزاحةُ
  **بأيام عملٍ** بالقاعدة نفسِها. **مؤجَّلٌ لتغييرٍ مستقلّ** كي لا يختلط إصلاحُ فحصٍ
  بتغييرٍ أمنيّ، ولأنّ `tvwall-check` **خارج CI** (يحتاج متصفّحاً) فلا يُسقط دمجاً.

- **v18.9.2626 — فحصٌ شاملٌ للمنصّة: سِنكا XSS باقيان من درسِ H5 · وأثرُ مخزونٍ لا يُسترجَع من درسِ C4 · وفحصٌ يسقط من نفسِه.**
  **الطلب (المالك).** فحصٌ شاملٌ ودقيقٌ واختبارٌ شاملٌ للمنصّة. شُغِّلت **كلُّ** الحزم
  (المنطقية · تسع حزمِ متصفّحٍ حقيقيّ · قواعدُ Firestore على محاكٍ حقيقيّ)، ثمّ قُرئ
  الكودُ بحثاً عن أصنافِ الأخطاء التي عولجت سابقاً — **هل بقي منها موضعٌ لم يُعالَج؟**
  **(١) بقيّةُ درسِ H5 — `esc` داخل نصِّ JS.** الدرسُ عولج في `v18.9vu` لكن بقي موضعان:
  **(أ)** مُنتقي الأصول في النواة — `selectAsset('${esc(a.id)}','${_jsq(a.name)} — ${esc(a.building)}')`:
  **الجارُ بـ`_jsq` والمبنى بـ`esc` في السلسلة نفسِها**. و`esc` تحوّل `'` إلى `&#39;`
  فيفكّها مُحلّلُ HTML قبل تصريف JS ⇒ تُكسَر السلسلةُ وتُحقَن شفرة. وأسماءُ المباني
  تُكتب من إعدادات المشروع — فيكفي دورٌ غيرُ زائرٍ ليحقنَ ما يعمل في جلسة الأدمن.
  **(ب) وأخطرُ: `esc` في تطبيق الفنيين لم تكن تهرّب العلامةَ المفردة إطلاقاً** — لا
  `'`→`&#39;` أصلاً، فالكسرُ مباشرٌ بلا حاجةٍ إلى فكِّ كِيان. وسبعةُ سِنكاتٍ تستعملها
  داخل نصِّ JS، منها اسمُ الفني في زرّ تغيير الـPIN. ومجموعةُ `technicians` مفتوحةٌ
  لأيّ مُصادَقٍ مجهول (C2 المؤجَّل) — **فالحاقنُ لا يحتاج حساباً أصلاً**.
  **العلاج:** `_jsq` في الموضعين، ونسخةٌ منها في تطبيق الفنيين مطابقةٌ لنظيرتها في
  النواة، و`esc` هناك صارت تهرّب المفردةَ أيضاً (وتُبقي `0` بدل أن تمحوَه).
  **والتحقُّق تنفيذيٌّ لا نصّيّ:** الحمولةُ تُمرَّر في معالجٍ حقيقيٍّ في DOM فتصل
  **نصّاً** لا شفرةً، وسبعُ قيمٍ نظيفةٍ (عربيّةٌ · فيها فاصلةٌ عليا · `\` · `"` · `<>` · `&`)
  تعود **حرفاً بحرف** — فالإصلاحُ لم يُفسد البيانات.
  **(٢) بقيّةُ درسِ C4 — أثرٌ لا يُسترجَع قبل حفظٍ لا يُنتظَر.** `_applyUnanchoredFix`
  (ربطُ بندٍ دخل بلا رصيد) يضيف الكميةَ للمخزون ويكتب قيدَ الحركة **في معاملةٍ ذرّية**،
  ثمّ يحفظ الطلبَ بـ`savePurchase` **بلا انتظار**. فإن سقط الحفظُ: الرصيدُ أُضيف
  والبندُ ما زال «بلا رصيد» في قاعدة البيانات (`itemId` غيرُ مكتوب و`hasUnanchoredStock`
  صحيح) ⇒ جهازٌ آخرُ — أو هذا الجهازُ بعد تحديث — يُعيد الربطَ **فتُضاف الكميةُ مرّتين**
  ويُكتب قيدُ `in` مكرَّرٌ في سجلّ الحركة. العلاج: `savePurchaseAwait` (الموجودةُ أصلاً
  لهذا الغرض في C4) + تحذيرٌ صريحٌ عند الفشل النهائيّ بأن الرصيد أُضيف فلا يُعاد الربط.
  **(٣) وفحصٌ كان يسقط من نفسِه لا من ارتدادٍ في الكود.** `perf-contract-check` كان
  **٢/٤٧ ساقطَين**، والتطبيقُ سليم: الفحصُ يبذر البلاغَ بـ`tickets.push(t)` ثمّ يقرأ من
  المرجع `t` نفسِه. ومستمعُ المحاكي حيٌّ — أوّلُ كتابةٍ تُعيد بناء `tickets` من المخزن
  فيَبْلى المرجعُ المحليُّ ويبقى عند حاله بينما يمضي التطبيقُ على الوثيقة الحيّة
  (وهو نصُّ تحذيرِ §5 حرفياً). أُثبت ذلك بمِسبارٍ قبل أيّ تعديل: `sameRef=false`
  بينما الوثيقةُ الحيّةُ تحمل `restoredAt` وكِلا قيدَي الخطّ الزمني ونسخةً واحدةً لا
  نسختين. صارت القراءةُ من المصفوفة الحيّة، **وأُضيف فحصٌ لعدم التضاعف** — فالسقوطُ
  الحقيقيُّ لو وقع يُميَّز من هذا.
  **الاختبارات.** `hail-tests.js` **2162/2162** (+12 حارساً: سِنكا النواة وتطبيقِ
  الفنيين · بناءُ `_jsq` هناك وتشغيلُها على حمولةٍ حقيقية · و`C4b` بشقّيه)،
  و`perf-contract-check` **48/48** (كان ٤٥/٤٧)، و`browser-scenarios` 58/58،
  و`ui-walkthrough` 61/61، و`tvwall-check` 72/72، و`contracts-check` 274/274،
  و`po-lifecycle-check` 57/57، و`cleaning-report-check` 13/13، و`photo-queue-check` 15/15،
  و`wa-routing-check` 19/19، وقواعدُ Firestore **87/87** على محاكٍ حقيقيّ.
  **وجُرِّبت الحرّاسُ معكوسةً** — أُعيد الكودُ إلى ما كان فسقطت، وسِنكٌ خاطئٌ مُصطنَعٌ
  التقطه الحارسُ العامّ: فهي تحرس فعلاً لا تمرّ صامتة.
  **ولم يُفتَح بندٌ جديدٌ في `docs/deep-review-2026-08.md`** — البنودُ المؤجَّلةُ هناك
  (المرحلة ٣: قواعدُ Firestore ومصادقةُ الفنيين) على حالها بقرارٍ واعٍ.

- **v18.9.2625 — قسمُ التعاقدات في الصلاحيات · ومفاتيحُ الصلاحيات من مصدرٍ واحد.**
  **الطلب (المالك).** التعاقداتُ كانت **بالدور وحدَه** (`VIEW_ROLES`): مديرُ مشاريعَ في
  مشروعٍ لا تعاقداتِ فيه يراها، وحجبُه عنه يستلزم تغييرَ دوره كلِّه.
  **ما أُضيف.** مفتاحُ `contracts` في شاشة الصلاحيات — في نموذج «مستخدم جديد» وفي
  نافذة تعديل صلاحيات مستخدمٍ قائم.
  **والحجبُ في مكانين لا واحد:** الخريطةُ في النواة (`_PERM_PAGE_MAP.contracts`)
  تمنع `showPage` المباشرة بصفحاته الثلاث، و**`contracts.canView` تقرأ المفتاحَ نفسَه**
  فتُزيل مجموعةَ القائمة الجانبية أصلاً وتردّ الرابطَ العميق وبطاقةَ اللوحة و«بانتظار
  إجراءك» وزرَّ «تفاصيل الطرف». وهي **بوّابةٌ واحدةٌ تقرؤها أحدَ عشرَ منفذاً** — فحجبُ
  زرٍّ بـCSS وحدَه بابٌ يبقى مفتوحاً بالعنوان.
  **والمفتاحُ حاجبٌ لا مانح** (اصطلاحُ النواة): الافتراضُ مسموح، والمستخدمُ القائمُ
  بلا حقل صلاحياتٍ لا يفقد شيئاً، والأدمنُ يتجاوز.
  **ودرسٌ جانبيٌّ استُخرج:** قائمةُ مفاتيح الصلاحيات كانت مكرَّرةً في **أربعة مواضع**
  (شبكةُ «مستخدم جديد» · تسمياتُ نافذة التعديل · مصفوفتا `permKeys`) — فمفتاحٌ يُضاف
  في ثلاثةٍ ويُنسى في الرابع **يُحفَظ بلا قيمة** أو **يُقرأ بلا خانةٍ تُعدّله**، بلا
  خطأٍ ظاهرٍ يُنبّه. صارت `_PERM_LABELS`/`_PERM_KEYS` **مصدراً واحداً** تشتقّ منه
  الثلاثةُ، والشبكةُ الساكنةُ في الترميز يحرسها فحصٌ يطابقها مفتاحاً مفتاحاً
  (وجُرِّب معكوساً: بحذف مربّعٍ واحدٍ سقط الحارس).
  **الاختبارات.** `hail-tests.js` 2150/2150 (+9) و`contracts-check.mjs` 274/274 (+6 —
  **تنفيذاً على المستخدم نفسِه**: تُبدَّل صلاحيتُه ثمّ يُسأل النظامُ ماذا يرى، ويُحاوَل
  الدخولُ المباشر بالعنوان، ويُتحقَّق من المستخدم القديم ومن تجاوز الأدمن).
  **بلا هجرةِ بيانات** — ولا تغييرَ في قواعد Firestore: الحجبُ عرضٌ لا حراسةُ خادم،
  وحراسةُ الكتابة تبقى في `firestore.rules` بأدوارها.

- **v18.9.2623 — وثائقُ الطرف: الملفُّ لا يضيع · ووثيقتان جديدتان · والبياناتُ تُشتقّ لا تُكتب مرّتين.**
  ثلاثةُ بلاغاتٍ من المالك في مسارٍ واحد — نموذجِ وثائق الطرف.
  **(١) الملفُّ المرفوع يختفي عند إضافة وثيقةٍ أخرى.** الجذر: `input[type=file]`
  **لا يُملأ برمجياً** (حاجزُ المتصفّح الأمنيّ)، وكلُّ فعلٍ في النموذج يُعيد رسمَه
  بـ`innerHTML` فيُتلف الحقلَ ومعه الملفُّ المختار. وكانت `syncDraft` تلتقط النصوصَ
  ولا تلتقط الملفّات، والحفظُ يقرأ الملفّات **من الشاشة** — فيجد آخرَ ما اختير فقط.
  الآن الملفُّ يُثبَّت في المسوّدة **فورَ اختياره** (`pickDocFile`)، وتلتقطه `syncDraft`
  قبل كل إعادةِ رسم، والحفظُ يقرأ **من المسوّدة لا من الـDOM**. والصفُّ يُعلن اسمَ
  الملفّ بشارةٍ قابلةٍ للإزالة — فحقلٌ يبدو فارغاً والملفُّ محفوظٌ **كذبٌ صامت**.
  **وحقولُ المسوّدة المؤقّتة لا تُكتب في القاعدة**: `docsForSave` تُسقط كلَّ مفتاحٍ
  يبدأ بشَرطةٍ سفلية — و`_file` جسمُ ملفٍّ حيٌّ ترفضه Firestore أصلاً.
  **(٢) وثيقتان جديدتان** للصفتين معاً: **شهادةُ الحساب البنكيّ (الآيبان)**
  و**العنوانُ الوطنيّ** — بعلامة `noExpiry` فحقلُ التاريخ اختياريٌّ فيهما.
  **(٣) البياناتُ تُشتقّ من الأعلى** (`DOC_AUTOFILL`/`docAutoValue`/`applyDocAutofill`):
  السجلُّ التجاريُّ يأخذ رقمَه **وانتهاءَه**، والبطاقةُ الضريبيةُ رقمَها، والحسابُ
  البنكيُّ آيبانَه، والعنوانُ الوطنيُّ عنوانَه — ممّا أُدخل مرّةً في البيانات الأساسية.
  **وليست راحةً فحسب:** إعادةُ الكتابة **مصدرُ حقيقةٍ ثانٍ** يفترق عن الأول بأوّل
  تصحيحٍ في أحدهما، فيُقرأ سجلٌّ تجاريٌّ في الأعلى وآخرُ أسفلَه.
  **وثلاثةُ قيودٍ على الاشتقاق:** الملءُ يصيب **الفارغَ وحدَه** فما كُتب بيدٍ لا
  يُدهَس · **وتبديلُ النوع يُسقط ما اشتُقّ للنوع السابق** ثمّ يشتقّ للجديد (وإلا بقي
  رقمُ السجل في صفٍّ صار «عنواناً وطنياً» — كشفه الفحصُ فعلاً) والمقارنةُ بقيمة
  الاشتقاق نفسِها لا بشارةٍ عامّة، فاليدُ تصمد · **والآيبانُ لا يُشتقّ إلا لمن يراه**
  (`canBank`) — فهو مقنَّعٌ في كل شاشة، وملؤُه في خانةٍ ظاهرةٍ تسريبٌ من بابٍ خلفيّ.
  **الاختبارات.** `hail-tests.js` 2141/2141 (+11) و`contracts-check.mjs` 268/268
  (+6 — منها **إعادةُ إنتاج البلاغ حرفياً**: يُختار ملفٌّ بحقل الملفّ الحقيقيّ
  (`setInputFiles`) ثمّ تُضاف وثيقةٌ ثانية، فيُتحقَّق أن الأولى ما زالت تحمله).
  **بلا هجرةِ بيانات** — الوثائقُ القائمةُ تعمل كما هي، والاشتقاقُ يملأ الفارغَ عند
  فتح النموذج ولا يكتب شيئاً حتى يُحفَظ الطرف.
- **v18.9.2621 — بنودُ طلب الشراء تختفي على الجوال: صنفٌ واحدٌ لمكوّنين لا صلةَ بينهما.**
  **البلاغ (المالك، من آيفون).** نافذةُ تفاصيل طلب الشراء تعرض شريطَ «بنود الطلب (٣)»
  وصفَّ الترويسة (`# · الكود · نوع الخامة · اسم المادة`) **ثمّ لا شيء** — تنتقل مباشرةً
  إلى «المالية والمرفقات». والبنودُ الثلاثةُ موجودةٌ في الوثيقة ومرسومةٌ في الـDOM.
  **الجذر: تصادمُ أسماء أصناف.** `po-items` اسمُ **صندوق جدول البنود** في نافذتَي
  التفاصيل (طلبُ الشراء وأمرُ الصرف). ثمّ أخذ **سطرُ ملخّص البنود في بطاقة القائمة**
  الاسمَ نفسَه ومعه `display:-webkit-box; -webkit-line-clamp:2; overflow:hidden` —
  وهو **أدنى في ورقة الأنماط** فيرثه الصندوق. النتيجة: الصندوقُ كلُّه يُقصّ إلى
  **سطرين** — شريطُ العنوان وصفُّ الترويسة، وتُقطع بقيةُ الجدول.
  **ولماذا الجوالُ وحدَه؟** WebKit (سفاري/آيفون) يطبّق قصَّ `-webkit-box` القديم على
  الصندوق ذي الأبناء الكتليّة، بينما Blink (كروم) يُكتّل العنصرَ (`flow-root`)
  فيُهمل القصّ. فالعطلُ **لا يظهر على المحرّك الذي نطوّر عليه** — يظهر على الجهاز
  الذي يُستعمل فيه النظام وحدَه.
  **الإصلاح.** سطرُ البطاقة صار `po-card-items` (اسمُه وقصُّه معاً)، و`po-items` بقي
  صندوقاً. **بلا تغييرٍ في أيّ منطق** — تسميةٌ وقاعدةُ نمطٍ واحدة.
  **حارسان لا واحد:** (١) في `hail-tests.js` — **لا `-webkit-line-clamp` ولا
  `-webkit-box` على `.po-items` أبداً**، ولا يتشارك المكوّنان صنفاً (وجُرِّب معكوساً:
  بإعادة الاسم القديم سقط الحارسان). (٢) في `po-lifecycle-check.mjs` — نافذةُ
  التفاصيل تُفتح على مقاس ٣٩٠px ويُفحَص **النمطُ المحسوب** (`webkitLineClamp==='none'`)
  لا الارتفاعُ وحدَه، لأن المحرّكَ الذي بين أيدينا لا يُظهر العطل؛ ومعه أن البنودَ
  الثلاثة مرسومةٌ **داخل** الصندوق، وأن الجدولَ العريض يُمرَّر أفقياً في صندوقه بلا
  إفاضةِ الصفحة.
  **الدرسُ الباقي:** صنفٌ عامُّ الاسم (`po-items`) لمكوّنٍ داخليٍّ بابٌ مفتوحٌ لدهسٍ
  صامت — والبادئةُ تسبق الاسمَ العامّ (`po-card-*` للبطاقة، `po-items-*` للصندوق).
  **الاختبارات.** `hail-tests.js` 2130/2130 · `po-lifecycle-check.mjs` ·
  `ui-walkthrough.mjs` · `browser-scenarios.mjs` · `contracts-check.mjs`.

- **v18.9.2618 — أرقامُ جوال الأطراف: صيغةٌ واحدةٌ مخزَّنة، ورقمٌ يُتّصل به لا يُنسَخ.**
  **الحاجة (طلبُ المالك).** سجلُّ الأطراف كان يعرف **من** الطرفُ وما وثائقُه وما
  تخصّصُه — ولا يعرف **كيف يُتّصل به**. فرقمُ المقاول في جوّال من أدخله، ومن يفتح
  بطاقتَه في اللحظة التي يحتاجه فيها يجده بلا وسيلةِ تواصل.
  **ما أُضيف.** حقلُ **رقم الجوال** (وصفةُ صاحبه) في البيانات الأساسية، و**جدولُ
  جهات اتصالٍ قابلٌ للتحرير** (اسمٌ · صفةٌ · رقم) — كان يُعرَض ولا يُكتب.
  **القرارُ المحوريُّ: التخزينُ مطبَّعٌ والعرضُ محلّيّ.** ما يُكتب بأيّ صيغةٍ
  (`0501234567` · `+966 50 123 4567` · `00966…` · `501234567` · **بأرقامٍ عربيةٍ
  هندية**) يُحفَظ صيغةً دوليةً واحدة، ويُعرَض `050 123 4567`. وبدون ذلك يصير الرقمُ
  الواحدُ رقمين: لا يُوجَد ببحثٍ برقمه، ولا يُكشَف تكرارُه، ولا يقبله مُرسِلُ واتساب.
  **ولذلك حارسٌ يقارن تطبيعَ الوحدة بتطبيع `functions/lib/whatsapp.js` حرفياً** —
  فما يُقبَل في السجل هو ما تقبله Meta، لا صيغةٌ تمرّ هنا وتُرفَض هناك.
  **والرقمُ يُتّصل به:** البطاقةُ والبلاطةُ تعرضانه رابطَ اتصالٍ (`tel:`) ورابطَ
  واتساب — نصٌّ مجرَّدٌ يعني نسخاً يدوياً وخطأً في رقمٍ واحد. و`stopPropagation`
  على بلاطة الشبكة لأنّها كلَّها زرُّ فتح.
  **والخطأُ يُمنَع لا يُحذَّر منه:** الحقلُ اسمُه «جوال» فالسعوديُّ فيه يبدأ بـ05
  (`9665XXXXXXXX`) — ورقمٌ ثابتٌ أو ناقصٌ يُرفَض حفظُه بنصٍّ يدلّ على العلاج، لأن
  خانةً فارغةً تُرى فارغة أمّا رقمٌ ناقصٌ محفوظٌ فيُقرأ صحيحاً حتى يومِ الحاجة.
  **والفراغُ مقبول** — طرفٌ قديمٌ بلا رقمٍ لا يقف تعديلُ اسمه عليه.
  **والتكرارُ تنبيهٌ لا منع:** رقمٌ يملكه طرفٌ آخر يُنبَّه عليه **باسم مالكه** (قد
  يكون مكتباً يمثّل مقاولين، وقد يكون الطرفَ نفسَه مسجَّلاً مرّتين).
  **والبحثُ بالرقم مسارٌ مستقلٌّ** لا حرفٌ في كومة النصّ: يطابق بأيّ صيغةٍ كتبها
  الباحث (مخزَّنةً · بصفرٍ · بلا مفتاح · جزئياً من ثلاثة أرقام) وعبر أرقام جهات
  الاتصال أيضاً. **ووثيقةُ العقد المطبوعةُ** تحمل جوالَ الطرف الثاني — الإنذارُ
  الرسميُّ قبل السحب يحتاج وسيلةَ تواصلٍ **مثبَتةً في العقد** لا رقماً في شاشة.
  **الدوالُّ النقيّة** (مصدرُ الحقيقة، ممنوعٌ تكرارُ منطقها في الترميز):
  `phoneDigits` · `normPhone` · `phoneOk` · `phoneHint` · `phoneFmt` ·
  `phoneVariants` · `vendorPhones` · `vendorMatchesPhone` · `phoneOwner`.
  **بلا تغييرٍ في القواعد:** الرقمُ حقلٌ عاديٌّ تحكمه `vendorUpdateOk` القائمة —
  الحسّاسُ المستثنى هو الآيبان وحدَه.
  **الاختبارات.** `hail-tests.js` 2128/2128 (+20 حارساً، منها حارسُ التطابق مع
  الخادم) · `contracts-check.mjs` 262/262 (+11: القراءةُ من بياناتٍ خام · روابطُ
  الاتصال · البحثُ بأربع صيغ · رفضُ الثابت · الحفظُ مطبَّعاً من أرقامٍ عربية) ·
  `ui-walkthrough.mjs` · `browser-scenarios.mjs`.
  **بلا هجرةِ بيانات:** الأطرافُ القائمةُ تعمل كما هي، وأرقامُها الخامُّ (إن وُجدت
  في `contacts`) تُقرأ وتُعرَض وتُبحَث مطبَّعةً بلا تعديلِ وثيقة — والتطبيعُ يُثبَّت
  في القاعدة عند أوّل حفظٍ للطرف.

- **v18.9.2616 — العلامةُ المائيةُ في مطبوعات التعاقد بنصف شدّتها.**
  **الحاجة (طلبُ المالك).** الورقةُ الرسمية جاءت بشدّة قالب الشركة الأصلية، فكانت
  العلامةُ تُقرأ خلف **جدول البنود والأرقام** في الصفحة الأولى وتزاحم ما يُوقَّع عليه.
  **ما تغيّر.** `opacity:.5` على `.lh-m` — فأدكنُ نقطةٍ فيها انتقلت من ٢٠٦ إلى ٢٣١
  من ٢٥٥ (خلفيةُ الورقة بيضاء، فالتخفيفُ يرفع القيمةَ نحو البياض).
  **القرار: التخفيفُ بالشفافية لا بصورةٍ ثانيةٍ أفتح** — فالصورةُ المستخرَجةُ من القالب
  تبقى مصدراً واحداً، والشدّةُ رقمٌ يُعدَّل في سطرٍ إن طُلب تفتيحٌ أو تغميقٌ لاحقاً.
  **وحارسٌ في الفحص كشف أثراً بنيوياً لم يكن متوقَّعاً:** المتصفّحُ يلفّ الطبقةَ
  المخفَّفةَ في **Form XObject بمجموعة شفافية** — كائنٌ مستقلٌّ **لكل صفحة**. فسقط
  حارسُ «الترويسةُ على كل صفحة» الذي كان يشترط **تطابقَ قواميس الموارد حرفياً**،
  والورقةُ سليمةٌ تماماً. صُحِّح العهدُ إلى ما كان يُقصَد فعلاً: **كلُّ صفحةٍ ترسم ثلاثَ
  طبقات، واثنتان منها (الترويسةُ والتذييل) الكائنُ نفسُه على كل الصفحات** — ويميّز
  الفحصُ الآن قاموسَ الصفحة من قاموس الغلاف الشفّاف (بـ`/MediaBox` بعده).
  **الاختبارات.** `hail-tests.js` 2108/2108 (+حارسُ حدٍّ يمنع ارتدادَ الشدّة فوق ٦٠٪)
  و`contracts-check.mjs` 251/251 — وجُرِّب الحارسُ معكوساً بعد تصحيحه: بإلغاء `fixed`
  صارت الطبقاتُ على صفحةٍ واحدةٍ فسقط. **بلا أثرٍ على البيانات.**

- **v18.9.2615 — الاسمُ يُقرأ كاملاً، والنصُّ لا يتداخل: ثلاثةُ عيوبٍ ظهرت على الشاشة المعلّقة.**
  **(١) نصُّ الحلقة يتداخل.** سطورُ مركز الحلقة (الكلمة · سببُها · التسمية · النسبة)
  كانت مقيسةً بعرض **الشاشة** (`vw`) بينما قُطرُ الحلقة محدودٌ بارتفاعها (`19vh`) —
  فعلى شاشةٍ **عريضةٍ قصيرة** يكبر النصُّ وتصغر الحلقة، فتتراكب السطورُ ويخرج بعضُها
  فوق القوس. **مقياسان مختلفان لعنصرٍ واحدٍ وحاويته**، وهو خطأٌ لا يظهر إلا على مقاسٍ
  بعينه. الآن الحلقةُ **حاويةُ قياس** (`container-type:size`) والنصُّ بوحداتها (`cqw`)،
  فيكبر ويصغر معها مهما كان المقاس — و`clamp` يبقى قبله احتياطاً لمتصفّحٍ بلا
  استعلامات الحاويات. ويحرسه فحصٌ **هندسيٌّ يقيس تقاطعَ السطور** على ثلاثة مقاسات.
  **(٢) اسمُ المبنى يُقصّ.** كان يُقتطع منه `"مبنى "` بمطابقةٍ **نصّيةٍ حرفية** — فلا
  تلتقط `مبني/مباني/مبانى`، ويبقى الاسمُ بطوله فيُقصّ. وأبعدُ من ذلك: مشروعٌ كلُّ
  مبانيه تبدأ بـ«دورة مياه» يضيّع نصفَ العرض على كلمتين لا تميّزان شيئاً. الآن
  `tvBldShort`/`tvBldLabels`: بادئةٌ عامّةٌ بأشكالها الأربعة، ثم **أطولُ بادئةِ كلماتٍ
  مشتركةٍ بين الجميع** — فيبقى ما يفرّق، وحارسٌ يمنع محوَ اسمٍ كاملاً. **وكلُّ مواضع
  الاختصار الثمانية** (اللوحتان · الرسوم · الشريط السفلي · سياقُ الذكاء) تقرأ القاعدةَ
  نفسَها — فلا اسمَ يُختصر هنا ويُقصّ هناك.
  **(٣) اسمُ المشروع يُقصّ في صفّ الرسم.** كان سطراً واحداً في ثُلث عرض البطاقة —
  و«صيانة وتشغيل دورات المياه بمدينة حائل» لا يُقرأ في ثمانين بكسل. الآن سطران
  وحصّةٌ أوسعُ من الشريط: الشريطُ يقول «كم» بطولٍ أقصرَ بلا خسارة، والاسمُ يُقرأ.
  **ولمزلقٍ ثالثٍ في الترتيب:** فرزُ «ضغط العمل» بالنوع (صيانةٌ ثم نظافة) كان **يُسقط
  مشروعَ النظافة الوحيدَ** كلَّما ضاقت الشاشةُ إلى ثلاثة صفوف — والمقامانِ منفصلانِ
  أصلاً، فالترتيبُ صار **بنسبة المشروع إلى أسوأ مشروعٍ من نوعه**، وبها يتصدّر قائدُ
  كل نوعٍ فلا يختفي نوعٌ بأكمله.
  **الاختبارات.** `hail-tests.js` 2107/2107 · `tvwall-check.mjs` 72/72 ·
  `ui-walkthrough.mjs` 61/61 · `browser-scenarios.mjs` 58/58 · `contracts-check.mjs` 251/251.

- **v18.9.2613 — مركزُ العمليات بعد أوّل تشغيلٍ حقيقيّ: فصلُ الإنجاز عن التأخّر ·
  واسمُ المبنى كاملاً · وما لا يتّسع يُعلَن عدداً.**

  ### أ) اسمُ المبنى يظهر كاملاً، وما لا يتّسع في الرسم يُعلَن عدداً
  **(١) بطاقةُ المبنى صفٌّ لا مربّع.** كانت عموداً (رقمٌ ضخمٌ فوق اسم) في شبكةِ
  **أربعةِ أعمدةٍ ضيّقة** (`minmax(7vw…)` ≈ ٩٥ بكسل)، والاسمُ محدودٌ بسطرين — فاسمٌ
  عربيٌّ طويلٌ مثل «الوحدة المركزية والنظم الجغرافية» يُقصّ، وعلى شاشةٍ قصيرةٍ
  **يختفي كلِّياً** لأن الرقمَ الضخمَ يلتهم ارتفاعَ الصف. **والمبنى يُعرَف باسمه لا
  برقمه** — فالاسمُ هو ما يجب ألّا يُقصّ. الصفُّ يعطيه **العرض** (وهو المتاح) بدل
  الارتفاع (وهو الشحيح): عمودان عريضان بدل أربعةٍ ضيّقة، والاسمُ أوّلاً ثم الرقم،
  والاسمُ الأصليُّ كاملٌ في `title`. طُبِّق في **اللوحتين** (لوحةُ العرض المفردة
  ولوحةُ المشروع في المركز) — بطاقةٌ واحدةٌ لا نسختان.
  **والحارسُ هندسيٌّ على الاسم نفسِه** لا على البطاقة: `scrollHeight/scrollWidth`
  لكل `‎.bname` في ١٧ مبنًى، وفحصٌ ثانٍ يزرع اسماً من ٣٢ حرفاً — فحصُ «البطاقةُ لا
  تفيض» كان يمرّ بينما الاسمُ داخلها مقصوص.
  **(٢) ما لا يتّسع في بطاقة الرسم يُعلَن.** بطاقاتُ الرسم تعرض أعلى ٣–٤ صفوفٍ
  (بحسب ارتفاع الشاشة)، فأربعةُ مبانٍ كانت تُقرأ **«هذه كلُّ مباني المشروع»**.
  الآن شارةُ «٤ من ١٦» على كل بطاقةٍ قُصّت (المباني · المشاريع · أنواع الأعمال)،
  وعنوانُ بطاقة التغطية صار **«أضعفُ المباني تغطيةً اليوم»** — يقول ما هو صراحةً
  بدل «حسب المبنى» التي تَعِد بالقائمة كلِّها. **والقائمةُ الكاملةُ حاضرةٌ إلى
  جوارها** في عمود «حالة المباني» — ولذلك لم تُوسَّع البطاقة: ستّةَ عشرَ صفّاً في
  بطاقةٍ ارتفاعُها ١٥٥ بكسل خطٌّ بحجم ٣ بكسل، أي إخفاءٌ آخرُ بلباسٍ مختلف.
  **الاختبارات.** `hail-tests.js` 2031/2031 · `tvwall-check.mjs` 70/70 ·
  `ui-walkthrough.mjs` 61/61 · `browser-scenarios.mjs` 58/58.

  ### ب) فصلُ «إنجاز بلاغات الشهر» عن «عدد المتأخّرات»، وعطلان كشفتهما شاشةُ الإنتاج
  **(١) الخلطُ الأصلي.** حلقةُ الجاهزية وشريطُ الإنجاز كانا يأخذان لونَ الصحّة
  (`--health`) — **والصحّةُ ليست الإنجاز**: هي عددُ البلاغات المتأخّرة عن SLA
  (`tvHealth`: ٣ فأكثر ⇒ حرِج). فمشروعٌ أنجز **٩٤٪** من بلاغات شهره ولديه **٤**
  متأخّرات كان يُرسَم إنجازُه **أحمرَ**: لونٌ يقول «سيّئ» ورقمٌ يقول «ممتاز» في
  مكانٍ واحد — **والناظرُ من بعيدٍ يقرأ اللونَ لا الرقم**. وكلمةُ «حرِج» فوق النسبة
  مباشرةً تُقرأ حكماً عليها. مقياسان مستقلّان ⇒ **ترميزان مستقلّان**:
  • الحلقةُ والنسبةُ وشريطُ البطاقة بنبرةِ الإنجاز (`tvRateTone`: ≥٩٠ أخضر ·
    ٧٥–٨٩ كهرماني · دون ٧٥ أحمر · بلا نسبةٍ محايد) — عتباتٌ لا تذكر التأخّر أصلاً،
    ويحرسه فحصٌ يمنع عودةَ أيّ ذكرٍ له داخلها.
  • الكلمةُ (حرِج/متابعة/مستقر) تبقى بلون الصحّة، **وسببُها مكتوبٌ تحتها صراحةً**
    («٤ متأخرة عن SLA» · «٨٣ مهمّة متأخّرة» في النظافة) — فلا تُقرأ وصفاً للنسبة.
  التطبيق في **اللوحتين معاً** (`#page-tv` ولوحةُ المشروع في المركز) من `tvRateTone`
  الواحدة، والفحصُ يقيس اللونَ المحسوب فعلاً في المتصفّح لا قاعدةَ CSS.
  **(٢) عطلٌ رصدته الشاشةُ الحيّة: منحنًى يناقض بلاطته.** مشروعٌ يعرض «بلاغات الشهر
  ١٢٨» ومنحناه يقول «وارد ٩٣» — **ونافذةُ الأربعةَ عشرَ يوماً تحتوي الشهرَ حتى اليوم**،
  فرقمٌ أصغرُ من مُحتواه تناقضٌ ظاهرٌ للعين. السبب: المنحنى كان يستثني المؤرشف قياساً
  على «بلاغات نشطة» — وهي وحدَها تستثنيه لأنها تسأل «ما يحتاج عملاً الآن»، أما
  `monthN`/`newToday`/`closedToday` فتَعدّ الكلَّ (الفارقُ كان ٣٥ بلاغاً مؤرشفاً بالضبط).
  **القاعدة: نافذةٌ واحدةٌ وسكّانٌ واحدون لكل ما يُعرَض في الشاشة نفسها** — ويحرسها
  فحصُ احتواءٍ: `وارد(١٤ يوماً) ≥ بلاغات الشهر` دائماً.
  **(٣) وعطلٌ ثانٍ: مقارنةُ وحدتَي عملٍ على مقامٍ واحد.** «ضغطُ العمل حسب المشروع»
  كان يقيس الكلَّ على أطول شريط، فمشروعُ نظافةٍ بـ**٨٣ مهمّة** يُقزّم مشروعَ صيانةٍ
  بـ**١١ بلاغاً** ويُقرأ «ضغطُه سبعةُ أضعافه» — ومقارنةُ مهمّةٍ ببلاغٍ لا تعني شيئاً
  (نفس قرار الشريط العلوي: بلاطةٌ لكل وحدة). الآن **مقامٌ لكل نوع**، والصفوفُ مجمّعةٌ
  بنوعها، **والوحدةُ مكتوبةٌ في القيمة** («١١ بلاغ» · «٨٣ مهمة»).
  **ومزلقٌ في الكتابة نفسِها:** الوحدةُ العربية داخل `‎.tvw-num` (وهي `direction:ltr`
  مع `unicode-bidi:isolate`) تنقلب فتُقرأ «بلاغ ١١» — الرقمُ وحدَه داخل العزل والوحدةُ
  خارجَه. **ومزلقٌ في الفحص:** الحلقةُ لها `transition: stroke .5s`، فقراءةُ اللون
  فور تغييره تُرجع اللونَ السابق — الفحصُ ينتظر قبل القياس، وإلا مرَّ حارسٌ كاذب.
  **الاختبارات.** `hail-tests.js` 2026/2026 (منها ١٣ حارساً جديداً للفصل والاحتواء) ·
  `tvwall-check.mjs` 68/68 · `ui-walkthrough.mjs` 61/61 · `browser-scenarios.mjs` 58/58.

- **v18.9.2609 — بطاقاتُ الشراء أصغرُ ومتجاورةٌ في شبكةٍ تقيس حاويَها.**
  **الحاجة (طلبُ المالك).** قائمةُ طلبات الشراء بطاقةٌ **بعرض الشاشة كاملاً** لكلّ طلب:
  في ملء شاشة ١٤٤٠ لا يظهر من تسعةٍ وخمسين طلباً إلا أربعة، ونصفُ عرض البطاقة فراغٌ
  أبيض. فمن يريد نظرةً على ما ينتظره يمرّر الشاشةَ خمسَ عشرةَ مرّة.
  **ما تغيّر.** `#purchases-list` صارت شبكةً، والبطاقةُ أصغرَ حشوةً وخطّاً: ثلاثةُ
  أعمدةٍ على ١٤٤٠ · عمودان على اللوحيّ طولاً وعرضاً · عمودٌ على الجوال — وثمانيةُ
  طلباتٍ في المشهد الواحد بدل أربعة.
  **القرارات.**
  (١) **الشبكةُ تقيس حاويَها لا الشاشة** (`repeat(auto-fill,minmax(min(100%,340px),1fr))`):
  عرضُ القائمة يختلف عند العرض الشاشيِّ نفسِه بحسب ظهور القائمة الجانبية — قِيس ٧٣٤px
  عند شاشة ١٠٢٤ (والجانبيةُ ظاهرة) و٧٨٠px عند ٨٢٠ (وقد انطوت). فنقطةُ انكسارٍ على
  عرض الشاشة تكذب على أحد المقاسين حتماً؛ وحدُّ عمودٍ واحدٍ يصدُق عليهما معاً.
  و`min(100%,340px)` يمنع فيضانَ الجوال: العمودُ لا يتجاوز عرضَ حاويه أبداً.
  (٢) **وبنيةُ البطاقة صارت أصنافاً لا أنماطاً سطرية.** هذا **شرطُ عمل الشبكة** لا
  ترتيبٌ جماليّ: النمطُ السطريُّ يتخطّى ورقةَ الأنماط وmedia query معاً — وهو العطلُ
  نفسُه الذي أفاض شبكةَ `.ast-stats` ٣٨px خارج شاشة الجوال (قيدُ v18.9.2517).
  فالهيكلُ الآن `po-row`/`po-main`/`po-badges`/`po-items`/`po-meta`/`po-actions`
  (وشريطُ الاستلام `po-rcv`/`po-bar`)، ولم يبقَ سطريّاً إلا **ما يُحسَب لكل طلب**:
  عرضُ شريط الاستلام ولونُه.
  (٣) **الأزرارُ صفٌّ يلتفّ لا عمودٌ ثابت** — في البطاقة الضيّقة كان العمودُ يقتطع
  ثلثَ عرضها؛ والالتفافُ يقرأ عرضَ **البطاقة** لا عرضَ الشاشة، فيصحّ في كل عددِ أعمدة.
  (٤) **وسطرُ البنود مقصورٌ على سطرين** (`line-clamp`): طلبٌ بتسعةَ عشرَ بنداً كان
  يُطيل بطاقتَه ثلاثةَ أضعافِ جارتها فتتفاوت صفوفُ الشبكة — والقائمةُ كاملةً في بطاقة
  الطلب بنقرة.
  (٥) **وشريطُ الصفحات وحالتا «لا نتائج»/«جارٍ التحميل» تمتدّ على الأعمدة كلّها**
  (`po-span`) — وإلا وقف شريطُ الصفحات عموداً بجانب بطاقة.
  **الاختبارات.** `hail-tests.js` 2079/2079 (منها ١٤ حارساً جديداً: الشبكةُ بـ`auto-fill`
  لا بعددٍ محفور · `min(100%,…)` ضدّ فيضان الجوال · **ولا تخطيطَ سطريّاً في هيكل
  البطاقة** · الامتدادُ على الأعمدة · قصرُ سطر البنود). و`po-lifecycle-check.mjs` 53/53
  و`ui-walkthrough.mjs` 61/61 و`browser-scenarios.mjs` 58/58 و`contracts-check.mjs` 251/251.
  وقِيس في متصفّحٍ حقيقيّ عند أربعة مقاسات (١٤٤٠ · ١٠٢٤ · ٨٢٠ · ٣٩٠): الأعمدةُ ٣·٢·٢·١
  و**صفرُ تمريرٍ أفقيٍّ للمستند** في الأربعة.
  **الأثرُ على البيانات:** لا شيء — عرضٌ محض.

- **v18.9.2607 — مطبوعاتُ التعاقد تخرج على الورقة الرسمية للشركة.**
  **الحاجة (طلبُ المالك).** العقدُ ومسودتُه وسندُ صرفِ أمر الدفع **تخرج من المنصّة**:
  تُوقَّع من مقاولِ الباطن، وتُرفَق بالتحويل البنكيّ، وتُطلَب في المراجعة بعد سنة.
  وكانت تخرج بترويسةٍ نصّيةٍ من صنع المنصّة — لا الورقةَ المعتمَدةَ التي تخرج بها
  بقيةُ مراسلات الشركة، ولا سجلَّها التجاريَّ ولا عنوانَها ولا وسائلَ التواصل. فوصل
  الطلبُ بمرفقٍ واحد: **«صفحاتُ التعاقد نفسُ الصفحة المرفقة»**.
  **ما أُضيف.** ثلاثُ صورٍ مستخرَجةٌ من قالب الشركة الورقيّ نفسِه
  (`letterhead-header.jpg` · `letterhead-footer.jpg` · `letterhead-watermark.png`)،
  وإطارُ ورقةٍ واحدٌ في `contracts.js` (`letterheadCSS`/`letterheadWrap`/`docHeadHTML`)
  تمرّ به المطبوعاتُ الثلاث: ترويسةٌ أعلى، وتذييلُ العناوين أسفل، وعلامةٌ مائيةٌ
  وسطى — **على كل صفحة**، والعقدُ يبلغ ثلاثَ صفحاتٍ فأكثر.
  **القرارات.**
  (١) **الهندسةُ مقيسةٌ من القالب لا مقدَّرةً بالعين**: عرضُ الترويسة ٢٠٢٫٥مم عند
  −٢٫٦٥مم وقمّتُها ٣مم من الرأس · التذييلُ ١٩١٫٨مم عند ٣٫٧٢مم وقاعُه ١١٫٥مم من
  الأسفل · العلامةُ ١٠٨٫٤مم — فما يخرج من الطابعة ينطبق على الورقة المعتمَدة.
  (٢) **والدرسُ الذي كلّف صياغةً كاملة**: `position:fixed` **يتكرّر** على كل صفحةٍ
  مطبوعةٍ (وهو المطلوب)، لكنّ المتصفّحَ **يحصره في صندوق المحتوى ويلوي الإزاحةَ
  السالبة** — قِيس أنّ `top:-20mm` يرتدّ إلى **أسفل** الصفحة لا إلى هامشها العلويّ،
  فظهرت الترويسةُ في قاع الورقة. فلا سبيلَ إلى الرسم في هامش `@page`: جُعل الهامشُ
  عند حافّة الصورة نفسِها (٣مم / ١١٫٥مم)، وحُجز فراغُ النصّ بصفَّي `thead`/`tfoot`
  — وهما وحدَهما ما تضمن المواصفةُ تكرارَه على كل صفحة.
  (٣) **وعناوينُ الصور تُقرأ من الصفحة لا تُكتَب في الوحدة** (`img#_lh_*` كما
  `_logo_src_`): نافذةُ الطباعة على iOS مستندُ `blob:` لا أصلَ له، فالمسارُ النسبيُّ
  فيه ميت — والمقروءُ من `img.src` مطلقٌ دائماً.
  (٤) **ومن وجدها غيرَ محمَّلةٍ** (`naturalWidth === 0`) **عاد إلى الترويسة النصّية**:
  ورقةٌ بلا زخرفٍ خيرٌ من ورقةٍ بمربّعاتٍ مكسورة.
  (٥) **وإطارٌ واحدٌ للمطبوعات الثلاث** لا نسخةٌ لكلٍّ — ولا اسمَ شركةٍ ولا شعارَ
  مكرَّرين فوق الترويسة: صار رأسُ المستند شريطاً نحيلاً (عنوانُ المستند + رقمُه).
  **الاختبارات.** `hail-tests.js` 2065/2065 (منها ١٤ حارساً جديداً: وجودُ الصور ·
  الهندسةُ بالمليمتر رقماً رقماً · **لا إزاحةَ سالبةً رأسيةً** · الإطارُ واحدٌ لثلاث
  مطبوعات). و`contracts-check.mjs` 251/251 في متصفّحٍ حقيقيّ — ومنها الحارسُ الحاسم:
  **تُطبَع الوثيقةُ إلى PDF فعلاً وتُقرأ مواردُ كل صفحةٍ منه**، فيسقط الفحصُ إن حملت
  الصورَ صفحةٌ دون أخرى (جُرِّب معكوساً: بإلغاء `fixed` صارت الصورُ على صفحةٍ واحدةٍ
  فسقط الحارس). `ui-walkthrough.mjs` 61/61 · `browser-scenarios.mjs` 58/58.
  **الأثرُ على البيانات:** لا شيء — تغييرُ عرضٍ وطباعةٍ محض، بلا حقلٍ ولا قاعدة.

- **v18.9.2605 — نوعُ الأعمال لكل طرف، ومرشّحُ «مقاولٌ أو مورّدٌ لتخصّصٍ معيّن».**
  **الحاجة (طلبُ المالك).** سجلُّ الأطراف كان يعرف **ماذا يكون** الطرف (مقاولُ باطنٍ أم
  مورّد) ولا يعرف **ماذا يعمل** (كهرباءَ أم تكييفاً أم عزلاً). فمن أراد مقاولَ كهرباءَ
  فتح السجلَّ وقرأ الأسماءَ واحداً واحداً — ومن لا يعرف السجلَّ عن ظهر قلبٍ يفوته
  نصفُ من فيه. والمحوران مستقلّان: مقاولُ باطنٍ للكهرباء ومورّدُ موادَّ كهربائيةٍ
  يتشاركان التخصّصَ ويفترقان في النوع، ومن يبحث عن الأوّل لا يريد الثاني.
  **ما أُضيف.** حقلُ `trades` على وثيقة الطرف — **قائمةٌ من ٣٨ تخصّصاً يُختار منها
  أو نصٌّ يُكتب يدوياً**، وأكثرُ من نوعٍ للطرف الواحد. وشاراتُه على بطاقة القائمة
  وفي بطاقة الطرف وفي خيار اختيار الطرف داخل طلب التعاقد. ومرشّحٌ خامسٌ في الشاشة
  («كل أنواع الأعمال») يعمل مع مرشّح النوع فيعطي «مقاولون · كهرباء» أو «موردون ·
  توريد مواد كهربائية»، وسطرٌ تحته يقول ما هو المرشَّحُ وكم عدده وزرُّ إلغاء.
  **القرارات.**
  (١) **القائمةُ والكتابةُ معاً — لا أحدُهما**: كتالوجٌ مغلقٌ يدفن أوّلَ تخصّصٍ لم
  نتوقّعه تحت «أخرى»، ونصٌّ حرٌّ محضٌ يُنتج بعد سنةٍ سبعَ تهجئاتٍ لـ«تكييف» لا يجمعها
  مرشّح. فالقائمةُ الطريقُ المعبَّد والكتابةُ مخرجُ الطوارئ.
  (٢) **و`normTrade` تلمّ الاثنين**: ما يُكتب مطابقاً لاسمٍ في القائمة **يصير مفتاحَها**
  (بالتطبيع العربيّ نفسِه: همزةٌ وتاءٌ مربوطةٌ وياءٌ ومسافات) — فلا تخصّصان بالاسم
  نفسِه، ولا «كهرباء» مكتوبةٌ يدوياً تختفي من نتيجة من اختار «كهرباء» من القائمة.
  وهذا هو الفرقُ بين بابِ مرونةٍ وبابِ دفن.
  (٣) **وخياراتُ المرشّح تضمّ ما كُتب يدوياً فعلاً** في السجل — تحت عنوانٍ مستقلٍّ
  («مكتوبة يدوياً») يجعل ما يستحقّ التوحيدَ لاحقاً ظاهراً بدل أن يتوارى.
  (٤) **«مقاول ومورّد» مقاولٌ حقّاً ومورّدٌ حقّاً** (`kindMatches`): المطابقةُ الحرفيةُ
  السابقةُ كانت تُسقطه من نتيجة «أرِني المقاولين» — فيبحث المشتري عمّن يعرف أنه
  موجودٌ فلا يجده. وهذا **تصحيحُ سلوكٍ قائم** لا إضافةٌ جديدة.
  (٥) **دالّةٌ نقيّةٌ واحدةٌ للبحث** (`vendorsByTrade`) تقرؤها الشاشةُ ويقرؤها الفحص —
  فلا مرشّحَ ثانٍ في شاشةٍ أخرى يفترق عنها.
  **الاختبارات.** `hail-tests.js` 2051/2051 (منها ٢٣ حارساً جديداً: التقاءُ المكتوب
  بالمختار · التطبيعُ العربيُّ على التخصّص · إزالةُ التكرار بالمفتاح لا بالحرف ·
  «مقاول ومورّد» في القائمتين · وثيقةٌ قديمةٌ بلا `trades` لا تسقط). `contracts-check.mjs`
  246/246 في متصفّحٍ حقيقيّ (منها ١٤ جديداً: المرشّحُ يُنتج ما تحسبه الدالّةُ النقيّة ·
  التخصّصُ المكتوبُ يدوياً قابلٌ للترشيح · الاختيارُ والكتابةُ يُنتجان قائمةً واحدةً
  والحفظُ يكتبها في الوثيقة). `ui-walkthrough.mjs` 61/61 · `browser-scenarios.mjs` 58/58.
  **الأثرُ على البيانات:** حقلٌ اختياريٌّ جديدٌ واحد (`trades`) على `global_vendors` —
  الأطرافُ القائمةُ تعمل بلا تعديلٍ وتظهر «لم يُحدَّد بعد» حتى تُملأ. **بلا تغييرٍ
  في القواعد** (المجموعةُ تحت القاعدة العامة كما كانت).

- **v18.9.2603 — مسودةُ العقد تظهر في كل مراحل الاعتماد وللجميع.**
  **الحاجة (طلبُ المالك).** المعتمِدون الأربعة (مديرُ المشاريع · المشتريات · المالية ·
  التنفيذيّ) كانوا يوقّعون على بطاقةٍ تعرض **بنوداً وأرقاماً وشروطاً تجاريةً مختصرة** —
  بينما ما سيوقّعه الطرفُ الآخرُ **وثيقةٌ كاملة**: نطاقُ عملٍ والتزاماتٌ وسلامةٌ وجزاءاتٌ
  وضمانٌ وأحكامٌ ختامية. فالماليةُ تعتمد قيمةً لا تعرف بأيّ شروطٍ ستُصرف، ومديرُ المشاريع
  يعتمد نطاقاً لا يرى صياغتَه التي سيُحاسَب عليها المستخلص. وأوّلُ من كان يرى الوثيقةَ
  كاملةً هو **المشتريات بعد اكتمال الاعتمادات** — حين لم يبقَ لأحدٍ اعتراض.
  **ما أُضيف.** قسمُ «مسودة العقد» على بطاقة الطلب (شروطاً قانونيةً منسوخةً من القوالب
  وماليةً متولّدةً من أرقام الطلب)، وزرُّ «طباعة مسودة العقد» — **في كل مرحلةٍ ولكلّ دور**.
  **القرارات.** (١) **لا وثيقةَ ثانية**: المسودةُ هي `contractFromRequest` — الدالّةُ
  التي ستُنشئ العقدَ فعلاً — مطبوعةً بورقة `contractPaperHTML` نفسِها (استُخرجت من
  `printContract` فصارت ورقةً واحدةً يقرؤها العقدُ ومسودتُه). فما يراه المعتمِدُ اليومَ
  هو ما سيخرج غداً حرفياً، ولا سبيلَ لافتراقهما لأن لا نسخةَ ثانيةَ تفترق.
  (٢) **تُعلن عن نفسها** (`crqDraftState`) بشريطٍ في رأسها: «مسودةٌ لا تُوقَّع» قبل اكتمال
  البوّابات · «معتمَدٌ بانتظار إنشاء العقد» · «صار عقداً — CTR-…» · «لا يُتعاقَد به» للملغى
  والمُعاد — بالمنطق نفسِه الذي يحرس سندَ صرف أمر الدفع، ولأن المنعَ من الطباعة يُنتج
  صورةَ شاشةٍ بلا وسمٍ أصلاً. وحين يوجد العقدُ تُحوَّل الطباعةُ إليه.
  (٣) **«للجميع» شرطٌ لا تفصيل**: لا بوّابةَ على القراءة — من يرى الطلبَ يرى مسودتَه،
  وحجبُها عمّن لم يحن دورُه يجعله يعتمد على وصفٍ شفهيٍّ حين يحين.
  (٤) **ولا رقمَ مخترَعاً**: المسودةُ بلا معرّف عقدٍ (الرقمُ يُولد مع العقد)، وتحمل رقمَ
  طلبها. ورقمٌ مخترَعٌ في ورقةٍ تُطبَع أسوأُ من لا رقم.
  (٥) **خاناتُ الاعتماد بمصدرٍ واحد**: `crqSignoffs` تخدم سندَ الصرف والمسودةَ معاً،
  ومسمّياتُها من `GATE_ROLES` نفسِها — و`payOrderSignoffs` صارت غلافاً عليها يضيف خانةَ
  السداد. (نسختان كانتا ستفترقان عند أوّل بوّابةٍ تُضاف.)
  **الاختبارات.** `hail-tests.js` 2025/2025 (منها ١٢ حارساً جديداً: تطابقُ المسودة مع
  `contractFromRequest` حرفياً · قيمتُها وشروطُها · وسمُ كل حالة · خاناتُ البوّابات للعقد
  ولأمر الدفع). `contracts-check.mjs` 232/232 في متصفّحٍ حقيقيّ (المسودةُ حاضرةٌ في
  المراحل الأربع جميعاً · ويراها دورٌ بلا بوّابة · والرقمُ المطبوع = المحسوب) ·
  `ui-walkthrough.mjs` 61/61 · `browser-scenarios.mjs` 58/58.
  **بلا أثرٍ على البيانات ولا على القواعد** — عرضٌ وطباعةٌ من وثيقةِ الطلب نفسِها.

- **v18.9.2601 — مركزُ العمليات: شريطُ تحليلاتٍ في شاشة «الكل» وفي لوحة كل مشروع.**
  **الحاجة.** اللوحةُ كانت تقول «كم» ولا تقول **«إلى أين يتّجه»** ولا **«أين يتركّز
  الضغط»** ولا **«هل نلحق بالمهلة؟»** — وهي أسئلةُ الناظر إلى شاشةٍ معلّقة، ولا يَنقر.
  **ما أُضيف.** شريطٌ أسفلَ كل شاشة: في «الكل» أربعُ بطاقاتٍ حدّاً أقصى (حركةُ البلاغات
  ١٤ يوماً وارداً ومغلقاً · ضغطُ العمل حسب المشروع بالمتأخّر مصبوغاً داخله · توزيعُ
  الالتزام بالمهلة · تغطيةُ اليوم لمشاريع النظافة · أكثرُ أنواع الأعمال)، وفي لوحة
  المشروع ثلاثٌ **بلغةِ نوعه**: الصيانةُ (اتّجاه · التزام · أنواع)، والنظافةُ (تغطيةُ
  المباني · شرائحُ التأخّر · أكثرُ المهام المستحقّة).
  **القرارات.** (١) **بلا مكتبةِ رسمٍ ولا شبكة** — الجدارُ قد لا يصله CDN، فمكتبةٌ
  خارجيةٌ = لوحةٌ فارغةٌ عند أوّل انقطاع؛ الرسمُ كلُّه SVG/HTML في سطورٍ معدودة.
  (٢) **محورٌ واحد** — لا مقياسانِ مختلفا الوحدة في رسمٍ واحد (نفس قرار الشريط
  العلوي: بلاغاتٌ مع بلاغات، ومهامٌّ مع مهامّ). (٣) **لا لونَ وحدَه** — كلُّ سلسلةٍ
  باسمها ورقمها في وسيلة الإيضاح، وكلُّ عمودٍ وصفٍّ يحمل قيمتَه. (٤) **مصدرٌ واحدٌ
  للأرقام** — الالتزامُ من `slaOf` نفسِها (مصدرُ `isOverdue`) والتغطيةُ من
  `coverageForTasks`، فلا رقمٌ في الرسم يخالف الرقمَ في البلاطة فوقه.
  **مزلقان رُصدا بالقياس لا بالنظر.** (أ) شريطٌ يُضاف تحت لوحةٍ ممتلئة **يقضم ارتفاعَ
  ما فوقه**: رقمُ نبض التشغيل وحلقةُ الجاهزية كانا مقيسَين بالعرض وحدَه، فقُصّت
  تسمياتُهما (**قيد التنفيذ · في الانتظار · أُغلقت اليوم**) بلا أثرٍ في أيّ رقم —
  صارا محدودَين بالارتفاع أيضاً، ويحرسه فحصٌ هندسيٌّ على ١٠٨٠ و٩٥٠ و٧٦٨.
  (ب) **الرسمُ يُقرأ معكوساً في سياقٍ عربيّ**: إحداثياتُ SVG تبدأ يساراً بينما
  تسمياتُ المحور تُقلب، فيقع أقدمُ يومٍ فوق أحدثِ نقطة — التسمياتُ `ltr` قسراً،
  والمدياتُ كُتبت «حتى ٣» لا «٢-٣» (شَرطةٌ بين رقمين تُرسم مقلوبة).
  **وصفٌّ لا يتّسع لا يُخفى بـCSS** (قصٌّ صامتٌ يُقرأ «هذا كلُّ شيء») بل لا يُحسَب
  أصلاً — عددُ الصفوف يتبع ارتفاعَ الشاشة، والعنوان «أكثرُ …» يقول إنّها قمّةُ القائمة.
  **الاختبارات.** `hail-tests.js` 2013/2013 (منها ٢٥ حارساً جديداً يُنفّذ دوالَّ
  السلاسل والتوزيع والرسم فعلاً: بَعثرةُ الأيام، والمؤرشفُ خارج المنحنى كما هو خارج
  البطاقة، ومقامٌ واحدٌ لكل الصفوف، والقطعةُ الصفريةُ لا تُرسم لكنّها مسمّاة).
  `tvwall-check.mjs` 63/63 في متصفّحٍ حقيقيّ · `ui-walkthrough.mjs` 61/61 ·
  `browser-scenarios.mjs` 58/58.

- **v18.9.2598 — درسُ تفعيل القالب المخصّص: «القالب معتمَد» لا يعني «القالب في الحساب الصحيح»
  (+ إعادةُ §3-ب التي فاتها الدمج).** اعتُمد قالبا السداد، فبُدِّل `.env` ونُشر — ففشل
  **كلُّ** إرسالٍ بخطأ Meta `#132001 Template name does not exist in the translation`.
  السبب: القالبان أُنشئا تحت **WABA غيرِ الذي يملك الرقمَ المُرسِل**؛ الحسابُ الإنتاجيُّ
  (`2149904325585354`) لم يكن فيه إلا `ticket_assigned` و`po_approval_needed` و
  `po_status_update`. **والقوالبُ لا تُنقَل بين الحسابات** — تُعاد كتابتُها في الصحيح.
  والعطلُ من صنفٍ خبيث: النظامُ سليمٌ تماماً والإعدادُ صحيحٌ شكلاً، فلا يظهر إلا لحظةَ
  أوّلِ إرسالٍ حقيقيّ. أُضيف في `docs/whatsapp-notifications-STATUS.md` **فحصٌ قاطعٌ قبل
  التبديل** (يسأل Meta عن قوالب الحساب الإنتاجيّ ويطبع الاسمَ واللغةَ والحالة) و**تراجعٌ
  فوريٌّ** يعيد القالبَ المستعار العامل في أقلّ من دقيقة — وقد نُفِّذ فعلاً، فالإشعاراتُ
  لم تبقَ معطّلةً إلا دقائق. وأُضيف المزلقُ رابعاً في **§3-ج**.
  **و§3-ج (منظومةُ الإشعارات) أُعيد:** كُتب في v18.9.2532 لكنّ دمجَ #223 التقط الفرعَ قبل تلك الدفعة
  فضاع من `main` — أُعيد إدراجُه، **ومصفوفةُ التغطية حُدّثت بمصدرٍ رابع** (التعاقدات:
  `crq/ext/chg/ctrRoute*`) فتبقى المصفوفةُ صادقةً لا لقطةً قديمة.
  **وثالثةٌ رُصدت أثناء التفعيل الفعليّ: `functions/.env` بلا `WHATSAPP_PHONE_NUMBER_ID`.**
  النشرُ يكتب بيئةَ الدالّة من الملفّ كاملةً — فما ليس فيه يُحذَف. وهذا السطرُ بلا افتراضٍ
  في الكود، فحذفُه يوقف **كلَّ** إرسال. ولم يظهر لأن `waSender` (وحدَها تقرؤه) كان **فشل
  تحديثُها** في نشرةٍ سابقة فبقيت على نسختها القديمة ومعها القيمة: **فشلُ النشر ستر
  العطل**، وأوّلُ نشرةٍ تنجح عليها كانت ستطفئ الإرسال. أُعيد السطرُ، وأُضيفت القاعدةُ في
  §3-ج بفحصٍ قبل النشر وآخرَ بعده.
  **وأُضيف `functions/wa-health.js`** — فحصُ صحّةٍ يدويٌّ يراجع الطبقاتِ الخمس في تقريرٍ
  واحد (الإعداد · القوالب في WABA الصحيح · النشرُ وبيئةُ `waSender` · مفتاحُ القتل
  وإخفاقاتُ الطابور · **أيُّ دورٍ بلا رقمٍ مُفعَّل**). يمسك العطلين أعلاه آلياً بدل أن
  يُكتشفا عند أوّل رسالةٍ لا تصل، ويقرأ أسماءَ القوالب **من `.env`** لا من قائمةٍ مكتوبةٍ
  فيه — فيبقى صادقاً بعد أيّ تبديل. **قراءةٌ محضة**، والباقي توثيقٌ بلا كودِ تطبيق.

- **v18.9.2596 — تحديثُ المرجع: §3-ب «حوكمةُ التعاقدات» — مرجعٌ دائمٌ بدل سجلٍّ زمنيّ:**
  **الحاجة.** بُنيت اليوم طبقةُ حوكمةٍ كاملةٌ (فصلُ المهام · الإرجاعُ لمرحلة · الحذفُ
  والتحرير · صياغةُ الذكاء · الغرامةُ بالريال)، وكانت كلُّها موثَّقةً في **§6 وحدَه** —
  وهو سجلٌّ زمنيٌّ يقول «متى تغيّر الشيء ولماذا» عبر عشرة قيودٍ متفرّقة. فمن يريد أن
  يعرف **ما الحكمُ اليوم** قبل تعديلٍ في `contracts.js` كان عليه أن يقرأ تاريخَ يومٍ
  كامل ويستنتج. **§3-ب يقول الحكمَ الحاليَّ مباشرةً** في ثماني نقاطٍ وجدولين: البوّابات ·
  فصلُ المهام ومهربُه وحدُّه الصريح · الإرجاعُ ووجهاتُه المشتقّة · **جدولُ ما يُحذف وما
  لا يُحذف** · ما يُعدَّل بعد الإرسال وما يُبطله · حدُّ الذكاء الاصطناعيّ · الغرامةُ
  ولغةُ الوثائق القديمة · وترجمةُ رفض الخادم.
  **وحُدِّث §2** (سطرا `contracts.js` و`firestore.rules`) و**§5** بدرسٍ منهجيٍّ يستحقّ
  مكاناً دائماً: **افحص العمليةَ لا القاعدة** — إن كانت العمليةُ معاملةً تكتب في
  وثيقتين فافحصها بشكلها (`writeBatch`)، فـFirestore يردّ المعاملةَ كلَّها إن رُفض
  جزءٌ منها. وهو الدرسُ الذي كلّفنا عطلاً في الإنتاج (v18.9.2588).
  **بلا تغييرٍ في الكود** — توثيقٌ فقط. `hail-tests` **1951/1951**.

- **v18.9.2591 — «حنان مديرةُ مشروعٍ وأحتاجها تعدّل مهامّ مشروع النظافة» (طلبُ المالك):
  صلاحيةٌ مانحةٌ جديدة «إدارة مهام النظافة» — تفتح التحرير بلا رفعِ دور:**
  **الحالة.** تحريرُ مهامّ النظافة (`canEdit` في `cleaning-operations.js`) كان
  للأدمن ومدير المشاريع وحدَهما، وحسابُ حنان دورُه المسجَّل «مشرف» — فتظهر لها
  الصفحةُ وأزرارُ التنفيذ وجولاتُ الجودة، ولا يظهر «مهمة جديدة» ولا زرُّ التعديل.
  **ولماذا لم نرفع دورَها؟** لأن الدورَ في هذا النظام **حزمةٌ واحدة**: `project_manager`
  يجرّ معه اعتمادَ طلبات الشراء (المرحلة الأولى) وسدادَ الموارد البشرية وغيرَه — فرفعُه
  لأجل تعديل مهمّةِ نظافةٍ يمنح ما لم يُطلَب. الحلُّ مفتاحٌ **مقصورٌ على ما طُلب**.
  **والفخُّ الذي تفاداه التصميم.** كلُّ مفاتيح الصلاحيات القائمة **حاجبة**: الافتراضُ
  مسموح، وتُقرأ `perms[k] !== false` — لأنها تحجب صفحاتٍ كانت مفتوحة. ولو أُضيف
  المفتاحُ الجديد بالاصطلاح نفسِه لَفتح التحريرَ **لكلّ مستخدمٍ قائم** بأثرٍ رجعيّ
  (وللمستخدم القديم بلا حقلِ `permissions` أصلاً) — أي تصعيدُ صلاحياتٍ صامت. فصار
  للمفاتيح نوعان بمصدرِ قراءةٍ واحدٍ في النواة (`_PERM_GRANT_KEYS` + `_permOn`):
  الحاجبُ على اصطلاحه، و**المانحُ لا يُقرأ إلا `=== true`**. والأدوارُ الاطّلاعية
  (زائر/مراقب) مستثناةٌ صراحةً — الخادمُ يردّ كتابةَ الزائر، وزرٌّ يَعِد بما يُرفَض
  عطلٌ لا صلاحية.
  **ما فُتح بالمفتاح** = مجموعةُ `canEdit` كاملةً كما هي في الوحدة: إنشاءُ المهامّ
  وتعديلُها وحذفُها · إطلاقُ المهامّ · التوليدُ بالذكاء الاصطناعي · ربطُ المشرفين
  بالمباني · حذفُ جولات الجودة. لم تُجزَّأ لأن تجزئتَها تُفرّق مصدرَ الحقيقة الواحد
  في الوحدة إلى مفاتيحَ لا يعرف المالكُ فروقَها.
  **الخادم.** `firestore.rules` لا تحتاج تغييراً: القاعدةُ العامة تسمح بالكتابة لكلّ
  ذي دورٍ غيرِ الزائر، ومجموعةُ `{projId}_cleaning_tasks` ليست من المستثناة — فالمنعُ
  كان في الواجهة وحدَها، والفتحُ فيها وحدَها.
  **التشغيل بعد الدمج:** لوحةُ الإدارة ← إدارة المستخدمين ← 🔐 الصلاحيات لحساب حنان
  ← علّم «إدارة مهام النظافة» ← حفظ (يُزامَن إلى `meta/users`)، ثم تُعيد الدخول.
  **التحقّق:** `hail-tests` **1979/1979** (بعد دمج `main`) — و**تسعةٌ من الحراس تنفيذيةٌ لا نصّية**:
  تُبدَّل `currentUser` في حاضنة الوحدة وتُسأل `canEdit` نفسُها (مشرفٌ ممنوحٌ يحرّر ·
  مشرفٌ بلا مفتاحٍ لا · `cleaningEdit:false` لا · مستخدمٌ قديمٌ بلا `permissions` لا ·
  زائرٌ ومراقبٌ ولو مُنحا لا · مديرُ المشاريع بدوره نعم · والتنفيذُ وجولاتُ الجودة كما
  كانا). وخمسةٌ بنيوية على النواة (فصلُ المانح · قراءةُ `_permOn` · المربّعُ غيرُ
  مؤشَّرٍ افتراضاً · المفتاحُ في مصفوفتَي الحفظ وتسميتَيه · وكتابتُه بعلامةٍ صريحة).
  **وأُثبت أنها تعضّ:** بإسقاط `_hasCleanEditPerm` من `canEdit` سقط فحصان، وبقراءة
  المفتاح باصطلاح الحاجب (`!== false`) سقط فحصُ «مشرفٌ بلا مفتاحٍ لا يحرّر».
  و`ui-walkthrough` **61/61** · `browser-scenarios` **58/58**. **وفي متصفّحٍ حقيقيّ**
  (فحصٌ عابرٌ بمُحاكي Firestore): بحساب مشرفٍ ممنوحٍ ظهرت فعلاً «مهمة جديدة» وزرُّ
  القلم على بطاقة المهمّة، وفُتح نموذجُ التعديل بنقرة؛ وبمشرفٍ بلا مفتاحٍ لم يظهرا
  وبقي «تنفيذ» كما كان.

- **v18.9.2590 — طباعةُ أمر الدفع: سندُ صرفٍ ورقيٌّ بمبلغٍ كتابةً وخاناتِ توقيع** (طلبُ المالك):
  **الفجوة.** أمرُ الدفع هو المستندُ الوحيدُ في مسار التعاقدات الذي **يخرج من المنصّة**:
  يُرفَق بالتحويل البنكيّ، ويُحفَظ في ملفّ المحاسبة، ويُطلَب في المراجعة الخارجية بعد سنة.
  والعقدُ كان يُطبَع (`printContract`) وأمرُ الدفع لا — فكانت المالية تصوّر الشاشة:
  صورةٌ بلا رقمِ مستندٍ ولا توقيعاتٍ ولا مبلغٍ كتابةً.
  **الإصلاح.** زرُّ «طباعة أمر الدفع» على بطاقة الطلب حين `engagement==="pay_order"`،
  و`printPayOrder` تبني سندَ صرفٍ A4 بنمط مطبوعات المنصة نفسِه (رأسٌ وشعارٌ ورقمُ
  المستند · أطرافُ الصرف والمستفيدُ **بهويته** من `identityOf` · بنودُ الصرف وإجمالياتُه ·
  المبلغُ كتابةً · بيانُ السداد إن سُدِّد · خاناتُ الاعتمادات والتوقيعات · تذييلٌ بوقت الطبع).
  **وثلاثةُ قراراتٍ تحكم الورقة:**
  (١) **كلُّ أرقامها من دوالِّ الوحدة النقية** (`lineTotal`/`linesTotal`/`crqValueOf`) لا من
  حسبةٍ محلّية — حسبةٌ واحدةٌ في الترميز كانت ستجعل الورقةَ تكذب على الشاشة بلا أن يسقط فحص.
  (٢) **الورقةُ تُعلن حالتَها** بشريطٍ من `payOrderPrintState`: أمرٌ لم تكتمل بوّاباتُه يخرج
  موسوماً **«غير صالحٍ للصرف»**، والملغى والمُعادُ للتصحيح **«لا يُصرَف»**، والمسدَّدُ
  «نسخةُ حفظٍ لا أمرُ صرفٍ جديد». والطباعةُ **متاحةٌ في كل مرحلة** عن قصد: منعُها قبل
  الاعتماد كان سيدفع إلى صورةِ شاشةٍ بلا وسمٍ أصلاً — والوسمُ هو الحماية لا المنع.
  (٣) **الآيبانُ يُقنَّع على الورق بقاعدة الشاشة نفسِها** (`canBank` — المالية والأدمن):
  تسريبُ الحساب البنكيِّ في ورقةٍ تُصوَّر وتُرسَل أخطرُ من تسريبه في شاشةٍ تُغلَق.
  **والتوقيعاتُ تُشتقّ لا تُكتَب:** `payOrderSignoffs` تقرأ حقولَ الاعتماد نفسَها التي تقرؤها
  `crqNextStage` (`pmApprovedAt`/`procApprovedAt`/`ceoApprovedAt`/`payment`)، وخانةُ التنفيذيِّ
  تظهر إن اعتمد فعلاً أو كان المبلغُ يبلغ سقفَه — فلا تطبع الورقةُ بوّابةً لا يمرّ بها الأمرُ
  ولا تُسقط بوّابةً مرّ بها. **والتفقيطُ** (`amountWords`) دالّةٌ نقيةٌ واحدة تُقرِّب بـ`r2`
  نفسِها، وتُصرِّف اسمَ العملة بالعدد (ريالٌ واحد · ريالان · ثلاثةُ ريالات) — فالرقمُ في
  السند مكتوبٌ مرّتين ولا يُحوَّر أحدُهما بقلم دون أن يفضحه الآخر.
  **وفتحُ نافذة الطباعة صار دالّةً واحدة** (`_emitPrint`) يقرؤها العقدُ وأمرُ الدفع معاً —
  السقوطُ إلى `window.open` ومعالجةُ الفشل والقيدُ في السجلّ في موضعٍ واحدٍ لا اثنين.
  **الاختبارات:** `node hail-tests.js` ✅ (+٢٠ حارساً: التفقيطُ وصيغُ العملة والتقريب ·
  وسمُ الحالة للمراحل الخمس · تطابقُ خاناتِ التوقيع مع مسار `crqNextStage`)،
  و`node contracts-check.mjs` ✅ (+٨: ظهورُ الزرّ · سندٌ كاملٌ بعنوانه · **الرقمُ المطبوع =
  المحسوب** · المبلغُ كتابةً مطابقٌ للتفقيط · وسمُ «صالحٌ للصرف» · خاناتُ التوقيع بلا بوّابةِ
  اعتمادٍ ماليّ · **قناعُ الآيبان بدورين مختلفين فعلاً**).

- **v18.9.2588 — «Missing or insufficient permissions» عند حذف العقد (بلاغُ المالك من
  الإنتاج): قاعدةٌ كتبتُها تمنع نصفَ المعاملة التي كتبتُها:**
  **الجذر.** حذفُ العقد **معاملةٌ بكتابتين**: حذفُ العقد **وإعادةُ طلبه** من
  `crq_converted` إلى `crq_approved` (وإلا بقي الطلبُ يشير إلى عقدٍ محذوف). لكنّ
  `crqUpdateOk` تمنع **أيَّ** تعديلٍ على طلبٍ محوَّل («الحالةُ النهائيةُ نهائية») —
  فتُرَدّ **المعاملةُ كلُّها** بـ`permission-denied`، ويفشل الحذفُ رغم أن قاعدةَ حذف
  العقد (`ctrDeleteOk`) تسمح به تماماً. أي: القاعدتان صحيحتان كلٌّ على حدة، والعمليةُ
  التي تجمعهما مرفوضة.
  **ولماذا لم يمسكه فحصُ المحاكي؟** لأنه جرّب `deleteDoc` **وحدَه** — أي القاعدةَ
  التي كتبتُها، لا **العمليةَ كما ينفّذها التطبيق**. وهذا هو الدرس: الفحصُ الأمنيُّ
  يجب أن يحاكي الكتابةَ المركّبةَ بشكلها، لا أن يسأل كلَّ قاعدةٍ على انفراد.
  **الإصلاح — بابٌ ضيّقٌ بأربعة شروطٍ معاً** (`crqUnconvertOk`): أدمن · من
  `crq_converted` وحدَها · إلى `crq_approved` بالضبط · وبمسح `contractId`. فلا يُفتح
  به مسدَّدٌ ولا ملغى، ولا يُنقل المحوَّلُ إلى بوّابةٍ أخرى، ولا يبقى الرابطُ.
  **والفحصُ صار بشكل العملية:** `writeBatch` يحذف العقدَ ويُحرِّر الطلبَ في دفعةٍ
  واحدةٍ كالمعاملة تماماً. **وأُثبت أنه يعضّ:** بإعادة القاعدة القديمة سقط بـ
  `7 PERMISSION_DENIED` — نصُّ خطأ الإنتاج حرفياً — ثمّ مرّ بالإصلاح.
  **وثالثة: الرسالةُ صارت تدلّ على العلاج.** رفضُ الخادم كان يصل للمستخدم سطراً
  إنجليزياً («Missing or insufficient permissions») يبدو عطلاً في النظام. `_errMsg`
  تترجمه إلى: «رفضَ الخادمُ العملية — غالباً قواعدُ Firestore لم تُنشَر بآخر نسخة»
  مع مكان العلاج. وتمرّ به كلُّ معالجات الأفعال في الوحدة (١١ موضعاً).
  **التحقّق:** `hail-tests` **1951/1951** · **`rules:check` 87/87** (٧ حالاتٍ جديدة:
  الدفعةُ المركّبة · ولا يُلغي التحويلَ غيرُ الأدمن · ولا إلى حالةٍ أخرى · ولا مع
  إبقاء الرابط · ولا المسدَّدُ ولا الملغى).

- **v18.9.2587 — «عقدٌ وطلبُ تعاقدٍ كانا تجربةً — أحتاج حذفهما» (طلبُ المالك): حذفُ عقدٍ
  لم يُوقَّع بعد، وتحريرُ طلبه في المعاملة نفسِها:**
  **العقدُ سجلٌّ ماليٌّ لا يُمحى — إلا في نافذةٍ واحدة:** ما بين إنشائه وتوقيعِ الطرف.
  وفي تلك النافذة **لم يُنتج أثراً**: لا مستخلصَ ولا أمرَ تغييرٍ ولا سداد. وهذه ليست
  دعوى: `createExtract` تشترط عقداً **سارياً**، و`CHG_CONTRACT_OK` كذلك — فحالةُ
  «بانتظار التوقيع» **وحدَها ضمانةٌ كافيةٌ على الخادم** أن لا أبناءَ له، ولا حاجةَ
  لاستعلامٍ لا تستطيعه القواعد أصلاً. وما بعدها لا يُحذف مهما كان الدور: الساري
  **يُفسَخ** (`terminate`) ولا يُمحى.
  **وحذفُه يُحرِّر طلبَه.** الطلبُ صار `crq_converted` وقتَ التحويل — فلو حُذف العقدُ
  وحدَه لبقي يشير إلى عقدٍ غيرِ موجود: سطرٌ ميتٌ في البيانات. فالمعاملةُ **الواحدةُ**
  تحذف العقدَ وتُعيد الطلبَ إلى «معتمَد» وتمسح `contractId` وتكتب قيداً في خطّه
  الزمنيّ (`unconverted`): كأنّ التحويلَ لم يقع. فيستأنف الطلبُ مسارَه — يُحوَّل ثانيةً
  أو يُلغى ثمّ يُحذف بالبابين اللذين فُتحا في v18.9.2553.
  **فتنظيفُ التجربة كاملٌ بثلاث خطواتٍ مسجَّلةٍ كلُّها:** حذفُ العقد ← إلغاءُ الطلب ←
  حذفُ الطلب. ولا خطوةَ منها صامتة.
  **والقاعدةُ على الخادم:** `ctrDeleteOk` = أدمن **و**`ctr_pending_signature`. وبقيت
  المستخلصاتُ وأوامرُ التغيير مقفلةً بلا بابٍ إطلاقاً (٤ مواضع `allow delete: if false`
  — والحارسُ يعدّها عدّاً لا «على الأقلّ»، فبابٌ يُفتح بلا قصدٍ يُسقط الفحص).
  **تصحيحٌ أثناء العمل:** فحصي الأوّل وقّع العقدَ الأصليَّ ليختبر «الساري لا يُحذف»،
  فأسقط ثلاثةَ فحوصٍ لاحقةٍ تقوم على بقائه «بانتظار التوقيع». صار للفحص **عقدُه
  الخاصُّ** يُنشئه ويوقّعه ويفسخه — فلا يُفسد ساحةَ غيره. وفحصٌ آخرُ كان يطابق الرقم
  «28» ليتأكّد أن السعرَ لا يُرسَل للنموذج، فسقط لأن **معرّفَ البند العشوائيَّ حوى
  «28»** — صارت المطابقةُ على الحقول لا على رقمٍ مجرّد.
  **التحقّق:** `hail-tests` **1948/1948** (٩ حرّاسٍ جديدة) · `contracts-check`
  **215/215** (٨ حرّاسٍ جديدة — منها المسارُ كاملاً: عقدٌ يُنشأ ويُحذف فيعود طلبُه
  «معتمَداً» ثمّ يُلغى ويُحذف فلا يبقى أثر) · **`rules:check` على المحاكي 81/81**
  (٥ حالاتٍ جديدة: الأدمن يحذف غيرَ الموقَّع · ولا غيرُه · ولا الساري ولا المفسوخ ولا المقفل).

- **v18.9.2584 — «الذكاء الاصطناعي في صياغة بنود الأعمال بشكل هندسي» (طلبُ المالك):
  محرّرُ صياغةٍ لا مُقايِسٌ ثانٍ:**
  **الحاجة.** بنودُ الأعمال تُكتب على عجلٍ في الميدان: «محارة»، «تركيب أبواب». وهذا
  الوصفُ يذهب إلى **وثيقةٍ يوقّعها الطرفان** ويُحاسَب عليها المستخلص — فبندٌ غامضٌ
  خلافٌ مؤجَّل: أيُّ محارةٍ؟ بأيّ سُمك؟ وهل تشمل الزوايا والشبك؟
  **الحدُّ الذي لا يُتجاوَز — الذكاءُ يصوغ الكلماتِ لا الأرقام.** الكميةُ وسعرُ
  الوحدة والإجماليُّ **لا تمرّ من هذا المسار إطلاقاً**: هي مالٌ يقرّره الإنسان،
  وإدخالُ نموذجٍ لغويٍّ عليها بابٌ لخطأٍ صامتٍ يوقّع عليه الطرفان. فالمقترَحُ
  **الوصفُ ووحدةُ القياس** فقط — والوحدةُ لأنها لغةُ البند لا قيمتُه. والقارئُ
  (`aiParseLines`) **لا يقرأ رقماً أصلاً**: لو أرسل النموذجُ `qty` أو `unitPrice`
  سقطا في العدم. (يحرسه فحصٌ يرسل رداً يحمل ٩٩٩٩ ويتحقّق أن الكميةَ والسعرَ لم يتغيّرا.)
  **ولا تطبيقَ صامت.** الاقتراحُ يُعرَض «قبل/بعد» جنباً إلى جنب، ويُطبَّق ببندٍ ببند
  أو دفعةً — بقرار المستخدم. والنموذجُ **لا يزيد بنداً ولا ينقص**: المطابقةُ
  بالمعرّف، وما لا يُطابق يُهمَل. وما لا تغييرَ فيه لا يُعرَض أصلاً.
  **وبناءً على القناة القائمة لا قناةٍ ثانية:** `_aiText` نفسُها (بروكسي Anthropic)
  التي تخدم أدواتِ البلاغات، ومترجمُ الأخطاء `_msgErr` نفسُه. ولا يُرسَل في النداء
  سعرٌ ولا إجماليّ — ما لا يلزم لصياغة الوصف لا يخرج من المنصة.
  **ويعمل النظامُ بدونه:** الزرُّ لا يظهر إن لم تكن طبقةُ الذكاء موجودةً في الصفحة،
  وردٌّ مشوَّهٌ أو فشلُ قناةٍ توستٌ عربيٌّ مفهومٌ لا انهيار — والنموذجُ يبقى قابلاً
  للتعبئة يدوياً. **وهو في الموضعين:** نموذجُ الطلب الجديد ومحرّرُ بنود الأدمن.
  **تصحيحٌ أثناء العمل:** كتبتُ أوّلاً أن الزرَّ يختفي حين لا تكون القناةُ «مهيّأة»،
  وأسقط الفحصُ الادّعاء: `_aiText` تعريفُ دالّةٍ عالميٌّ **موجودٌ دائماً**، والناقصُ
  قد يكون رابطَ البروكسي وحدَه — ويُستكمَل بعد لحظة. فصار الشرطُ وجودَ الطبقة، وترك
  الإعدادِ الناقص لرسالته الصريحة (سلوكُ بقية أدوات الذكاء في المنصة).
  **التحقّق:** `hail-tests` **1939/1939** (١٧ حارساً جديداً) · `contracts-check`
  **207/207** (١٠ حرّاسٍ جديدة بجذعٍ للقناة في الصفحة — لا شبكةَ في الفحص).

- **v18.9.2562 — فصلُ المهام والإرجاعُ لمرحلةٍ يعمّان المستندات الثلاثة (إتمامُ ما بدأه
  v18.9.2555 و2556):**
  **الفجوةُ التي فتحناها بأنفسنا.** القاعدتان طُبِّقتا على **طلبات التعاقد وحدَها**،
  فبقي شخصٌ واحدٌ يمرّر المستخلصَ وأمرَ التغيير من بوّابةٍ إلى بوّابة. **والمستخلصُ
  أولى المستندات بهما**: هو موضعُ خروج المال **شهرياً** لا مرّةً واحدة كأمر الدفع.
  **والمنطقُ نسخةٌ واحدةٌ لا ثلاث.** حقولُ الاعتماد في الثلاثة متطابقةُ الأسماء
  (`pmApprovedAt`…)، ولا يفترق مستندٌ عن آخرَ إلا في شيئين: **ترتيبِ بوّاباته**
  و**دالّةِ توجيهه**. فصارا وسيطين تأخذهما `docRewind`/`docRewindTargets` و
  `gateActMode` — و`crq*`/`ext*`/`chg*` أغلفةٌ رقيقة. ثلاثُ نسخٍ من المنطق كانت
  ستنحرف عند أوّل تعديل، وهو ما تكرّر في هذا المشروع مرّاتٍ (درسُ «مصدرٌ واحد»).
  **وخصوصيةُ المستخلص:** توجيهُه دالّةٌ في **صافيه** لا في قيمةٍ مخزَّنة، فالصافي
  يُحسب من العقد ويُمرَّر — ووجهاتُ إرجاعه تختلف باختلافه (دون سقف التنفيذيِّ لا
  بوّابةَ له أصلاً).
  **والحدودُ نفسُها انتقلت معهما:** الرفضُ/الإعادة لا يُمنع · وسببُ غياب الزرّ يُقال
  صراحةً (`sodNoteHTML` نصٌّ واحدٌ للثلاثة) · وزرُّ الاعتماد يحمل اسمَ بوّابته ·
  و«بانتظار إجراءك» تحترم القاعدةَ في الثلاثة · والإرجاعُ للأدمن بسببٍ إلزاميٍّ
  ولا يُرجَع منتهٍ (أمرُ تغييرٍ **مطبَّقٌ** لا يُرجَع — أثرُه وقع على العقد).
  **والسدادُ تحت القاعدة في الاثنين** (سدادُ المستخلص وسدادُ أمر الدفع) — فمن اعتمد
  لا يصرف.
  **ما بقي خارجها عمداً:** «التطبيق على العقد» (`applyChange`) و«إنشاء العقد» تنفيذٌ
  لقرارٍ اعتُمد لا اعتمادٌ جديد، فلا يدخلان فصلَ المهام.
  **التحقّق:** `hail-tests` **1922/1922** (١٦ حارساً جديداً) · `contracts-check`
  **197/197** (١٦ حارساً جديداً) — ومنها المشهدان منفَّذَين في متصفّح: سدادُ مستخلصٍ
  يُمنع على من اعتمده متى وُجدت ماليةٌ أخرى، وإرجاعُه من الشاشة إلى بوّابة التنفيذيِّ
  بسقوط اعتمادها وبقاء اعتماد مدير المشاريع؛ وأمرُ تغييرٍ يُمنع ويُرجَع كذلك.

- **v18.9.2560 — «كيف نفّذ المشتريات وأشعر المستودع بلا اعتماد التنفيذي؟» (سؤالُ المالك على
  PO-202608-0155): ختمُ التنفيذي مقرونٌ بمبلغه · وحارسُ حالةٍ لـ«تم الشراء»:**
  **أوّلاً تصحيحُ القراءة:** التنفيذيُّ **اعتمد فعلاً** في ذلك الطلب — القيدُ موجودٌ
  17:57 باسمه، وسابقٌ لقيد «تم الشراء» لا لاحقٌ له. ودليلُه في الكود لا في ترتيب
  الجدول وحدَه: خيارُ «تم الشراء» لا يظهر لمسؤول المشتريات إلا من `proc_executing`،
  و`getAvailableStatuses` تُرجع له قائمةً فارغةً عند `pending_ceo`. لكنّ السجلَّ كشف
  ما هو أخطرُ من السؤال.
  **الجذرُ الأول — ختمٌ بلا مبلغ:** كان `ceoApprovedAt` علامةً مجرّدة، تُكتب مرّةً
  ولا تُمحى ولا سقفَ لها. فاعتمادٌ على 2,639 ر.س يظلّ سارياً لو صار الطلبُ 200,000،
  و`openSendToFinanceModal` تقرأ وجودَه لا تغطيتَه فتمرّر الطلبَ للمالية بلا تنفيذيّ.
  وكلُّ وحدةٍ أخرى في المنصّة تقرن الختمَ بسقفه وتُعيد التصعيد فوقه (`contracts.js`
  في crq/ext/chg، و`hr-payments.js`) — وطلبُ الشراء وحده كان شاذّاً عن القاعدة.
  ومعه بابٌ خلفيّ: `p.status==="finance_returned"` كان تصريحَ مرورٍ **مطلقاً**، يكفي
  لفتحه أن تُعيد الماليةُ الطلبَ مرّةً ثم تُرفع قيمتُه.
  **العلاج:** `poCEOCovers(p)` — الختمُ يُكتب بمبلغه واسمِ من اعتمده ويُعاد ختمُه في
  كلّ اعتمادٍ جديد، والتخطّي مشروطٌ بأن يغطّي السقفُ القيمةَ الحالية. والختمُ بلا
  مبلغ (طلباتُ ما قبل هذا الإصدار) لا يُعتدّ به — عينُ عُرف `contracts.js:281`.
  **الجذرُ الثاني — رجوعٌ يُبقي الاعتماد:** في هذا الطلب بالذات أرجع الأدمن الحالةَ
  من «بانتظار استلام المستودع» إلى «تمت مراجعة المستودع» والختمُ باقٍ. فصار الطلبُ
  راجعاً إلى ما قبل بوّابة التنفيذي وهو يحمل اعتمادَه. العلاج: `PO_PRE_CEO_STATUSES`
  — الرجوعُ إلى أيٍّ منها يُبطل الختمَ (`poClearCEOStamp`) **ويُقيَّد في السجل**،
  في البابين معاً: `doUpdatePurchaseStatus` ونافذةُ تعديل المسؤول. والمحوُ بقيمةٍ
  فارغةٍ لا بـ`delete`: `savePurchase` يكتب بـ`merge:true`، والحقلُ المحذوفُ محلّياً
  يبقى في Firestore — فالمحوُ الصامتُ لا يمحو شيئاً.
  **الجذرُ الثالث — «تم الشراء» ينفرد بتخطّي الحارسين:** اعتراضُ `wh_receiving` في
  `doUpdatePurchaseStatus` يقع **قبل** القراءة الطازجة و**قبل** `allowed.includes`،
  و`doNotifyWarehouse` — وهي الكاتبُ الوحيد — لم تكن تفحص الحالةَ أصلاً بل دورَ
  المستخدم وحده. فمن فتح النافذة والطلبُ «قيد التنفيذ» ثمّ رفضه التنفيذيُّ أو أرجعه
  الأدمن، يظلّ قادراً على إشعار المستودع باستلامِ طلبٍ لم يعد معتمداً. العلاج:
  `_poFreshStatus` تقرأ الحالةَ من الخادم بمهلة 6ث **قبل رفع أيّ ملف** (لئلّا يُرفع
  مرفقٌ يتيمٌ لطلبٍ سيُرفض) وقبل أيّ كتابة؛ غيرُ الأدمن يُردّ، والأدمنُ يتجاوز
  بتأكيدٍ يسمّي ما يتجاوزه وبتنويهٍ داخل قيد السجل نفسِه.
  **ما لم يُعالَج هنا (مقصودٌ ومؤجَّل):** (١) قفزُ الأدمن فوق البوّابات بلا جدولِ
  انتقالاتٍ مشروعة — وقع مرّتين في هذا الطلب (17:54 و18:00). (٢) `global_purchases*`
  ليست في `ctrLocked()` فتحكمها القاعدةُ العامّة `allow write: if hasRole() &&
  !isViewer()` — أيُّ دورٍ غير الزائر يكتب أيَّ حالةٍ على أيّ طلبٍ مباشرةً، فكلُّ
  بوّابات المشتريات في المتصفّح وحده. وهي «المرحلة ٣» المؤجَّلة في
  `docs/deep-review-2026-08.md`، وتستحقّ عملاً منفصلاً لأنها تمسّ كلَّ الطلبات دفعةً.
  **أثرٌ بعد الدمج:** طلبٌ قائمٌ اعتمده التنفيذيُّ قبل هذا الإصدار (ختمٌ بلا مبلغ)
  سيُحال إليه ثانيةً عند «إرسال للمالية» — نقرةُ اعتمادٍ واحدةٌ على طلبٍ يراه، وهي
  الجهةُ الآمنةُ للخطأ.
  **الفحوص:** `hail-tests.js` 1906/1906 (+36 حارساً في §23-أ يُنفّذون المنطقَ
  المستخرَجَ لا يقرؤونه: تغطيةُ السقف · الختمُ القديمُ بلا مبلغ · إعادةُ الختم ·
  الرجوعُ المُبطِل بحالة PO-202608-0155 عينِها · `finance_returned` · ترتيبُ الحارس
  قبل الرفع) · `po-lifecycle-check.mjs` 53/53 · `ui-walkthrough.mjs` 61/61 ·
  `browser-scenarios.mjs` 58/58 — بلا أخطاء جافاسكربت.

- **v18.9.2558 — طلبان من المالك قبل الدمج: غرامةُ التأخير **بالريال**، وتعديلُ بنود
  الطلب **للأدمن**:**
  **(١) الغرامةُ بالريال لا بالنسبة.** كانت نسبةً من قيمة العقد — والنسبةُ رقمٌ لا
  يراه أحد: المقاولُ يفاوض على ريالاتٍ في اليوم، والمهندسُ يريد أن يقرأ في الوثيقة
  المبلغَ المتَّفقَ عليه لا نسبةً يحسبها عند كل مطالبة. فصارت **مبلغاً يومياً وسقفاً
  بالريال**.
  **والوثائقُ القديمةُ لا تُكسَر ولا تُترجَم:** عقودٌ ساريةٌ خُزِّنت بالنسبة، وتحويلُها
  حسابياً عند القراءة كان سيغيّر أرقاماً وقّع عليها الطرفان. فالوسمُ (`mode`) يحفظ لكلّ
  وثيقةٍ لغتَها — والقديمةُ تُقرأ وتُطبَع نسبةً **كما وُقِّع عليها**. وكلُّ حسابٍ
  (المستخلص · الغرامةُ المقترَحة · بندُ الوثيقة الورقية · بصمةُ المالية · البطاقتان)
  يمرّ بـ`penaltyPerDay`/`penaltyCap` — فلا موضعان يفترقان.
  **(٢) تعديلُ البنود للأدمن.** كانت البنودُ والقيمةُ مجمَّدتين بعد الإرسال لسببٍ
  وجيه: «وقّع المعتمِدُ على رقمٍ وسُدِّد غيرُه». والبابُ الجديد **لا ينقض العهدَ بل
  يحترمه بطريقٍ آخر**: القيمةُ تُعاد حسابُها من البنود، ثمّ `crqRevalidate` تُسقط بصمةَ
  المالية ويسقط اعتمادُ التنفيذيِّ بقيمته، ثمّ `crqNextStage` تُرجع الطلبَ إلى
  بوّابتهما — فما وُقِّع على رقمٍ قديمٍ **يُبطَل لا يُمرَّر**. واعتمادُ مدير المشاريع
  والمشتريات يبقى (بصمتُهما — الطرفُ والتنافس — لم تتغيّر). وهذا أوّلُ استعمالٍ حقيقيٍّ
  لـ`crqRevalidate` منذ كُتبت في المرحلة ٢.
  والسببُ إلزاميّ، وعتبةُ أمر الدفع تُفحَص بعد التعديل، والمنتهي لا تُعدَّل بنودُه،
  والمحرّرُ مسودّةٌ محلّيةٌ حتى الحفظ (لا كتابةَ جزئيةً أثناء الطباعة).
  **وقاعدةُ الخادم اتّسعت بمقدار الطلب لا أكثر:** `unchanged(['value','lines'])` صارت
  `|| isAdmin()` — والطرفُ وشكلُ الارتباط والمشروعُ تبقى مجمَّدةً **للأدمن نفسِه**.
  **التحقّق:** `hail-tests` **1870/1870** (٢٣ حارساً جديداً) · `contracts-check`
  **181/181** · **`rules:check` على محاكي Firestore 76/76** (أربعُ حالاتٍ جديدة:
  الأدمن يعدّل البنودَ والقيمة · وغيرُه لا · ولا يبدّل الأدمنُ الطرفَ ولا المشروع).

- **v18.9.2556 — «إرجاع الطلب إلى مرحلةٍ معيّنة للاعتماد» (طلبُ المالك): للأدمن، بوجهاتٍ
  مشتقّةٍ من مسار الطلب لا مكتوبةٍ يدوياً:**
  **الفجوة.** «رفض/إعادة» يُرجع الطلبَ إلى **مُنشئه** دائماً — خطوةٌ واحدةٌ إلى الخلف
  مهما كان الخطأ. فإن اكتُشف بعد اعتماد التنفيذيِّ أن المشتريات وقّعت على مرشَّحٍ خطأ،
  لم يكن أمام الأدمن إلا إلغاءُ الطلب وإعادةُ إنشائه من الصفر: يضيع خطُّه الزمنيُّ
  ورقمُه، ويُعاد كلُّ اعتمادٍ صحيحٍ بلا سبب.
  **الآلية — بلا آلةِ حالاتٍ ثانية.** الإرجاعُ **ليس حالةً جديدة**: يُمسح اعتمادُ
  البوّابة المقصودة **وما بعدها**، ثمّ `crqNextStage` وحدَها تقرّر أين يقف الطلب.
  فمصدرُ الحقيقة يبقى واحداً، ويستحيل أن يقف الطلبُ في مكانٍ لا تعرفه الآلة. وما قبل
  البوّابة المختارة يبقى صحيحاً — لا يسقط توقيعٌ بلا سبب.
  **وقائمةُ الوجهات تُشتقّ لا تُكتب.** لكلّ طلبٍ مسارُه: أمرُ الدفع بلا بوّابةِ اعتمادٍ
  ماليّ، وما دون سقف التنفيذيِّ بلا بوّابته. فالوجهةُ تُقبَل إن كانت الآلةُ **ستقف
  عندها فعلاً** — نجرّبها (`crqRewind`) ونسأل النتيجة. ولولا ذلك لعرضنا وجهةً يقفز
  الطلبُ فوقها فيبدو الإرجاعُ كذباً. وتُستبعَد الوجهةُ التي لا تغيّر الحالةَ الحالية.
  **والسببُ إلزاميّ** — هذا الفعلُ يُسقط توقيعاتِ آخرين، فمن حقّهم أن يقرؤوا في الخطّ
  الزمنيِّ **من أين** أُرجع الطلبُ ولماذا. والمنتهي لا يُرجَع: المحوَّلُ صار عقداً،
  والمسدَّدُ خرج ماله، والملغى مغلق. والفحصُ داخل المعاملة على الوثيقة الطازجة.
  **قواعدُ الخادم لم تحتج تعديلاً:** `crqUpdateOk` تسمح لصاحب بوّابة الحالة الحالية
  بتحريكها، والأدمن عضوٌ في كلّ بوّابةٍ وفي الافتراض (`crqGate` ⇒ `['admin']`).
  **التحقّق:** `hail-tests` **1847/1847** (١٥ حارساً جديداً) · `contracts-check`
  **171/171** — منها المسارُ كاملاً من الشاشة: طلبٌ عبَر بوّاباتِه الأربع ⇐ زرُّ
  «إرجاع لمرحلة» ⇐ وجهاتٌ مشتقّة ⇐ سببٌ إلزاميّ ⇐ وقوفُه عند المشتريات وسقوطُ
  اعتماداتها وما بعدها وبقاءُ اعتماد مدير المشاريع والقيمةِ والبنود.

- **v18.9.2555 — «بعد الاعتماد من المفترض يختفي زر الاعتماد» (بلاغُ المالك): فصلُ المهام —
  من وقّع بوّابةً لا يوقّع التاليةَ، بمهربٍ يمنع التعطيل:**
  **ما رآه المالك ليس زرّاً عالقاً.** السجلُّ الزمنيُّ في صورته يقول «اعتماد — مدير
  المشاريع»، والحالةُ انتقلت إلى «بانتظار اعتماد المشتريات». فالزرُّ **اختفى ثمّ عاد
  لبوّابةٍ أخرى** — لأن الأدمن عضوٌ في كلّ بوّابة (`GATE_ROLES`) عمداً منذ «مديرُ
  المشاريع هو نفسُه الأدمن». لكنّ الأثرَ الحقيقيَّ أخطرُ من الالتباس: **شخصٌ واحدٌ
  يمرّر الطلبَ من الإنشاء إلى السداد بنقراتٍ متتالية**، فتصير البوّاباتُ الأربعُ
  أربعَ نسخٍ من رأيٍ واحد — صورةَ رقابةٍ بلا رقابة.
  **القاعدة:** من اعتمد بوّابةً لا يعتمد التاليةَ على **الطلب نفسِه**.
  **والمهربُ شرطٌ لا تفصيل:** في فريقٍ لا ثانيَ فيه لتلك البوّابة يصير المنعُ باباً
  مسدوداً — الطلبُ يقف بلا أحدٍ يحرّكه. فالمنعُ مشروطٌ بوجود **مستخدمٍ آخرَ يملك
  البوّابة فعلاً** (يُقرأ من `USERS` — مصدرِ «إدارة مستخدمي المشتريات»)؛ وإلا بقي
  الزرُّ وسُجِّل الاعتمادُ **«نيابةً»** في الخطّ الزمني مع عَلَمٍ `delegatedApproval`.
  فالقيدُ يظهر في السجلّ ولو لم يمنع.
  **وقرارٌ واحدٌ تقرؤه الشاشةُ وطبقةُ البيانات معاً** (`crqActMode` ⇒ `none`/`act`/
  `delegate`/`blocked`) — فلا ينحرف زرٌّ عن قاعدة. والفحصُ على **الوثيقة الطازجة**
  داخل المعاملة: قد يكون معتمِدٌ آخرُ حرّك الطلبَ بعد آخر لقطةٍ رآها المتصفّح.
  **وثلاثةُ قيودٍ على القاعدة نفسِها:**
  (١) **الرفضُ/الإعادة لا يُمنع أبداً** — لا يراكم سلطةً، ومنعُه كان سيحبس الطلبَ بلا مخرج.
  (٢) **سببُ غياب الزرّ يُقال صراحةً** — زرٌّ يختفي بلا تفسيرٍ يُقرأ عطلاً لا قاعدة،
  وهو أصلُ البلاغ. وزرُّ الاعتماد صار يحمل **اسمَ بوّابته** («اعتماد — المشتريات»).
  (٣) **المطابقةُ باسم الدخول** لا بالاسم المعروض (تغييرُ الاسم لا يُسقط القيد)، وللوثائق
  القديمة بلا `*ApprovedByUser` تُطابَق بالاسم المعروض — فلا يسقط القيدُ عنها بصمت.
  **والاتّساقُ كاملٌ:** «بانتظار إجراءك» **وعدّادُ «بانتظار دورك»** يحترمان القاعدةَ —
  عدّادٌ يعدّ ما لا زرَّ له يبعث المستخدمَ يبحث عن عملٍ ليس له (درسُ «بانتظار التوقيع»).
  **وحدُّها الصريح:** ضبطٌ إجرائيٌّ لا حاجزٌ أمنيّ — الأدمن يملك القاعدةَ ويستطيع
  التجاوزَ من أدوات المطوّر. غايتُه أن يجعل تجاوزَ الفصل **فعلاً واعياً مسجَّلاً**
  لا نقرةً عابرة. (والقاعدةُ لم تُطبَّق بعدُ على المستخلصات وأوامر التغيير — بندٌ تالٍ.)
  **التحقّق:** `hail-tests` **1832/1832** (١٦ حارساً جديداً) · `contracts-check`
  **161/161** — منها المشهدُ نفسُه مُعاداً في متصفّح: بلا مسؤول مشترياتٍ يعتمد نيابةً،
  وبوجوده يختفي الزرُّ ويظهر سببُ غيابه ويبقى الرفضُ متاحاً.

- **v18.9.2553 — طلبان من المالك: بوّابةُ المشتريات في أمر الدفع · وحذفُ الطلبات الملغاة للأدمن:**
  **(١) «احتاج يكون اعتماد للمشتريات بعد مدير المشاريع وبعد ذلك سداد المالية».**
  كان أمرُ الدفع يتخطّى المشترياتِ واعتمادَ المالية معاً — بحجّةٍ مكتوبةٍ في الكود:
  «لا نطاقَ يُراجَع ولا شروطَ تجارية». وهي حجّةٌ **تنظر إلى الورقة لا إلى المال**:
  أمرُ الدفع صرفٌ لطرفٍ خارجيّ، ومراجعةُ المشتريات هي التي تسأل «أهذا الطرفُ صحيحٌ
  وسعرُه معقول؟» — ولا يُعوّضها اعتمادُ مدير المشاريع (يراجع الحاجةَ لا التوريد) ولا
  سدادُ المالية (يُنفّذ ولا يُقرّر). فصار المسار: **مدير المشاريع ← المشتريات ←
  (التنفيذي فوق السقف) ← سدادُ المالية**.
  وبقيت المالية **مُسدِّدةً لا مُعتمِدة**: بوّابةُ اعتمادها للشروط التجارية والمحتجز،
  ولا شروطَ في أمر الدفع — فالمسارُ زاد بوّابةً واحدةً لا اثنتين. والتغييرُ **سطرٌ
  واحدٌ في `crqNextStage`** لأن المسار دالةٌ واحدةٌ للعقد ولأمر الدفع منذ المرحلة ٢،
  ولم يلزم الخادمَ شيء: `crq_pending_proc` مُوجَّهةٌ أصلاً في `config.js` فتصل
  المشترياتِ رسالتُها من أوّل يوم.
  **(٢) «إمكانية حذف الطلبات الملغاة للأدمن».** الحذفُ كان ممنوعاً في كلّ مجموعات
  التعاقدات (`allow delete: if false`) — والقاعدةُ صحيحةٌ لأن الطلبَ أثرٌ ماليّ. لكنّ
  **الملغى استثناءٌ حقيقيّ**: ورقةٌ ماتت *قبل* أن تُنتج أثراً — لا عقدَ ولا سداد —
  فبقاؤها في الشاشة ضجيجٌ لا سجلّ. فُتح البابُ بحدَّين لا واحد: **الأدمن** *و*
  **الحالةُ الملغاة**، في ثلاث طبقاتٍ تقول القاعدةَ نفسَها: الزرُّ لا يظهر إلا
  عليهما · و`deleteRequest` تقرأ **الوثيقة الطازجة** (لقطةٌ قديمةٌ كانت ستسمح بحذف
  طلبٍ خرج من الإلغاء في متصفّحٍ آخر) · و`crqDeleteOk` في `firestore.rules` ترفض على
  الخادم ولو زُوِّرت الواجهة. والحذفُ **يُسجَّل في التدقيق**: ما حُذف يبقى مذكوراً.
  **التحقّق:** `hail-tests` **1815/1815** · `contracts-check` **153/153** (منها
  المسارُ الجديدُ منفَّذاً في متصفّح، وأربعةُ حرّاسٍ للحذف من طبقة البيانات، ومسارُ
  الشاشة كاملاً: لا زرَّ على حيّ ← زرٌّ على الملغى ← ضغطٌ ← سقوطٌ من القائمة) ·
  و**`rules:check` على محاكي Firestore الحقيقيّ 72/72** (ستُّ حالاتٍ جديدة: حيٌّ لا
  يُحذف ولو للأدمن · الملغى يُحذف للأدمن · لا للمشتريات ولا لمدير المشاريع · ولا
  المسدَّدُ ولا المحوَّلُ إلى عقد).

- **v18.9.2552 — «احتاج يظهر زر يفتح تفاصيل الطرف» (طلبُ المالك): خانةُ الطرف كانت نصّاً ميتاً:**
  في بطاقتَي الطلب والعقد يظهر اسمُ الطرف مجرّداً — **وعليه يقع القرار**: المعتمِدُ يوافق
  على صرفٍ لطرفٍ لا يرى وثائقَه ولا صلاحيتَها ولا حالتَه (أمحظورٌ هو؟) ولا أداءَه السابق،
  وكلُّ ذلك في سجلّ الأطراف على بُعد تنقّلٍ يدويٍّ وبحثٍ بالاسم. أُضيف زرُّ **«تفاصيل
  الطرف»** داخل الخانة نفسِها (`vendorCell`) — سطرٌ خاصٌّ به تحت الاسم فلا يُزاحمه على
  الجوال.
  **ولا زرَّ يَعِد ولا يفي:** لا يظهر إلا حين يكون خلفه شيءٌ يُفتَح — معرّفُ طرفٍ مخزَّن
  **وصلاحيةُ اطّلاعٍ** على السجل؛ والصلاحيةُ تُفحَص في `openVendorFrom` نفسِها لا على
  الزرّ وحده (قاعدةُ المنصة: الرفضُ يقع في الدالّة لا في الترميز).
  **وبلا مؤقّتِ انتظار:** `showPage(PAGE_VENDORS)` تُشغّل مزامنةَ الأطراف، ولقطتُها تُعيد
  الرسمَ وحدَها متى وصلت — فلا تبقى «تعذّر العثور على الطرف» معلّقةً لطرفٍ موجودٍ لم
  يُحمَّل بعد.
  **التحقّق:** `hail-tests` **1804/1804** (ستةُ حرّاسٍ جديدة)، و`contracts-check`
  **146/146** — ومنها فحصٌ **يضغط الزرَّ فعلاً** ويتحقّق أن بطاقةَ الطرف نفسِه فُتحت
  (لا قائمتُهم ولا «تعذّر العثور») لا أن الزرَّ مكتوبٌ في الترميز.

- **v18.9.2548 — «عند إنشاء طلب تعاقد يكرّر مرتين» (بلاغُ المالك): بطاقتان بالمعرّف نفسِه —
  والجذرُ تعويضُ الكمون لا إرسالٌ مكرّر:**
  **الدليلُ في الصورة قبل الكود.** البطاقتان تحملان **`CRQ-2608-0002` نفسَه**. ولو كان
  الإرسالُ مكرّراً (نقرتان · إعادةُ إرسالٍ بعد فشلٍ ظاهر) لاختلف المعرّفان — العدّادُ
  في `genReqId` معاملةٌ ذرّية لا تُعطي رقماً مرتين. فالوثيقةُ **واحدةٌ في قاعدة البيانات**،
  والتكرارُ في **المرآة المحلّية** التي تُرسَم منها الشاشة.
  **الجذر.** `createRequest` كانت تضيف الوثيقةَ يدوياً بعد نجاح `set()`
  (`_reqs.unshift(doc)`). لكنّ مستمعَ `onSnapshot` **يعرض الكتابةَ محلّياً قبل أن يُحلَّ
  وعدُ `set()`** (تعويضُ الكمون — سلوكُ Firestore الموثَّق لا حالةُ سباقٍ نادرة): فاللقطةُ
  تُعيد بناء `_reqs` وفيها الطلبُ الجديد، ثمّ تُضيفه الإضافةُ اليدويةُ نسخةً ثانية.
  وأثرُه ليس تجميلياً: العدّاداتُ («بانتظار دورك» · «قيد الاعتماد») تعدّ الصفَّين.
  **الإصلاح.** `_mirror(arr, doc, front)` — **إسنادٌ بالمعرّف** لا إضافةٌ عمياء: إن وُجد
  المعرّفُ استُبدل، وإلّا أُدرِج. تمرّ به كلُّ إضافةٍ إلى المرايا الأربع (الطلبات ·
  العقود · المستخلصات · أوامر التغيير) — وكانت العلّةُ نفسُها كامنةً في `createExtract`
  و`createChange` بانتظار من يراها. والترتيبُ لم يعُد يهمّ: سبقت اللقطةُ الوعدَ أو تأخّرت.
  **والعلّةُ الثانيةُ في الصورة نفسِها: «اعتماد الطلب» بزرِّ حذفٍ أحمرَ وسلّةِ مهملات.**
  `showConfirm` افتراضاتُها للحذف، ونداءاتُ الوحدة كانت تمرّر العنوانَ والنصَّ فقط —
  فالمربّعُ يسأل عن اعتمادٍ وزرُّه يقول «حذف». الآن `_confirm` تأخذ **نيّةَ الإجراء**
  (`kind`: `approve`/`reject`/`danger`/`neutral`) وافتراضُها محايدٌ لا مدمِّر.
  وبالمناسبة: `showConfirm` **تَرفُض** الوعدَ عند «إلغاء»، فكانت نقرةُ الإلغاء تهبط في
  `catch` وتُظهر «⚠ تعذّر الإجراء» بلا خطأ — الآن `_confirm` تُحلّ دائماً بـ`true`/`false`.
  **★ ولماذا لم يمسك الفحصُ هذا؟** لأن مُحاكي Firestore في فحوص المتصفّح كان يستدعي
  `onSnapshot` **مرّةً واحدةً عند التركيب ولا يعود أبداً** — فصنفُ «تعويض الكمون» كلُّه
  كان خارج مرمى الفحص بنيوياً. الآن المستمعون **أحياء**: كلُّ كتابةٍ تُبثّ لمشتركي
  المجموعة كما في الإنتاج. وأوّلُ تشغيلٍ بعد التحويل كشف بذرةً غيرَ أمينةٍ في
  `contracts-check` (طلبُ شراءٍ يُدفَع في المصفوفة لا في المخزن، فيختفي عند أوّل كتابة) —
  صارت البذرةُ تُكتب في المخزن.
  **التحقّق:** `hail-tests` **1798/1798** (ثمانيةُ حرّاسٍ جديدة: `_mirror` بالمعرّف ·
  لا إضافةَ عمياءَ في المصدر · كلُّ نداءِ تأكيدٍ يُصرّح بنيّته · «إلغاء» تُحلّ لا تُرفَض)،
  و`contracts-check` **144/144** (منها بطاقةٌ واحدةٌ في الشاشة ونسخةٌ واحدةٌ في الذاكرة
  بعد إنشاءٍ حقيقيٍّ ومستمعٍ حيّ)، و`browser-scenarios` **58/58**، و`ui-walkthrough` **61/61**.

- **v18.9.2545 — «مديرُ المشاريع هو نفسُه الأدمن»: دورٌ احتياطيٌّ يمنع ضياعَ الإشعار، وفحصٌ ينفّذ التوجيه:**
  **الطلب.** «احتاج يكون مديرُ المشاريع هو نفسَه الأدمن». والأدمنُ يملك **كلَّ بوّابةٍ**
  في المنصة أصلاً (كلُّ `roles` في `contracts.js` تشمله) — فالناقصُ **أن تصله رسائلُها**.
  **الجذرُ الأعمّ.** `wa(chg): لا مستلم للدور project_manager — تخطّي` سطرٌ في سجلٍّ لا
  يقرؤه أحد، **والإشعارُ يضيع بصمت**؛ ومَن ينتظره الاعتمادُ لا يعلم أنّ شيئاً انتظره.
  فالحلُّ ليس ترقيعَ دورٍ واحدٍ بل **دورٌ احتياطيٌّ عامّ** (`WA_ROLE_FALLBACK`، افتراضُه
  `admin`): حين لا يُوجَد لدورٍ مستلمٌ واحد تذهب الرسالةُ إليه.
  **«عند الفراغ فقط» شرطٌ لا تفصيل.** لو أُضيف الأدمن نسخةً في كلّ رسالةٍ لتلقّى صورةً
  من كلّ حدثٍ في المنصة **فتوقّف عن قراءتها** — فيضيع الإشعارُ بطريقةٍ أخرى. ويُستنفَد
  الدورُ الحقيقيُّ في **كلّ** المواضع (المركزيُّ ثمّ مستنداتُ المشاريع) قبل الارتداد،
  ولا يرتدّ الاحتياطيُّ على نفسه، ويُعطَّل كليّاً بقيمةٍ فارغة.
  **★ وفحصٌ جديدٌ ينفّذ الطبقةَ الخادمية لا يقرؤها:** `wa-routing-check.mjs`. كانت هذه
  الطبقةُ محروسةً **بتعابيرَ نمطيةٍ فقط** — تُثبت أنّ السطرَ مكتوبٌ لا أنّه يفعل ما يقول.
  وأخطاءُ التوجيه **صامتةٌ بطبعها**: مستلمٌ لا يُوجَد · خانةٌ زائدة · نصٌّ حرٌّ يتسرّب —
  لا يظهر منها شيءٌ في النظام، والرسالةُ لا تصل فحسب. الفحصُ يستدعي الدوالَّ الحقيقيةَ
  على Firestore مُحاكاةً ويتحقّق من **الناتج**: مَن المستلم، وكم خانةً، وما محتواها.
  بلا شبكةٍ ولا متصفّحٍ ولا Java ⇒ **مُدرَجٌ في CI** مع بقية الفحوص. ✅ **19/19**.
  **وأوّلُ تشغيلٍ له كشف كذبةً في تعليقي أنا:** كُتب أنّ القيمةَ الفارغة تُعطّل الارتداد،
  و`||` تعامل السلسلةَ الفارغةَ معاملةَ «غيرِ مضبوطة» فترتدّ إلى `admin`. صارت `??` —
  **والتعليقُ الذي لا يُنفَّذ ليس عقداً بل أمنية**.
  **الاختبارات.** `hail-tests` ✅ **1790/1790** (+٥) · `wa-routing-check` ✅ **19/19**.
  **ما بعد الدمج:** خادميّ — `firebase deploy --only functions`. ولا يعمل الارتدادُ ما لم
  يكن للأدمن نفسِه **رقمٌ وموافقةٌ صريحة** (`phone` + `waOptIn`).

- **v18.9.2543 — إشعارُ «بانتظار التوقيع»: رفعُ تناقضٍ بين ما نعرضه وما نرسله:**
  **الجذر.** المرحلةُ ٨ أدرجت «عقدٌ بانتظار التوقيع» في بطاقة «بانتظار إجراءك» بوصفه
  انتظاراً حقيقياً، **واستثنت حالاتِ العقد كلَّها من توجيه واتساب** بحجّة أنّ «صاحبَها
  أمام الشاشة». والاثنان لا يجتمعان: **ما يستحقّ بطاقةً يستحقّ رسالة**.
  **الحسم.** القاعدةُ تبقى — **لا إشعارَ بلا منتظِر** — فإيقافُ العقد واستئنافُه وإقفالُه
  وفسخُه يفعلها صاحبُها وهو ينظر إلى الشاشة، فلا تُشعِر أحداً. **والاستثناءُ واحدٌ
  معلَّل:** العقدُ يولد بانتظار **فعلٍ من غير مَن أنشأه** — أن يُطبَع ويُوقَّع مع الطرف
  ثمّ تُرفع نسختُه — وهو انتظارٌ **يحجز المالَ ويمنع كلَّ مستخلصٍ** حتى يقع.
  **والمستلمُ المشتريات وحدَها**، وهي مَن أنشأت العقد وتملك علاقةَ الطرف. يجوز الفعلُ
  لمدير المشروع والأدمن أيضاً، **لكنّ الرسالةَ تذهب لصاحب المسؤولية لا لكلّ من يملك
  الزرّ** — وإلّا صار الإشعارُ جماعياً بلا صاحبٍ فيتجاهله الجميع.
  **ولا يُشعَر مُنشئُ العقد بفعل نفسه:** هو المستلمُ نفسُه، فمجموعةُ «إشعار المالك» فارغة.
  **حارسٌ يمنع تمدّدَ الاستثناء.** الفحصُ **يعدّ مفاتيحَ** خريطة حالات العقد ويشترط
  **واحداً لا أكثر**، ويتأكّد أنّ الإيقافَ والاستئنافَ والإقفالَ والفسخَ ليست فيها. فلا
  تُضاف حالةٌ لاحقاً بحسن نيّةٍ فيصير التوجيهُ ضجيجاً — والقاعدةُ مكتوبةٌ في الفحص لا
  في تعليقٍ يُقرأ ولا يُلزم.
  **الاختبارات.** `hail-tests` ✅ **1785/1785** (+٥، وحلّت محلَّ حارسٍ قديمٍ صار خطأً).
  **ما بعد الدمج:** خادميٌّ — `firebase deploy --only functions`.

- **v18.9.2541 — بطاقةُ أداء الطرف، والتعاقداتُ في لوحة المعلومات (المرحلتان ١٠ و١١):**
  **(١) الأداء — وقائعُ لا تقدير.** السؤالُ الذي تجيبه: «هل نتعاقد معه ثانيةً؟» — وكان
  يُجاب بالذاكرة، ومَن يتذكّر أنّ هذا المقاولَ تأخّر أربعين يوماً في عقدٍ قبل سنة؟
  `vendorScorecard` دالّةٌ نقيّةٌ تجمع من عقوده ومستخلصاته وأوامر تغييره: العددَ والقيمةَ
  والمسدَّدَ والتأخّرَ والمستخلصاتِ المُعادةَ والفسخ.
  **★ ولا درجةَ مخترَعةً ولا نجوم — قرارٌ لا إغفال.** «٤٫٢ من ٥» من عقدين توهم بدقّةٍ لا
  تحتملها البيانات، وتُخفي خلف رقمٍ واحدٍ وقائعَ متعارضة (تأخّر لكن لم يُفسَخ · غالٍ لكن
  لا نزاع). نعرض الوقائعَ ومعها **إشاراتٌ مسمّاةٌ لكلٍّ سببُها الظاهرُ في نصّها**، والحكمُ
  للقارئ. ويحرسه فحصٌ يتأكّد أن `score`/`rating`/`stars` **غيرُ موجودةٍ أصلاً** في الناتج.
  **وطرفٌ بلا عقودٍ لا تُخترَع له أصفار:** القسمُ يقول «لا عقودَ معه بعد — لا أداءَ يُقاس».
  **★ وتأخّرُ عقدٍ منتهٍ يُقاس بتاريخ إقفاله لا باليوم** — وإلّا صار كلُّ عقدٍ قديمٍ
  «متأخّراً» بمرور الزمن، فتتّهم البطاقةُ مقاولاً سلّم في موعده قبل سنتين. يحرسه فحصٌ
  يقارن القياسَ بتاريخين متباعدين ويشترط تساويَهما.
  **(٢) اللوحة.** الوحدةُ كانت تظهر في صفحتها وفي الموازنة و«بانتظار إجراءك» — وتغيب عن
  الشاشة التي يفتحها المالكُ أوّلَ الصباح. بطاقةٌ تعرض: العقودَ الساريةَ وقيمتَها،
  و**الملتزَمَ به المتبقّي** (قيمةُ السارية − ما سُدِّد منها)، والمستخلصاتِ المنتظِرةَ
  للسداد، وتحذيراً لما تجاوز مدّتَه أو انتظر التوقيع. **وتختفي عمداً حين لا عقدَ سارياً
  ولا منتظِرَ توقيع** — بطاقةُ أصفارٍ ضجيجٌ لا معلومة. يحرسه فحصُ متصفّحٍ يقيس **تطابقَ
  ظهورها مع وجودِ ما يُعرَض** لا يفترض ظهورَها.
  **★ وعطلٌ في المرحلة ٨ صُحّح هنا:** كان اللفّان (`hookMyTasks`/`hookDash`) مربوطَين
  بـ`renderCtrs` — أي **لا تظهر البطاقتان إلا بعد زيارةِ صفحة التعاقدات مرّةً على الأقلّ**،
  ومَن لا يفتحها أبداً هو أوّلُ من يحتاجهما. صارا يُركَّبان في `init` وفي مراقب الـDOM.
  **الاختبارات.** `hail-tests` ✅ **1780/1780** (+٢٢) · `contracts-check` ✅ **142/142** ·
  `ui-walkthrough` 61/61 · `browser-scenarios` 58/58 · `po-lifecycle-check` 53/53 ·
  `perf-contract-check` 47/47.

- **v18.9.2540 — قالبا التعاقدات المخصّصان: الكودُ جاهزٌ لهما، وزرُّ الرسالة صار يعمل:**
  **(١) الفخُّ الذي وثّقناه أمس أُغلق.** `hr-payments` تبدّل **عددَ الخانات** حسب القالب
  المضبوط، و`lib/contracts.js` كانت ترسل خمساً دائماً — فضبطُ قالبٍ رباعيٍّ كان سيجعل
  Meta ترفض **كلَّ** رسائل التعاقدات **بلا أن يتغيّر سطرٌ في النظام**. الآن `approvalParams`
  تفرز **بمطابقة اسم القالب** (`usingPoApprovalTemplate`) لا بعَلَمٍ يُنسى: ٥ للمستعار
  ({{5}} «عدد البنود» = 1) و٤ للمخصّص. **ويحرسه فحصٌ تنفيذيّ** يضبط البيئةَ ويعيد تحميل
  الوحدة ويتحقّق من الشكلين ثمّ يستعيد الافتراض — فمسارٌ يستيقظ بعد أسابيع لا يُترك
  لقراءةِ سطرٍ تُطمئن ولا تُثبت.
  **والسياقُ يختلف بين القالبين عمداً:** نصُّ المستعار يقول «طلب شراء» فتعوّضه {{2}}
  بـ«مستخلص **عقد** — المشروع»، ونصُّ المخصّص يقول «مستند تعاقدات» أصلاً فتكفي «مستخلص —
  المشروع» بلا حشو.
  **(٢) ★ عطلٌ حقيقيٌّ كُشف بالفحص لا بالبلاغ: زرُّ «فتح المستند» لم يكن يعمل أصلاً.**
  موجِّهُ الروابط في `index.html` يعرف `HRP-` وحدَها ويعتبر كلَّ ما عداه **طلبَ شراء** —
  فرسالةُ مستخلصٍ تنتهي بـ«تعذّر إيجاد الطلب — قد يكون في مشروع آخر». **زرٌّ يَعِد ولا
  يفي أسوأُ من زرٍّ غائب**: المستخدمُ يظنّ العطلَ في بياناته فيبحث حيث لا شيء. أُضيفت
  البادئاتُ الأربع، **وقرارُها للوحدة نفسِها** (`contracts.ownsId`) فلا تُنسَخ القائمةُ
  في موضعين فتتفرّعا. و`openById` تنقل المستخدمَ لمكانه فوراً ثمّ تفتح المستندَ فور وصول
  لقطته **بحدٍّ زمنيٍّ صريح** (٢٠ ثانية) — لا انتظارَ أبديٍّ لمعرّفٍ خاطئٍ أو مستندٍ حُذف،
  ولا فتحَ لمن لا يملك الصلاحية.
  **(٣) نصُّ القالبين وخطواتُ تقديمهما** في `docs/whatsapp-notifications-STATUS.md §المرحلة ٦`،
  مبنيّةً على عقبتَي التقديم اللتين وقعتا فعلاً في الموارد البشرية: **سطرٌ ختاميٌّ ثابتٌ
  إلزاميّ** (Meta ترفض قالباً ينتهي بمتغيّر)، و**خانةُ العيّنة تريد الرابطَ كاملاً**.
  ⇒ يومَ يعتمد Meta القالبين: **سطران في `.env` ونشرٌ، بلا تعديل كود**.
  **الاختبارات.** `hail-tests` ✅ **1758/1758** (+٨) · `contracts-check` 137/137 ·
  `ui-walkthrough` 61/61 · `browser-scenarios` 58/58 · `po-lifecycle-check` 53/53.
  **ما بعد الدمج:** موجِّهُ الروابط في المتصفّح يعمل بالدمج؛ وشكلُ الخانات **خادميّ**
  فلا يسري حتى `firebase deploy --only functions`.

- **نشرُ المرحلة ٨ على الإنتاج (١٠ أغسطس ٢٠٢٦) — توثيقٌ لا تغييرَ كود:**
  `firebase deploy --only functions` من Cloud Shell. **الناتج: ستُّ دوالٍّ منشورة**
  (`crqRouteCreate/Update` · `extRouteCreate/Update` · `chgRouteCreate/Update`) — كلُّها
  `v2` على `google.cloud.firestore.document.v1.created/updated`، `us-central1`, `nodejs20`.
  والقديمةُ (`waSender`/`waRetry`/`poRoute*`/`hrpRoute*`) حُدِّثت بلا حذفِ شيء.
  **التحقّقُ جرى من خارج المنصّة** — بكتابة مستندٍ في `global_contract_changes` مباشرةً
  بـ`firebase-admin`، فيفصل طبقةَ الخادم عن الواجهة كلَّها. السجلُّ ردّ:
  `wa(chg): لا مستلم للدور project_manager (CHG-WATEST-…) — تخطّي` — أي أنّ **المحفّزَ
  أُطلق، والمستندَ قُرئ، والحالةَ `chg_pending_pm` تُرجمت إلى دورها، والبحثَ عن رقمه جرى**.
  السلسلةُ كاملةٌ عاملة؛ والمفقودُ بياناتٌ لا كود.
  **ورسالةٌ حقيقيةٌ وصلت الجوّال** بحقن مستندٍ في `wa_outbox` — فثبت الطرفُ الآخر:
  التوكنُ صالحٌ والقالبُ يقبل خمسَ خاناتٍ والتسليمُ يعمل، **وخانةُ السياق ظهرت
  «أمر تغيير عقد — …»** كما صُمّمت.

  **ثلاثةُ بنودٍ مرصودةٍ لم تُعالَج:**
  **(١) `project_manager` بلا رقمٍ مفعَّل.** ثلاثةُ أدوارٍ جاهزة (مشتريات · مالية · تنفيذيّ)
  والرابعُ لا. وهو **أكثرُ الأدوار انتظاراً في هذه الوحدة** (بوّابةُ كلّ مستخلصٍ وكلّ أمرِ
  تغييرٍ تبدأ عنده). الشرطان: `phone` **و**`waOptIn === true` — والثاني هو المنسيُّ عادةً،
  وهو ليس زينةً: الإرسالُ بلا موافقةٍ صريحةٍ يعرّض القالبَ للإيقاف فتنقطع إشعاراتُ الجميع.
  **(٢) النصُّ الثابتُ في القالب المستعار يقول «طلب شراء»** ولو كان المستندُ أمرَ تغيير —
  الثمنُ المعروفُ لاستعارة قالبٍ معتمَدٍ ليعمل النظامُ لحظةَ النشر، وخانةُ السياق تخفّفه
  ولا تشفيه.
  **(٣) ★ فخٌّ كامنٌ ينفجر يومَ يُعتمَد قالبٌ مخصّص:** `hr-payments.js` تبدّل **عددَ
  الخانات** حسب القالب المضبوط (٥ للمستعار · ٤ للمخصّص)، **و`lib/contracts.js` ترسل خمساً
  دائماً**. فضبطُ `WA_CTR_APPROVAL_TEMPLATE` على قالبٍ رباعيّ سيجعل Meta ترفض الرسائل
  بخطأ عدد المتغيّرات — **وتنقطع إشعاراتُ التعاقدات بلا أن يظهر شيءٌ في النظام**. لم يُعالَج
  بعد (لا يؤذي اليوم: القالبُ مستعارٌ والعددُ صحيح)، والعلاجُ نسخُ المبدّل نفسِه.

- **v18.9.2537 — وحدةُ التعاقدات (المرحلة ٨): ثلاثُ فجواتِ ربطٍ كانت تجعل النظامَ صحيحاً وغيرَ مستعمَل:**
  **الجذرُ المشترك.** الوحدةُ بُنيت كاملةً ومعزولةً — وهذا صحيح — لكنّ العزلَ صار وحشةً:
  لا تلتقي بالمشتريات حيث يجب، ولا تظهر حيث يفتح المعتمِدُ شاشتَه، ولا تصل جوّالَه.
  **(١) طلبُ الشراء المرتبطُ بعقد — الحارسُ الذي كان باباً بلا مفتاح.** المرحلةُ ٥ بنَت
  `poIsUnderContract`: طلبُ شراءٍ يحمل `contractId` يُستبعَد من مصروف الشراء لأنه محسوبٌ
  في العقد. **ولا شاشةَ كانت تكتب ذلك الحقل** — فموادُّ تُشترى لعقدٍ بطلبِ شراءٍ تُحسب
  **مرّتين** في الموازنة. الآن: تبويبُ «طلبات الشراء» في بطاقة العقد يربط ويفكّ، والربطُ
  **من جهة العقد** لا من الطلب (هناك يعرف القارئُ ما يربط وبماذا، وهناك يظهر الأثر).
  وتفاصيلُ طلب الشراء تعلن بشارةٍ أنه جزءٌ من عقدٍ ومستبعَدٌ من مصروف الشراء — فلا يبحث
  قارئُها عن ماله في خانةٍ لن يجده فيها.
  **وعهدُ العزل ضُيّق ولم يُلغَ.** كان الحارسُ يمنع أيَّ لمسٍ لـ`global_purchases`، فأسقطته
  هذه المرحلة. ولم أحذفه: **حقلُ الربط وحدَه** مسموح (يُفحَص اسماً اسماً)، ولا إنشاءَ طلبٍ
  ولا تحريكَ حالةٍ ولا مساسَ بمبالغه، ولا مستمعَ ثانياً للمجموعة. الثقبُ مقصودٌ ومقيسٌ لا
  اتّساعٌ في الجدار — وهو **عكسُ التسريب**: به لا يُحسب المالُ مرّتين.
  **(٢) «بانتظار إجراءك».** لوحةُ المشتريات تعرض بطاقةَ ما ينتظر دورَك؛ والتعاقداتُ كانت
  غائبةً عنها تماماً — فمديرُ المشاريع الذي ينتظره مستخلص، والماليةُ التي ينتظرها سداد،
  عليهم أن **يتذكّروا** فتحَ الشاشة. الآن بطاقةٌ ثانيةٌ بجوارها تجمع أربعةَ مصادر (طلبٌ ·
  عقدٌ بانتظار توقيع · مستخلص · أمرُ تغيير)، **مبنيّةً من البوّابات نفسِها** التي تحرس
  الأزرار لا من قائمةٍ ثانيةٍ تنحرف عنها، والأقدمُ أوّلاً. ونُفِّذت **بلفّ** `renderPOMyTasks`
  لا بتعديلها (نمطُ `hookShowPage`) — فالوحدةُ ما زالت لا تطلب من `index.html` إلا وسمَ
  `<script>` واحداً كما هي منذ المرحلة ١.
  **(٣) واتساب — الفخُّ نفسُه للمرّة الثالثة.** `_notify` تكتب في **جرس التطبيق** من متصفّح
  صاحب الإجراء وحده؛ فمَن ينتظره اعتمادٌ لا يعلم به ما لم يفتح النظام. هذا بعينِه العطلُ
  الذي بلّغ عنه المالك في الموارد البشرية (v18.9.2518) — **ورُصد هنا قبل أن يقع**.
  `functions/lib/contracts.js` يوجّه ثلاثَ مجموعاتٍ بمنطق `purchases.js` حرفياً.
  **ثلاثٌ لا أربع:** حالاتُ العقد نفسِه (توقيع · إيقاف · إقفال · فسخ) يفعلها صاحبُها وهو
  أمام الشاشة، فلا منتظِرَ لها في جواله — **وإشعارٌ بلا منتظِرٍ ضجيجٌ يُدرَّب الناسُ على
  تجاهله**. والقالبُ يستعير قالبَي الشراء المعتمَدَين فتعمل الإشعاراتُ لحظةَ النشر، وخانةُ
  السياق تقول «مستخلص عقد» لئلّا تقرأ الماليةُ «طلب شراء» فتبحث في الشاشة الخطأ (درسُ HRP
  المدفوعُ ثمنُه مرّة). ولا مبالغَ في المتن ولا أسماءَ أطراف.
  **الحارسُ الذي يمنع تكرار الفجوة.** فحصٌ يشتقّ بوّاباتِ الوحدة من `contracts.js` نفسِه
  (`GATE_ROLES`/`EXT_GATES`/`CHG_GATES`) ويطابقها بخرائط الخادم — فبوّابةٌ تُضاف بلا مستلمٍ
  تُسقط الاختبارات بدل أن تسقط الإشعاراتُ بصمت.
  **الاختبارات.** `hail-tests` ✅ **1737/1737** (+٣١) · `contracts-check` ✅ **137/137**
  (الربطُ يُكتب فعلاً، والطلبُ يسقط من مصروف الشراء أمام أعيننا) · `rules-check` ✅ 67/67 ·
  `ui-walkthrough` 61/61 · `browser-scenarios` 58/58 · `po-lifecycle-check` 53/53.
  **وبابُ النصّ الحرّ أُغلق قبل أن يُفتح.** كشف الفحصُ العميق (v18.9.2534) أنّ خانةً حرّةً
  في «أخرى» بالموارد البشرية كانت تُخرج اسمَ موظّفٍ إلى الجوّال. **والبابُ نفسُه كان في
  هذا التوجيه**: خانةُ السياق كانت تحمل `projectName`، واسمُ المشروع **اليدويّ** نصٌّ حرٌّ
  يكتبه مقدّمُ الطلب بيده — و«استراحة فلان» اسمُ مشروعٍ طبيعيٌّ تماماً. فصار اليدويُّ
  يخرج «مشروع يدويّ» بلا اسم، وتبقى مشاريعُ القائمة المسجّلة (قائمةٌ مغلقةٌ يديرها المالك)
  كما هي. يحرسه فحصان. **رُصد قبل النشر لا بعده** — والوحدةُ لم تكن قد أُرسلت رسالةً واحدة.
  **ما بعد الدمج.** توجيهُ واتساب يحتاج نشرَ الدوالّ: `firebase deploy --only functions`.
- **v18.9.2534 — فحصٌ عميقٌ بعد موجة الإشعارات والتعاقدات: قنبلتان موقوتتان في الفحوص،
  وبابٌ ثانٍ لقاعدةٍ كانت محروسةً في بابٍ واحد.**
  شُغّلت المنظومةُ كاملةً (`hail-tests` · تسعةُ فحوصِ متصفّحٍ · محاكي القواعد · ختمُ
  الإصدار). **الحصيلةُ: عطلٌ واحدٌ ظاهرٌ، وثانٍ كان سيظهر بعد أحد عشر يوماً، وثالثٌ
  لا يظهر في أيّ فحصٍ لأنه في المحتوى لا في المنطق.**

  **(١) فحوصٌ تسقط بتقدّم الزمن لا بارتداد الكود — «قنبلةٌ موقوتة» لا فحص.**
  `perf-contract-check` سقط **من نفسه** (٤٦/٤٧): «خطةٌ بلا استحقاقٍ سابق» توقُّعُها
  محفورٌ «يوم ٨» على مرساةِ ١ أغسطس، فلمّا صار اليومُ ٩ أغسطس لحقت `ppmNextDue`
  دورتَها الصحيحةَ إلى ١٥ — **الدالّةُ أصابت والفحصُ أخطأ**. وبتاريخٍ ماضٍ كالمرساة
  يُطمَس أصلاً ما تقصده الحالة (البناءُ من التاريخ المُمرَّر)، لأن حلقةَ اللحاق تغطّي
  عليه. الآن المرساةُ في المستقبل (`Date.now() + 60 يوماً`) والتوقُّعُ **يُحسَب بنفس
  قاعدة الدالّة**، فصار الفحصُ يقيس ما يدّعيه.
  والأخطرُ أنّ الصنفَ نفسَه كان **مزروعاً وصامتاً** في `contracts-check`: إقامةُ الطرف
  السادس محفورةٌ بـ٢٠٢٦-٠٨-٢٠ وفحصُها «الموشكةُ على الانتهاء تُنبَّه» — و`DOC_SOON_DAYS=30`
  تجعلها «توشك» اليومَ و**«منتهية» بعد ٢٠٢٦-٠٨-٢٠**. لم يُستنتَج بالقراءة: أُثبت
  بمحاكاة اليوم التالي ⇒ **١٢٦/١٢٧**. الآن كلُّ تاريخِ انتهاءٍ في القالب إزاحةٌ مسمّاةٌ
  عن اليوم (`_dayOff`): سالبٌ = منتهية · ≤٣٠ = توشك · أكبرُ = سارية.
  **ولماذا هذا أسوأُ من لا فحص:** إشارةٌ حمراءُ كاذبةٌ في CI تُستهلَك ثقةُ قارئها، فتُدفَن
  تحتها إشارةٌ صادقةٌ يوماً ما. ويحرسه فحصان **أُثبت أنهما يعضّان** (أُعيد حفرُ التاريخين
  فسقطا). ومرساةُ الخطة القديمة المحفورةُ في ٢٠٢٠/٢٠٢٦ **ليست قنبلة** وتُركت: حلقةُ
  اللحاق تُقدّمها إلى ما بعد اليوم أبداً، فالتوقُّعُ (مضاعفُ التكرار · مستقبليّ) لا يتغيّر.

  **(٢) النصُّ الحرُّ في «أخرى» يخرج إلى الجوّال — القاعدةُ محروسةٌ في بابٍ ومفتوحةٌ في
  بابٍ آخرَ يؤدّي إلى المكان نفسِه.** قاعدةُ الوحدة أن الإشعارات **بلا أسماء موظفين**،
  ولذلك استُثني «البيانُ المختصر» من كلّ خانةٍ في القالبين ويحرسه فحص. لكنّ
  `workTypeLabel` **الخادميةَ** كانت تُرجع في «أخرى» النصَّ الحرَّ الذي يكتبه مسؤولُ
  الموارد البشرية — وهو **البيانُ المختصرُ باسمٍ آخر**: خانةٌ حرّةٌ تحمل «تجديد إقامة
  فلان» بالسهولة نفسِها، فتذهب إلى {{2}} في رسالةٍ تخرج إلى جوّالٍ وتمرّ بخوادم Meta
  وتبقى في سجلّ المحادثة. لم يظهر في أيّ فحصٍ لأنه **صحيحٌ منطقياً**: الخانةُ موجودةٌ
  وعددُها مطابقٌ والرسالةُ تُقبَل — والعطلُ في **المحتوى**. رُصد بتنفيذ `approvalParams`/
  `statusParams` على نوعٍ «أخرى» لا بقراءتهما. الآن خانةُ النوع من القائمة المغلقة
  دائماً و«أخرى» تبقى «أخرى». **وداخلَ النظام لم يُمسّ شيء** — الوحدةُ في المتصفّح
  وجرسُها يعرضان النصَّ كاملاً، والفرقُ ليس في السرّية بل في المدى: قارئُ الشاشة صاحبُ
  صلاحيةٍ، والرسالةُ تخرج. ويحرسه ثلاثةُ فحوص: لا يتسرّب النصُّ · **ولا خانةَ تفرغ بذلك**
  (Meta ترفض الفارغة) · والعرضُ الداخليُّ باقٍ كما هو (فلا يُعمَّم القيدُ حيث لا يلزم).

  **ما فُحص ولم يُرَ فيه عطل:** توجيهُ الإشعارات إلى «أينما سُجّل الرقم»
  (`findByRoleAnywhere`/`findRequesterAnywhere`) — مسارُ `meta/projects` وحقلُه
  ومعرّفُ كلّ مشروعٍ مطابقةٌ لما يكتبه التطبيق فعلاً، والمركزيُّ أوّلاً بلا كلفةٍ
  إضافيةٍ في الحالة الغالبة · شكلُ خانات القالبين (٥ للمستعار · ٤ و٣ للمخصّص) نُفِّذ
  فعلاً بالبيئتين وطابق `docs/whatsapp-notifications-STATUS.md` حرفاً بحرف · مزامنةُ
  `adminSaveUserWa` مركزياً بنفس نمط الشاشات الثماني الأخرى · وقواعدُ Firestore على
  محاكٍ حقيقيّ ٦٧/٦٧. **وبقي مرصوداً بلا معالجة:** `Database is closing/hidden` من
  طبقة حفظ جلسة Firebase Auth (قيدُ v18.9.2522) — لم يظهر في هذه الجولة.
  **التحقّق:** `hail-tests` ✅ **1718/1718** (+١١ حارساً، أُثبت عضُّ الثلاثة الحرجة منها
  بإعادة زرع العطل) · `contracts-check` ✅ 127/127 · `perf-contract-check` ✅ **47/47**
  (كان 46/47) · `rules-check` ✅ 67/67 · `browser-scenarios` 58/58 · `ui-walkthrough`
  61/61 · `po-lifecycle-check` 53/53 · `tvwall-check` 51/51 · `cleaning-report-check`
  13/13 · `photo-queue-check` 15/15 · `stamp:check` متّسق.
  **ما بعد الدمج:** تصحيحُ «أخرى» **خادميٌّ** — لا يسري حتى يُنشَر:
  `firebase deploy --only functions`.

- **v18.9.2530 — أوّلُ رسالةِ سدادٍ حقيقيةٍ كشفت حدَّ استعارة القالب: «طلب شراء» على رسالةِ
  موارد بشرية.** التجربةُ على جوّال المالك أعطت: «**طلب شراء** بانتظار سدادك · **المشروع**:
  الموارد البشرية — إقامات · **عدد البنود**: 1». الكلماتُ الغليظةُ **نصٌّ ثابتٌ في القالب
  المعتمَد لدى Meta** لا تبلغه المتغيّرات — فالمالية تقرأ «طلب شراء» فتذهب لشاشة
  المشتريات ولا تجد الطلب. (زرُّ «فتح الطلب» يعالجها، لكن من لا يضغطه يبحث في الخطأ.)
  الاستعارةُ كانت مقايضةً واعية أدّت غرضَها — وصلت الإشعاراتُ لحظةَ النشر — والآن
  تُستبدَل. **قرارُ المالك: قالبان مخصّصان** (`hrp_approval_needed` · `hrp_status_update`)،
  نصُّهما الكامل وخطواتُ تقديمهما في `docs/whatsapp-notifications-STATUS.md` — ومعها
  عقبتان حقيقيتان وقعتا أثناء التقديم وصُحّح التوثيق بهما: **(أ)** Meta ترفض قالباً
  ينتهي بمتغيّر («Variables can't be at the start or end») فلزم سطرٌ ختاميٌّ ثابتٌ في
  `hrp_status_update`؛ **(ب)** خانةُ «Add sample URL» تريد **الرابطَ كاملاً** لا قيمةَ
  المتغيّر وحدَها. كلتاهما توثيقٌ لا كود — عددُ المتغيّرات لم يتغيّر.
  **والكودُ جاهزٌ لهما مسبقاً:** `approvalParams`/`statusParams` تختاران شكلَ الخانات
  **بمطابقة اسم القالب المضبوط** (`usingPoApprovalTemplate`) لا بعَلَمٍ منفصلٍ يُنسى —
  فقالبُ الشراء المستعار يأخذ ٥ خاناتٍ ({{2}} تعوّض النصَّ الثابت بـ«الموارد البشرية —
  النوع» و{{5}} «عدد البنود» = 1)، والمخصّصُ يأخذ ٤ ({{2}} النوعُ وحدَه، ولا خانةَ بنود).
  ⇒ يومَ يعتمد Meta القالبين يكفي سطران في `.env` ونشرٌ، **بلا تعديل كود**.
  **ولا بيانٌ مختصرٌ في أيّ خانة:** نصٌّ حرٌّ قد يحمل اسمَ موظّف («تجديد إقامة فلان»)،
  وقاعدةُ الوحدة أن الإشعارات بلا أسماء موظفين (كما في `_notifyStage`).
  **التحقّق:** `hail-tests` **1707/1707** (٩ حرّاسٍ جديدة). أهمُّها **تنفيذيّ**: يضبط
  البيئة على القالب المخصّص ويُعيد تحميل الوحدة ويتحقّق من الشكل الرباعيّ ثم يستعيد
  الافتراض — فمسارٌ يستيقظ بعد أسابيع لا يُترك بلا فحصٍ ينفّذه. وعطلُه كان سيكون صامتاً
  تماماً: عددُ خاناتٍ لا يطابق القالبَ ⇒ Meta ترفض **كلَّ** رسائل السداد بلا تغيّرِ سطر.

- **v18.9.2527 — رقمُ الواتساب لا يضيع بحسب الشاشة التي أُدخل منها.** سؤالُ المالك بعد v18.9.2518
  («المالية والتنفيذي مفعَّلٌ لهم أرقامٌ وتصلهم رسائلُ المشتريات — هل تصلهم رسائلُ السداد؟»)
  كشف فرقاً صامتاً: الرقمُ يُخزَّن في **موضعين** حسب شاشة الإدخال — «إدارة مستخدمي
  المشتريات» تُزامنه مع المركزيّ `meta/users` (`_upsertUserCentral`)، بينما **لوحةُ الأدمن
  داخل المشروع** (`adminSaveUserWa`) كانت تكتبه في `meta/{projectId}_users` وحده.
  وتوجيهُ الشراء لا يتأثّر (يقرأ مستندَ مشروعِ الطلب **ثم** المركزيّ)، أمّا سدادُ الموارد
  البشرية فمجموعتُه عامّةٌ بلا `projectId` فكان مرجعُه المركزيَّ وحده. ⇒ **مستخدمٌ بعينه
  تصله رسائلُ الشراء ولا تصله رسائلُ السداد** — فرقٌ لا يفسّره شيءٌ للمستخدم، وكان سيظهر
  بعد النشر لا قبله. أُغلق من طرفيه: (أ) `findByRoleAnywhere`/`findRequesterAnywhere` في
  `recipients.js` — المركزيُّ أوّلاً، **ولا تُمسح مستنداتُ المشاريع إلا إن خلا** (فلا كلفةَ
  في الحالة الغالبة) وبسقفٍ صريح `MAX_PROJECT_DOCS=25` فلا تتحوّل لقراءةٍ مفتوحة؛
  (ب) `adminSaveUserWa` صارت تُزامن مركزياً كنظيرتها — فالشاشتان سلوكٌ واحد ولا يتولّد
  الانحرافُ من جديد. **التحقّق:** `hail-tests` 1697/1697 (٥ حرّاسٍ جديدة، منها سيناريو
  تنفيذيٌّ ثانٍ: **المركزيُّ غائبٌ تماماً والأرقام كلُّها في مستند مشروع** ⇒ الرسالةُ تصل)
  · `browser-scenarios` 58/58 · `ui-walkthrough` 61/61.

- **v18.9.2523 — أوامرُ التغيير: البابُ الوحيدُ لتغيير قيمة العقد:**
  **المشكلة.** العقدُ وُقّع على مبلغ، ثمّ زاد العملُ أو نقص. وأمامنا ثلاثةُ طرقٍ اثنتان
  منها تُفسدان السجلّ: تعديلُ رقم العقد الأصليّ (فتضيع الحقيقة — لا أحدَ يعرف بعد شهرٍ
  أنّ العقد بدأ بـ١٠٠ وصار ١١٥ ولا لماذا)، أو عقدٌ ثانٍ لعملٍ هو امتدادُ الأوّل (فرقمان
  لالتزامٍ واحد). والثالثةُ هي الصحيحة.
  **الناتج.** `global_contract_changes` — مستندٌ مستقلٌّ يمرّ **بأربع بوّابات**، وحين
  يُعتمَد **يُضاف إلى العقد ولا يستبدله**: `value` يبقى الأصليَّ أبداً، ويُلحَق الأمرُ
  في `changeOrders`، و`contractValue` تجمعهما. فالتاريخُ محفوظٌ والرقمُ الحاليُّ صحيح،
  وبطاقةُ العقد تعرض **المعادلةَ** لا الرقمَ وحدَه: «الأصليّ + زيادات − خفوضات = الحاليّ».
  **ما كان ينتظر هذا الباب.** `contractValue` و`contractLineQty` تجمعان أوامرَ التغيير
  منذ المرحلة ١، ورسالةُ حارس المستخلص تقول «يلزم أمرُ تغييرٍ معتمَد» — وكان البابُ الذي
  تشير إليه غيرَ مبنيّ. الآن رفعُ الكمية بأمرِ تغييرٍ يجعل المستخلصَ الممنوعَ ممكناً،
  والمحتجزُ والمقدَّمُ يُحسبان من **القيمة النافذة** تلقائياً (كشفه الفحصُ: توقُّعٌ
  محفورٌ بـ1680 صار 1708 بحقّ — فصار الفحصُ يحسب بدل أن يفترض).
  **العتبةُ بالقيمة المطلقة.** خفضُ العقد ٥٠ ألفاً قرارٌ بثقل رفعِه ٥٠ ألفاً — فلا يمرّ
  خفضٌ كبيرٌ من تحت عتبة التنفيذيّ لأنّ إشارتَه سالبة.
  **ثلاثةُ حرّاسٍ لا واحد.** (١) أمرٌ بلا أثرٍ (لا مالَ ولا مدّة) مرفوض. (٢) **الخفضُ لا
  ينزل بالبند تحت المنفَّذ**: بندٌ اعتُمد منه ٨٠ وحدةً لا تُخفَّض كميتُه إلى ٧٠ — العملُ
  نُفِّذ وشُهد به، وخفضُه يُبطل ما سبق بأثرٍ رجعيّ. (٣) **ولا تنزل قيمةُ العقد تحت ما
  سُدِّد فعلاً** — مالٌ خرج لا يُلغى بمستند. والحارسُ يُعاد **وقتَ التطبيق** على الوثيقة
  الطازجة لا وقتَ الإنشاء فقط: بين الاعتماد والتطبيق قد يُعتمَد مستخلصٌ يقلب الحكم.
  **وأمرٌ مفتوحٌ واحدٌ لكلّ عقد** — كحارس «مستخلصٌ مفتوحٌ واحد»: أمران معلّقان يُعتمدان
  في اللحظة نفسِها يُطبَّقان على قيمتين مختلفتين.
  **الموازنة.** الأمرُ المعلَّقُ يظهر في «قيدَ الاعتماد» لا في «متعاقَدٌ عليه»، والمطبَّقُ
  داخلٌ أصلاً في قيمة العقد فلا يُحسب مرّتين. والخفضُ المعلَّقُ ينقص المتوقَّعَ لا يزيده.
  **الخادم.** القواعدُ امتدّت للمجموعة الجديدة: لا إنشاءَ بلا سببٍ في المستند، ولا تتغيّر
  قيمةُ الأمر ولا بنودُه بعد الإرسال (وإلا اعتُمد رقمٌ وطُبِّق غيرُه)، و**`value` العقد
  مجمَّدٌ حتى مع أمرٍ معتمَد** — التطبيقُ يُلحق بـ`changeOrders` وللمشتريات وحدَها.
  **الاختبارات.** `hail-tests` ✅ **1606/1606** (+٣٣) · `contracts-check` ✅ **127/127**
  (الدورةُ كاملةً في متصفّحٍ حقيقيّ بأدوارها الأربعة، والنقرةُ المكرّرةُ لا تُطبّقه مرّتين)
  · `rules-check` ✅ **67/67** · `ui-walkthrough` 61/61 · `browser-scenarios` 58/58 ·
  `po-lifecycle-check` 53/53.
  **شرطُ تشغيل.** وظيفةُ القواعد في CI تحتاج **Java 21 فأعلى** — `firebase-tools`
  يرفض ما دونها صراحةً، وسقطت الوظيفةُ على 17 في أوّل تشغيل. يحرسه فحصٌ بالرقم.

- **v18.9.2522 — قواعدُ Firestore للتعاقدات: نقلُ الحارس من المتصفّح إلى الخادم:**
  **الجذر.** كلُّ قيدٍ بنيناه في وحدة التعاقدات — «لا اعتمادَ بغير صاحب البوّابة» ·
  «لا عقدَ يولد سارياً» · «لا سدادَ بلا إيصال» — مكتوبٌ في المتصفّح. فهو يمنع الخطأ
  ولا يمنع **التعمّد**: مَن يفتح أدواتِ المطوّر يكتب في القاعدة مباشرةً متجاوزاً
  الدورةَ كلَّها. بابٌ محكمٌ في جدارٍ لم يُبنَ بعد.
  **الاكتشافُ الذي غيّر شكلَ الحلّ.** القاعدةُ العامة `match /{document=**}` تسمح لأيّ
  دورٍ غيرِ الزائر بالكتابة في كلّ شيء، وقواعدُ Firestore **تُقيَّم بـ«أو»**: يكفي
  تطابقُ بلوكٍ واحدٍ للسماح. فإضافةُ بلوكاتٍ صارمةٍ للتعاقدات **لا تقيّد شيئاً** ما لم
  تُستثنَ من العامة أوّلاً. لذلك جوهرُ التغيير سطرٌ واحد: `!ctrLocked(document[0])` —
  وما عداه تفصيل. ويحرسه فحصٌ باسمه، فحذفُه يُسقط الاختبارات لا يمرّ بصمت.
  **ما صار محروساً على الخادم.** الآيبانُ لا يتغيّر إلا بيد المالية أو الأدمن (ناقلُ
  الاحتيال الأوّل) · لا يُنشئ الطلبَ إلا مديرُ المشاريع وعند البوّابة الأولى بالضبط ·
  لا يحرّك الحالةَ إلا صاحبُ البوّابة الحالية · قيمةُ الطلب وبنودُه وطرفُه لا تتغيّر
  بعد الإرسال (وإلا وقّع المعتمِدُ على رقمٍ وسُدِّد غيرُه) · العقدُ يولد «بانتظار
  التوقيع» ولا يسري إلا بتسجيله · جدولُ انتقالات العقد بحذافيره · لا سدادَ مستخلصٍ
  بلا إيصال · **ولا حذفَ لطلبٍ ولا عقدٍ ولا مستخلصٍ من العميل أبداً** — وهو ما يفعله
  التطبيقُ أصلاً (لا `delete()` واحدةٍ فيه).
  **الفخُّ الذي كاد يُسقط كلَّ سدادٍ أخيرٍ في المنصة.** جدولُ الانتقالات يقول إن
  «الإنهاءَ الفنّيّ» لمدير المشاريع — لكنّ **سدادَ المستخلص الختاميّ يُنهي العقد في
  معاملة المالية نفسِها**. قاعدةٌ تنسخ الجدولَ حرفياً كانت سترفض تلك الكتابة فيسقط
  آخرُ مستخلصٍ من كلّ عقد. الاستثناءُ مكتوبٌ ومعلَّلٌ في القواعد، ويحرسه فحصٌ لئلّا
  يُحذَف بحسن نيّةٍ عند «تنظيف» القواعد لاحقاً.
  **القراءةُ بقيت مفتوحةً — قصداً.** تقييدُها يجعل جدولَ الموازنة يعرض **أصفاراً
  صامتةً** لمن لا يملكها بدل رفضٍ ظاهر، ورقمٌ خاطئٌ أسوأُ من بابٍ مغلق. تضييقُ القراءة
  خطوةٌ مستقلّةٌ تحتاج مراجعةَ كلِّ شاشةٍ تقرأ هذه المجموعات.
  **الاختبار — وهو نصفُ العمل.** قاعدةٌ أمنيةٌ لا تُختبَر بقراءة سطرها. `rules-check.mjs`
  يشغّل **محاكي Firestore حقيقياً** ويكتب بأدوارٍ حقيقيةٍ في التوكن: ✅ **54/54** في
  خمسة أقسام، آخرُها **المساراتُ الحقيقيةُ كما ترسلها الوحدة** (الوثيقةُ كاملةً بـ`merge`
  لا الحقلُ المتغيّر وحدَه) — فقاعدةٌ تمنع المهاجمَ والمستخدمَ الشرعيَّ معاً تعطيلٌ لا
  حراسة. مربوطٌ بـCI في وظيفةٍ مستقلّةٍ على Java. وفي `hail-tests` سبعةُ حرّاسِ اتّساقٍ
  يمنعون أن تُضاف مجموعةٌ أو انتقالُ حالةٍ في الوحدة فتتخلّف عنها القواعدُ بصمت.
  **الاختبارات.** `hail-tests` ✅ **1573/1573** (+٩) · `rules-check` ✅ **54/54**.
  **ما بعد الدمج.** القواعدُ لا تسري حتى تُنشَر: `firebase deploy --only firestore:rules`.
  **✅ نُشرت على الإنتاج (٩ أغسطس ٢٠٢٦) وتحقّقنا حيّاً.** قُورنت نسخةُ اللوحة بالمستودع
  قبل النشر فكانتا متطابقتين حرفاً بحرف — لا تعديلَ لوحةٍ ضاع. والفرقُ الفعليُّ في
  الأسطر القديمة **سطرٌ واحد** (`&& !ctrLocked(document[0])` في القاعدة العامة) وما عداه
  إضافةٌ خالصة: ٧٨ سطراً ⇐ ٢٩٩، بلا حذفِ بلوكٍ واحدٍ من تطبيق الفنيين أو سجلّ التدقيق.
  ثمّ من Console التطبيق بدور `admin` حقيقيّ: `ctr_active` ⇐ **`permission-denied`**،
  و`ctr_pending_signature` ⇐ **قُبل**. الطرفان مثبَتان على الإنتاج لا على المحاكي وحدَه.
  (سطورُ التحقّق ومصيدتاه في §5.)
  **باقٍ مرصود:** بقيةُ النظام (المشتريات والمخزون والرواتب) ما زالت تحت القاعدة العامة —
  هذا القفلُ للتعاقدات وحدَها، وتعميمُه بندٌ مستقلّ. ورُصد أثناء التحقّق خطأٌ **ليس من
  القواعد**: `server login error: Database is closing/hidden` من طبقة حفظ جلسة Firebase
  Auth في IndexedDB (`persistence_user_manager`) — يقع في التصفّح الخاصّ أو حين يُخفى
  التبويبُ أثناء الدخول أو بفتح نسختين من النظام. انتهى بمصادقةٍ ناجحة، لكنه المرشَّحُ
  الأوّل لتفسير «دخلتُ ثمّ خرجتُ عند تحديث الصفحة» إن تكرّر. **لم يُعالَج بعد.**
- **v18.9.2518 — «إشعارات سداد الموارد البشرية لا تصل للمسؤولين» (بلاغُ المالك): ثلاثُ قنواتٍ
  كانت الثلاثُ مقطوعةً عن الوحدة.** الوحدة أُضيفت **بعد** بناء طبقة الإشعارات، فبقيت
  خارجها كلِّها بلا أن يُلاحَظ — كلُّ ما كان لها `addNotification` في جرسِ التطبيق.
  **(١) الجذرُ الأكبر: الجرسُ نفسُه لم يكن يُزامَن أصلاً لمن يعتمد الطلب.**
  `startNotifSync()` — المستمعُ الذي يقرأ وثيقةَ الإشعارات المشتركة `meta/notifications` —
  كانت تُستدعى في `enterApp` وحدها (مسارُ الدخول عبر مشروع). وأدوارُ المشتريات المركزية
  (**المالية · مسؤول الموارد البشرية · المشتريات · المستودع**) تدخل من `openGlobalPurchases`
  مباشرةً بلا مرورٍ بها (`_isGlobalOnlyRole` ⇒ `openGlobalPurchases()` في `restoreSession`
  و`login`). فجرسُ المالية كان يعرض ما ولّده متصفّحُها هي فقط: كلُّ إشعارٍ كتبه مستخدمٌ
  آخر — ومنه «بانتظار سداد المالية» الذي يكتبه متصفّحُ المعتمِد — لا يصل إطلاقاً. وهذا
  يفسّر ما رآه المالك: شارةُ السايدبار «٧» تعمل (تُحسب محلياً من المجموعة) والجرسُ صامت.
  أُضيف `startNotifSync()` في `openGlobalPurchases` بجوار `startHailNotifications()`؛
  الدالّة idempotent (تفكّ مستمعها قبل التركيب) فالاستدعاءُ من مسارين آمن.
  **(٢) لا تنبيهَ لحظيّاً.** البلاغاتُ وطلباتُ الشراء تُظهر توست `HailNotify` منذ v18.9ub؛
  السدادُ وحدَه بلا واحد — فالمعتمِدُ الذي لا يفتح الجرس لا يعلم أن طلباً ينتظره. أُضيف
  نوعٌ `hr` في `HailNotify.js` (أيقونةُ بطاقةِ هويةٍ ولونُ الموارد البشرية)، و`_liveToast`
  في `hr-payments.js` يعمل على لقطة المزامنة بثلاثة حرّاس: اللقطةُ الأولى **خطُّ أساسٍ
  صامت**، والحالةُ التي لم تتغيّر لا تُنبّه، ومن نقل الحالة بنفسه لا يُنبَّه بفعله (آخرُ
  قيدٍ في الخطّ الزمني باسمه). والتنبيهُ **لمن دورُه الآن وحده** (`_awaitsMe` = منطقُ
  `pendingForMe` نفسُه) فلا يصير ضجيجاً لكل من يملك الاطلاع.
  **(٣) لا واتساب — والقناةُ الوحيدة التي تصل الجوال فعلاً.** طبقةُ الخادم تغطّي البلاغات
  (`ticketAssign*`) والشراء (`poRoute*`) ولا مشغّلَ لـ`global_hr_payments`. أُضيف
  `functions/lib/hr-payments.js` بنفس منطق `purchases.js`: `HRP_ROUTING` (بانتظار مدير
  المشاريع→`project_manager` · التنفيذي→`ceo` · المالية→`finance`) ومشغّلا
  `hrpRouteUpdate`/`hrpRouteCreate`، وصاحبُ الطلب يُنبَّه بالرفض والإعادة والإغلاق — لا
  بإلغائه هو (فعلُه). **بلا مبالغ في نصّ الرسالة** كقاعدة الشراء. ومنعُ التكرار يحمل
  `occurrence` (M14) فالإعادةُ الثانيةُ بنفس الانتقال تصل ولا تُبتلع.
  **قرارُ القالب:** الافتراضُ هو **قالبا الشراء المعتمَدان** (`po_approval_needed` /
  `po_status_update`) لا قالبان جديدان — فتعمل الإشعارات **لحظةَ النشر** بلا انتظار
  مراجعة Meta، وبنيةُ القالبين محايدة ({{2}} خانةُ سياقٍ نكتب فيها «الموارد البشرية —
  نوعُ العمل»، و{{5}} عددُ البنود = ١ دائماً). وعند اعتماد قالبين مخصّصين لاحقاً يكفي
  `WA_HRP_APPROVAL_TEMPLATE`/`WA_HRP_STATUS_TEMPLATE` بلا تعديل كود.
  ولأن زرّ «فتح الطلب» يمرّر المعرّف على قالب الشراء، صار الرابطُ العميق يوجَّه
  **بالبادئة لا باسم المتغيّر**: `HRP-` ⇒ وحدة السداد، وقُبِل `?hrp=` أيضاً.
  و`findRequester` صارت تطابق `createdByUser` مع `createdBy` (وحدةُ السداد تخزّن اسمَ
  الدخول في الأول والاسمَ المعروض في الثاني) — وإلّا لم يُعرَف صاحبُ الطلب فتسقط رسالتُه.
  **التحقّق:** `node hail-tests.js` **1650/1650** (٤٤ حارساً جديداً، منها ١٥ **تُنفِّذ
  التوجيهَ فعلاً** على محاكي Firestore: من يصله ماذا، وأن التكرارَ يُدمج، وأن الإلغاء
  ومفتاحَ القتل لا يُرسلان، وأن بيانات الموارد البشرية لا تصل المستودعَ ولا المشتريات)
  · `browser-scenarios` 58/58 · `ui-walkthrough` 61/61 · `po-lifecycle-check` 53/53.
  **بعد الدمج:** الكودُ الخادميّ لا يعمل حتى يُنشر — `firebase deploy --only functions`
  (تفاصيل §5 في `docs/whatsapp-notifications-STATUS.md`)، وتُدخَل أرقامُ **المالية
  والتنفيذي ومدير المشاريع ومسؤول الموارد البشرية** مع «تفعيل واتساب» في «إدارة مستخدمي
  المشتريات» — فمن لا رقمَ له لا تصله رسالة. القناتان (١) و(٢) تعملان بالدمج وحده.

- **v18.9.2516 — أداةُ توحيد المورّدين بعد أوّل استعمالٍ حقيقيّ: ثلاثةُ أعطالٍ بلّغ عنها المالك:**
  **(١) «توحيد» لا يفعل شيئاً — تصادمُ اسمٍ يمحو الدالّة.** `renderVendorMerge` كانت
  تحفظ العناقيد في `window._vmClusters` — **وهو اسمُ الدالّة نفسِها** `function
  _vmClusters(names)`. فالإسنادُ يمحو الدالّة من النطاق العام: أوّلُ رسمٍ ينجح، ثم كلُّ
  رسمٍ تالٍ يرمي «_vmClusters is not a function» — ويبتلعه `try{}catch{}` في
  `_vmRefreshViews`. فالتوحيدُ **كان يُحفَظ فعلاً** لكنّ القائمة لا تتحدّث أبداً، فيبدو
  الزرُّ معطَّلاً؛ وإعادةُ فتح النافذة تفشل صامتةً لأن الاستثناء يسبق `openModal`.
  الآن الحالةُ في `_vmLastClusters` — متغيّرٌ لا يصطدم باسم دالّة.
  **(٢) «الرتاج» و«الريتاج» عنقودان لا واحد.** الترشيحُ كان يقارن الكلماتِ **حرفياً**
  (`indexOf` على مصفوفة الكلمات)، فياءٌ واحدةٌ زائدةٌ تجعل مورّداً واحداً مجموعتَين
  متجاورتَين — أربعُ صيغٍ لمورّدٍ واحدٍ في عنقودَين. الآن `_vmLev1` (مسافةُ تحريرٍ ≤١
  بفحصٍ خطّيٍّ بلا مصفوفة) للكلمات التي طولها ≥٤ — فالقصيرةُ يقلب حرفٌ واحدٌ معناها —
  والتجميعُ صار **اتّحادَ مجموعات** (union-find) لا فحصاً ثنائياً منفصلاً، فصيغتان
  تربطهما ثالثةٌ تقعان في عنقودٍ واحد. والعنقودُ المكتملُ توحيدُه يسقط من القائمة.
  **(٣) الاسمُ المركّب «أ + ب».** طلبٌ ورّده أكثرُ من مورّد يُخزَّن `actualVendor` سلسلةً
  واحدة (`_vends.join(" + ")`)، فكان يُعامَل **مورّداً ثالثاً مستقلّاً**: صفٌّ في الأداة
  بإنفاقٍ صفر، ولا يستفيد من أي مرادفٍ معتمدٍ لأجزائه. الآن `vendorSplitNames` يفكّكه:
  `vendorCanonical` يوحّد **كلَّ جزءٍ على حدة** ثم يعيد التركيب بلا تكرار (وجزآن يؤولان
  لاسمٍ واحدٍ يصيران اسماً واحداً)، وجامعُ الأسماء يعرض المورّدَين صفَّين حقيقيَّين.
  وحصّةُ المركَّب لا تُقسَّم تخميناً — تُنسَب في المؤشّرات لمورّديه من سنداتهم.
  **+ دمجٌ يدويّ.** الترشيحُ الآليُّ يلتقط المتشابهَ إملائياً فقط، فأُضيف قسمٌ يختار فيه
  المسؤول **أيَّ أسماءٍ** بمربّعات اختيارٍ وبحث ويوحّدها — والاسمُ المعتمد يجب أن يكون
  من المحدَّد نفسِه. وصار الاختيارُ بمربّعاتٍ في العناقيد أيضاً، فيُستثنى اسمٌ من مجموعةٍ
  مرشَّحةٍ بلا إلغاء المجموعة كلِّها.
  **الاختبارات.** `hail-tests` ✅ **1608/1608** (+١٥ حارساً) · رحلةُ المتصفّح 61/61 ·
  السيناريوهات 58/58 · دورةُ طلب الشراء 53/53. وفحصٌ في متصفّحٍ حقيقيٍّ ببيانات المالك
  نفسِها: الصيغُ الأربع عنقودٌ واحد · المركَّبُ مفكَّكٌ إلى مورّدَيه · الضغطةُ تُحدّث
  القائمة ويجتمع الإنفاق ١٥٬٦٠٧ تحت الاسم المعتمد · والدمجُ اليدويّ يعمل خارج الترشيح.

- **v18.9.2514 — المورّد الواحد صفٌّ واحد: توحيدُ أسماء المورّدين بجدول مرادفاتٍ صريح:**
  **الجذر.** لاحظ المالك في «أعلى ٥ موردين إنفاقاً» ثلاثةَ أعمدةٍ لمورّدٍ واحد:
  «عالم الرتاج» ٣٬٤٨٦ · «شركة عالم الرتاج» ٥٬٦٠٥ · «شركة عالم الرتاج للتجارة» ٣٬٦٧٤.
  والسبب أن اسمَ المورّد **نصٌّ حرٌّ يُكتَب يدوياً في كل طلب** وفي أربعة حقولٍ مختلفة
  (`vendor` · `actualVendor` · `grnDocs[].vendor` · `items[].vendor`)، وكلُّ تجميعٍ في
  النظام يُفهرِس على السلسلة **كما كُتبت**: `byVendor` في المؤشّرات، وملخّصُ التقارير
  على الشاشة وفي PDF. فالصيغةُ الواحدة مفتاحٌ مستقلّ — وأكبرُ المورّدين قد يسقط من
  القمّة لأن حصّتَه مبعثرةٌ على صيغ اسمه (١٢٬٧٦٥ ر.س هنا رُسمت ثلاثةَ أرقامٍ أصغر).

  **لماذا لا تطبيعٌ تلقائيّ.** كان الأسهلُ حذفَ «شركة/مؤسسة/للتجارة/المحدودة» وتطبيعَ
  الحروف ثم الدمجَ بلا استئذان — وهو **خطرٌ ماليّ**: «شركة عالم الرتاج» و«شركة عالم
  الرتاج للتجارة» قد يكونان كيانَين بسجلَّين تجاريَّين مختلفَين، ودمجُهما آلياً يزوّر
  توزيعَ الإنفاق بدل أن يصلحه. فالقاعدة: **النظام يرشّح، والمستخدم يقرّر.**

  **ما بُني.** مفتاحان لا واحد: `vendorMatchKey` تطبيعٌ حرفيٌّ محافظ (تشكيل · تطويل ·
  همزة · تاء مربوطة · ياء · مسافات) **لا يحذف كلمةً** — وهو مفتاحُ البحث في جدول
  المرادفات؛ و`vendorFuzzyKey` يُسقط الصيغَ القانونية و«ال» التعريف ويرتّب الكلمات —
  **للاقتراح وحده**. وفوقهما `vendorCanonical` (الاسمُ المعتمد لصيغة) و`vendorCanonParts`
  (دمجُ حصص الصيغ في صفٍّ واحد)، تُستدعيان في كلّ موضع تجميع: مخرجاتُ `poVendorBreakdown`
  الثلاثة (فتغطّي المؤشّرات والتقارير وPDF معاً)، و`_kpiVendorOf`، وفلترُ التقارير في
  طرفيه (بناءُ القائمة والمقارنة) فلا تتكرّر الصيغُ خياراتٍ ولا يُصفّي الفلترُ صيغةً واحدة.

  **الأداة.** زرُّ «توحيد أسماء الموردين» في المشتريات المركزية (للمسؤول وحدَه، بجوار
  «دمج مشروع يدوي»): يجمع كلَّ صيغ الأسماء الواردة **فعلاً** في الطلبات بعددها وإنفاقها،
  ويعرضها عناقيدَ موسومةً «تطابق قوي» (نفس مفتاح الترشيح) أو «تطابق محتمل» (تضمّنُ
  كلماتٍ مميّزة)، ولكلّ عنقودٍ يختار المسؤول الاسمَ المعتمد ويضغط «توحيد». والقرارُ
  يُسجَّل في سجل التدقيق اعتماداً وتراجعاً.

  **الطلباتُ لا تُمَسّ.** الجدولُ مستندٌ واحد (`meta/global_vendor_aliases`) والتوحيدُ
  **طبقةُ عرضٍ قابلةٌ للتراجع بضغطة** — فما كُتب في كل طلبٍ وسندِه وفاتورتِه يبقى كما هو،
  خلافاً لأداة «دمج مشروع يدوي» التي تعيد كتابة الوثائق. ولا سلاسلَ مرادفات: صيغةٌ كانت
  معتمدةً ثم دُمجت تُحوَّل تبعيّاتُها معها إلى الاسم النهائي.

  **الاختبارات.** `hail-tests` ✅ **1593/1593** (+٣١ حارساً، منها المتلازمان: بالاعتماد
  تُدمَج الصيغُ الثلاث في صفٍّ واحدٍ بمجموعٍ محفوظٍ ١٢٬٧٦٥، وبلا اعتمادٍ تبقى ثلاثةَ
  صفوفٍ — فلا دمجَ آليّ) · رحلةُ المتصفّح 61/61 · السيناريوهات 58/58 · دورةُ طلب الشراء
  53/53. وفحصٌ يدويٌّ في متصفّحٍ حقيقيّ على الأداة نفسها: العنقودُ يُرشَّح، والتوحيدُ
  يجمع ١٢٬٧٦٥ تحت اسمٍ واحد، والجدولُ يُحفَظ، والطلباتُ سليمة، والتراجعُ يعيد الصيغَ.

- **v18.9.2512 — وحدةُ التعاقدات (المرحلة ٥): الربطُ بالموازنة ومنعُ الازدواج المحاسبيّ:**
  **الطلب.** أن يظهر أثرُ التعاقدات في موازنة المشروع كما يظهر أثرُ المشتريات — بلا أن
  يُحسب ريالٌ واحدٌ مرّتين.
  **الناتج.** `contractRollup` في `contracts.js` — دالةٌ نقيّةٌ تأخذ القوائمَ فتُختبَر بلا
  Firestore، تُخرج لكلِّ بندِ موازنةٍ ثلاثةَ أرقام: **قيدَ الاعتماد** (طلباتُ تعاقدٍ لم
  تنتهِ بعد) · **متعاقَدٌ عليه** (قيمةُ العقد − ما استُخلص منه) · **المصروف** (المستخلصاتُ
  المسدَّدةُ وأوامرُ الدفع المسدَّدة). وجدولُ موازنة `project-management.js` صار **سبعةَ
  أعمدة**، والمتبقّي يخصم الأربعةَ معاً: المصروف + المرتبط (شراءٌ جارٍ) + المتعاقَدَ عليه
  + قيدَ الاعتماد.
  **لماذا «قيمةُ العقد − المستخلَص» لا قيمةُ العقد كاملةً.** المستخلَصُ المسدَّدُ صار
  مصروفاً في عمودِه؛ فلو بقي داخلَ «متعاقَدٌ عليه» أيضاً لظهر المالُ نفسُه في خانتين
  وأكل من المتبقّي ضعفَه. العمودان **متكاملان لا متداخلان**: ما دُفع مصروف، وما بقي التزام.
  **حارسُ منع الازدواج المحاسبيّ.** طلبُ شراءٍ يحمل `contractId` جزءٌ من عقدٍ مَحسوبٍ
  أصلاً في العمودين التعاقديّين — فعدُّه في مصروف الشراء يُنتج رقمين لعملٍ واحد.
  تستبعده `rollupByCategory`، والقاعدةُ **تُقرأ من وحدة التعاقدات** (`poIsUnderContract`)
  ولا تُنسَخ — نسخُ القاعدة يعني تفرّعَها لاحقاً بصمت.
  **المالُ المدفوعُ لا يتبخّر بإقفال العقد.** رصد فحصُ المتصفّح هذا في أوّل تشغيل: كان
  `spent` محسوباً داخلَ حارس «العقدُ التزامٌ قائم»، فما إن يُقفَل العقد (`ctr_closed`)
  أو يُفسَخ حتى تختفي مستخلصاتُه المسدَّدةُ من مصروف الموازنة — والمالُ خرج من الخزينة
  فعلاً. فُصل الحسابان: **الالتزامُ المتبقّي** يسقط بانتهاء العقد، و**المصروف** لا يسقط
  أبداً. يحرسه فحصان بالاسم.
  **الربطُ اختياريّ.** عقدٌ بلا بندِ موازنةٍ — لمشروعٍ مُدخَلٍ يدوياً بلا موازنة — يقع
  تحت «غير مصنّف» ويظهر صفُّه في الجدول؛ فلا يختفي المال لأن حقلاً تُرك فارغاً.
  **الاختبارات.** `hail-tests` ✅ **1564/1564** (+١٩ حارساً) · `contracts-check.mjs` ✅
  **109/109** — يقيس **الرقمَ المرسومَ في الجدول** لا المحسوبَ وحدَه، ويزرع طلبَ شراءٍ
  حقيقياً ثم يربطه بعقدٍ ليُثبت سقوطَه من مصروف الشراء · `ui-walkthrough` ✅ 61/61 ·
  `browser-scenarios` ✅ 58/58 · `po-lifecycle-check` ✅ 53/53.

- **v18.9.2510 — سدُّ ثغرةٍ في مرشّح مسارات CI، وحارسٌ يمنع تكرارها:**
  **الجذر.** `.github/workflows/hail-tests.yml` يُشغَّل عند `push` **بمرشّح مسارات**،
  وتعليقُه يحذّر حرفياً: «كل ملفٍ يفحصه hail-tests — أي إغفال يعني أن push مباشراً له
  لا يُشغّل CI». وقد وقع الإغفالُ مرّتين: **`contracts.js`** بقيت خارج القائمة عبر أربعة
  PRs (غطّاها `pull_request:` فمرّت، لكن دفعاً مباشراً إلى `main` يمسّها وحدَها ما كان
  ليُشغّل فحصاً)، و**`performance-contract.js`** كانت خارجها من قبلُ أصلاً.
  **الإصلاح.** أُدرجت الوحدتان، وأُضيف **حارسٌ عامّ** يشتقّ القائمةَ المطلوبة من
  **الوحدات المحقونة فعلاً في `index.html`** (وسومُ `<script src="…?v=">`) لا من قائمةٍ
  مكتوبةٍ ثانيةٍ تنحرف — فأيُّ وحدةٍ جديدةٍ تُحقَن ولا تُدرَج في CI تُسقط الاختبارات فوراً.
  وهو الحارسُ الذي كشف `performance-contract.js` بنفسه أوّلَ تشغيل.
  **الاختبارات.** `hail-tests` ✅ **1545/1545**.

- **v18.9.2508 — الوثيقةُ التعاقدية: شروطٌ نصّيةٌ وحالةُ توقيعٍ ومخرَجٌ ورقيّ:**
  **الجذر.** سأل المالك: «هل يصدر العقدُ ورقياً ليوقّعه المقاول؟ وما وضعُ بنوده وشروطه
  الجزائية؟» والفحصُ أثبت فجوةً ثلاثية: (١) **لا مخرَجَ طباعةٍ إطلاقاً** — النظامُ يحمل
  البيانات ولا يُنتج وثيقةً تُوقَّع، ولا مكانَ لحفظ النسخة الموقّعة؛ (٢) ما بُني باسم
  `lines` هو **بنودُ الأعمال** (المقايسة) لا **بنودُ العقد القانونية** — ولا وجودَ
  للأخيرة أصلاً؛ (٣) الشروطُ الجزائيةُ **أرقامٌ بلا نصّ**: `perDayPct`/`capPct` تُحرّك
  الخصمَ فعلاً، لكن لا صياغةَ تُلزم المقاول.
  **العقدُ لا يسري قبل التوقيع (قرارُ المالك).** حالةٌ جديدةٌ `ctr_pending_signature`
  يُولَد فيها العقد؛ ولا يصير سارياً — **ولا تُقبل عليه مستخلصات** (يغطّيه الحارسُ
  القائم تلقائياً) — إلا برفع **النسخة الموقّعة**، وهي **إلزاميةٌ في المعاملة** فلا
  يسري عقدٌ بضغطةٍ بلا إثبات. والفسخُ وحدَه ممكنٌ قبل التوقيع؛ أمّا الإيقافُ والإنهاءُ
  فمن السريان فقط.
  **الشروطُ نصٌّ لا رقم.** قوالبُ يديرها الأدمن (`meta/contract_clause_templates`)
  بسبعة تصنيفات (نطاق · عامة · جودة · سلامة · جزائية · ضمان · ختامية) وتسعةُ بنودٍ
  افتراضيةٍ تعمل من أوّل يوم، **تُنسَخ إلى العقد قيماً لا مراجع** — فتعديلُ القالب بعد
  سنةٍ **لا يمسّ عقداً وُقِّع عليه**؛ الإشارةُ كانت ستُعيد كتابةَ تاريخٍ مُوقَّع. ولكلّ
  عقدٍ تحريرُ نسختِه وحدَها.
  **والشروطُ الماليةُ تتولّد نصّاً من الأرقام ولا تُخزَّن** (`financialClauses`): القيمةُ
  والمقدَّمُ والمحتجزُ والغرامةُ والضمان — تُشتقّ عند كل عرضٍ وطباعةٍ من قيم العقد
  النافذة، فلا يتناقض المطبوعُ مع ما يخصمه سُلَّمُ المستخلص. **وشرطٌ بلا رقمٍ لا يُطبَع**
  فلا محتجزَ صفريٌّ في وثيقةٍ رسمية.
  **المخرَجُ الورقيّ.** `printContract` عبر `_openPrintWindow` القائمة في النواة (ومعها
  معالجةُ iOS المجرَّبة): ترويسةُ الشركة · **الطرفان بهويتيهما** من `identityOf` (سجلٌّ
  تجاريٌّ للمنشأة وهويةٌ/إقامةٌ للشخص — وهنا يثمر سجلُّ الأطراف) · موضوعُ العقد ·
  جدولُ بنود الأعمال بملخّصه · الشروطُ مجمَّعةً بتصنيفها · **وحقولُ توقيع الطرفين**.
  **الاختبارات.** `hail-tests` ✅ **1544/1544** (+٢٧ حارساً)، و`contracts-check.mjs`
  ✅ **100/100** — بمقاطعةٍ فعليةٍ لنافذة الطباعة والتحقّقِ من محتوى الوثيقة: هويةُ
  الشخص فيها، ونصُّ الغرامة يحمل **٠٫١٪ وسقفَ ١٠٪**، وحقولُ التوقيع؛ ثم **رفضُ توقيعٍ
  بلا نسخة** ⇐ رفعُها يجعل العقد سارياً ⇐ **ومستخلصٌ قبل التوقيع مرفوض**.

- **v18.9.2507 — وحدةُ التعاقدات (المرحلة ٤): المستخلصُ التراكميُّ وسُلَّمُ خصوماته وسدادُه:**
  **الناتج.** تبويبُ المستخلصات في بطاقة العقد صار حيّاً: شريطُ إنجازٍ ونسبةٌ وجدولُ
  مستخلصات، ونموذجُ إعدادٍ **تراكميّ** يبدأ من كميات «سبق اعتماده» لا من صفرٍ أعمى،
  ودورةُ اعتمادٍ **أقصرُ عمداً** (مدير المشاريع ⇐ التنفيذيُّ فوق السقف ⇐ سدادُ المالية)
  — فالنطاقُ والطرفُ والسعرُ حُسمت في العقد، والمستخلصُ لا يقرّر إلا **كم أُنجِز**.
  **«المستخلَص سابقاً» يُحسب ولا يُخزَّن.** `prevGrossOf` تجمعه من المستخلصات
  **المعتمدةِ أو المسدَّدة** وحدَها لهذا العقد؛ ورقمٌ مخزَّنٌ كان سينحرف عن مصدره بعد أوّل
  تعديلٍ أو حذف. **والحرّاسُ الثلاثة:** (١) التراكميُّ **يُمنَع** فوق كمية العقد — وهو
  الحارسُ المانعُ الوحيد في المنظومة لأن تجاوزَه خطأٌ لا اجتهاد، ويرفع سقفَه **أمرُ
  تغييرٍ معتمد**؛ (٢) **التراجعُ** عمّا اعتُمد ممنوعٌ أيضاً — يُنتج فترةً سالبةً لا يعرف
  النظامُ كيف يسدّدها؛ (٣) **مستخلصٌ مفتوحٌ واحدٌ لكلّ عقد**، وإلا تصارع رقمان على
  «سابقاً» وانحرف الرصيدُ بلا خطأٍ ظاهر. وكلُّها في **طبقة البيانات** لا في الشاشة.
  **سُلَّمُ الحساب هو شكلُ المعادلة نفسِها.** يُرسَم من `extNet` وحدَها بلا حسابٍ في
  الترميز، ويعرض **الخصوماتِ المفعَّلةَ فقط** فلا صفوفَ أصفارٍ تُشتّت. والمبلغُ المسدَّد
  **صافي السلّم** لا رقماً يُدخله المستخدم، و**لقطةُ السلّم تُحفَظ وقت السداد** (`settled`)
  فلا يُعاد حسابُها بعد تغيّر شيء. واستهلاكُ الدفعة المقدمة **يُراكَم على العقد في
  المعاملة نفسِها** فيعرف المستخلصُ التالي ما تبقّى، و**المستخلصُ الختاميُّ يُنهي العقد
  فنّياً** في تلك المعاملة أيضاً.
  **الغرامةُ تُقترَح ولا تُفرَض.** `lateDaysOf`/`suggestedPenalty` تحسبان التأخّرَ عن مدة
  العقد وتقترحان المبلغَ بسقفه، والزرُّ يطبّقه — ويبقى الحقلُ يدوياً، فواقعُ التفاوض
  ليس معادلة.
  **ونتيجةٌ صادقةٌ من التنفيذ:** خصمُ **المواد المصروفة** — أعلى بنود العائد في التصميم —
  **لا يُحتسَب آلياً**: أوامرُ الصرف تسجّل الكميات **بلا تكلفة**، ولا تحمل أصنافُ المخزون
  سعرَ وحدةٍ ولا متوسّطَ تكلفة. فنُفِّذ حقلاً يدوياً بنصٍّ يشرح السبب، وأتمتتُه تتطلّب
  **تسعيرَ مخزونٍ** أوّلاً — بندٌ مستقلٌّ يستحقّ قرارَ المالك.
  **الاختبارات.** `hail-tests` ✅ **1517/1517** (+٣٠ حارساً)، و`contracts-check.mjs`
  ✅ **86/86** — بدورةٍ ماليةٍ كاملةٍ في Chromium: تراكميٌّ فوق العقد **يُعطّل الإرسال
  ويشرح السبب لحظياً** ⇐ كميةٌ سليمةٌ تُعيده ⇐ **السلّمُ المرسوم = المحسوب** ⇐ لا مستخلصَ
  ثانياً قبل إغلاق المفتوح ⇐ اعتمادٌ ⇐ رفضُ سدادٍ بلا إيصال ⇐ سدادٌ بلقطةٍ محفوظة
  واستهلاكِ مقدَّمٍ ٣٬٣٦٠ ⇐ مستخلصٌ ثانٍ يقرأ «سابقاً» ١٦٬٨٠٠ محسوباً ⇐ ختاميٌّ يُنهي
  العقد ⇐ إقفالٌ يُفرِج عن ١٬٦٨٠.

- **v18.9.2505 — وحدةُ التعاقدات (المرحلة ٣): التحويلُ إلى عقدٍ ساري وبطاقةُ العقد:**
  **الناتج.** زرُّ «إنشاء العقد» على الطلب المعتمَد (للمشتريات والأدمن)، وصفحةُ **العقود**
  الثالثة، وبطاقةٌ بأربعة تبويبات (نظرة عامة · البنود · المستخلصات · السجل)، ودورةُ حالةٍ
  تشغيلية: ساري ⇄ موقوف ⇒ منتهٍ فنّياً ⇒ مقفل، والفسخُ للأدمن وحدَه.
  **الفصلُ بين البناء والنقل — القرارُ المركزيّ.** البناءُ **دالةٌ نقيةٌ**
  `contractFromRequest(req, id, now, by)` تُفحَص وحدَها بلا Firestore، و**المعاملةُ تنقلها**
  فتحرس الذرّية. خلطُهما كان يعني منطقَ تحويلٍ لا يُفحَص إلا بمتصفّح. والعقدُ **يرث ولا
  يُعيد الإدخال**: الطرفُ والبنودُ والقيمةُ ووضعُ الضريبة والشروطُ التجارية وشكلُ المشروع
  القياسيُّ تنتقل كما هي — فلا يظهر في العقد رقمٌ لم يره معتمِد. وبندُ الموازنة الفارغُ
  **يبقى فارغاً**: الربطُ اختياريٌّ في العقد كما في الطلب.
  **حارسُ عدم التكرار داخلَ المعاملة لا قبلها.** القراءةُ المسبقةُ تسمح لضغطتين متزامنتين
  بالمرور معاً فينشأ عقدان لطلبٍ واحد. وحين يكون الطلبُ محوَّلاً سلفاً **يُرجَع معرّفُ
  عقده لا خطأٌ غامض** — فالضغطةُ الثانية غالباً نقرةٌ مكرّرة، والمستخدمُ يريد أن يرى
  عقده لا رسالةَ فشل. ولا يُنشأ عقدٌ من طلبٍ غير معتمَد، ولا من دورٍ لا يملك ذلك.
  **الأرقامُ مشتقّةٌ لا مُدخَلة.** مبلغُ الدفعة المقدمة من نسبتها على **قيمة العقد
  النافذة** (`advanceAmountOf`)، والمُفرَجُ من المحتجز عند الإقفال يُحسب في المعاملة
  نفسِها. و`ctrCanTransit` **مصدرٌ واحدٌ تقرأه الأزرارُ والمعاملةُ معاً** — فلا يمرّ
  انتقالٌ لأن زرّاً ظهر بالخطأ، ولا يُخفى زرٌّ يملكه صاحبُه.
  **وحارسٌ جديدٌ عامّ:** فحصٌ يتحقّق أن **كلَّ اسم أيقونةٍ تستعمله الوحدة موجودٌ في مجموعة
  المنصة** — فـ`_ic` يُرجع فراغاً للاسم المجهول، وكان `flag` كذلك حتى كشفه الحارس.
  **الاختبارات.** `hail-tests` ✅ **1487/1487** (+٢٦ حارساً)، و`contracts-check.mjs`
  ✅ **70/70** — بسلسلةٍ متّصلة: طلبٌ بشروطٍ تجاريةٍ حقيقيةٍ يُملأ في النموذج ⇐ أربعُ
  بوّابات ⇐ **تحويلٌ بمعاملة** ⇐ عقدٌ سارٍ ورث القيمةَ ٣٣٬٦٠٠ والمحتجزَ ٥٪ والمقدَّمَ
  المشتقَّ ٣٬٣٦٠ ⇐ ضغطةٌ ثانيةٌ لا تُنشئ عقداً ثانياً ⇐ دورةُ الحالة الكاملة ⇐ الإقفالُ
  يُفرِج عن **١٬٦٨٠** ⇐ ولا إجراءَ بعده.

- **v18.9.2503 — الربطُ بالموازنة اختياريّ، والمشروعُ اليدويُّ مواطنٌ كاملٌ في التعاقدات:**
  **الجذر.** طلبُ المالك: «قد يكون العقدُ لمشروعٍ مُدخَلٍ يدوياً ولا موازنةَ له». والفحصُ
  كشف فجوةً أوسعَ من السؤال: نموذجُ الطلب كان يعرض **المشاريعَ المسجّلة وحدَها** من
  `_projectsList` — فالمشروعُ اليدويُّ لم يكن قابلاً للاختيار أصلاً — وكان `budgetCategoryKey`
  يبدأ بقيمةٍ افتراضية (`subcontractor`) فيُربَط الطلبُ ببندٍ لا وجودَ له.
  **القاعدة الآن.** عقدٌ بلا بندِ موازنةٍ **عقدٌ صحيحٌ تامّ**، وكلُّ ما يفقده ظهورُه في
  مقارنة «المخطَّط مقابل المتعاقَد عليه». و`budgetLinkState` تُرجع **ثلاثَ حالاتٍ لا
  اثنتين**: `no_budget` (لا موازنةَ أصلاً ⇒ **لا منتقٍ ولا تحذيرٌ ولا لوم**، ورسالةٌ
  صريحةٌ أن الربط اختياريّ) · `unlinked` (له موازنةٌ ولم يُربَط ⇒ إشارةٌ محايدةٌ تشرح ما
  يفوته) · `linked` (تُقارَن القيمةُ بالمخطَّط). ولا حالةَ رابعةَ اسمُها «خطأ». وقسمُ
  المقايسة **يختفي** لمشروعٍ بلا مقايسة بدل جدولٍ فارغٍ يوحي بنقص.
  **والمشروعُ اليدويُّ صار مواطناً كاملاً.** القائمةُ تُقرأ من `projectMgmt._allProjects`
  (المسجّلة + اليدوية بلا تكرار) لا من نسخةٍ محلية، ومعها خيارُ **«مشروع يدويّ جديد»**
  يكشف حقلَ اسمٍ حرٍّ لمشروعٍ لم يظهر في أيّ طلبٍ بعد. والتخزينُ **بالشكل القياسيّ نفسِه**
  الذي فرضه `v18.9sz` (`projectId:"__OTHER__"` + `isCustomProject` + `projectName`) عبر
  دالةٍ نقيةٍ واحدة `normalizeProjectRef` — ومعرّفُ `__MPN__:` معرّفُ **عرضٍ** داخليٌّ
  **لا يُخزَّن**، فتخزينُه كان سيُنتج مشروعاً ثالثاً لا تعرفه أيُّ شاشةٍ أخرى. والعرضُ
  عبر `docProjectName` والتجميعُ عبر `docProjectKey` (`__CUSTOM__:`+الاسم) — اصطلاحُ
  النواة حرفياً، فلا يختلف اسمُ المشروع ولا تجميعُه بين شاشتين.
  **الاختبارات.** `hail-tests` ✅ **1461/1461** (+١٦ حارساً)، و`contracts-check.mjs`
  ✅ **51/51** — بإرسالِ عقدٍ حقيقيٍّ لمشروعٍ يدويٍّ **من النموذج نفسِه** (لا بحقنِ حالة):
  حقلُ الاسم الحرّ يظهر، ولا منتقيَ موازنةٍ ولا جدولَ مقايسة، والوثيقةُ تُخزَّن
  `__OTHER__`+`isCustomProject`+الاسم بقيمةِ ١٠٬٨٠٠ وبندِ موازنةٍ فارغ.

- **v18.9.2500 — وحدةُ التعاقدات (المرحلة ٢): طلبُ التعاقد ودورتُه وأمرُ الدفع:**
  **الناتج.** ثلاثُ قدراتٍ فوق دوالِّ المرحلة ١ نفسِها بلا إعادة كتابة: (١) **طلبُ تعاقدٍ
  يُولَد من مقايسة المشروع** — يختار المستخدمُ بنوداً من `meta/{projId}_boq` القائمة
  فيرث الطلبُ الكميةَ والوحدةَ وسعرَ الوحدة **وبندَ الموازنة**، وتُقارَن قيمتُه فوراً
  بالمخطَّط فيظهر تحذيرُ تجاوزٍ **لا يمنع** الإرسال (القرارُ ٢ القائم). (٢) **دورةُ اعتمادٍ
  بأربع بوّابات** عبر `crqNextStage` وحدَها. (٣) **مسارُ أمر الدفع** المختصر: بطاقةُ اختيارٍ
  تُقفَل فوق العتبة **بنصٍّ يشرح السبب** لا بزرٍّ ميّتٍ بلا تفسير.
  **بصمتا الاعتماد — الفكرةُ المركزية.** كلُّ معتمِدٍ يوقّع على شيءٍ بعينه، فإن تغيّر
  ذلك الشيءُ سقط توقيعُه **وحدَه** لا كلُّ التوقيعات: `crqProcKey` تغطّي **الطرفَ
  والتنافس** (ما تفحصه المشتريات)، و`crqFinanceKey` تغطّي **القيمةَ ووضعَ الضريبة
  والشروطَ التجارية وبندَ الموازنة** (ما تفحصه المالية). فتعديلُ نسبةِ محتجزٍ يُعيد الطلبَ
  للمالية ولا يزعج المشتريات، وتبديلُ الطرف يُعيده للمشتريات ولا يزعج المالية. وبصمةُ
  المشتريات **مرتَّبةٌ حتمياً** فلا يُسقطها إعادةُ ترتيب المرشّحين وحدها.
  **الحرّاسُ في طبقة البيانات لا على الأزرار.** كلُّ إجراءٍ **معاملةٌ** تقرأ الوثيقة
  الطازجة ثم تطبّق التعديل وتحدّث الذاكرةَ بنتيجتها؛ وتَرفض المعاملةُ نفسُها: بوّابةً
  ليست لدور المستخدم · رفضاً بلا سبب · سداداً بلا إيصال · سداداً من غير المالية أو من
  غير حالة الانتظار · وإجراءً على حالةٍ نهائية. والحالةُ **تُشتقّ من `crqNextStage`**
  ولا تُكتب يدوياً في أيّ موضع، وقيمةُ الطلب **تُحسب من بنوده** لا من حقلٍ حرٍّ يكتبه مُنشئُه.
  **جسرُ المقايسة بلا نسخ.** مسارُ المقايسة والموازنة يُقرأ من `projectMgmt._loadBoq`/
  `_loadBudget` المُضافَين لواجهتها — **ولا يُنسَخ `_safeKey`**: نسختان من قاعدة اشتقاق
  مفتاح المشروع تنحرفان، فيقرأ طلبُ التعاقد مقايسةَ مشروعٍ غيرِ الذي تكتبه إدارةُ المشاريع.
  **الاختبارات.** `hail-tests` ✅ **1445/1445** (+٢٥ حارساً للبصمتين والبوّابات وحرّاس
  الكتابة والجسر)، و`contracts-check.mjs` ✅ **44/44** — بدورةٍ **كاملةٍ حقيقيةٍ** في
  Chromium: بندٌ من المقايسة يرث كميتَه، وشخصٌ يضبط الضريبةَ على `none` تلقائياً،
  والإجماليُّ المرسوم = المحسوب، وأربعُ بوّاباتٍ بالترتيب ⇐ «جاهزٌ لإنشاء العقد»، وأمرُ
  دفعٍ بـ١٥٠٠ يقفز إلى السداد مباشرةً، ورفضُ سدادٍ بلا إيصال، ورفضُ بوّابةٍ لدورٍ لا يملكها.

- **v18.9.2497 — التعاقدُ مع شخصٍ طبيعيٍّ لا مع منشأةٍ فقط (استكمالُ المرحلة ١):**
  **الجذر.** سأل المالك: «قد يكون التعاقدُ مع شخصٍ لا مؤسسة، وحينها بهويته». والفحصُ
  أثبت الفجوة: المرحلةُ ١ كانت تفترض **منشأةً بسجلٍّ تجاريّ** — حقلُ `crNumber` هو
  المعرّف، وقائمةُ الوثائق تبدأ بالسجل التجاري وتضمّ الزكاةَ والسعودةَ والتأمينات،
  ومنعُ التكرار **بالاسم**. فتسجيلُ شخصٍ كان يعني سجلاً تجارياً وهمياً أو حقولاً فارغة.
  **وليس نقصَ حقلٍ بل ثلاثةُ فروقٍ جوهرية.** (١) **مستندُ الهوية**: سجلٌّ تجاريٌّ
  للمنشأة، وهويةٌ وطنيةٌ/إقامةٌ/خليجيةٌ/جوازٌ للشخص — وتُقرأ كلُّها من مصدرٍ واحد
  `identityOf(vendor)` فلا تتكرّر شروطُ «إن كان شخصاً» في عشرة مواضعَ تفترق بعد شهر.
  (٢) **الضريبة**: الشخصُ غالباً غيرُ مسجَّل ⇒ `suggestVatMode` تقترح له **`none`**
  تلقائياً — وهذا هو الرابطُ العمليُّ بين هذه الصفة وأوضاع الضريبة الثلاثة المبنيّة
  سابقاً؛ والاقتراحُ **يُقترَح ولا يُفرَض** (`taxRegistered` يتقدّم عليه). (٣) **مفتاحُ
  التفرّد**: `duplicateOf` صارت **بالرقم لا بالاسم** — فرقمُ هويةٍ مكرَّرٌ **يُمنَع**،
  أمّا اسمٌ مكرَّرٌ لشخصٍ فـ**تنبيهٌ لا يمنع**: منعُه كان سيوقف تسجيل «محمد أحمد»
  الثاني، وهو خطأٌ يوقف العمل لا يحميه (واسمُ المنشأة يبقى مانعاً — هو معرّفُها).
  **وتوابعُ ثلاثة.** الوثائقُ تتبع الصفة (`docTypesFor`): لا زكاةَ ولا سعودةَ لشخص،
  ولا رخصةَ عملٍ لمنشأة — وما سُجِّل قبل تبديل الصفة **يبقى ظاهراً** فلا تُمحى وثيقةٌ
  بتبديلٍ عابر. وانتهاءُ الهوية دخل **محرّك الانتهاء نفسَه** (`allExpiring`) — بدونه
  تمرّ إقامةٌ منتهيةٌ بلا تنبيه؛ ونصُّ تحذيرها صريحٌ («إقامة منتهية — راجعها قبل
  الإسناد») ويبقى **تحذيراً لا منعاً** اتّساقاً مع فلسفة المنصة. وحقلُ **اسم صاحب
  الحساب** أُضيف للبيانات البنكية — حسابُ الشخص يجب أن يكون باسمه، والتحويلُ لطرفٍ
  ثالثٍ يُفقد الإثبات.
  **التوافق.** صفةٌ غائبةٌ ترتدّ إلى «منشأة» — فأيُّ طرفٍ سُجِّل قبل هذا التغيير يبقى
  كما هو بلا هجرةِ بيانات.
  **الاختبارات.** `hail-tests` ✅ **1408/1408** (+١٥ حارساً للصفة والهوية والتفرّد
  والوثائق)، و`contracts-check.mjs` ✅ **25/25** — بأشخاصٍ حقيقيّين في الشاشة:
  بطاقةٌ تعرض «إقامة» لا «السجل التجاري»، وجنسيةً بدل الرقم الضريبيّ، و«بلا ضريبة»
  وضعاً مقترَحاً، ونموذجاً تتبدّل حقولُه بتبديل الصفة **بلا فقدِ ما كُتب**.

- **v18.9.2495 — وحدةُ التعاقدات (المرحلة ١): النواةُ والدوالُّ النقيةُ وسجلُّ الأطراف:**
  **الطلب.** ثلاثةُ متطلّباتٍ من المالك تحكم البناء: (١) عقودٌ تُضاف عليها ض.ق.م وعقودٌ
  لا؛ (٢) الاتفاقُ دون **٣٠٠٠ ريال** يُسدَّد بـ**أمر دفع**، ومع ذلك يبقى إصدارُ عقدٍ
  لمبلغٍ صغيرٍ ممكناً؛ (٣) النظامُ في **ملفٍ منفصل** بمنطق تصميم المنصة نفسِه، على مراحل.
  **الناتج.** `contracts.js` — وحدةٌ مستقلّةٌ (IIFE) تركّب صفحتها ومجموعتَها الجانبية
  ذاتياً وتلفّ `showPage`، فلا تطلب من `index.html` إلا وسمَ <script> واحداً.
  **الضريبةُ ثلاثةُ أوضاعٍ لا وضعان.** `excl` · `incl` · **`none`** — ومقاولُ الباطن
  غير المسجَّل ضريبياً هو سببُ الثالث: اعتبارُ فاتورته «شاملة» يستخرج ١٥٪ وهميةً من
  مستحقّه ويُبلغ عن ضريبةِ مدخلاتٍ لا سندَ لها. والتقريبُ **على سعر الوحدة** ثم الضربُ
  في الكمية (اصطلاحُ `po-lifecycle-check`) — والفرقُ حقيقيّ: `10.10 × 3` بوضع `excl`
  يعطي **34.86** على الوحدة و34.85 على السطر، وحارسٌ يثبّت الأول.
  **أمرُ الدفع يسمح ولا يُلزم.** `payOrderAllowed` تُرجع صحيحاً **دون** العتبة فقط؛
  وفوقها **ممنوعٌ قطعاً** — وإلا صار أمرُ الدفع باباً خلفياً يتجاوز بنودَ العقد
  ومحتجزَ الضمان والمستخلصات. والعتبةُ تُقرأ من `meta/global_purchase_config` بالحقل
  `contractPayOrderThreshold` — **الوثيقةُ نفسُها** التي تحمل سقفَ التنفيذي، فلا وثيقةَ
  إعداداتٍ ثانيةٌ تنحرف. و**دالةُ توجيهٍ واحدةٌ `crqNextStage`** للعقد ولأمر الدفع معاً:
  أمرُ الدفع يتخطّى المشتريات واعتمادَ المالية، **ولا يتخطّى بوّابة التنفيذي** — السقفُ
  سقفُ المال لا سقفُ نوع الورقة. وبحارسَي `hr-payments`: البوّابةُ من **القيمة** لا من
  اختيار المُنشئ، واعتمادُ التنفيذي **يسقط** إن رُفعت فوق `ceoApprovedAmount`.
  **العزل.** مجموعاتٌ خاصةٌ تماماً (`global_vendors` · `global_contract_requests` ·
  `global_contracts` · `global_contract_extracts`) و**لا لمسَ لـ`global_purchases` ولا
  للمخزون ولا للبند المستعاض** — يحرسه فحصٌ نصّيّ، للسبب الذي عُزلت لأجله `hr-payments`.
  **سجلُّ الأطراف.** أوّلُ شاشةٍ ظاهرة: أطرافٌ بوثائقهم وتنبيهِ سريانها (`docExpiryState`
  ⇐ توكنز `--sla-ok/warn/crit` نفسِها)، وبحثٌ **بالتطبيع العربيّ** (همزة/تاء مربوطة/ياء)
  فيُطابَق الاسمُ الحرُّ التاريخيّ، و**الآيبان حقلٌ حسّاس**: مقنَّعٌ لغير المالية والأدمن،
  وتعديلُه مقيَّدٌ في السجل. والمحظورُ وحده يمنع الإسناد؛ الوثيقةُ المنتهية **تُحذّر ولا
  تمنع** (فلسفةُ الفحص الوقائي في `finance-audit`).
  **التصميم.** بلا لونٍ جديدٍ ولا خطٍّ جديد: توكنزُ المنصة وكلاساتُها الجاهزة، والأرقامُ
  مونوسبيس بـ`tabular-nums` و`direction:ltr` — **حارسٌ يرفض أيَّ لونٍ صريحٍ في نمط
  الوحدة**، فتتبع الثيمين معاً بلا صيانةٍ منفصلة.
  **الاختبارات.** `hail-tests` ✅ **1393/1393** (+٥١ حارساً جديداً على الدوالّ النقية
  والعزل والتصميم)، وفحصُ متصفّحٍ جديد `contracts-check.mjs` ✅ **15/15** بدخولٍ ونقرٍ
  فعليّين يتحقّق أن **الرقم المرسوم هو الرقم المحسوب**، والثيمِ الداكن، والجوّالِ بلا قصّ.
  **التالي:** طلبُ التعاقد ودورتُه وأمرُ الدفع (م٢) ⇐ العقد (م٣) ⇐ المستخلصات (م٤)
  ⇐ الربطُ بالموازنة (م٥).

- **v18.9.2494 — هيكلةُ مسار التعاقد: طلبُ تعاقدٍ ← اعتمادٌ ← عقدٌ ← مستخلصات (وثيقةٌ فقط، بلا كود):**
  **الطلب.** بعد اعتماد التصوّر العام، طُلبت **الهيكلةُ القابلةُ للتنفيذ** لمسارٍ محدّد:
  طلبُ تعاقدٍ تصدره إدارةُ المشاريع ⇐ دورةُ اعتماد ⇐ إنشاءُ العقد ⇐ مراحلُ المستخلصات.
  **الناتج.** `docs/contract-lifecycle-design.md`. قراراتُه البنيوية الستّة:
  (١) **ثلاثُ مجموعاتٍ مستقلّة** لا وثيقةٌ تنتفخ — الطلبُ قد يُرفَض فلا ينشأ عقد (وحشرُه
  في العقد يُنتج «عقوداً ميّتة»)، والمستخلصُ يتكرّر شهرياً فمصفوفةً داخل العقد تعني وثيقةً
  تنمو بلا حدٍّ ومعاملاتٍ متصارعةً على وثيقةٍ واحدة — وهو الثمنُ المدفوعُ في `finance-audit`؛
  والمستخلصاتُ **مسطَّحةٌ** بـ`contractId` لا مجموعةً فرعية، فمستمعٌ واحدٌ يكفي.
  (٢) **الطلبُ يُولَد من مقايسة المشروع** (`meta/{projId}_boq` القائمة) لا من فراغٍ — فيرث
  الكميةَ وبندَ الموازنة، وتُقارَن قيمتُه فوراً بالمتبقّي (تحذيرٌ لا منع، قرارُ ٢ القائم).
  (٣) **أربعُ بوّاباتٍ** (مدير المشاريع · المشتريات · المالية · التنفيذيُّ شرطياً) في
  **دالةٍ نقيةٍ واحدة** `crqNextStage` — نمطُ `hrPayments._nextStage` بحارسيه: البوّابةُ من
  **القيمة** لا من اختيار المُنشئ، واعتمادُ التنفيذي **يسقط** إن رُفعت فوق `ceoApprovedAmount`؛
  ويُضاف حارسٌ ثالث: **تعديلُ شرطٍ تجاريٍّ بعد اعتماد المالية يُرجعه لبوّابتها**.
  (٤) **التحويلُ للعقد بمعاملةٍ واحدة** على نمط `rfq_converted`، بحارس idempotency فلا
  عقدان من طلبٍ واحد، ورابطٌ في الاتجاهين (`requestId`/`contractId`) — فسلسلةُ التوقيع
  قابلةٌ للتتبّع من العقد، وهو أولُ ما يسأل عنه المدقّق. والعقدُ **ينشأ سارياً لا مسودةً**.
  (٥) **المستخلصُ تراكميٌّ** بسُلَّمٍ من ثماني خطواتٍ في دالةٍ نقيةٍ واحدة `extNet` تقرؤها
  الشاشةُ والتقريرُ والـPDF (درسُ `po-lifecycle-check`: الرقمُ المرسوم = المحسوب)، وثلاثةُ
  حرّاس: التراكميُّ **يمنع** تجاوزَ كمية العقد، ومستخلصٌ مفتوحٌ **واحدٌ** لكلّ عقد،
  و«المستخلَصُ سابقاً» **يُحسب لا يُخزَّن**. ودورتُه أقصرُ عمداً (النطاقُ والسعرُ حُسما في العقد).
  (٦) **أمرُ التغيير طلبُ تعاقدٍ من نوعٍ آخر** (`kind:"change_order"`) لا محرّكٌ ثانٍ — فيمرّ
  البوّاباتِ نفسَها، ويُحسب سقفُ التنفيذي على **قيمة العقد الجديدة** لا على الزيادة وحدها.
  **الأثر الماليّ.** الموازنةُ تصير **خمسَ خانات** بإضافة «قيدَ الاعتماد» — يُرى الالتزامُ
  **قبل** توقيعه، وهي اللحظةُ الوحيدةُ التي يبقى المنعُ فيها بلا كلفة.
  **الاختبارات.** ١٣٤٢/١٣٤٢ ناجحة (وثيقةٌ فقط — لا منطقَ تغيّر). ثمانيةُ أسئلةٍ تنتظر
  قرارَ المالك في §١٢ قبل أيّ كود، أوّلُها عددُ البوّابات وسقفُ التنفيذي للعقود.

- **v18.9.2493 — إدارةُ التعاقدات مع مقاولي الباطن والموردين: مقترحٌ للنقاش (وثيقةٌ فقط، بلا كود):**
  **الجذر.** النظامُ يعرف **الشراء** ولا يعرف **التعاقد**: المورّدُ نصٌّ حرٌّ يُكتب باليد
  في كل طلبٍ وعرض (فلا تاريخَ له ولا مقارنة ولا فحصَ امتثال)، وبندُ الموازنة
  `subcontractor` «مقاول باطن» قائمٌ في `project-management.js` **بلا ما يملؤه** — لا
  أمرَ إسناد ولا مستخلصَ مقاولٍ ولا محتجزَ ضمان. فالأعمالُ المسندة إمّا تُحشَر في طلبات
  شراءٍ فتُفسد مؤشّرات التوريد والمخزون، أو تخرج من النظام فلا تُعرَف قيمةُ التزاماتنا.
  **المقترح.** `docs/subcontractor-contracts-plan.md`. قرارُه المحوريُّ أن **أمر الإسناد
  ليس طلب شراء** — الشراءُ يُغلَق بالكمية المستلَمة ويُغذّي المخزون ويُدفَع كاملاً،
  والإسنادُ يُصرف بنسبة الإنجاز التراكمية ولا يمسّ المخزون ويُحتجَز منه ويُخصَم منه
  (نفسُ عزلِ `hr-payments.js` عن `global_purchases`، وأعمالُ المقاول أولى به مبلغاً).
  وخمسُ طبقات: سجلُّ الأطراف `global_vendors` بهجرةٍ بلا كسرٍ عبر محرّك التطبيع العربي
  القائم في `finance-audit.js` · بطاقةُ أداءٍ **مشتقّةٌ كلياً من بياناتٍ مخزَّنةٍ اليوم**
  (timeline الطلب · المستلَم/المطلوب · محرّك المقارنة) بلا حقلٍ جديد · العقدُ بأنواعه
  الأربعة `global_contracts` بسلسلةِ اعتمادٍ تقرأ `CEO_APPROVAL_THRESHOLD` الموحّد ·
  المستخلصُ التراكميُّ بخصوماته الخمسة — أبرزُها **خصمُ المواد المصروفة للمقاول من
  `global_issue_orders`** (بياناتٌ موجودةٌ لا تُستعمَل) · والربطُ بالأداء والضمان الذي
  يُغلق فجوةَ `warrantyDate` الموثّقة في `performance-contract-assessment-2026-08.md §م٥`.
  **الحارسُ الماليّ.** التجميعُ في الموازنة يصير رباعياً (موازنة │ متعاقَدٌ عليه │ مرتبط
  │ مصروف)، ومعه قرارٌ يجب أن يُكتب في §3 مع حارسِ اختبار: **منعُ الازدواج المحاسبي** —
  مصروفُ `subcontractor` من المستخلصات المعتمدة **فقط**، وطلبُ شراءٍ بـ`contractId`
  يُخصَم من سقف عقده ولا يُضاف فوقه. **الاختبارات.** ١٣٤٢/١٣٤٢ ناجحة (وثيقةٌ فقط —
  لا منطقَ تغيّر). سبعةُ أسئلةٍ تنتظر قرارَ المالك في §١١ قبل أيّ كود.

- **v18.9.2493 — خطأ الذكاء الاصطناعي يقول سببَه وحلَّه بالعربية بدل جملةٍ إنجليزيةٍ خام:**
  **الجذر.** حين يفشل نداءُ Claude لسببٍ في **الحساب** لا في النظام — نفادُ الرصيد
  أوّلاً — يرمي الـProxy رسالةَ Anthropic كما هي: «Your credit balance is too low…».
  و`_msgErr` كان يعالج حالتين فقط (المهلة والشبكة) ويمرّر ما عداهما حرفياً، فيظهر
  للمستخدم توستٌ عربيٌّ يحمل جملةً إنجليزيةً بلا معنى له: يقرأها **عطلاً في التطبيق**
  فيُبلّغ عن خللٍ وهميّ، بينما العلاجُ شحنُ رصيدٍ لا إصلاحُ كود. ظهر عملياً في «صياغة
  احترافية للتقرير (AI)» داخل نافذة إغلاق البلاغ.
  **الإصلاح.** `_aiErrAr(m)` — مترجمٌ واحدٌ لأخطاء المزوّد المعروفة: نفادُ الرصيد
  (بالوجهة: `console.anthropic.com › Plans & Billing`) · تجاوزُ حدّ الطلبات (429) ·
  ازدحامُ الخوادم (529) · مفتاحٌ غير صالح (401) · إعدادُ Proxy خاطئ (403/404). كلُّ
  رسالةٍ تذكر **السبب والحل**، ورسالةُ الرصيد تنفي عطلَ النظام صراحةً وتقترح الكتابةَ
  اليدوية. ما لا يُطابَق **يبقى نصُّه كما هو** فلا يُبتلَع تشخيصٌ جديد. يسري على كل
  ميزات الذكاء في `index.html` (التصنيف · تحليل الصورة · البلاغات المتكررة · صياغة
  الإغلاق · الملخص التنفيذي · الخطابات) لأنها كلُّها تمرّ بـ`_msgErr`، وعلى
  `tech-app.html` بنسخةٍ موجّهةٍ للفنّيّ (تحيله للمسؤول لا لصفحة فوترةٍ لا يملكها).
  **التحقق.** `node hail-tests.js` — ١٣٥٤/١٣٥٤، منها ١٢ حارساً جديداً يُنفّذان
  الدالّتين فعلاً على رسالة الرصيد الحقيقية ويتحققان: عربيةٌ خالصة · لا تسريبَ للنصّ
  الإنجليزيّ · «النظام سليم» مذكورة · الخطأُ المجهول يمرّ بلا ابتلاع.
  **ملاحظة للمالك.** هذا التعديل يُحسّن **عرضَ** الخطأ فقط؛ عودةُ الميزة للعمل تتطلّب
  شحنَ رصيد حساب Anthropic — لا شأن للتطبيق به.

- **v18.9.2491 — دورةُ حياة طلب الشراء وحساباتُه: فحصُ متصفّحٍ دائمٌ بدل تشغيلٍ عابر:**
  **الجذر.** طلبُ الشراء هو المسارُ الذي يُحاسَب عليه المال، وحساباتُه محروسةٌ في
  `hail-tests` **نصّاً مصدرياً** وفي `browser-scenarios` **تدفّقاً** (اعتماد · استلام ·
  حذف). لكنّ سؤالاً بقي بلا حارس: **هل الرقمُ على الشاشة هو الرقمُ المحسوب؟** حسابٌ
  صحيحٌ في الذاكرة ورقمٌ آخر في البطاقة عطلٌ لا يكشفه إلا تشغيلٌ حقيقيٌّ يقرأ الـDOM.
  **الإصلاح.** فحصٌ دائم **`po-lifecycle-check.mjs`** (٥٣/٥٣) يشغّل دوالَّ التطبيق
  نفسها في Chromium ثم يقرأ ما رُسم. العهودُ المحروسة: (١) ض.ق.م تُقرَّب **على سعر
  الوحدة** ثم تُضرب في الكمية لا على إجمالي السطر — ٣٣٫٣٣ × ٧ ⇒ **٢٦٨٫٣١**، والتقريبُ
  على السطر يعطي رقماً آخر؛ (٢) التكلفة الفعلية من **المستلَم** لا المطلوب (خلل
  `v18.9tl`: مطلوب ١٠ مستلم ٥ ⇒ ٢٨٫٧٥ لا ٥٧٫٥)، و`_poHealItems` يصحّح الجزئيَّ وحده
  بلا مسِّ السليم ولا غيرِ المُدقَّق؛ (٣) سندُ استلامٍ مكرَّرٌ لا يُضخّم التكلفة ولا
  الكمية (`v18.9tk`) والنمطُ القديم بلا بنودٍ ما زال يُحسب من السندات؛ (٤) البند غير
  المستلَم لا تكلفة فعلية له، وطلبٌ بلا بندٍ مستلَم يرجع للمخزَّن لا لصفرٍ كاذب؛
  (٥) دورةٌ كاملة — مقدَّرٌ ١٢٦٫٥ يثبت عبر ست مراحل، ثم استلامٌ جزئيٌّ ⇒ فعليٌّ ٩٧٫٧٥
  ومستلَمٌ ٨ — **ويطابقه المرسوم**: `97.75 ر.س` و`الاستلام 8 / 13`.
  **درسٌ في الفحص نفسه.** التأكيدُ يُكتب على ما يراه المستخدم: أولُ صياغةٍ رسمت الصفحة
  **خلف طبقة الدخول** فمرّت قراءةُ الـDOM ولم تكن اللقطةُ دليلاً على شيء. الآن دخولٌ
  حقيقيٌّ (باعتراض `AUTH_LOGIN_URL` كما في `ui-walkthrough`) ونقرٌ فعليٌّ على
  «المشتريات المركزية»، وفحصٌ صريحٌ أن طبقةَ الدخول اختفت قبل اللقطة.
  **وعُثر في الطريق على عطلٍ أخطر — ختمُ الإصدار يتراجع بصمت.** رفعُ الإصدار لإضافة
  الفحص كشف أن `npm run stamp` ختم **`v18.9.116`** بينما المختومُ فعلاً `v18.9.130`.
  **الجذر:** `git rev-list --count HEAD` في استنساخٍ **ضحل** لا يفشل — **ينجح برقمٍ
  أقلّ** (عمقُ الاستنساخ لا عددُ الـcommits). و`computeVersion` كان يحرس حالة الفشل
  وحدها (`catch`)، فالنجاحُ الكاذب يمرّ. والأثرُ أوسعُ من رقم: الإصدارُ يهبط في اثنين
  وعشرين موضعاً دفعةً واحدة — كلُّ `?v=` وكلُّ `MODULE_BUILD` — فتعود الوحداتُ إلى كاشٍ
  **أقدم** ولا يصل الإصلاحُ للمستخدم (وهو بعينه الخللُ الذي بُني له كاشفُ الوحدات
  القديمة)، ويرى المستخدمُ في التذييل رقماً ينقص. **والأدهى:** كلُّ الأختام السابقة
  اشتُقّت من استنساخٍ ضحل — `v18.9.130` خُتم عند عدٍّ حقيقيٍّ قدره **٢٤٨٩**؛ فتصاعدُ
  ١١٦ → ١٢٨ → ١٣٠ كان تقاربَ أعماقٍ لا اشتقاقاً صحيحاً. **الإصلاح:** حارسان في
  المولِّد — `assertFullHistory` يرفض الاستنساخ الضحل صراحةً بأمرِ الإصلاح
  (`git fetch --unshallow`)، و`assertNoDowngrade` يرفض أيَّ رقمٍ أدنى من المختوم حالياً
  أياً كان السبب (فرعٌ مبتور · قاعدةٌ عُدِّلت). كلاهما **قبل الكتابة** لا بعدها، ويحرسهما
  ثلاثةُ فحوصٍ في `hail-tests`. و`--check` يبقى بلا git كما هو (Actions تفحص commit دمج).
  **أثرٌ مقصودٌ ومرئيّ:** بالتاريخ الكامل صار الرقم الصحيح **`v18.9.2491`** — قفزةٌ
  لمرّةٍ واحدة تُصحّح الانحراف، ثم يتزايد بواحدٍ لكل commit كما صُمِّم.
  **الاختبارات:** `hail-tests` ✅ 1342/1342، و`po-lifecycle-check` ٥٣/٥٣، وبقيةُ فحوص
  المتصفّح خضراء بلا تغيير (61 · 58 · 51 · 47 · 15 · 13). لا تغييرَ في منطق التطبيق —
  حارسان وفحصٌ دائم.

- **v18.9.130 — أيقونةُ مركز العمليات في بوّابة المشاريع: مربّعٌ فارغٌ لأن الـsvg بلا أبعاد:**
  **الجذر.** حين صارت العلامةُ تُقرأ من `_ICON` (v18.9.128) استُبدل الترميزُ المضمَّن
  بـ`_svgIcon("radar")` — و`_svgIcon` يُخرج `<svg>` **بلا `width`/`height`** (يعتمد على
  قاعدةِ حجمٍ في الـCSS كما تفعل القائمة الجانبية: `.sidebar-nav-btn .s-icon svg`).
  ولا قاعدةَ لزرّ البوّابة، فانكمش الـsvg داخل حاوية flex إلى **صفر** وظهر المربّعُ
  فارغاً. وهو **النظيرُ المعاكس** للدرس المعروف في الوحدات (svg بلا أبعادٍ *يتمدّد*
  ليملأ حاويته): الأبعادُ الغائبة تُنتج صفراً أو ملءَ الشاشة بحسب الحاوية — وكلاهما عطل.
  **الإصلاح.** قاعدةٌ واحدة `#tvwall-btn-ico svg{width:21px;height:21px;stroke-width:1.9}`
  بجوار قواعد أيقونات المركز.
  **الحارس.** الفحصُ لم يكن ليمسك العطل: كان يسأل «هل يوجد وسمُ svg؟» والجوابُ نعم
  والمربّعُ فارغ. الآن **قياسٌ هندسيّ** لأبعاد الأيقونتين (البوّابة والقائمة) —
  **والبوّابةُ تُقاس وهي ظاهرة**، فقياسُ عنصرٍ مخفيٍّ صفرٌ دائماً ويُنتج فحصاً كاذباً.
  `hail-tests` كلها خضراء، و`tvwall-check` ٥١/٥١ (٢١×٢١ في البوّابة · ١٨×١٨ في القائمة).

- **v18.9.128 — علامةُ مركز العمليات من مجموعة المنصة، وشبكةُ مبانٍ لا تقصّ بطاقة:**
  **(١) العلامة.** زرُّ «مركز العمليات» في القائمة الجانبية كان يحمل إيموجي 🛰️ بين
  أيقوناتٍ متّجهةٍ أحاديةِ اللون — لأنّ `applyNavIcons` تُلبس الأيقونات **حسب
  `data-page`**، والمركزُ شاشةٌ عليا لا صفحة فلا `data-page` له، فبقي الإيموجي وحدَه
  ظاهراً. الآن **علامةُ الرادار دخلت `_ICON` نفسَها** (قوسان وشعاعٌ ونبضةُ مركز، بلغة
  بقية الأيقونات)، وتُضبَط للزرّ صراحةً في `applyNavIcons`، **ويقرؤها زرُّ بوّابة
  المشاريع من المصدر نفسه** (`#tvwall-btn-ico` يُملأ في `renderProjectGrid`) — فلا
  نسخةَ ثانيةً من المسار تنحرف عن الأولى.
  **(٢) بطاقةُ المبنى المقصوصة.** في مشروعٍ بسبعةَ عشرَ مبنًى كانت شبكةُ «حالة المباني»
  خمسةَ صفوفٍ في أربعة أعمدة، وارتفاعُ الصفّ ثابتٌ (`minmax(64px,auto)`) فتفيض عن
  عمودها وتُقصّ البطاقةُ الأخيرة — **ومبنًى غائبٌ عن شاشةِ متابعةٍ أسوأُ من مبنًى
  صغير**. الآن `grid-auto-rows:minmax(0,1fr)`: الصفوفُ تتقاسم ارتفاعَ العمود مهما
  كثرت المباني، مع سقفٍ لارتفاع البطاقة يمنع تمدّدَ بطاقةٍ واحدةٍ على العمود كلِّه حين
  تقلّ. **الاختبارات:** `hail-tests` كلها خضراء، و`tvwall-check` ٥٠/٥٠ وفيه حارسٌ
  هندسيّ: كلُّ بطاقةِ مبنًى **داخل حدود شبكتها** في مشروعٍ بسبعةَ عشرَ مبنًى مزروعاً.

- **v18.9.123 — رقمُ الإصدار يُشتقّ ولا يُؤلَّف: ختمٌ آليٌّ واحدٌ لثمانيةَ عشرَ موضعاً:**
  **الجذر.** كان الإصدارُ **يُختار يدوياً عند إنشاء الفرع** («الحرف التالي»: ai → aj)
  ثم يُكتب بيد المطوّر في ثمانيةَ عشرَ موضعاً: `APP_VERSION` · تذييل الدخول · أحدَ
  عشرَ `?v=` · ستّةُ `MODULE_BUILD`. ونتيجتُه محتومة: فرعان ينطلقان من نفس القاعدة
  يختاران **نفس الحرف** فيتصادمان عند الدمج — وقع ذلك **ثلاث مرّاتٍ متتالية**
  (`ag` · `aj` · `aj`). وكلُّ موضعٍ منسيٍّ = وحدةٌ تُخدَم من الكاش القديم فلا يصل
  الإصلاحُ للمستخدم — وهو الخللُ نفسُه الذي بُني له كاشفُ الوحدات القديمة (v18.9tg).
  **الحلّ.** `sync-version.mjs`: الإصدارُ `v18.9.<عدد الـcommits>` — عددٌ يتزايد حتماً
  مع كل دمج، فلا اجتهادَ ولا حرفَ يُخمَّن. والمطوّر **لا يكتب رقماً إطلاقاً**: يكتب
  العلامة `vNEXT` في قيد §6 هذا ويشغّل `npm run stamp`، فيُكتب الرقمُ في كل المواضع
  دفعةً واحدة. والأهدافُ **مكتشَفةٌ لا معدودة**: أي وحدةٍ جديدة تحمل `MODULE_BUILD`
  أو وسمَ `?v=` تُختَم تلقائياً بلا تعديل السكربت — فنسيانُ وحدةٍ لم يعد ممكناً.
  **فصلٌ مقصود بين المولِّد والثابت.** `--write` يقرأ git، أمّا **الثابتُ المفحوص** في
  hail-tests وCI فهو «كلُّ الأختام متطابقة · لا `vNEXT` باقية · للإصدار قيدٌ في §6»
  ولا يُقارَن بعدّاد git: فـGitHub Actions تفحص **commit دمجٍ** للـPR، فعدّادُه يختلف
  عمّا كان لدى المطوّر، وربطُ الفحص بالعدّاد يُسقط الـPR بلا خطأٍ حقيقيّ.
  **+ سطرُ الإصدار صار قصيراً.** سجلُّ التغييرات كان يسكن **سطرَ `APP_VERSION` نفسَه**
  (١٤٥٧ حرفاً)، فكلُّ فرعين يتعارضان على سطرٍ واحدٍ ضخم. نُقل السجلُّ إلى تعليقٍ
  تاريخيٍّ مجمَّدٍ فوقه، وبقي السطرُ **٣٠ حرفاً** مولَّداً آلياً — فالتعارضُ إن وقع
  صار سطراً واحداً يحلّه أمرٌ واحد: `npm run stamp`.
  **الاختبارات:** ✅ 1307/1307 (+١٤ حارساً جديداً) وفحصُ الختم `npm run stamp:check` أخضر،
  **الاختبارات:** ✅ 1334/1334 (+١٤ حارساً) وفحصُ الختم أخضر · رحلةُ المتصفّح 61/61 ·
  السيناريوهات 58/58 · العقد 47/47 · مركزُ العمليات 48/48 · التقرير المجمّع 13/13 ·
  طابور الرفع 15/15.

- **v18.9aj — مركزُ العمليات يتكلّم لغةَ كل مشروع: مشاريعُ النظافة تُقاس بمهامّها لا ببلاغاتها:**
  **الجذر.** النظافةُ عملٌ **استباقيٌّ بالكامل** — ٩٥٪ منه جدولٌ يوميٌّ مخطَّط والشكوى
  استثناء — فبطاقةُ مشروع النظافة في المركز كانت تعرض «بلاغات نشطة: صفر»: رقمٌ صحيحٌ
  لا يعني شيئاً، بينما جدولُ اليوم كلُّه خارج الشاشة.
  **ما تغيّر.** بطاقةُ مشروع النظافة ولوحتُه تعرضان **المهامّ**: مستحقّة الآن (الرقم
  الكبير) · متأخّرة · نُفِّذت اليوم · مهام نشطة، والشريطُ **تغطيةُ اليوم** بدل إنجاز
  بلاغات الشهر، وحلقةُ الجاهزية تدور على التغطية، وحالةُ المباني تَعدّ المهامَّ
  المستحقّة، والشريطُ السفلي يمرّر المهامَّ المتأخّرة باسم مشروعها بجوار بلاغات الصيانة.
  **والحسابُ كلُّه من وحدة النظافة نفسها** (`statsForTasks`/`coverageForTasks`/`splitTasks`)
  — لا نسخةَ ثانيةً من قواعد الاستحقاق والتأخّر والإجازة. والقاعدةُ الوحيدة التي كانت
  تسأل عن **المشروع المفتوح** (الإجازة: الجمعة/السبت لا ضغطَ فيهما) تُشغَّل داخل سياقٍ
  **متزامنٍ** يُعلن أن القائمة لمشروع نظافة (`_inCleanCtx`) ويُستعاد في `finally` —
  فالحسابُ واحدٌ في الشاشتين، ويومُ العطلة يُعرَض «إجازة» لا صفراً كاذباً.
  **والنوعُ يُكشف من مصدره الواحد** (`isCleaningProjectRec`: سجلّ المشروع ثم مستند
  الموازنة — نفس مصدرَي الوحدة)، وعندها **يُفكّ مستمعُ البلاغات ويُركَّب مستمعُ المهام**:
  تحوّلٌ واحدٌ في الجلسة لا تناوبٌ متكرّر، فلا تُقرأ بلاغاتٌ لا تُعرَض.
  **والشريطُ العلوي لا يجمع وحدتَي عملٍ في بلاطةٍ واحدة** — رقمٌ بوحدتين لا يعني شيئاً:
  مجموعةُ بلاغاتٍ (نشطة · متأخرة عن SLA · أُغلقت اليوم) ومجموعةُ مهامّ (مستحقّة ·
  متأخّرة · نُفِّذت اليوم)، **كلٌّ تظهر عند وجود نوعها وحده** — فمَن كلُّ مشاريعه صيانةً
  يرى شريطَه كما كان.
  **الأيقونات.** رموزُ الإيموجي داخل المركز (المشاريع · البلاغات · المتأخرة · المنجَز ·
  الزمن · أزرار التدوير وملء الشاشة والرجوع · أيقونة المشروع الاحتياطية) استُبدلت
  بأيقونات `_ICON` نفسِها التي تستعملها المنصّة، بحجمٍ صريحٍ نسبةً للنصّ (`.tvl-i`) —
  فالـSVG بلا أبعادٍ يتمدّد في حاويات flex، والإيموجي يختلف رسمُه بين الأجهزة داخل شاشةٍ
  واحدةٍ معلّقة. **رسائلُ toast مستثناةٌ عمداً**: الإيموجي فيها اصطلاحُ المنصة كلِّها.
  وحُذف عدُّ المشاريع من زرّ بوّابة المشاريع (البطاقاتُ فوقه تعرضها — والعدُّ تكرار).
  **الاختبارات.** `node hail-tests.js` ١٢٩٣/١٢٩٣ (حرّاسٌ جدد: الحسابُ من الوحدة لا نسخةٌ
  ثانية · السياقُ متزامنٌ ويُستعاد · الموزِّعُ بالنوع · فكُّ مستمع البلاغات عند الكشف ·
  الشريطُ لا يخلط الوحدتين · لا إيموجي في المركز)، و`node tvwall-check.mjs` ٤٨/٤٨ في
  متصفّحٍ حقيقي بمشروع نظافةٍ مزروع — **والتوقّعُ فيه يُشتقّ من قاعدة الإجازة نفسها**
  فيثبت في كل يوم (جُرِّب اليومان: عملٍ وعطلة).
- **v18.9ak — الصور المكسورة في التقرير المصوَّر: طابورُ رفعٍ مؤجَّل بدل رابطٍ محلّيٍّ ميّت:**
  **الجذر.** عند فشل رفع صورةٍ إلى Storage — شبكةٌ ضعيفةٌ داخل المباني أو مهلةُ الـ٤٥
  ثانية — كان النظام يحفظ **الرابط المحلّي المؤقّت** بدلاً منها:
  `p.finalizedUrl = p.storageUrl || p.preview`. ورابطُ `blob:` يعيش داخل التبويب الذي
  التقط الصورة **فقط**؛ يُكتب في Firestore كأنه صورةٌ دائمة، فيراه صاحبُه سليماً لحظتها
  ثم يموت عند أول تحديثٍ للصفحة، وعلى كل جهازٍ آخر، وإلى الأبد — فيصل العميلَ تقريرٌ
  مصوَّرٌ فيه مربّعاتٌ مكسورة، **وتضيع الصورةُ الأصلية** لأنها لم تُرفَع قطّ ولم تُحفَظ
  في مكانٍ يُعاد منه.
  **ولماذا بقي صامتاً.** ثلاثةُ عوامل: (١) `safeUrl` يسمح بـ`blob:` فيمرّ الرابطُ الميّت
  كأنه سليم؛ (٢) `doClose` تعدّ الصورة الفاشلة «جاهزة» (`filter(p=>!p.uploading)` —
  والفاشلةُ ليست قيد الرفع) فتحفظها **بلا أي تنبيه**؛ (٣) الإصلاحُ التلقائي
  (`onLateSuccess`) لا يعمل إلا إن بقي التبويبُ مفتوحاً حتى ينجح الرفع — وهو ما لا يحدث.
  ستةُ مواضعَ كانت تفعلها: إنشاءُ بلاغ · إغلاقُه · استكمالُ الرفع في الخلفية · تعديلُ
  صورة البلاغ · تعديلُ صور الإغلاق (`index.html`) · إغلاقٌ واستكمالٌ من `tech-app.html`.
  **الحلّ.** **لا يُحفَظ رابطٌ محلّيٌّ في قاعدة البيانات إطلاقاً.** الصورةُ التي تعذّر
  رفعُها تُخزَّن **بجسمها الكامل** (Blob) في IndexedDB — تخزينٌ يبقى بعد إغلاق التبويب
  وإعادة تشغيل الجهاز — عبر وحدةٍ جديدة `photo-queue.js`، وتُعاد المحاولة تلقائياً عند
  عودة الشبكة (`online`) وعند العودة للتبويب (`visibilitychange`) وكلَّ دقيقةٍ **ما دام
  في الطابور عنصر** (فلا نبضَ دوريٌّ في الحالة العادية)، ثم يُلحَق الرابطُ الدائم بالبلاغ.
  **لماذا `arrayUnion` لا كتابةُ المصفوفة كاملة:** الرفعُ المؤجَّل قد ينجح بعد دقائق وقد
  يكون البلاغ عُدِّل من جهازٍ آخر؛ كتابةُ `photos` كاملةً من نسخةٍ قديمةٍ تمحو صورَ
  الآخرين. `arrayUnion` تُلحق ذرّياً على الوثيقة الطازجة، وهي مُتسانِدة على نفس الرابط
  فإعادةُ المحاولة لا تُكرّر الصورة. والكتابة `set(...,{merge:true})` لا `update` — فوثيقةٌ
  لم تصل بعدُ تُفشل `update`. والترتيبُ مقصود: الرفعُ ← الإلحاقُ ← الحذفُ من الطابور، فلو
  انقطع التيّار بين خطوتين بقي العنصرُ فأُعيدت المحاولة (تكرارُ رفعٍ أهونُ من صورةٍ تضيع).
  **ولا يُسقَط عنصرٌ من الطابور تلقائياً أبداً** — بعد ٢٥ محاولةً تتوقّف المحاولةُ
  التلقائية ويبقى العنصر، والحذفُ يدويٌّ صريحٌ فقط (`drop`).
  والبلاغُ يُغلق فوراً بلا تعطيلِ الفنيّ، مع تنبيهٍ صريحٍ «حُفظت على الجهاز وتُرفَع
  تلقائياً» وعدّادٍ عند الإقلاع — فلا يظنّ أحدٌ أن الصورة وصلت وهي لم تصل.
  **+ مربّعٌ صريحٌ بدل الصورة المكسورة.** `imgBroken` يستبدل أيقونةَ المتصفّح الغامضة
  بمربّع «تعذّر تحميل الصورة» على الشاشة، و**تُطوى الخانةُ كلّها عند الطباعة**
  (`@media print{.photo-dead{display:none}}`) فلا يخرج للعميل تقريرٌ فيه فراغاتٌ مكسورة.
  يُستدعى بصيغة `window.imgBroken&&imgBroken(this)` فلا يرمي في نافذة الطباعة المنبثقة.
  **ملاحظة نطاق:** هذا يمنع تولّد صورٍ مكسورةٍ جديدة، ولا يُحيي روابطَ `blob:` قديمةً
  مخزَّنةً بالفعل — أصلُها لم يُرفَع قطّ فلا سبيل لاستعادته؛ تلك البلاغات تحتاج إعادة
  تصوير. أمّا صورُ **النظافة** فوحدتُها لم تحفظ `blob` قطّ (`photos: map(p=>p.url).filter(Boolean)`)،
  فكسرُها إن وُجد سببُه رابطٌ صحيحٌ لا يُفضي إلى ملف (حذفٌ من Storage · إعادةُ توليد رمز
  التنزيل · قواعدُ Storage المؤجَّلة للمرحلة ٣) — والمربّعُ الصريح يجعله ظاهراً بدل أن يُخمَّن.
  **الاختبارات:** ✅ 1320/1320 على `v18.9ak` (+ فحصُ متصفّحٍ حقيقيٍّ للطابور والمربّع الصريح في `photo-queue-check.mjs` — ١٥/١٥).

- **v18.9ai — مركزُ العمليات: تدويرٌ تلقائيٌّ يعرض لوحةَ كل مشروعٍ كاملةً، وإعادةُ تسميةٍ وعلامة:**
  **الجذر.** شاشةُ البطاقات (v18.9ag) تُجيب «أيُّ مشروعٍ يحتاجني الآن؟» ولا تُجيب «وماذا
  يجري داخله؟» — والناظرُ إلى شاشةٍ معلّقةٍ على الجدار **لا يَنقر**، فبقيت التفاصيل خلف
  نقرةٍ لا تقع.
  **التدوير.** دورةٌ تمرّ على شاشة «الكل» ثم **لوحةَ عملياتٍ كاملةً لكل مشروع** بترتيب
  بطاقاته (الأسوأ أولاً)، كلُّ شاشةٍ ٢٠ ثانية (`TVWALL_ROT_MS`). لوحةُ المشروع بنفس أعمدة
  لوحة العرض TV وبمكوّناتها ذاتها (`.tvw-kpi`/`.tvw-beacon`/`.tvw-blds` — لا نسخةَ ثانيةً
  من التصميم): نبضُ التشغيل (متأخرة SLA · نشطة · اليوم)، وحالةُ التشغيل (حلقةُ **إنجاز
  بلاغات الشهر** + قيد التنفيذ/في الانتظار/أُغلقت اليوم)، وحالةُ المباني.
  **المباني من مستند إعدادات المشروع** (`meta/{id}_settings`) لا من البلاغات، فتظهر
  المباني الخاليةُ من البلاغات كما في اللوحة تماماً — وتعذّرُ القراءة ليس عطلاً: تُشتقّ
  حينها من البلاغات. قراءةٌ واحدةٌ لكل مشروعٍ عند الفتح.
  **الشريطُ العلوي يتبع المعروض** (شريطٌ واحدٌ لا شريطان): مجاميعُ كل المشاريع على شاشة
  «الكل»، ومقاييسُ المدة على لوحة المشروع (اليوم/الأسبوع/الشهر/أُغلقت اليوم/نشطة/متوسط
  زمن الإغلاق). **ولا إجماليَّ تراكميٍّ على لوحة المشروع**: النافذةُ أحدثُ ٤٠٠ بلاغ،
  و«إجمالي البلاغات» منها رقمٌ مقتطَعٌ يُقرأ «هذا كل شيء» — ورقمُ مدةٍ صادقٌ خيرٌ منه.
  **مؤشّرُ التدوير بنيةُ معلومات لا زينة:** النقطةُ الأولى «الكل»، وكلُّ نقطةٍ بعدها
  **بلون صحّة مشروعها** — فالشريطُ ملخّصُ حالةٍ حيٌّ ولو كانت شاشةُ مشروعٍ آخرَ معروضة.
  والنقرُ على نقطةٍ ينتقل إليها ويُعيد ضبط المؤقّت، وزرٌّ صريحٌ يوقف التدوير ويستأنفه
  (شاشةٌ واحدةٌ أو تدويرٌ موقوف ⇒ لا مؤقّت أصلاً). وإعادةُ الترتيب أثناء العرض **تُبقي
  المعروضَ على مشروعه** لا على رقم الشاشة — وإلّا قفزت الشاشة عند كل تبدّل حالة.
  **التسمية والعلامة:** «جدار المشاريع» ← **«مركز العمليات»** — لغةُ المنصّة نفسها
  («مركز عمليات الصيانة» في ترويسة لوحة TV)، بعلامةِ **رادار** (قوسان ونبضةُ مركز) بدل
  أيقونة الشاشة، و🛰️ في القائمة الجانبية. حارسٌ في `hail-tests` يمنع عودةَ الاسم القديم
  إلى نصوص الواجهة.
  **الاختبارات.** `node hail-tests.js` كلها خضراء، و`node tvwall-check.mjs` ٣٥/٣٥ في
  متصفّحٍ حقيقي (التدويرُ ينتقل وحده · لوحةُ المشروع بأرقامها · الشريطُ يتبع المعروض ·
  الإيقافُ والاستئناف)، و`ui-walkthrough` ٦١/٦١ و`browser-scenarios` ٥٨/٥٨
  و`perf-contract-check` ٤٧/٤٧.

- **v18.9ah — التقرير المصوّر لأعمال النظافة صار مجمّعاً: ملفٌّ يُرسَل بدل ملفٍّ يُهجَر:**
  **الجذر.** جدولُ النظافة يوميّ، فالشهرُ الواحد مئاتُ التنفيذات، وكلُّ تنفيذٍ حتى أربع
  صور، والتقريرُ المصوّر كان يرسم بطاقةً لكل تنفيذ. قياسُ تقريرٍ حقيقيٍّ قدّمه المالك:
  **٢١٢ صورةً تشغل ١٧٫٤ من أصل ٢٠٫٤ ميجابايت (٨٥٪ من الملف) في ٤١ صفحةً فقط** — وتقريرُ
  نظافةٍ شهريٌّ كاملٌ يتجاوز المئة ميجابايت فلا يُرسَل عبر بريدٍ ولا يُفتَح على هاتف.
  والعميلُ لا يطلب أربعمئة بطاقةٍ متطابقة، يطلب **إثباتَ الاستمرارية**.
  **الحلّ.** الوضعُ الافتراضي لتقارير النظافة صار **مجمّعاً**: بطاقةٌ واحدةٌ لكل
  (مبنى × نوع عمل) تحمل الفترةَ وعددَ التنفيذات وأيامَ العمل والمشرفَ ونسبةَ بنود الفحص،
  ومعها **ثلاثُ صورٍ موزّعةٌ على الفترة** (أوّلٌ · منتصفٌ · آخِر). الاختيارُ **حتميٌّ
  بالترتيب الزمني** — نفسُ المدخلات ⇒ نفسُ الصور دائماً، فلا «إعادة سحبٍ» حتى تظهر أجملُ
  الصور (نفس مبدأ عيّنة الرقابة المالية في §3-أ)؛ ونأخذ **صورةً واحدةً من كل تنفيذٍ أولاً**
  فيتوزّع الدليلُ على أيامٍ مختلفة، ولا نُكمل من بقيّة صور التنفيذ الواحد إلا إذا لم تكفِ
  التنفيذاتُ العدد. **ولا تُحذَف صورةٌ من النظام إطلاقاً** — التغييرُ في العرض وحده، وكلُّ
  الصور تبقى في سجلّ التنفيذ وفي التخزين، والبطاقةُ تُصرّح «٣ صور معروضة من ٣٠ صورة موثَّقة».
  الأثرُ المقيس: من ٢١٢ صورة/٢٠٫٤ م.ب إلى ~٤٠ صورة/~٣٫٧ م.ب (**−٨٢٪**).
  **التفاصيل.** فلترُ «شكل تقرير النظافة» (مجمّع | مفصّل) يُحقَن لمشاريع النظافة وحدها
  ويعيد السلوكَ القديم عند الحاجة، و**غيابُ العنصر يعني «مجمّع»** — فالافتراضُ الآمن هو
  الأصغر. البطاقةُ المجمّعة ترسمها وحدةُ النظافة (`_aggCardHTML`) وتستدعيها النواة من
  `cardHtml` عند `t._agg`، وتُبنى من `t.photos` الحيّة فيسري حذفُ صورةٍ عليها فوراً بلا
  حالةٍ مخبّأة، ومع سقوطٍ آمنٍ للبطاقة العادية إن غابت الوحدة. والعدُّ في الغلاف والملخّص
  صار **بالتنفيذات لا بالبطاقات** (`_prUnits` تقرأ `_aggRuns`) وإلّا قرأ العميلُ «٤٠ بلاغاً»
  بينما نُفِّذ ٤٠٠ عمل؛ وأيامُ العمل **اتّحادُ** أيام البطاقات لا مجموعُها (الأيام تتداخل بين
  المباني)؛ ولوحةُ الأولوية — وكلُّ تنفيذاتِ النظافة روتينيةٌ فعمودٌ واحدٌ بلا معنى — تُستبدَل
  بتوزيع التنفيذات على المباني.
  **+ عدّاد الحجم قبل الطباعة:** شريطٌ `no-print` فوق التقرير يعرض عددَ الصور والحجمَ
  المتوقَّع (٨٥ ك.ب للصورة قياساً + ٤٠٠ ك.ب للنصّ والرسوم) ويُحذّر فوق ٨٠ صورة (~٧ م.ب — ما فوقه يُتعب البريد والهاتف) — فلا
  يفاجئ الحجمُ أحداً بعد الطباعة. يعمل لكل التقارير المصوّرة لا النظافة وحدها.
  **قرارٌ مُرجَأ:** تصغيرُ الصور لحظة الطباعة (640px عبر canvas، −٧٠٪ إضافية وبأثرٍ رجعي)
  يتطلّب ضبطَ CORS على bucket التخزين، وقرَّر المالك تأجيله. البديلُ بلا CORS: رفعُ نسخةٍ
  مصغّرةٍ وقت التصوير (للصور الجديدة فقط).
  **الاختبارات:** ✅ 1247/1247 على `v18.9ah` (+ رحلة المتصفّح 61/61، والسيناريوهات المنطقية 58/58، والعقد القائم على الأداء 47/47، وجدار المشاريع 22/22، وفحصُ تشغيلٍ حقيقيٍّ للتقرير المجمّع 13/13).

- **v18.9ag — جدارُ المشاريع: كل لوحات TV في شاشةٍ واحدة (متابعةُ كل المشاريع من مكان واحد):**
  **الجذر.** المتابعةُ كانت تفرض دخولَ كل مشروعٍ على حدة وانتظارَ تحميله ثم الخروجَ
  والتبديل — فلا توجد لحظةٌ واحدةٌ تُرى فيها حالةُ كل المشاريع معاً، ومَن يملك مشاريعَ
  عدّة يكتشف الحرِجَ منها متأخراً.
  **ما أُضيف.** شاشةٌ عليا مستقلّة (`#tvwall-screen`) تُفتح من **بوّابة المشاريع بعد
  الدخول** ومن زرٍّ في القائمة الجانبية داخل المشروع: لكل مشروعٍ بطاقةُ لوحةٍ مصغّرة —
  البلاغاتُ النشطة (الرقمُ الكبير)، والمتأخرةُ عن SLA، وقيدُ التنفيذ، وبلاغاتُ اليوم،
  وما أُغلق اليوم، وشريطُ **إنجاز بلاغات الشهر** (المغلق من بلاغات الشهر — نسبةٌ محصورةٌ
  ٠–١٠٠ على نافذةٍ مضمونةٍ داخل الحد، لا نسبةً تراكميةً تعتمد على تاريخٍ خارج النافذة)،
  وتذييلٌ بآخر إغلاقٍ وأقدمِ بلاغٍ متأخر. البطاقاتُ مرتّبةٌ **الأسوأ أولاً** (المتعذّرُ
  قراءتُه ← الحرِج ← المتابعة ← المستقر)، وفوقها شريطُ مجاميعِ كل المشاريع، وتحتها شريطٌ
  متحرّكٌ يجمع البلاغاتِ التي تحتاج إجراءً عبر المشاريع كلها **باسم مشروعها**. والنقرُ
  على بطاقةٍ يفتح مشروعها.
  **ثلاثةُ قراراتٍ تحكم الشاشة:**
  **(١) عزلٌ تام.** لا تلمس `CURRENT_PROJECT` ولا `tickets` ولا أيَّ متغيّرٍ للمشروع
  المفتوح — لها مخزنُها الخاص لكل مشروع (`_tvwall.data[pid]`). خلطُ بيانات مشروعين أخطرُ
  عطلٍ في هذه المنصّة (درس `applyProjectConfig`/`switchProject`)، ولذلك يمرّ فتحُ مشروعٍ
  من بطاقته — إن كان مشروعٌ آخر مفتوحاً — بمسار `switchProject` الرسمي: الدخولُ المباشر
  يترك مستمعي المشروع السابق حيّين على مجموعاته.
  **(٢) مستمعٌ لكل مشروعٍ يُركَّب مرةً واحدةً في الجلسة ويبقى حيّاً** حتى تسجيل الخروج
  (`_tvwallUnsubAll` في `logoutToLogin` هو موضعُ الفكّ الوحيد). التركيبُ والفكُّ عند كل
  فتحٍ/إغلاقٍ يراكم targetId ويُطلق خلل Firestore الداخلي (درس v18.9sz)، والاستقصاءُ
  الدوري بـ `get()` كل دقيقة قراءاتٌ مدفوعةٌ بلا داعٍ. **الرسمُ وحده يتوقّف عند الإغلاق،
  لا المستمع** — وإعادةُ حسابٍ كل ٣٠ ثانية لأنّ SLA يتغيّر بمرور الوقت بلا حدثِ بيانات.
  **(٣) لا تصنيفَ محلياً.** المتأخّرُ من `isOverdue`، وصحّةُ اللوحة من `tvHealth`
  المستخرَجة من `renderTVDashboard` (كانت مكتوبةً حرفياً بداخلها)، ومشاريعُ المستخدم من
  `_visibleProjectsFor` المستخرَجة من `renderProjectGrid` — فلا يختلف رقمُ الجدار عن رقم
  لوحة المشروع، ولا يعرض الجدارُ مشروعاً لا يظهر في البوّابة.
  **النافذةُ مُعلَنة:** أحدثُ ٤٠٠ بلاغٍ لكل مشروع (`TVWALL_SYNC_LIMIT`)؛ فإن امتلأت
  تُعرَض ملاحظةٌ على البطاقة — لا اقتطاعَ صامتاً يُقرأ «هذا كل شيء». وفشلُ قراءة مشروعٍ
  يظهر بطاقةَ خطأٍ صريحة لا فراغاً. **الصلاحية:** المراقبُ وأدوارُ المشتريات المركزية
  محجوبون (نفس قيد `#page-tv`).
  **الاختبارات.** `node hail-tests.js` — ١٢٢٤/١٢٢٤ خضراء، وفيها حرّاسٌ جدد: `tvHealth`
  مصدرٌ واحد ولا عتباتٍ محليةً في `renderTVDashboard`، و`_visibleProjectsFor` مصدرٌ واحد
  تقرؤه البوّابة والجدار، والجدارُ لا يُسنِد `CURRENT_PROJECT` ولا يقرأ `tickets`، ولا
  يُعيد تركيب مستمعٍ قائم، والإغلاقُ يوقف الرسم لا المستمع، وفكُّ المستمعين عند الخروج،
  وحسابُ البطاقة يُنفَّذ فعلاً (النشط/المتأخر/اليوم/نسبة الشهر ٠–١٠٠ و`null` عند غياب
  البيانات لا صفراً ملفَّقاً). ويُضاف فحصُ متصفّحٍ دائم **`tvwall-check.mjs`** (٢٢/٢٢)
  يزرع ثلاثة مشاريع بحالاتٍ مختلفة ويتحقّق من الأرقام والترتيب وبقاءِ المستمعين وفتحِ
  مشروعٍ من بطاقته وأنّ الجدار لم يلمس المشروع المفتوح. و`ui-walkthrough`/
  `browser-scenarios`/`perf-contract-check` خضراء بلا تغيير.
- **v18.9af — المرحلة ٢ للعقد القائم على الأداء: الحقول التي تُغذّي المؤشرات، ووضعٌ تجريبيٌّ قبل الاستلام:**
  **الجذر.** الاستلام بعد شهرين إلى ثلاثة، ونافذةُ التدريب ضيّقة — فالمطلوب أن تُلتقط
  البياناتُ وتتشكّل العادةُ **قبل** أن يصير المال على المحكّ.
  **(١) الطوابع الزمنية.** العقد يقيس **ثلاثة أزمنةٍ منفصلة** والمنصةُ كانت تقيس واحداً:
  `respondedAt` (وصولُ الفني — يُكتب في **تطبيق الفنيين** عند «بدء التنفيذ» حيث يقع الحدث
  فعلاً، ومن زرٍّ في تفاصيل البلاغ للمشرف)، و`restoredAt` (عودةُ الخدمة — قد تسبق الإغلاق
  الإداري بأيام)، و`clockStops[]` (إيقافُ الساعة بسببٍ موثَّقٍ **لحظةَ حدوثه**: موافقةُ الجهة ·
  قطعةُ غيارٍ من مورّد · موقعٌ مغلق · انقطاعُ تيارٍ عام · عملٌ داخل الضمان — وبلا توثيقٍ
  تُحتسب كلُّها ضدّنا، وهو أرخصُ إصلاحٍ وأكبرُ بابِ تسرّبٍ للدرجات).
  **ولا يصحّ اشتقاقُ الاستجابة من `assignedAt`**: الإسنادُ فعلٌ مكتبيّ والاستجابةُ وصولٌ
  ميدانيّ، والخلطُ يُنتج رقماً يُحتجّ به وهو غير صحيح. **ولا تُصطنَع قيمةٌ عند الغياب**:
  البلاغُ بلا طابعٍ يبقى **غير مقيس**، وتُعرَض نسبةُ التغطية بدل تلفيقِ رقمٍ يُجمّل المتوسّط.
  **(٢) جدولةُ الوقائي — عطلٌ صامتٌ كان يُفسد أثقل مؤشرٍ في البطاقة (٣٠٪).** ثلاثةُ مواضعَ
  تُقدّم الخطة بثلاث طرائق: التوليدُ التلقائي **من المرساة** (صحيح)، والاعتمادُ اليدوي
  والإغلاقُ **من اليوم** — فيُعاد ضبطُ البرنامج المعتمد على تاريخ التنفيذ في كل دورة (خطةٌ
  شهريةٌ نُفِّذت يوم ٢٠ تنتقل لدورة الـ٢٠ أبداً)، والإغلاقُ يُقدّمها **مرةً ثانية** بعد تقديمها
  عند التوليد، بينما الإغلاقُ من تطبيق الفنيين **لا يفعل** — فيختلف الجدولُ باختلاف مكان
  الإغلاق. الآن `ppmNextDue` **مصدرٌ واحد** يحفظ المرساة ويلحق الدوراتِ الفائتة بحارسٍ صلب
  (وبلا مُعدِّلات Date إطلاقاً — درس v18.9vt)، والتقديمُ **عند التوليد وحده**، والإغلاقُ
  يسجّل `lastCompletedAt` ولا يلمس الجدول. والبلاغُ الوقائي صار يحمل `scheduledFor` =
  استحقاقَه المخطَّط **مُلتقَطاً قبل التقديم** — وبدونه لا جواب لسؤال «هل نُفِّذ في موعده؟».
  والامتثالُ يُحسب من **البلاغات** (`ppmId` + `scheduledFor` + `closedAt`) لا من حقلٍ على
  الخطة: مصدرٌ واحد، ويعمل أياً كان مكان الإغلاق.
  **(٣) الوضع التجريبي** (`perfTrial`): عَلَمٌ ثانٍ يشغّل العقدَ على مشروعٍ قائمٍ **بلا
  غرامات** — شريطٌ صريحٌ أعلى القسم وكلُّ مبلغٍ موسومٌ «تدريبيّ»، فلا يُظنّ رقمٌ تدريبيٌّ
  التزاماً. **(٤) بطاقةُ تغطية البيانات**: أولُ شهرٍ لا معنى فيه لـ«درجة» — الحقولُ لم تكن
  تُملأ ومؤشرٌ بلا بياناتٍ يُقرأ صفراً، **لا لتقصير الفريق**. فتبدأ التجربة بسؤال «كم مما
  نحتاجه صرنا نلتقطه؟» وهو المؤشر الحقيقي للجاهزية؛ ومقامٌ صفرٌ يُعرَض «—» لا صفراً كاذباً.
  الأزرارُ كلُّها مبوَّبةٌ بـ`isPerfProject` — **والمشاريعُ التقليدية لا يتغيّر لها شيء**،
  وهو نفس عهد المرحلة ١ ويحرسه اختبار.
  **بندٌ مفتوحٌ كشفه فحصُ المتصفّح — يحتاج قرار المالك:** `PPM_FREQ_DAYS` يجعل «شهري»
  = **٣٠ يوماً** لا شهراً تقويمياً. فخطةٌ استحقاقُها ١ يناير تسير: ٣١ يناير ← ٢ مارس ←
  ١ أبريل… **فتتخطّى فبراير كاملاً**، وتُنتج ١٢٫١٧ دورةً في السنة. وهذا مقبولٌ في العقد
  التقليدي، لكن بطاقةَ الأداء تقيس الامتثال **شهرياً بالتقويم** — فشهرٌ بلا استحقاقٍ قد
  يُقرأ إخلالاً. **لم تُغيَّر الدلالة هنا عمداً**: تغييرُها يمسّ كلَّ الخطط القائمة في كل
  المشاريع، وهو قرارٌ تشغيليٌّ لا تقنيّ. الخيارات: إبقاؤها ٣٠ يوماً · أو «شهري» = نفسُ
  اليوم من الشهر التالي · أو تكرارٌ تقويميٌّ جديدٌ بجانب القائم.
  **الاختبارات.** ١١٧٧/١١٧٧ ✅ (٢٢ حارساً جديداً: المرساةُ لا اليوم · لحاقُ الدورات بحارس ·
  بلا مُعدِّلات Date · لا تقديمَ يدويٌّ باقٍ · الإغلاقُ لا يلمس الجدول · `scheduledFor` **قبل**
  التقديم في المسارين · الطابعُ يُكتب مرةً واحدة · لا اشتقاقَ من `assignedAt` · البوّابة ·
  التجربةُ بمقارنةٍ صارمة · التغطيةُ شهريةٌ وتستبعد عمليات التوريد · لا درجةَ كاذبة).

- **v18.9ae — المشروع القائم على الأداء: نوعٌ جديدٌ من المشاريع، لا منصةٌ ثانية (المرحلة ١):**
  **الجذر.** العقد الجديد (أمانة حائل — المرحلة الثانية) يُحاسَب شهرياً ببطاقة أداءٍ متوازن:
  درجةٌ من ١٠٠ على خمس مجموعاتٍ موزونة، والنزول عن الحد الأدنى يخصم من المستخلص، وثلاث
  مراتٍ متتاليةٍ تحته تُتيح الفسخ. ومشاريعنا القائمة **كلها تقليدية** — فلا يصحّ أن يرث
  مشروعٌ تقليديٌّ شاشاتِ عقدٍ لا ينتمي إليه، ولا أن تُبنى منصةٌ موازية بجانب القائمة.
  **المعمار.** عَلَمٌ **واحد** على سجلّ المشروع (`perfContract`) ومعه إعداداته (الحد الأدنى
  للسنة الأولى · المبلغ الشهري المقطوع · تاريخ بدء الاحتساب)، تقرؤه دوالُّ نواةٍ واحدة —
  `perfConfig` / `isPerfProject` / `perfContractYear` / `perfContractMonth` / `perfMinScore` /
  `perfInGrace` — على نمط `poIsClosed`، فلا تعريفَ محلّياً في شاشةٍ يتباعد عن غيره.
  التحقّق `perfContract===true` **صارمٌ عمداً**: قيمةٌ نصّيةٌ من Firestore (`"false"`) لا يجوز
  أن تُشعل عقداً. والعَلَم **متعامدٌ على `type`** لا نوعٌ رابع — مشروع صيانةٍ قد يكون بعقد
  أداء، بخلاف سابقة `type==="cleaning"` (تصنيفٌ حصريّ). الحقول مولَّدةٌ بدالةٍ واحدة
  (`perfFieldsHTML`) وتُقرأ بقارئٍ واحد (`_readPerfFields`) للنافذتين، والنسبة تُخزَّن
  **كسراً (0–1)** لا مئويةً — وحدةُ قياسٍ واحدة. وإطفاءُ العَلَم يُبقي الإعدادات المخزَّنة
  (فإعادةُ تفعيلٍ تستعيدها) وهو آمنٌ لأن `perfConfig` تبوّابها بالعَلَم وحده.
  **الوحدة.** `performance-contract.js` تركّب صفحتها وزرَّها ذاتياً وتلفّ `showPage` كوحدة
  النظافة: بطاقةُ العقد (المبلغ · سنة العقد · الحد الأدنى المحسوب بارتفاع ٥٪ سنوياً · شهر
  العقد ومهلة الشهرين)، وهيكلُ البطاقة (المجموعات الخمس بأوزان الاستشاري ومستهدفاتها،
  والنتيجةُ السنوية **تُحسب** من الأوزان لا تُكتب فلا تنحرف عنها — ٧٨٫٥٪ للسنة الأولى)،
  ومقياسُ الغرامة بالريال. **ولا تُعرض درجةٌ محسوبةٌ عمداً** (حارسُ اختبارٍ يمنعها): حسابها
  يحتاج حقولاً لم تُبنَ بعد (زمن الاستجابة · تاريخ استحقاق الوقائي · سجلّ المخالفات)،
  **ورقمٌ كاذبٌ في عقدٍ يُحاسَب عليه المال أسوأ من لا رقم**. المشاريع التقليدية: صفر تغيير
  (لا زرّ ولا صفحة ولا حقل)، ويحرسه اختبارٌ على بوّابة `perfConfig`.
  **عطلٌ كشفه فحصُ المتصفّح (وحرسه اختبار).** الزرّ لم يظهر إطلاقاً في مشروع الأداء:
  `nav.insertBefore(btn, anchor.nextSibling)` — وأزرارُ القائمة أكثرُها **ليست أبناءً
  مباشرين** لـ`.sidebar-nav` بل داخل مجموعات `#grp-*`، فيرمي `NotFoundError` ويُسقط
  الحقنَ كلَّه **صامتاً**. الآن الإدراج نسبةً إلى **والد المرساة** داخل مجموعة «الأداء»
  بجوار «مؤشرات الأداء» (يرث `sidebar-child` وطيَّ المجموعة، وارتفاعُها بـ`scrollHeight`
  يتّسع تلقائياً)، محاطاً بحارسٍ يسقط إلى `appendChild`.
  **الاختبارات.** ١١٥٥/١١٥٥ ✅ (٢٦ حارساً جديداً: البوّابة الصامتة · المقارنة الصارمة ·
  ارتفاع الحد الأدنى بسقف ١ · احترام يوم الذكرى في السنة والشهر · المولِّد/القارئ الواحد ·
  إزالةُ الزرّ لا إخفاؤه · تحويلُ الفتح المباشر · مغادرةُ الصفحة عند تبديل المشروع ·
  منعُ الدرجة الكاذبة · مجموعُ الأوزان ١ · النتيجةُ السنوية ٧٨٫٥٪ · البصمة والتسجيل).
  ويُضاف فحصُ متصفّحٍ دائم **`perf-contract-check.mjs`** (٣٣/٣٣) يثبت العهد بمشروعين
  حقيقيين: التقليديُّ بلا زرٍّ ولا صفحة وفتحُها قسراً يُحوَّل للوحة، والقائمُ على الأداء
  يعرض القسم بأرقامٍ صحيحة. مع `browser-scenarios` ٥٨/٥٨ و`ui-walkthrough` ٦١/٦١.
  **المراجع:** `docs/performance-project-type-plan.md` (الخطة) و`docs/consultant-kpi-mapping.md`
  (البطاقة الفعلية). المرحلة ٢ القادمة: الطوابع الزمنية وتاريخ استحقاق الوقائي.
- **v18.9ad — إغلاقُ كل بندٍ مفتوحٍ في الفحص العميق (H7 M6 M8 M14 M21 M22 M23 M24):**
  بعد وسم التقرير في v18.9ac بقيت ثمانيةُ بنودٍ `🔴 مفتوح`. أُغلقت كلها، فلم يبقَ إلا
  المؤجَّل بقرارٍ واعٍ (المرحلة ٣، وقرارا المنتج H9/M1، وسباقُ M3).
  **(H7) الجردُ كان يُبوّب بلقطةٍ ويُسوّي بالحيّ.** `_classify`/`_summarize` تقيسان مقابل
  `s.systemQty` — لقطةُ الرصيد لحظةَ **إنشاء** الجرد — بينما `_apply` يُسوّي مقابل الرصيد
  **الطازج داخل المعاملة**. فالبوّابةُ التي ترفع «الفروقات الكبيرة» للمدير التنفيذي كانت
  تقيس فرقاً **غير الذي يُطبَّق**: لقطة 100، يُصرف 50 أثناء العدّ (الحيّ 50)، العدّ 98 ⇒
  التصنيف `−2` (صغير، يمرّ بلا اعتماد) بينما التسوية الفعلية **`+48`** — والتقرير المخزَّن
  يقول `−2` أيضاً. الآن `_baseQty(s)` **مصدرٌ واحد** للأساس في التصنيف والبوّابة والشاشة
  والطباعة والتقرير، و`systemQty` يبقى موثَّقاً بجانب `baseQty` فيُقرأ الفرقُ بين اللقطة
  والواقع. (الحارس ينفّذ السيناريو نفسه: 48 مقابل −2، و`big` يقلب من false إلى true.)
  **(M6) فقدانُ تحديثٍ صامتٌ في مستند الموازنة.** الكتابات **الأربع** (`saveBudget`
  /`saveBoq`/`saveCleaning`/`saveType`) كانت `set(…,{merge:true})` بقيمٍ مشتقّةٍ من
  `_budgetCache` — يُحمَّل مرّةً ولا يُبطَل — و`merge` لا يدمج **داخل** المصفوفة بل
  يستبدلها. فأدمنٌ بكاشٍ صباحيّ يكتب `finishes:0` فوق تعديلِ زميلٍ للتوّ بلا أي إنذار.
  الآن `_budgetTx` يقرأ المستند **داخل المعاملة** ويمرّر الطازجَ للحاسب. (`saveType`
  كانت رابعةً غابت عن حسباني — **كشفها الحارس** حين طلب «لا `set` مباشرة».)
  **(M8) الاستعاضة تُحتسب مرتين.** الطلبُ `isSubstitute` يدخل موازنة المشروع **ورصيد
  الاستعاضة معاً**، فيظهر «تجاوزُ موازنة» على بنودٍ ليست من العقد. لم نُسقطه من المجموع —
  ذلك قرارٌ محاسبيٌّ ليس لنا ويغيّر أرقامَ كل مشروع — بل **فُصل وأُعلن** في البطاقة:
  المجموع كما هو، وبجانبه كم منه مموَّلٌ بالاستعاضة.
  **(M14) إشعارُ إسنادٍ يسقط بصمت.** مفتاحُ منع التكرار كان
  `sha1(type|entityId|transition|recipient)` — يخلط بين **إعادةِ إطلاقٍ لنفس الكتابة**
  (at-least-once، يجب أن تُدمج) و**حدثٍ جديدٍ بنفس الانتقال** (أحمد→محمد→أحمد، يجب أن
  يُرسَل). أُضيف `occurrence` = طابعُ الكتابة نفسها: ثابتٌ عبر إعادة الإطلاق، مختلفٌ لكل
  إسناد. وكان يلزم مصدرٌ له — فصار العميل يكتب `assignedAt` عند الإسناد وعند الإنشاء
  بفني. وغيابُه يُبقي السلوك القديم حرفياً (توافقٌ رجعي محروس).
  **(M21) تقريرٌ شهريٌّ بأرقام يوم.** قسمُ التغطية كان `coverageByBuilding()` — **لقطةُ
  اليوم** — تحت تقريرٍ معنون «الفترة: <شهر>»، فتصديرُه يوم جمعةٍ يُظهر «لا مهامَّ لليوم»
  وكأن المشروع بلا عمل طوال الشهر. صارت الصفوف من `_monthLog` (تنفيذاتُ الشهر وأيامُ
  العمل لكل مبنى)، ولقطةُ اليوم تبقى معروضةً لكن **موسومةً بيومها**.
  **(M22) مقارنةٌ بأساسين.** التقديريُّ من محرّك التسعير **صافٍ قبل الضريبة** وأسعارُ
  الموردين كما وردت في عروضهم — فيبدو التقديري أرخص ~15% دائماً ويُضلِّل قرار «الأفضل».
  لا نُحوّل رقماً (أساسُ كل عرضٍ قد يختلف) بل **نُسمّي الأساس** في رأس العمود وفي تذييل
  الجدول.
  **(M23) صفرٌ صامت.** صنفُ كتالوجٍ بلا سعر كان يُدرَج بصفرٍ بلا كلمة، فيغذّي التحليلَ
  سعرَ وحدةٍ مبخوساً. الصفر يبقى مسموحاً (قد يكون مقصوداً) لكنه **يُعلَن** لحظةَ الإدراج.
  **(M24) الأخيرُ يدهس الأول.** الكتالوجان مجموعةٌ **مشتركة**، ومحرّران على البند نفسه:
  `update(data)` بلا فحص. الآن تُلتقط بصمةُ `updatedAt` لحظةَ فتح المحرّر وتُتحقَّق داخل
  معاملة؛ التعارضُ **يُرفض ويُشرَح** («عدّل زميلٌ هذا البند…») لا «خطأ في الاتصال».
  **الاختبارات.** `node hail-tests.js` → 1129/1129 · `ui-walkthrough` 61/61 ·
  `browser-scenarios` 58/58.

- **v18.9ac — دفعةٌ ثانية من الفحص العميق، وتصحيحُ التقرير نفسه:**
  **الجذر الأول: وثيقةٌ تكذب.** طُلب تنفيذُ «الدفعة القابلة للتنفيذ الآن» (H4/H5/H6/C3/C4)،
  فتبيّن عند قراءة الكود أنها **مُصلَحةٌ كلُّها منذ v18.9vu** — لكن
  `docs/deep-review-2026-08.md` ما زال يعرضها مفتوحةً وترويسته تقول «لم تُطبَّق إصلاحات».
  وثيقةٌ في هذه الحالة أسوأ من لا وثيقة: تُنفق ساعةً في إعادة إصلاح المُصلَح، وتُخفي ما بقي
  فعلاً. الآن **كل بندٍ من ٤١ موسومٌ بحالته**: `✅ مُصلَح vX` / `⏳ مؤجَّل` (بسببه) /
  `🔴 مفتوح` / `⚪ إنذار كاذب`، والترويسة تُعلن أن الوثيقة **حيّة لا لقطة**.
  **ويحرسها فحص** يسقط إن بقي بندٌ بلا وسم أو عادت الترويسة القديمة.
  **إنذارٌ كاذب يستحق درساً (M19).** «`toggleRoundBuilding` تحذف تقييمات مبنًى آخر» — الكود
  سليمٌ منذ v18.9vh: مفتاحُ الخليّة يفصل بـ`U+0001`، محرفُ تحكّمٍ **غير مرئي** سقط من نصّ
  التقرير في وصف العطل **وفي علاجه معاً**، فبدا السطران متطابقين والمطابقةُ بالاسم وحده.
  **الدرس: لا تصف محارف التحكّم بنسخها — سمِّها (`U+0001`).**
  **البنود التي بقيت مفتوحةً فعلاً وأُصلحت هنا:**
  **(M7)** `saveBoq` كان يتجاهل نتيجة `saveBudget` (وهي تبتلع خطأها وترجع `false`) — فتُعرَض
  «✅ حُفظت المقايسة» بينما الموازنة لم تُحدَّث، فيخالف تبويبُ المقايسة النظرةَ العامة بلا أن
  يعلم أحد. الآن يُفحَص الإرجاع ويُعلَن الفشل الجزئي صراحةً.
  **(M9)** `poForProject` كان يطابق `p.projectId===projId` بصرامة بينما النواة تعتبر الفارغ
  = `"hail"` (`_poProjKey`/`_poProjMatch`) — فطلبٌ قديم بلا `projectId` يُحسب في اللوحة
  والمؤشّرات تحت hail **ويختفي من بطاقة مشروعه**: رقمان متناقضان لنفس المشروع. وُحِّد
  الاصطلاح.
  **(M20)** `doneToday` كانت تقارن أولَ عشرة أحرفٍ من `lastExecuted` (طابعُ `toISOString`
  — **UTC**) بـ`_today()` (تاريخٌ **محلّي**). في +٣ يُخزَّن عملُ الساعة ١:٠٠ صباحاً بتاريخ
  **اليوم السابق**، فتظهر المهمة «غير منفَّذة» وتُحسَب نسبةُ التغطية خطأً كلَّ يومٍ بين
  ٠٠:٠٠ و٠٢:٥٩. العلاج في الطرفين: التنفيذ يكتب `lastExecutedDate` محلّياً، و`execDay(t)`
  تُحوّل الطابعَ القديم إلى تاريخٍ محلّي **بدل اقتطاعه** — فتنتفع السجلاتُ القائمة بلا
  ترحيل، وشاشتا «آخر تنفيذ» تعرضان اليوم الصحيح. الحارس يحقن مُنسِّقاً يحاكي +٣ فيُثبت
  الفرق (٠٩ محلّياً مقابل ٠٨ في الاقتطاع القديم).
  **خانة «ملاحظات / مواصفات البند» في «طلب شراء جديد».** v18.9ab أظهر ما يُنقَل من طلب
  التسعير، لكن الحقل نفسه لم يكن موجوداً في نموذج الشراء المباشر — فمن ينشئ طلباً مباشرةً
  لا يستطيع كتابة مقاسٍ أو درجةِ خامةٍ أو شرطِ توريد. يُخزَّن في `notes` **نفسه** فيقرأه
  `poItemNote` في التفاصيل ونسختَي الطباعة بلا مسارٍ ثانٍ، ويظهر في جدول بنود النموذج قبل
  الإرسال، ويُمسح مع بقية حقول البند وعند تفريغ النموذج.
  **ما بقي مفتوحاً** (موسومٌ في التقرير): H7، M6، M8، M14، M21، M22، M23، M24 — إضافةً إلى
  المرحلة ٣ (قواعد Firestore ومصادقة الفنيين) وقرارَي المنتج H9/M1 وسباقِ M3.
  **الاختبارات.** `node hail-tests.js` → 1088/1088 · `ui-walkthrough` 61/61 ·
  `browser-scenarios` 58/58.

- **v18.9ab — الملاحظات تُكتَب ولا تُقرَأ: ملاحظةُ البند وملاحظةُ طلب التسعير:**
  **البلاغ.** «إذا فيه ملاحظة في طلب الشراء في خانة الملاحظات لا تظهر في تفاصيل طلب
  الشراء.» الفحص في المتصفّح فرّق بين ملاحظتين مختلفتين تماماً — الأولى تظهر، والثانيتان
  لا:
  **(١) `items[].notes` — ملاحظةُ البند.** حقل «ملاحظات / مواصفات» لكل بند موجودٌ في
  نموذج **طلب التسعير** (`nrfq-item-notes-<i>`)، ويُحفَظ في `rf.items[].notes`،
  و`_rfqToPO` ينقله بأمانة إلى بنود طلب الشراء — ثم **لا يعرضه أحد**: لا جدولُ البنود في
  `openPurchaseDetail`، ولا `printPurchaseDetail`، ولا `printPurchaseDetailNoPrices`
  (وهي نسخةُ المورد والمستودع، وأوجبُهنّ بالملاحظة). فالمقاسُ ودرجةُ الخامة وشرطُ
  التوريد يضيع بين مَن طلب ومَن يشتري ومَن يستلم. دالةٌ واحدة `poItemNote(it)` تخدم
  المواضع الثلاثة فترسم الملاحظة سطراً تحت اسم المادة — مصدرٌ واحدٌ فلا ينحرف موضعٌ عن
  آخر كما انحرفت شارةُ الاستبدال قبلها (v18.9rt).
  **(٢) `rf.notes` — ملاحظةُ طلب التسعير، كانت تُدهَس.** `_rfqToPO` يكتب
  `notes: "تحويل من طلب تسعير … — عرض: …"` **فوق** ملاحظات طلب التسعير كلياً، فالتفاصيلُ
  الفنية التي كتبها الطالب لحظة الإنشاء لا تصل طلبَ الشراء أصلاً — يراها في طلب التسعير
  ولا يجدها في الطلب الناتج، وهذا بعينه ما وصفه البلاغ. الآن `notes` تُركَّب: ملاحظتُه
  أولاً وسطرُ المصدر أسفلها، وطلبُ تسعيرٍ بلا ملاحظات يبقى بسطر المصدر وحده بلا سطرٍ فارغ.
  **الأسطر المتعددة.** بعد التركيب صارت الملاحظة سطرين، و`.d-desc` وكتلتا الطباعة كانت
  تطويهما في سطرٍ واحد — أُضيف `white-space:pre-line` للثلاثة.
  **ما لم يتغيّر عمداً.** ملاحظاتُ الطلب (`p.notes`) كانت تُعرض أصلاً وما زالت (حارسُ
  تراجع)، ونموذجُ «طلب شراء جديد» لا خانةَ ملاحظاتٍ فيه للبند — الحقل مِن طلب التسعير
  وحده، والإصلاح في العرض لا في إضافة حقلٍ لم يُطلب.
  **الاختبارات.** قسم «ملاحظاتُ الطلب والبند تظهر في التفاصيل والطباعة» في
  `hail-tests.js` ينفّذ `poItemNote` وتعبيرَ تركيب `notes` **المستخرَج من المصدر نفسه**،
  ويثبّت الاستدعاءات الثلاثة و`pre-line` في المواضع الثلاثة. `node hail-tests.js` →
  1059/1059 · `ui-walkthrough` 61/61 · `browser-scenarios` 58/58.

- **v18.9aa — الاستلامُ الجزئي يعود لتنفيذ المشتريات: حارسٌ للقاعدة، وسجلٌّ يقرأه إنسان:**
  **المراجعة.** سؤالٌ عن الطلب `PO-202608-0123`: هل يعود الطلب تلقائياً «قيد تنفيذ
  المشتريات» حين يُستلَم جزئياً وتبقى بنود؟ قراءةُ سجل أحداثه تُثبت أن القاعدة **تعمل**:
  الطلبُ أصلاً ثلاثةُ بنود، ووصل بدلها بندان إضافيان (24 تيوب + نقل مواد) لم يكونا فيه.
  عند تدقيق ٠٣/٠٨ ١١:٤٤ رصد `_poOutstanding` البنودَ الثلاثة بصفرٍ مستلم، فوضع
  `doWarehouseAudit` الطلبَ في `proc_executing` وقيّد «بنود لم تكتمل بعد: …» وأشعر
  المشتريات بإكمال التوريد. والحالةُ الظاهرة بعدها «بانتظار استلام المستودع» جاءت من
  إجراءٍ لاحقٍ لمسؤول المشتريات (١٣:٠٠ «تم الشراء — أُشعِر المستودع» مع رفع الفاتورة)،
  لا من خللٍ في العودة. واعتمادُ البندين الإضافيين ١٥:٠١ لم يُقفل الطلب: `_poSettleExtras`
  لا تعمل إلا على `pending_extra`.
  **الثغرة الحقيقية.** القاعدة كانت **بلا أي حارسٍ تنفيذي** في `hail-tests.js` رغم أنها
  مطبَّقةٌ في موضعين مستقلّين يجب أن يبقيا متّسقين: `doWarehouseAudit` بعد كل تدقيق،
  و`_poSettleExtras` بعد البتّ في آخر بندٍ إضافي — والثاني هو الذي **يُقفل** الطلب حين لا
  نقص، فانحرافُه يُقفل طلباً ناقصاً بلا صوت. أُضيف قسم «استلامٌ جزئي ⇒ العودة لتنفيذ
  المشتريات» يستخرج `_poItemKey`/`_waPrevRcv`/`_poOutstanding`/`_poSettleExtras` من
  `index.html` ويُنفّذها على نموذج الطلب نفسه: البنودُ الثلاثة تُرصَد نقصاً رغم استلام 25
  قطعةً إضافية، والمستلم **تراكميٌّ عبر كل السندات** لا الدفعة الأخيرة (4+3 من 10 ⇒ ناقص،
  +3 ⇒ مكتمل)، والمغطّى كاملاً من المخزون ليس نقصَ توريد، وفرعُ النقص يسبق فرعَ الإقفال
  مصدرياً ويحمل `code:"proc_executing"` فيصحّ اشتقاق المراحل. وثلاثةُ فحوصٍ على
  `_poSettleExtras`: نقصٌ باقٍ ⇒ `proc_executing` بقيدٍ في السجل، لا نقص ⇒ `closed`،
  بندٌ لم يُبَتّ فيه ⇒ لا يُمَسّ الطلب.
  **سجلُّ الأحداث لا يعرض رمزاً خاماً.** القيدُ الثاني في كل طلب كان يُخزَّن
  `event:"pending_pm"` حرفياً، فيظهر «pending_pm» وسط سجلٍ عربي في الشاشة وفي PDF.
  `poTimelineEvent(tl)` تترجم أيَّ نصٍّ هو رمزُ حالةٍ (`PO_STATUS`/`RFQ_STATUS`) عند
  العرض إلى «تغيير الحالة: …» — العلاجُ في العرض وحده فتنتفع الطلباتُ القائمة كلُّها بلا
  ترحيل — ومصدرُ الإنشاء صار يكتب العربية أصلاً. `code` يبقى مرجعَ الاشتقاق كما هو.
  **الاختبارات.** `node hail-tests.js` → 1047/1047.

- **v18.9z — «مَن» في نافذة البلاغ: ثلاثةُ معانٍ لا اسمان متناقضان:**
  **الجذر.** في نافذة تفاصيل البلاغ يظهر اسمان لحدثٍ واحد: حقلُ «مسجّل البلاغ» في
  شبكة التفاصيل، وسطرُ «تم تسجيل البلاغ» في سجل الأحداث — وكثيراً ما يختلفان، فيبدو
  الأمر خطأً في البيانات. وليس كذلك: الحقلان مصدران مختلفان منذ لحظة الإنشاء. الشبكة
  تقرأ `createdByName||createdBy` أي **الحسابَ الذي أدخل البلاغ** (يُلتقط من
  `currentUser` عند الحفظ)، بينما `timeline[0].by` يُخزَّن فيه **مشرفُ البلاغ**
  المختار في النموذج (`supervisor||"النظام"`، وكذلك في بيانات `mk()` التجريبية حيث
  `by:supervisor`) — ولهذا يطابق دائماً ما تراه بجانب «المشرف» في الترويسة. وثالثُهما
  `reporter` أي **المُبلِّغ عن العطل** (ساكنٌ أو موظف، حقلٌ نصيٌّ اختياري في النموذج)
  كان يُحفظ ولا يُعرض في النافذة أصلاً. فثلاثةُ معانٍ لـ«مَن»، اثنان منها بعنوانين
  متشابهين في نافذةٍ واحدة، وثالثٌ مخفيّ.
  **العلاج — في العرض وحده، بلا مساسٍ بالمخزَّن.** لم يُغيَّر ما يُكتب في `timeline`
  ولا في `createdByName`، فالبلاغاتُ المحفوظة سابقاً تنتفع بالتوضيح فوراً ولا تُعاد
  كتابةُ سجلٍّ تاريخي. (١) صار عنوانُ الحقل **«مسجّل البلاغ (الحساب)»** فيسمّي معناه.
  (٢) أُضيف صفُّ **«المُبلِّغ عن العطل»** إلى الشبكة عند وجود `reporter` فقط — فلا
  يظهر صفٌّ فارغ في البلاغات التي لا مُبلِّغَ نصياً فيها. (٣) سطرُ السجل يمرّ عبر
  `timelineWho(t,ev)`: إن كان الحدثَ تسجيلاً وكان `createdByName` موجوداً ومخالفاً
  لـ`ev.by` عرض **«الحساب (المشرف: فلان)»**، وإلا اكتفى بـ`ev.by` كما كان — وهذا
  يغطّي البلاغاتِ القديمة والتجريبية التي لا `createdByName` لها، والحالةَ التي يكون
  فيها المُدخِلُ هو المشرفَ نفسه فلا يتكرّر الاسم.
  **الاختبارات.** حارسٌ في `hail-tests.js` (§«مَن سجّل البلاغ») يمنع الارتداد إلى
  `esc(ev.by)` المباشر في السجل، ويثبّت وجود `timelineWho` وصفَّ المُبلِّغ المشروط،
  ويفحص سلوكياً الحالاتِ الأربع (اختلاف/تطابق/غياب `createdByName`/حدثٌ غيرُ تسجيل).
  ‏`node hail-tests.js` ⇐ 1029/1029 ناجحة.

- **v18.9y — «كل المهام» ببطاقات المباني، والمجدولُ القادم يظهر ويُنفَّذ قبل موعده:**
  **الجذر.** لوحةُ اليوم — وهي وحدها التي تعرض بطاقات المباني — لا تعرض بحكم تعريفها
  إلا المستحقَّ اليوم (`isDue || doneToday`)، وشاشةُ «كل المهام» كانت جدولاً مسطّحاً
  غيرَ مقسَّم. فمهمةٌ تبدأ غداً لا يراها المستخدم في أيّ شاشةٍ عملية ولا يقدر على
  التصرّف فيها قبل موعدها.
  **العلاج.** لـ«كل المهام» عرضان يتشاركان مرشِّحاً واحداً: **بطاقات المباني**
  (الافتراضي) بنفس مفردات اللوحة `.co-groups/.co-group/.ppm-card` وترتيب المباني بنفس
  `_bldOrder`، أربعُ بطاقاتٍ لكل مبنى (`ALL_CARDS_PER_BLD=4` — أعلى من سقف اللوحة ٢ لأن
  هذه شاشةُ جدولٍ لا لوحةَ يوم) وزرُّ «عرض N مهام أخرى» يفتح **نافذة المبنى نفسها**
  بنطاق `all` أي كلِّ مهامّ المبنى لا مهامّ اليوم؛ و**جدول** يحتفظ بالنظرة الإدارية
  (المشرف/التكرار/**الاستحقاق**/آخر تنفيذ/البنود) واسمُ المهمة فيه صار يفتح تفاصيلها.
  بطاقةُ المهمة القادمة تحمل **تاريخ استحقاقها** إلى جانب شارة «بعد N يوم»، وأزرارُ
  التنفيذ/التحرير عليها كما في اللوحة — فالتنفيذ المبكر ممكنٌ من الشاشة نفسها (وهو
  المطلوب: «تبدأ غداً لكن لا تظهر اليوم كي أقدر عليها»). المرشِّح بعدّادات:
  الكل/متأخّرة/اليوم/قادمة/موقوفة، ويسري داخل نافذة المبنى أيضاً فلا يختلف العدُّ
  بين الشاشة ونافذتها.
  **القرار الجوهري: تصنيفٌ جدوليٌّ مستقلٌّ عن `isDue`/`isOverdue`.** الأخيرتان
  تُصفَّران في الإجازة (الجمعة/السبت) عمداً — لا ضغطَ يوم عطلة — وهو صحيحٌ للوحة اليوم
  والتغطية، لكن بناءَ شاشة الجدول عليهما كان يجعل **كلَّ** المهامّ «قادمة» يوم الجمعة
  فتختفي المتأخّرة والمستحقّة من الشاشة كلها. لذلك `schedClass`/`schedSort` تقرآن
  `nextDueDate` مقابل اليوم وحده (متأخّرة/اليوم/قادمة/موقوفة)، وحارسٌ في `hail-tests`
  يمنع تسرُّب `isDue`/`isOverdue` إليهما مستقبلاً.
  **الاختبارات.** `node hail-tests.js` كاملةً + ١٣ حارساً جديداً في §١٩ (التصنيف
  والترتيب والسقف ونطاق النافذة والافتراضي البطاقي وحارس الإجازة).
- **v18.9x — حذف طلب السداد للمسؤول، وأيقونتا الموارد البشرية بلغة السايدبار:**
  **(١) الحذف.** `hrPayments.remove` — للمسؤول وحده وفي أي حالة. الترتيب مقصود كلّه:
  تأكيدٌ يسمّي نوع العمل والبيان والمبلغ والحالة (نصٌّ في فقرةٍ واحدة لأن `#confirm-msg`
  بلا `white-space:pre-line` فأسطر `\n` تنهار إلى مسافات — أُصلح النصّ لا المكوّن
  المشترك)، ثم **قيدٌ في سجل التدقيق قبل الحذف** بكل ما يعرّف المحذوف: رقمه ونوع عمله
  ومبلغه وحالته وقت الحذف ومُنشئه — فالوثيقة تزول ولا تحمل أثرها، ولولا القيد لاختفى
  الطلب بلا ما يُسأل عنه أحد. ثم حذف الوثيقة، ثم حذف المرفقات من التخزين **بأفضل جهد
  وبعد نجاح الحذف**: مستندات الإقامات والتأشيرات بياناتُ أشخاص وتركها يتيمةً بعد حذف
  سجلّها أسوأ من فقدها، لكنّ فشل ملفٍ يجب ألّا يُبقي وثيقةً محذوفةً في القائمة. وفشلُ
  الحذف نفسه يرفع المعرّف من القائمة السوداء فيعود الطلب مع اللقطة التالية بدل أن يختفي
  من الشاشة وهو باقٍ في الخادم. `_deletedIds` تمنع لقطةً في الطريق من إعادة المحذوف —
  نفس حارس `_deletedPurchaseIds` في النواة.
  **(٢) الأيقونتان.** كانتا إيموجي خاماً (🪪) وسط سايدبار كلّه SVG. الجذر: المنصة
  تستبدل إيموجي السايدبار بأيقونات `_ICON` عند الإقلاع عبر `applyNavIcons`، وهي تقرأ
  من خريطتَي `NAV_ICON_BY_PAGE` و`NAV_ICON_BY_HDR` — ولم تكن صفحتا الوحدة ولا مجموعتها
  مسجّلةً فيهما، فبقي الإيموجي كما كُتب في HTML. سُجِّلت الثلاثة: مجموعة «الموارد
  البشرية» ← `users`، و«طلبات سداد الموارد البشرية» ← `banknote`، و«طلب سداد جديد» ←
  `filePlus` كسائر شاشات «جديد» في المنصة (`new-purchase`/`new-rfq`/`custody-new`).
  **(٣) ما كشفه فحص بقيّة السايدبار — نظامان لا واحد.** بدا أن أربع صفحاتٍ أخرى
  ناقصة، والحقيقة أدقّ: في `index.html` **مُطبِّقان** لأيقونات السايدبار يعملان على
  `DOMContentLoaded` بالترتيب — `applyNavIcons` (خرائط `NAV_ICON_BY_*`، مفرداتها
  `_ICON`) ثم `injectSidebarIcons` (خرائط `SB_*_ICON`، مفرداتها `SB_ICON_PATHS`،
  أُضيفت في v9hd). الثاني يُسجَّل بعد الأول فيعمل بعده، ولا يكتب إلا حين يجد اسماً
  في خريطته — فالنتيجة **تتالٍ**: `SB` يغلب حيث يعرف، و`NAV` يملأ ما لا يعرفه.
  عليه: `po-audit` و`labor-catalog` و`price-analysis` كانت **مغطّاة أصلاً** من `SB`
  ولم تكن معطّلة قطّ، والمعطّل فعلاً اثنان فقط: **الرقابة المالية** و**سجل الأخطاء**
  (غائبان عن الخريطتين معاً). سُجِّلا في `NAV` (فـ`SB` لا يعرفهما فيمرّ التتالي)،
  وأُزيلت القيود الثلاثة التي كنتُ أضفتها لصفحاتٍ يملكها `SB`: قيدٌ مُظلَّل لا يُنفَّذ
  أبداً، وإن خالف الغالب (`pieChart` مقابل `barchart`) صار فخّاً يقرؤه من يصحّح عطلاً
  فيظنّه المصدر. ووُثّق التتالي بتعليقٍ عند الخريطتين.
  **الدرس في الحارس:** فحصُ خريطةٍ واحدة كان **ينجح لسببٍ خاطئ** ويُخفي أن الغالب هو
  الآخر. الحارس الآن يفحص التتالي كما هو: زرٌّ أو رأسُ مجموعةٍ غائبٌ عن **الاثنين**
  يُسقط الفحص، وكل اسم أيقونة يجب أن يكون معرَّفاً في مفردات نظامه (وإلا رُسم فراغاً).
  **بند قائم (خارج النطاق):** 29 قيداً في `NAV_ICON_BY_PAGE` مُظلَّلة من `SB` منذ
  v9hd — لا أثر لها، وتنظيفها تغييرٌ في مشتركٍ لم يُطلَب.
  **الاختبارات:** 988 → **1004**، كلها ناجحة (16 فحصاً جديداً): الحذف للمسؤول وحده،
  والقيد يسبق الحذف، والمرفقات تُحذف بعده، والقائمة السوداء تُنظَّف عند الفشل، وتغطية
  أيقونات السايدبار عبر التتالي كاملاً. وفحصُ متصفّحٍ يؤكّد أن **53 عنصر سايدبار**
  (أزرار + رؤوس مجموعات + زرّ الخروج) تعرض SVG بلا إيموجي خام واحد. و`ui-walkthrough` و`browser-scenarios` ورحلة المسار خضر.

- **v18.9wz — نافذة سداد أعمال الموارد البشرية داخل المشتريات المركزية:**
  **لماذا:** رسوم الإقامات ورخص العمل والتأشيرات كانت تُسدَّد خارج المنصة بلا مسار
  اعتماد ولا سجل — فلا يُعرف من أَذِن، ولا يُقاس ما أُنفق، ولا يبقى المستند مربوطاً
  بالمبلغ. وإدخالها كطلبات شراء كان سيلوّث تكاليف المشاريع ومؤشّرات التوريد بمصروفٍ
  إداريٍّ ليس شراءً.
  **ما صار:** وحدة مستقلة `hr-payments.js` (نمط `finance-audit.js`) بمجموعة سايدبار
  خاصة وصفحتين — `#page-hr-payments` (القائمة + التفاصيل) و`#page-new-hr-payment`
  (طلب جديد) — ومجموعة Firestore منفصلة `global_hr_payments` بعدّادٍ خاص
  (`HRP-YYMM-NNNN`). الطلب مختصر عمداً: نوعية الأعمال (إقامات / رخص عمل / تأشيرات /
  خروج وعودة / نقل كفالة / تأمين طبي / مكتب عمل / جوازات / أخرى) + بيان مختصر +
  تكلفة السداد + مستند مرفق، وبيانات تحويل اختيارية (المستفيد/الآيبان بنفس تحقق
  `SA`+22 رقماً المستخدم في مسار الشراء).
  **التسلسل:** مسؤول الموارد البشرية ينشئ ← مدير المشاريع (إن اختار المُنشئ ذلك) ←
  المدير التنفيذي إن بلغت التكلفة `CEO_APPROVAL_THRESHOLD` ← المالية تسدّد بإيصال
  إلزامي، وبتسجيل السداد **يُغلق الطلب** (دفعة واحدة كاملة — لا دفعات جزئية هنا).
  الرفض في أي بوابة (والإعادة من المالية) يردّ الطلب لمسؤول الموارد البشرية بسببٍ
  مكتوب، فيصحّح ويعيد الإرسال ويُعاد حساب المسار من جديد.
  **قرارات مقصودة (وما تحرسه):**
  (١) **بوابة التنفيذي تُحسب من التكلفة لا من اختيار بشري.** خيار «يحتاج مدير
  المشاريع» يخصّ مدير المشاريع وحده؛ لو رُبطت بوابة التنفيذي باختيار المُنشئ لأمكن
  تمرير أي مبلغ للمالية باختيار «لا».
  (٢) **السقف يُقرأ من `CEO_APPROVAL_THRESHOLD` الموحّد** (المصدر نفسه الذي تقرأه
  طلبات الشراء والتسعير، قابل للتعديل من `meta/global_purchase_config`) — لا رقم
  مكتوب في الوحدة يفترق عنه بصمت.
  (٣) **التوجيه كلّه في دالة نقية واحدة `_nextStage(req, threshold)`** تُستدعى عند
  الإنشاء وبعد اعتماد مدير المشاريع وبعد اعتماد التنفيذي وبعد كل تعديل/إعادة إرسال —
  فلا مساران يفترقان. واعتماد التنفيذي **يسقط تلقائياً** إن رُفعت التكلفة فوق
  `ceoApprovedAmount` (المبلغ الذي رآه ووقّع عليه)، فلا يُسدَّد مبلغٌ لم يره أحد.
  (٤) **كل كتابة عبر `runTransaction`** على الوثيقة الطازجة مع فحص الحالة المتوقّعة —
  فلا يدهس معتمِدان متزامنان عمل بعضهما، والاعتماد على حالةٍ قديمة يُرفض بـ «stale».
  (٥) **العزل عن المشتريات:** لا قراءة ولا كتابة في `global_purchases`، ولا دخول في
  تقارير المشتريات ولا `poActualCost` ولا المخزون ولا البند المستعاض.
  (٦) **الصلاحيات:** العرض لـ `hr_officer` + `project_manager` + `ceo` + `finance` +
  `admin` فقط؛ مجموعة السايدبار مخفية في HTML وتُظهرها الوحدة وحدها
  (`hrPayments.refreshNav`)، ومسؤول الموارد البشرية يهبط على شاشته لا على طلبات
  الشراء، وسايدباره مقصور عليها (`body.role-hr-officer`) فلا يملك ما لا يحتاجه.
  (٧) **المرفقات** تحت `po/hr/{id}/…` — داخل بادئة `po/` القائمة عمداً، لأن قواعد
  Storage تُدار خارج المستودع ومسارٌ جذريّ جديد قد يُرفَض صامتاً عند الرفع.
  **تنظيفان في الطريق:** `_isGlobalOnlyRole` صار المصدر الواحد لقائمة أدوار الوضع
  المركزي — كانت مكرّرة حرفياً في أربعة مواضع (الدخول، مُنتقي المشاريع، زرّ التبديل،
  الخروج) وكان الدور الخامس سيُنسى في أحدها فيقع في شاشة مشاريع لا تخصّه؛
  و`_notifVisible` صار المصدر الواحد لفلترة جرس الإشعارات (كانت مكرّرة في أربعة
  مواضع أيضاً)، ويُخفي إشعارات الموارد البشرية عمّن لا يملك الاطلاع على الوحدة.
  **ملاحظة أمنية مؤجّلة:** الفلترة بالدور تقع في العميل — وثيقة الإشعارات ومجموعة
  `global_hr_payments` تخضعان للقاعدة العامة في `firestore.rules` (أي دورٍ يقرأ).
  تقييدها بالدور يقع في المرحلة ٣ من `docs/deep-review-2026-08.md`.
  **لغة التصميم — تستعير ولا تخترع:** الصفحتان مبنيّتان بمكوّنات المنصة نفسها لا
  بمفرداتٍ خاصة: `page-hero` و`card`/`card-title`، ولوحة `dash-top`/`stat-tile`
  (كلُّ بطاقةٍ فلترٌ بنقرة كما في لوحة المشتريات)، و`filters`/`filters-row`،
  و`form-group`/`form-label`/`form-row`، وفي التفاصيل `d-hero`/`d-facts`
  و`d-sec-label`/`d-grid`/`d-item` والخط الزمني `dtl`، وشريط المراحل هو
  **شريط سير عمل طلب الشراء نفسه** (`po-wf`/`po-step`/`po-link`) بنفس أيقونات
  `PO_STAGES` لنفس البوّابات، والشارات `badge b-po-*` بنفس خريطة `poStatusBadge` —
  فبوّابة التنفيذي هنا تُقرأ كما تُقرأ هناك بلا تعلّمٍ ثانٍ. الألوان توكنز فقط
  (`--primary/--accent/--warn/--danger/--info`)، والأيقونات من `_ICON` لا إيموجي في
  متن الصفحة (الإيموجي يبقى في السايدبار حيث تستعمله المنصة). ورقة أنماط الوحدة
  **عشرة أصناف `.hrp-*` لا غير** — ما لا نظير له في المنصة فقط (بطاقة الطلب بشريطٍ
  جانبي ملوّن بالحالة، والمرفقات)، ويحرس ذلك فحصٌ يسقط إن عرّفت الوحدة أيّ صنفٍ
  خارج `.hrp-*`.
  **العنصر المميّز:** شريط المسار في نموذج الطلب الجديد — نفس مكوّن `po-wf` لكن
  **قبل** الإنشاء لا بعده: يُعاد رسمه مع كل رقمٍ يُكتب في التكلفة ومع تبديل خيار مدير
  المشاريع، وموضعه مباشرةً تحت الحقلين اللذين يحرّكانه. بقيّة الشاشات تقول «أين وصل
  الطلب»؛ هذا يقول «إلى أين سيذهب» — فقاعدة سقف التنفيذي تُرى لحظةَ القرار لا بعد
  الإرسال.
  **ما كشفته لقطات الشاشة (فاتح/داكن/جوال):** (١) الطلب المغلق كان يُظهر مرحلة
  «إغلاق» بإطار المرحلة الجارية — قاعدة `.po-step.active` تلي `.done` في ورقة المنصة
  فتغلبها، فيبدو المغلق منتظِراً؛ صارت المرحلة المنجَزة لا تُرسَم جارية أبداً.
  (٢) `direction:ltr` على حاوية مبلغ البطاقة كان يقلب «ر.س» إلى يمين الرقم؛ الرقم
  وحده يُعزَل الآن كما في بطاقة طلب الشراء. (٣) رأس التفاصيل كان يكرّر المعرّف ونوع
  العمل والبيان ثلاث مرات متتالية — الشريط العلويّ صار يحمل المعرّف والوحدة، و`d-hero`
  السجلَّ نفسه. (٤) بوّابة التنفيذي صارت تحمل وسمها البنفسجي `po-wf-tag ceo` في كل
  موضع بدل «نعم» خضراء عامّة.
  **الاختبارات:** 934 → **988**، كلها ناجحة (54 فحصاً جديداً): دالة التوجيه تُنفَّذ فعلاً
  على كل تفريعة (السقف شامل `≥`، «لا» لمدير المشاريع لا تتخطّى التنفيذي، اعتمادٌ قديم
  بلا `ceoApprovedAmount` لا يمرّر مبلغاً فوق السقف)، وحُرَّاس العزل عن `global_purchases`
  و`savePurchase` والمخزون، وتطابق `MODULE_BUILD`، وتوحيد `_isGlobalOnlyRole`
  و`_notifVisible`، وثمانية حرّاس للغة التصميم (استعارة مكوّنات المنصة، ولا صنف خارج
  `.hrp-*`، ولا مرحلة منجَزة تُرسَم جارية). و`ui-walkthrough` (61) و`browser-scenarios`
  (58) خضراوان، والجوال بلا تمرير أفقي عند 390px.
  **ما كشفته رحلة متصفّح فعلية على المسار كاملاً (31 فحصاً: إنشاء ← مدير مشاريع ←
  تنفيذي ← سداد ← إغلاق):** زرّ السايدبار كان يعيد رسم **تفاصيل آخر طلبٍ فُتح** بدل
  القائمة — وأوضحُ ظهورٍ له بعد إنشاء طلبٍ جديد (يُفتح تفصيله تلقائياً)، فيبدو الزرّ
  معطّلاً. أُضيفت `hrPayments.list()` (تصفّر `_curId` قبل العرض) وحارسٌ يمنع الارتداد.

- **v18.9wy — فحصٌ ذاتيّ لعقد الفهرس: النظام يكشف الانزياح بنفسه:**
  **لماذا:** v18.9wx أصلح **العرض** — يصطفّ بالهوية فلا يستعير بندٌ صفَّ جاره. لكنّ
  الشفاء عند العرض **يُخفي** أمرين: أن المخزَّن ما زال منزاحاً، وأن مساراً آخر قد
  يكسر العقد غداً. وقد نجا العطل الأصلي من 924 فحصاً لأن العقد كان مكتوباً في
  **تعليق** (`grnDoc.items يبقى بطول p.items وبنفس الفهارس`) — والتعليق لا يُسقِط بناءً.
  **ما صار:** يُفحَص العقد نفسه عند **كل تحميل** داخل `_poHealItems` (وهي تمرّ على كل
  طلب في اللقطة الأولى والحيّة): الطول متساوٍ، وهوية `p.items[i]` = هوية
  `p.auditItems[i]`. وعند الخرق يُعلَن ولا يُصلَح صامتاً:
  (١) قيدٌ في **«🐞 سجل الأخطاء»** (`captureError` بنوع `align`) يسمّي الطلب والسطر
  المنزاح والبندين المتقابلين — فتُعرف الطلبات المصابة بلا انتظار من ينتبه.
  (٢) **شريطٌ أحمر للمسؤول** في تفاصيل الطلب: الأرقام المعروضة مُصحَّحة تلقائياً،
  والمخزَّن يُشفى بفتح «تعديل الطلب» وحفظه (v18.9wx يعيد الاصطفاف عند الحفظ).
  **قرارات مقصودة:** لا كتابة صامتة للمخزَّن عند التحميل — تُلغي الدليل وتخاطر بدهس
  وثيقةٍ من جلسةٍ أخرى؛ بلاغٌ **واحد لكل طلب في الجلسة** فاللقطات الحيّة تتكرر ولا
  يُغرَق السجل؛ التشخيص في **خريطةٍ في الذاكرة** لا حقلاً على الوثيقة، فلا يتسرّب إلى
  Firestore عند أول حفظ؛ والفحص كلّه داخل `try` — وثيقةٌ مشوّهة لا تُعطّل تحميل الطلبات.
  **الاختبارات:** 924 → **937**، كلها ناجحة. تشمل: رصد الانزياح بتسمية السطر
  والبندين، صمتَ الطلب السليم (لا إزعاج كاذب)، بلاغاً واحداً رغم تكرار اللقطات،
  سقوطَ القيد بعد الشفاء، ووثيقةً مشوّهة لا ترمي.
  **الباقي من خطة الكشف (لم يُنفَّذ بعد):** اختبارُ خاصيةٍ على **تسلسل** العمليات
  (إنشاء → استلام → تعديل → استلام ثانٍ) — وهو ما كان سيكشف العطل قبل النشر؛ ومسحٌ
  ليليّ في `functions/` يُنتج تقرير صحّةٍ لكل الطلبات — يكشف الوثائق المصابة التي
  **لم يفتحها أحد**. راجع `docs/deep-review-2026-08.md` عند جدولتهما.

- **v18.9wx — الفهرس ليس هوية: بادج «تغيّر السعر» كان يقارن البند بتقدير جاره:**
  **البلاغ (PO-202608-0130):** «ديانه رمل» سعرها في الكتالوج 140 وفي الفاتورة 140،
  ومع ذلك ظهر تحتها «↗ تغيّر من 14.79». و«بردورة اسمنتي 10×30×50سم» — بندٌ **أُضيف
  عند الاستلام** ولم يكن في الطلب أصلاً — ظهر تحته «↘ تغيّر من 140».
  **الجذر:** `p.auditItems` و`p.grnDocs[].items` تُكتَبان بطول `p.items` وبترتيبها —
  عقدٌ **موضعيّ** يعتمد عليه كل قارئ. لكن `doAdminEditPurchase` كانت تكتب
  `pf.items = _paeItems` **بلا لمس `auditItems`**. فحين حذف المسؤول (15:35) البندَ
  «بلدورة زراعة» من طلبٍ **مُدقَّق**، انزاح كلُّ ما بعده صفّاً واحداً: صار البند رقم 4
  يقرأ صفَّ البند 3، و5 يقرأ صفَّ 4. فقُورن سعر «ديانه رمل» بتقدير «الاسمنت» (14.79)،
  وسعرُ البند المضاف بتقدير «ديانه رمل» (140). لا أحد غيّر سعراً — التقديران المعروضان
  ليسا تقديرَي هذين البندين أصلاً.
  **الأخطر (صامت):** `_waPrevRcv` يقرأ `g.items[idx]` من سندٍ مُوقَّعٍ سابقاً، فيجد صفَّ
  بندٍ آخر؛ وحارس الهوية (v18.9nr/v18.9ts) يردّه **صفراً** = «لم يُستلَم شيء سابقاً».
  فأيّ استلامٍ لاحق يُملأ نموذجه بالكمية **كاملةً** بدل المتبقّية — و**يتضاعف رصيد
  المخزون** عند التأكيد. بلا رسالة ولا أثر.
  **العلاج (أربع نقاط، الهوية مفتاحاً لا الفهرس):**
  (١) `_poItemKey` — سُلَّم هوية واحد: `itemId` ← الكود ← الاسم (نفس سُلَّم `_waPrevRcv`).
  (٢) `_poAlignRows(items, rows)` تُرجع صفّاً لكل بند بهويته أو `null`؛ والفهرسُ نفسه
  يُقدَّم حين تطابق هويتُه — فبندان لنفس الصنف في طلبٍ واحد يبقيان على صفّيهما ولا
  يلتهم الأول صفَّ الثاني. البندُ الذي لا صفَّ له يُرجع `null` (لا صفَّ جارٍ مُستعار)،
  فيرجع البادج إلى تقدير البند نفسه — وهو الصواب.
  (٣) بادج تفاصيل الطلب وصندوق «تغيّر الأسعار» يقرآن المصطفَّ. وفي الصندوق صار
  «الفعلي» = سعر البند الحالي (ما يعرضه الجدول) لا `unitPrice` المجمَّد: فيتصحّح
  بتصحيح المسؤول، ولا يُقرأ **صفراً** للبند المغطّى من المخزون فيُشهَّر «هبوطاً» كاذباً
  بكامل تقديره؛ وبندٌ حُذف من الطلب لم يعد يُحاسَب فيه.
  (٤) `doAdminEditPurchase` يعيد اصطفاف `auditItems` عند كل حفظ (المحذوف يسقط،
  والمضاف يأخذ صفّاً صفرياً بشكل «المغطّى من المخزون») — فيُشفى المخزَّن لا العرضُ وحده.
  و`_waPrevRcv` يبحث عن صفّ البند بهويته حين لا يطابق الفهرس، وبتعدّد المطابقات لا
  يخمّن (يبقى على الفهرس). **لم تُمسّ `grnDocs` المخزَّنة**: سندُ الاستلام وثيقةٌ
  مُوقَّعة تُقرأ ولا تُعاد كتابتها.
  **الاختبارات:** قسم «21ب» في `hail-tests.js` — يعيد بناء PO-202608-0130 حرفياً
  (٦ بنود، يُحذف «بلدورة زراعة»)، يُثبت الانزياح بالفهرس الخام، ثم يتحقق أن المصطفَّ
  يعيد لكل بندٍ صفَّه، وأن البادج يصمت على الثلاثة ويبقى ظاهراً على التغيّر الحقيقي
  (غراء 41 ← 40.87)، وأن `_waPrevRcv` يقرأ 20 لا 0 للاسمنت المنزاح، مع حالتَي «بندان
  لنفس الصنف» وحرّاس المسار. **905 → 924 فحصاً، كلها ناجحة.**
  **خطوة ما بعد الدمج:** لا شيء — الشفاء تلقائي عند العرض؛ ويُحفَظ المخزَّن مصطفّاً
  عند أول حفظٍ لاحق من نافذة تعديل المسؤول.

- **v18.9ww — تجاوب الجوال: إصلاح عطلَي تمرير أفقي رُصدا بفحص Playwright عند 375px:**
  أول فحص تجاوب منهجي للتطبيق (34 صفحة × 3 مقاسات: 375/768/1440) عبر الـskill
  المثبّت في v18.9wu. قِيس لكل صفحة: فيضان المستند الأفقي، العناصر المتجاوزة لحدود
  الشاشة واتجاه تجاوزها، الجداول الأعرض من حاويها، ومساحات اللمس. ثم **مرحلة تمييز**
  تفحص لكل عنصرٍ متجاوز هل بين أجداده حاوٍ يقصّه (`overflow-x:hidden`) أو يمرّره
  (`auto/scroll`) — وهي التي فصلت العطل الحقيقي عن المقصود.
  **(١) سجل التدقيق (`renderAuditLog`):** `<table class="report-table">` كان بلا حاوي
  تمرير، فيدفع الصفحة كلّها للتمرير الأفقي **80px** على الجوال — بينما جدول «تدقيق
  المشتريات» المجاور في الملف نفسه ملفوفٌ بـ`overflow-x:auto`. والأدهى أن الصنف
  `.report-table-wrap` كان **معرَّفاً في CSS (§`@media(max-width:700px)`) وغير مستعمَل
  إطلاقاً** — نمطٌ نُوي ولم يُطبَّق. طُبِّق الآن على الجدول.
  **(٢) أوامر الصرف (`renderIssueOrders`):** شبكة `.ast-stats` بنمطٍ سطريّ
  `grid-template-columns:repeat(3,1fr)` **يتخطّى** media query الجوال
  (`@media(max-width:760px){.ast-stats{grid-template-columns:1fr 1fr}}`) لأن الأنماط
  السطرية أعلى أولوية من ورقة الأنماط — فتفيض البطاقة الثالثة **38px**. حُوِّلت إلى
  `repeat(auto-fit,minmax(150px,1fr))`، وهو نفسه ما تستعمله شبكتان أُخريان في الملف.
  **حارسان في `hail-tests.js`** يمنعان الارتداد: الأول يشترط أن يكون
  `.report-table-wrap` معرَّفاً **ومستعمَلاً** على جدول السجل، والثاني يرفض أي شبكة
  `.ast-stats` بعدد أعمدة ثابت في نمطٍ سطريّ.
  **ما لم يُمسّ عمداً:** فيضان 3px في صفحة `tv` — لوحةُ عرضٍ للشاشات الكبيرة
  (`font-size:clamp(10px,.9vw,14px)` مع `nowrap`) لا هدفٌ للجوال، وتعديلها يخاطر
  بتخطيط الشاشة المستهدفة. وباقي ما رصده الفحص **مقصود وسليم**: شريط أخبار `tv`
  يقصّه `.tvw-ticker-view`، وشبكة `kpi` وأزرار `monthly-compare` وجداول
  `item-catalog`/`labor-catalog`/`price-analysis` كلها داخل حاويات `overflow-x:auto`
  قائمة فعلاً — أي أن المشروع يطبّق النمط بانتظام، والعطلان كانا استثناءين.
  تحقّق بعد الإصلاح: **صفر تمرير أفقي** في الصفحتين. **905/905** · 59/59 · 58/58.

- **v18.9wv — تجاهل ملفات التنفيذ المؤقتة لمنفّذ skill الـPlaywright (`.gitignore`):**
  منفّذ الـskill (`run.js`) يكتب — بتصميمه — ملفاً عابراً
  `.temp-execution-<timestamp>.js` داخل مجلد الـskill عند كل تشغيل، ويحذف ملفات
  التشغيلات السابقة في بداية التشغيل التالي (تأخيرٌ متعمّد كي تكتمل العمليات
  اللا-متزامنة الطويلة بأمان). أثرُ ذلك أن ملفاً واحداً يبقى غير متتبَّع في git بعد
  كل استخدام للـskill — ظهر عملياً بعد أول تشغيل حقيقي (اختبار صفحة الدخول، 9/9).
  أُضيف النمط `.temp-execution-*.js` إلى `.gitignore` (يطابق في أي مسار) فلا تعود
  حالة المستودع تتلوّث. لا تغيير في أي منطق تطبيق — رفع الإصدار التزاماً بقاعدة
  «أي تعديل».

- **v18.9wu — تثبيت skill أتمتة المتصفح Playwright ضمن أدوات التطوير (`.claude/skills/playwright-skill`):**
  بطلب المستخدم «ثبت الskill» (المصدر: github.com/lackeyjb/playwright-skill).
  نُسخ مجلد الـskill الداخلي (`SKILL.md`, `run.js`, `lib/helpers.js`, `API_REFERENCE.md`,
  `package.json`) إلى `.claude/skills/playwright-skill/` ليكتشفه Claude Code تلقائياً
  في هذا المشروع — يتيح كتابة وتنفيذ أتمتة Playwright مخصّصة عند الطلب (اختبار صفحات،
  تعبئة نماذج، لقطات شاشة، فحص التجاوب) عبر المنفّذ الموحّد `run.js`، مع كتابة سكربتات
  الاختبار المؤقتة في `/tmp` حصراً (لا تلوّث المستودع). **تكييف وحيد للبيئة:** أُضيفت
  دالة `patchChromiumExecutableFallback()` في `run.js` تجعل `chromium.launch` يستخدم
  Chromium المثبّت مسبقاً (`/opt/pw-browsers/chromium` + `--no-sandbox` — نفس نمط
  `ui-walkthrough.mjs`) عندما تكون بنية Playwright المُدارة غير موجودة، لأن تنزيل
  المتصفحات محجوب في بيئة العمل (playwright 1.57 يطلب بنية 1234 والمتوفر 1194) —
  قابلة للتخصيص عبر `PW_CHROMIUM_FALLBACK`، وخيارات المستخدم تتقدّم على الاحتياطي.
  تحقّق: تنفيذ سكربت inline عبر `run.js` نجح (إطلاق متصفح + قراءة محتوى). لا تغيير
  في أي منطق تطبيق — ملفات أدوات تطوير فقط (رفع الإصدار التزاماً بقاعدة «أي تعديل»).

- **v18.9wt — توثيق مرجعي دائم لمنظومة الرقابة المالية (§3-أ في هذا الملف):**
  بطلب المستخدم «حدث notes»: سجل §6 يؤرّخ التغييرات تباعاً، لكن NOTES بوصفه «المرجع
  الدائم لمنطق العمل» كان يفتقد توصيفاً مجمّعاً للمنظومة التي بُنيت عبر wk→ws.
  أُضيف **§3-أ** يجمع في موضع واحد: آلية العينة الهجينة الحتمية وثوابتها، محرك
  المقارنة ومصادره الأربعة، دورة حالة العينة وإلزامياتها، الفحص الوقائي، التخزين
  والتزامن ودروسه (الذاكرة المحلية/التحميل المتوازي)، الصلاحيات، الأمان وما أُجّل
  للمرحلة ٣، والأفكار المعتمدة المنتظرة — مع ملاحظة معايرة العتبات بعد أول دورة
  ببيانات حقيقية. وحُدّث سطر الوحدة في دليل الملفات (§2). لا تغيير في أي كود —
  توثيق فقط (رفع الإصدار التزاماً بقاعدة «أي تعديل»). **902/902**.

- **v18.9ws — الرقابة المالية: تحصين onclick بـ`_jq` + الذاكرة المحلية تُحدَّث مع كل معاملة (`finance-audit.js`):**
  حصيلة فحص ذاتي استباقي (بطلب المستخدم). **(١) أمني:** 7 مواضع `onclick` كانت تمرر
  معرّفات من Firestore عبر `esc` داخل سياق سلسلة JS — نفس صنف ثغرة v18.9vu-H5
  (`esc` تحوّل `'` إلى `&#39;` فتُفكّ داخل الخاصية وتكسر سياق الـ JS: XSS مخزّن
  نظرياً عبر وثيقة بمعرّف خبيث في ظل قواعد Firestore الفضفاضة). أُضيفت **`_jq`**
  (جسر `_jsq` النواة: تهريب `\` و`'` أولاً ثم محارف HTML) واستُبدلت في المواضع
  السبعة، بحارس يمنع عودة النمط. **(٢) رحلة E2E كاملة في Chromium** — إنشاء ←
  مراجعة«ملاحظة» ← رد المشتريات ← إغلاق بتصعيد ← حذف عينة (مسؤول) ← «مطابق» ←
  إغلاق الدورة ← محاولة إعادة سحب (9/9) — كشفت خللاً حقيقياً: `closeAudit` كان
  يُرفض ظلماً («بنود مفتوحة») لأن الذاكرة المحلية `_audits` تبقى على الحالة القديمة
  حتى تصل لقطة onSnapshot رغم نجاح كل المعاملات (وفي الإنتاج تعرض الشاشة حالةً
  قديمة لحظياً بعد كل حفظ). الحل: **`_applyLocalDoc`** تُحدّث النسخة المحلية فوراً
  بنتيجة كل معاملة (`_txAudit` بعد النجاح) — والإدراج المتفائل للإنشاء صار من نفس
  المصدر. **(٣)** لقطة ثيم داكن أكدت سلامة كل الألوان التوكنية (`color-mix`).
  حرّاس +3. **902/902** (+ المتصفح 59/59 والسيناريوهات 58/58 ورحلة الدورة 9/9).

- **v18.9wr — الفحص الوقائي عند إنشاء طلب الشراء (`finance-audit.js` + ربط خفيف في `index.html`):**
  تنفيذ أعلى مقترحات تطوير الرقابة قيمةً (اعتمده المالك): بدل انتظار التدقيق الشهري
  ليكتشف أن مورداً آخر كان أرخص، **محرك مقارنة التدقيق نفسه يعمل لحظة الإدخال**:
  بعد كل إضافة/حذف بند في «طلب شراء جديد» تستدعي النواة (`renderPurchaseItemsTable`)
  دالة **`financeAudit.renderPrecheck`** (استدعاء محروس + حاوية `np-precheck-host` —
  سطران فقط في النواة) فتُقارن البنود المدخلة بالتاريخ الشرائي (آخر 6 أشهر) وكتالوج
  الأسعار بالتطبيع العربي (`_itemBench` — نفس الدوال النقية المُختبَرة)، ويظهر تنبيه:
  «سعرك 12 مقابل 9.5 من مؤسسة النور (طلب PO-x) — وفر محتمل 250 ر.س». **يُنبَّه فقط**
  عند بديل أرخص يتجاوز التسامح (5%) — صمتٌ تام عندما الأسعار سليمة (الضوضاء تعوّد
  على التجاهل)، والتنبيه **استرشادي لا يمنع الإرسال** (القرار للمشتريات — قد يبرَّر
  الفرق بالجودة/سرعة التوريد). أُعيد هيكلة `_refRows` إلى **`_refRowsAt(refISO,
  excludeId)`**: مركز نافذة التاريخ لحظةُ الإغلاق للتدقيق اللاحق و«الآن» للفحص
  الوقائي — مصدر واحد للمنطقين. حرّاس +3. **899/899** (+ المتصفح 59/59
  والسيناريوهات 58/58).

- **v18.9wq — الرقابة المالية: تسريع التحميل الأولي + حذف اسم مورد حقيقي من مثال (`finance-audit.js`):**
  أبلغها المستخدم (آيباد): «جارٍ تحميل بنود التدقيق…» تطول كثيراً. **الجذر:** الصفحة
  كانت تنتظر أول لقطة `onSnapshot` وحدها، ومصافحة تيار Watch قد تستغرق ثوانيَ طويلة
  على اتصال بارد. **ثلاث طبقات:** (١) **`get()` فوري بطلب واحد يوازي فتح التيار** —
  يعود أسرع بكثير فيظهر المحتوى فوراً وأيهما وصل أولاً يعرض (حارس `_loaded`) والتيار
  يتكفل بالتحديث الحي بعد لحوقه. (٢) **مهلة أمان 8 ثوانٍ** — لا دوّار أبدياً مهما حدث.
  (٣) عند الخطأ/المهلة **رسالة اتصال صادقة** «تعذّر تحميل بيانات التدقيق» مع زر
  «🔄 إعادة المحاولة» (`retryLoad`) بدل «لا توجد دورات» المضللة — وأي لقطة لاحقة
  تصحح تلقائياً (`_connIssue` يُصفَّر مع كل لقطة ناجحة). كما حُذف اسم المورد الحقيقي
  «عالم الريتاج» من مثال حقل الملاحظات (صار «المورد Y») بطلب المستخدم — بحارس يمنع
  عودته. حرّاس +3. **896/896** (+ المتصفح 59/59 والسيناريوهات 58/58).

- **v18.9wp — الرقابة المالية: مرفقات أدلة على العروض اليدوية (`finance-audit.js`):**
  بطلب المستخدم (من مقترحات التطوير): حقل ملف اختياري (صورة/PDF) بجوار إدخال العرض
  اليدوي في نافذة المراجعة — يرفع **دليل العرض** (صورة عرض المورد البديل الذي وجده
  المدقّق) إلى Storage بنفس نمط رفع الفاتورة في النواة: انتظار المصادقة
  (`_waitForFirebaseAuth`)، ضغط الصورة فوق 300KB (`_compressImage` 1800/0.78 —
  PDF يُرفع كما هو)، المسار `finance_audits/{الشهر}/{الطلب}/mq_{ts}`، ثم
  `getDownloadURL` ويُخزَّن `{fileUrl,fileName}` مع العرض في `manualQuotes`.
  **فشل الرفع لا يضيف العرض** (حتى لا يُظنّ أن الدليل أُرفق) برسالة تفرّق بين غياب
  خدمة التخزين وفشل الاتصال، وحارس `_mqBusy` يمنع النقر المزدوج أثناء الرفع.
  الرابط 📎 يظهر (عبر `safeUrl`) في قائمة العروض بنافذة المراجعة، **وفي نافذتي
  رد المشتريات والإغلاق** (`_mqReadonlyHtml` — المشتريات ترى العرض البديل ودليله
  لتكتب مبررها على بيّنة)، وفي تقرير الطباعة. حرّاس +3. **893/893**
  (+ المتصفح 59/59 والسيناريوهات 58/58).

- **v18.9wo — الرقابة المالية: مؤشر تحميل + نافذة المراجعة بحجم نافذة تفاصيل الطلب (`finance-audit.js`):**
  أبلغها المستخدم من الآيباد: (١) عند دخول الصفحة قبل وصول أول لقطة Firestore كانت
  تظهر «لا توجد دورات تدقيق بعد» المضللة — أُضيف علم **`_loaded`** يُرفع مع أول
  snapshot (وعند خطأ المزامنة أيضاً — فلا تحميل أبدي) وقبله بطاقة «جارٍ تحميل بنود
  التدقيق…» بدوّار `.fa-spin` يحترم `prefers-reduced-motion`. (٢) نافذة المراجعة
  كانت **تفيض خارج الشاشة** — لا رأس ولا زر حفظ ظاهرين ولا تمرير: جذرها أن صندوق
  `showCustomModal` في النواة عرضه 420px **بلا `max-height` ولا `overflow`**. أُضيف
  صنف **`fa-modal-wide`** يوسّع الصندوق لحجم نافذة تفاصيل طلب الشراء نفسه
  (`max-width:min(1180px,96vw)` + `max-height:92vh` + `overflow-y:auto`، وعلى الجوال
  97vh) وبخلفية توكن الثيم بدل `#fff` الصلبة — تجاوزٌ بـ`!important` (الأنماط الأصلية
  inline — نفس درس v18.9wd) يُطبَّق على نافذة الوحدة وحدها بعد فتحها (`_widenModal`)
  دون مساس ببقية الـ 19 نافذة التي تستخدم showCustomModal. حرّاس +3. **890/890**
  (+ المتصفح 59/59 والسيناريوهات 58/58).

- **v18.9wn — الرقابة المالية: حذف طلب من العينة (المسؤول فقط) + أسباب الإدراج الآلي ظاهرة نصاً (`finance-audit.js`):**
  بطلب المستخدم أمران: (١) زر 🗑 في صف العينة يحذف الطلب من دورة التدقيق — **للمسؤول
  وحده** (حارس داخل `removeSample` لا إخفاء زر فقط) وعلى الدورة المفتوحة فقط، بتأكيد
  صريح ومعاملة على الوثيقة الطازجة، ويُقيَّد الحذف في سجل التدقيق بمصدر الطلب وحالته
  عند الحذف — فإخراج طلبٍ من الرقابة قرار حسّاس لا يمرّ صامتاً. (٢) سأل المستخدم
  «ما هو المُدرج آلياً» — والجذر أن أسباب الإدراج كانت في tooltip (`title`) لا يظهر
  على شاشات اللمس (آيباد/جوال): صارت الأسباب ظاهرةً نصاً صغيراً تحت شارة المصدر في
  جدول العينة مباشرة. حرّاس +3. **887/887** (+ المتصفح 59/59 والسيناريوهات 58/58).

- **v18.9wm — صفحة الرقابة المالية بلغة تصميم المنصة + حذف جملة من العنوان الفرعي (`finance-audit.js`):**
  بطلب المستخدم أمران: (١) حُذفت جملة «هل كان هناك مورد أرخص لنفس البنود؟» من العنوان
  الفرعي (حارس يمنع عودتها). (٢) أُعيد بناء طبقة العرض بلغة النواة نفسها بدل البطاقات
  المرتجلة: هيرو **`page-hero`** (التدرّج الكحلي + `ph-ico` + أزرار شفافة بيضاء) للقائمة
  والتفاصيل معاً، بطاقات **`stat-card`** بشريط `--sc` وأرقام JetBrains Mono جدولية،
  جدول بنمط pm-table (`.fa-table` — صفوف قابلة للنقر بتظليل hover)، حبوب حالة/حكم/مصدر
  موحّدة (**`.fa-pill`** بنقطة لون تقرأ `--pc` من توكنز الثيم عبر `color-mix` —
  **أُزيلت كل ألوان hex المثبّتة** التي كانت تنكسر في الثيم الداكن)، **شريط نزاهة
  العينة** (`.fa-seed` بصمة monospace بأيقونة قفل + معايير الدورة المخزّنة في سطر واحد)،
  شريط تقدّم `closed/total` لكل دورة، بطاقات إجماليات تراكمية في القائمة، حالة فراغ
  بأيقونة ودعوة فعل، وصناديق تنبيه نافذة المراجعة توكنية (`fa-alert`/`fa-flag`).
  كل الأنماط في ورقة **`fa-css`** تُحقن مرة واحدة (نمط `pm-css`). صفر تغيير في
  المنطق/التخزين — طبقة عرض فقط، والطباعة بقيت hex عمداً (ورق أبيض). حرّاس +3.
  **884/884** (+ المتصفح 59/59 والسيناريوهات 58/58).

- **v18.9wl — تثبيت قاعدة «كل تغيير = تحديث NOTES» بحارس اختبار (`hail-tests.js` + `CLAUDE.md` + `NOTES.md`):**
  بطلب المستخدم: توثيق NOTES كان قاعدةً نصيةً بلا إنفاذ («مع كل تغيير جوهري») فيمكن
  نسيانها. الآن القاعدة **إلزامية لكل تغيير** ويحرسها فحصان في `hail-tests.js`:
  وجود «§6 سجل موجز بأهم التعديلات»، ووجود قيدٍ للإصدار الحالي حرفياً (`**vX.Y — `)
  مطابقاً لـ`APP_VERSION` — فأي رفع إصدارٍ يُدفع بلا قيده في NOTES يُسقط الاختبارات
  (نفس فلسفة حارس تطابق الإصدار/cache-busters). حُدّثت الصياغة في `CLAUDE.md` §المرجع
  ورأس `NOTES.md`. (كان القيد أصلاً v18.9wk وتزامن مع دمج وحدة الرقابة المالية بنفس
  الرقم، فرُفع إلى wl عند حل التعارض — تعديلان لا يتشاركان إصداراً.) فحوص +2. **881/881**.

- **v18.9wk — وحدة الرقابة المالية على المشتريات (`finance-audit.js` — جديدة + ربط في `index.html`):**
  بطلب المالك: أسلوب رقابي شهري للمالية على المشتريات — النظام (لا البشر) يحدد عينة من
  الطلبات **المغلقة فعلياً** في الشهر السابق لتدقيقها بعد التنفيذ (هل كان مورد آخر يورّد
  نفس البنود بسعر أقل؟). **آلية العينة الهجينة:** (١) طبقة **عشوائية حتمية** — seeded
  (البذرة من مفتاح الشهر + معرّفات كل المرشّحين، وبصمتها تُخزَّن مع الدورة) فلا يمكن
  «إعادة السحب» حتى تخرج عينة مريحة والاختيار قابل للتحقق؛ مرجّحة بالقيمة (الطلب الأغلى
  احتماله أعلى)؛ الحجم 10% بين 3 و10 (ثوابت أعلى الوحدة). (٢) طبقة **استثناءات آلية**
  تُدرج (بعلامة مميزة) أي طلب فيه إشارة خطر: فعليّه تجاوز أرخص عرض كامل مسجّل عليه
  (صافي×1.15 ضريبة + 5% تسامح)، أو تجاوز التقدير 25%، أو مبلغ ≥20000 بلا أي مقارنة
  أسعار. **المراجعة:** لكل عينة يبني النظام مقارنة استرشادية لكل بند — صافي سعر وحدتنا
  (من `itemCost−vat÷rcvQty`) مقابل أفضل بديل من التاريخ الشرائي (الطلبات المغلقة الأخرى
  خلال 6 أشهر، مطابقة بالتطبيع العربي كنمط stocktake) ومن كتالوج الأسعار، مع أرخص عرض
  على الطلب نفسه (`_pcGroupsFromPO` كاملي التغطية) وعروض يدوية يضيفها المدقّق — والوفر
  المحتمل يُحتسب تلقائياً. **الحكم ودورة الرد:** مطابق/فرق مقبول يُغلقان مباشرة؛
  ملاحظة/مخالفة تُحال للمشتريات ليكتبوا مبررهم (جودة/سرعة توريد/حد أدنى…) ثم المالية
  تغلق: قبول الرد أو **تصعيد للتنفيذي** — مع إشعارات وقيود تدقيق في كل خطوة، وتقرير
  شهري للطباعة (الالتزام + الوفر). **التخزين:** `global_finance_audits` وثيقة لكل شهر؛
  كل الكتابات معاملات على الوثيقة الطازجة (نمط substitute-budget) فلا تضارب بين جلستين،
  وإنشاء الدورة create-if-absent داخل معاملة (شهرٌ له دورة يُفتح ولا يُعاد سحبه).
  **الصلاحيات:** الإنشاء/المراجعة/الإغلاق للمالية والأدمن، الرد للمشتريات والأدمن،
  العرض لهم وللتنفيذي؛ زر القائمة الجانبية مخفي افتراضياً وتُظهره الوحدة للمخوَّلين.
  الوحدة نقية التحميل (لا DOM عند التحميل — قابلة للاختبار في sandbox) ومسجّلة في كاشف
  الوحدات القديمة، وبصمتها `MODULE_BUILD` محروسة بالاختبارات. فحوص +38. **879/879**
  (+ المتصفح 59/59 — الرحلة فتحت الصفحة الجديدة بلا أخطاء — والسيناريوهات 58/58).

- **v18.9wj — فحص دور المشرف يقبل القيمة العربية «مشرف» (`cleaning-operations.js` + `index.html`):**
  أبلغها المستخدم (حساب حنان — مشرف): زرّا «التنفيذ» و«جولات الجودة» غائبان رغم نشر
  v18.9wi. **الجذر:** قائمة الأدوار في إدارة المستخدمين تخزّن دور المشرف بالقيمة
  **العربية «مشرف»** (لا `supervisor`) — بينما `canExecute`/`canQuality` في وحدة النظافة
  تقارنان بالمفتاح الإنجليزي وحده، فلا يجتاز المشرفون الحقيقيون أيَّ فحص. أُضيف
  **`_isSupRole()`/`_isTechRole()`** يقبلان القيمتين (supervisor/«مشرف»، technician/«فني»)
  وبُنيت عليهما الصلاحيتان — من غير لمس الحسابات المخزّنة ولا تغيير قيم القائمة (تغييرها
  كان سيكسر الحسابات القائمة). وشمل الإصلاح إشعارات البلاغات الجديدة (`_hnRoleIn`) التي
  كانت تستثني «مشرف». **درس معماري:** أي فحص دور جديد يجب أن يمرّ بالفحصين المطبَّعين
  لا بمقارنة نصية مباشرة. فحوص +5.

- **v18.9wi — جولات الجودة للمشرف + هجرة إعادة تسمية المشرف (`cleaning-operations.js` + `index.html`):**
  بطلب المستخدم، أمران: (١) **المشرف يُنشئ جولة جودة** — كانت الجولات مقصورة على
  الأدمن/مدير المشروع (`canEdit`). أُضيفت **`canQuality()`** = الإدارة + المشرف (لا الفني)
  تفتح زرّ «جولات الجودة» والعرض والإنشاء، مع حصر نموذج الجولة بمباني المشرف المربوطة
  به عبر **`_qualityBuildings()`** (تقاطع `_buildings()` مع `myBuildings()` — غير المربوط
  يرى الكلّ كسياسة النطاق العامة، ومربوطٌ بلا مبانٍ يرى رسالة إرشاد). حذف الجولات يبقى
  للإدارة وحدها (حارس داخل `removeRound` لا إخفاء زرّ فقط)، وحارسا `newRound`/`saveRoundForm`
  يمنعان غير المخوَّل. (٢) **إعادة تسمية مشرف تُهاجر بيانات النظافة** (سارة ← ساره):
  الاسم مخزّن نصاً في أربعة مواضع — تجاوز المهمة (`t.supervisor`)، سطر التنفيذ
  (`r.supervisor`)، **مفتاح** خريطة الربط (`supervisorBuildings` — نطاق المشرف
  `myBuildings` يطابق اسم حسابه بهذا المفتاح **حرفياً**، فبقاؤه القديم يعمي المشرف عن
  مبانيه)، ومُنشئ الجولة (`r.by`). الآن `adminSaveSupervisor` يستدعي
  **`cleaningOps.onSupervisorRenamed(old,new)`** على نمط هجرة المبنى (v18.9wg): استعلامان
  بالحقل للمهام والسجل، دمجُ مفتاح الربط إن وُجد الاسمان، وتحديث الذاكرة المحلية وقيد
  تدقيق وتوست. **ملاحظة تشغيلية:** ليرى المشرفُ مهامَّه وحدها يجب أن يطابق **اسم حسابه**
  في إدارة المستخدمين اسمَ المشرف في خريطة «المشرفون والمناطق» حرفياً (مثال: «ساره الشمري»)
  ودوره `supervisor` — غير المطابق/غير المربوط يرى كل المهام (لا نُعمي المستخدم بصمت).

- **v18.9wh — «التغطية حسب المنطقة» و«المهامّ المتأخّرة» بعمودين (`cleaning-operations.js`):**
  بطلب المستخدم: القائمتان كانتا صفاً كاملَ العرض لكل عنصر فتطول الصفحة مع كثرة المباني.
  صارتا شبكة عمودين (`.co-2col` — قاعدة الأساس **قبل** ميديا الجوال وإلا غلبتها) في
  اللوحة التنفيذية (التغطية + المتأخّرات) والمتابعة اليومية (التغطية)، وعموداً واحداً
  على الجوال (≤560px). هوامش عناصر التغطية داخل الشبكة مصفّرة والمسافة من gap وحده.
  فحوص +4. **830/830** (+ المتصفح 58/58 والسيناريوهات 58/58).

- **v18.9wg — إعادة تسمية مبنى تُهاجر بيانات النظافة تلقائياً (`index.html` + `cleaning-operations.js`):**
  المستخدم غيّر اسم مبنى في لوحة الإدارة فبقيت المهام على الاسم القديم — بيانات النظافة
  تخزّن الاسم **نصاً** في أربعة مواضع: المهام (`t.building`)، سجلّ التنفيذ (`r.building` —
  تغطية الشهر وتقرير العميل يجمعان به)، ربط المشرفين (`supervisorBuildings`)، وتقييمات
  جولات الجودة (`ratings[].building`). الآن `adminSaveBuilding` بعد نجاح الحفظ يستدعي
  **`cleaningOps.onBuildingRenamed(old,new)`** فتُهاجَر المواضع الأربعة دفعةً واحدة:
  استعلام `where("building","==",old)` للمهام والسجل، استبدالٌ في خريطة الربط ثم `saveCfg`،
  وقراءة الجولات (عددها صغير) وتعديل الحامل للاسم وحده — مع تحديث الذاكرة المحلية
  (`_tasks`/`_rounds`) وقيد تدقيق وتوست، والفشل يُنبّه بإعادة المحاولة. مسوّرة
  بـ`isCleaningProject` فلا أثر على الصيانة/المشتريات. فحوص +7. **826/826**
  (+ المتصفح 58/58 والسيناريوهات 58/58).

- **v18.9wf — صلاحية «تشغيل النظافة» في إدارة المستخدمين (`index.html` + `cleaning-operations.js`):**
  عند إضافة مستخدم جديد لم تكن صلاحية تشغيل النظافة ضمن «تحديد الصلاحيات». أُضيف مفتاح
  **`cleaning`** على نمط بقية المفاتيح: مربّع «تشغيل النظافة» في نموذج الإضافة **ونافذة
  «صلاحيات ومشاريع»** (تعديل مستخدم قائم — الشارات تعرضه تلقائياً)، وإدخاله في مصفوفتَي
  الحفظ (`adminAddUser`/`saveUserPerms`)، وربطه في `_PERM_PAGE_MAP` بصفحة `cleaning-ops`
  فتحجب النواة فتحها المباشر عبر `showPage`. **وزرّ الوحدة** (المحقون ديناميكياً بعد
  `applyPermissions`) يقرأ `window._blockedPages` بنفسه فيختفي عن المحجوب ويعود عند المنح.
  المستخدم القديم بلا المفتاح = مسموح (توافق خلفي كبقية المفاتيح)، والأدمن يتجاوز دائماً.
  فحوص +5. **819/819** (+ المتصفح 58/58 والسيناريوهات 58/58).

- **v18.9we — إطلاق المهام في وقت محدد: لا تأخير وهمياً أثناء التجهيز (`cleaning-operations.js`):**
  مرحلة تجهيز المباني: المهام تُنشأ اليوم والتشغيل الفعلي يبدأ لاحقاً، فتُستحق فوراً
  ويتراكم «تأخير» بلا داعٍ. **(١) زر «إطلاق المهام»** في هيرو التشغيل (لمالكي الصلاحية):
  تختار مبنى بعينه (بعدد مهامه) أو «كل المباني» وتاريخَ بدءٍ، فتُعاد جدولة استحقاق كل
  المهامّ النشطة في النطاق إلى ذلك التاريخ (`saveTask({id, nextDueDate})` بدمجٍ لا يمسّ
  بقية الحقول) — فلا استحقاق ولا تأخير قبله، **ويُصفَّر أي تأخيرٍ متراكم**. الماضي مرفوض،
  والجمعة/السبت تُرحَّل لأول يوم عمل (`_nextWorkingDay`)، مع تدقيق «إطلاق مهام النظافة».
  **(٢) حقل «تاريخ بدء التنفيذ»** في نموذج التوليد بالذكاء الاصطناعي: المولَّد يُستحق من
  ذلك التاريخ لا من اليوم (الماضي يُقصّ لليوم). `_launchTargets(tasks, bld)` نقية مكشوفة
  ("" = كل المباني، الموقوفة تُستثنى)، ونموذجا الإطلاق والتوليد لا يفتحان معاً. فحوص +10.
  **814/814** (+ المتصفح 58/58 والسيناريوهات 58/58).

- **v18.9wd — الجذر الفعلي لبقاء نافذة «مهام أخرى» بعرض الشاشة (`cleaning-operations.js`):**
  رغم إصلاحَي wb/wc أبلغ المستخدم أن النافذة «ما زالت كما هي» بعرض الشاشة والإصدار واصلٌ
  (wc في التذييل). **التشخيص التجريبي:** أُعيد الإنتاج بمحاكاة CSS النواة كاملاً مع CSS
  الوحدة في Chromium حقيقي، وكشف `CSS.getMatchedStylesForNode` (عبر CDP) القاعدةَ الفائزة:
  النواة تفرض `width:100% !important` على `.card` ضمن قاعدة الداشبورد الجماعية
  (`.dash-top,...,.card,.filters,.buildings-grid`) — فتقهر أي عرضٍ عادي على النافذة
  (التي تحمل صنف `card`). **الحل:** عرض النافذة `min(440px,96vw) !important` — ورقة
  الوحدة تُحقن لاحقاً فتفوز عند تساوي الأهمية والتخصيص. **تحقّق فعلي** مع CSS النواة:
  440px في المنتصف بخلفية معتمة على 1900px، و96vw على 390px. حارسٌ يثبّت الـ!important
  في الوحدة **ووجودَ** قاعدة النواة القاهرة (إن أُزيلت مستقبلاً يُنبَّه لمراجعة الحاجة).
  **درسٌ عام:** عند وراثة صنفٍ من النواة (كـ`.card`) افحص قواعدها الـ!important قبل
  تجاوز خصائصها. فحوص +1. **804/804** (+ المتصفح 58/58 والسيناريوهات 58/58).

- **v18.9wc — نافذة «مهام أخرى»: أضيق (440px) والتفاصيل داخلها (`cleaning-operations.js`):**
  بطلب المستخدم: العرضُ صار كنافذة تفاصيل طلب الشراء أو أصغر (**440px** بدل 560)، والضغطُ
  على بطاقة مهمةٍ داخل النافذة يعرض تفاصيلها **داخل النافذة نفسها** (بيانات + قائمة فحص +
  سجلّ تنفيذٍ يُحمَّل) بزرّ «→ رجوع» للقائمة — لا انتقالَ لصفحة كاملة. **إعادة هيكلة:**
  جسمُ التفاصيل استُخرج لدالةٍ موحّدة `_taskDetailBodyHTML(t, log)` تستعملها صفحةُ التفاصيل
  الكاملة والنافذةُ معاً (مصدر واحد — أي تحسين مستقبلي يظهر في الموضعين). الالتقاط عبر
  `data-tid` على البطاقة و`stopPropagation` في مرحلة **capture** (يمنع onclick الأصلي الذي
  يفتح الصفحة الكاملة)، وأزرار تنفيذ/تحرير تبقى تُغلق النافذة ليظهر ما فتحته، والحالة
  تُصفَّر عند الإغلاق. فحوص +7 وتحديث 3. **803/803** (+ المتصفح 58/58 والسيناريوهات 58/58).

- **v18.9wb — نافذة «مهام أخرى»: أصغر وفي المنتصف + ESC (`cleaning-operations.js`):**
  بملاحظة المستخدم أن نافذة wa غير متناسقة مع الشاشة (عريضة وبطاقاتها تتناثر): صارت
  **في منتصف الشاشة تماماً** (رأسياً وأفقياً) بعرضٍ أقصى **560px** وارتفاعٍ أقصى 80vh،
  وبطاقاتها **عمودٌ واحدٌ** مرتّب يتمرّر داخلياً (`.co-bld-modal .co-tasklist{1fr}`).
  و**ESC** يغلقها — المستمع يُركَّب عند الفتح ويُفكّ عند الإغلاق (لا مستمع دائم).
  فحوص +3. **796/796** (+ المتصفح 58/58 والسيناريوهات 58/58).

- **v18.9wa — «مهام أخرى» في نافذة منبثقة لا تمدُّداً لأسفل (`cleaning-operations.js`):**
  بطلب المستخدم: زرُّ «عرض N مهام أخرى» في لوحة اليوم/المتابعة اليومية صار يفتح **نافذةً
  منبثقة** فيها كلُّ مهامّ اليوم للمبنى (بترتيب الأولوية + شارة المنجز + زرّ إغلاق) بدل
  توسيع القائمة في مكانها وإطالة الصفحة. النافذة تُركَّب على `body` مباشرةً فتعمل من
  الشاشتين، وقائمتها تُبنى عند الفتح من نفس مصدر اللوحة (`visibleTasks` + `isDue/doneToday`
  + `dueStatus`). الخلفية تُغلق، وأيُّ إجراءٍ داخلها (تنفيذ/تحرير/تفاصيل) **يغلقها أولاً**
  ليظهر ما فتحه — بمستمع capture فلا يعطّله `stopPropagation` في أزرار البطاقة — وتُغلق مع
  تبديل الشاشة/المشروع. أُزيلت حالة التوسيع في المكان (`_expandedBlds`) نهائياً. فحوص
  مُحدَّثة +8. **793/793** (+ المتصفح 58/58 والسيناريوهات 58/58).

- **v18.9vz — إصلاحان من بلاغ المستخدم (`index.html` + `cleaning-operations.js`):**
  **(١) جلب أرشيف البلاغات بلا فهرس مركّب:** كان يفشل بـ`FirebaseError: The query requires
  an index` — `where("archived")+orderBy(createdAt)` يتطلّب فهرساً مركّباً، ومجموعات
  البلاغات مسمّاة لكل مشروع (`{id}_tickets`) فيلزم إنشاء الفهرس **يدوياً لكل مشروع جديد**
  (لا يُغطّى بـ`firestore.indexes.json` الثابت). أُزيل الـ`orderBy` الخادمي والفرز صار
  محلياً بعد الجلب (الأحدث أولاً — يقبل ISO وTimestamp) — فلا فهارس مطلوبة لأي مشروع
  حاضراً أو مستقبلاً. **(٢) عمود المبنى في لوحة النظافة 360→400px:** ستة أعمدة كانت
  تنضغط في صفٍّ واحد على الشاشات العريضة بدل نزول السادس لصفٍّ جديد. فحوص +2. **790/790**.

- **v18.9vy — ترتيب مباني اللوحة: الأكثر تأخّراً أولاً (`cleaning-operations.js`):**
  بطاقات المباني في «لوحة اليوم» و«المتابعة اليومية» كانت تُرتَّب أبجدياً فيُدفن المبنى
  المتعثّر وسط القائمة. الآن **مصدر واحد** `_bldOrder(byB)` تمرّ عبره الشاشتان: الأكثر
  مهامّاً **متأخّرةً** أولاً، ثم الأكثر **متبقّياً** اليوم (isDue نفسها — المنفَّذة اليوم
  لا تُحسب)، وعند التعادل الكامل أبجدياً (ترتيب مستقرّ). فيتصدّر ما يحتاج معالجة، ومع
  العرض المضغوط (vx) تُظهر أول شاشةٍ أهمَّ مبنيين وأهمَّ بطاقتين فيهما. فحوص +5.
  **788/788** (+ المتصفح 58/58 والسيناريوهات 58/58).

- **v18.9vx — عرض مضغوط في لوحة النظافة: بطاقتان لكل مبنى + زر توسيع (`cleaning-operations.js`):**
  «لوحة اليوم» و«المتابعة اليومية» كانتا تعرضان كلَّ بطاقات كل مبنى فتطول الصفحة وتتفاوت
  الأعمدة. الآن يعرض كل مبنى **أول بطاقتين فقط** بترتيب الأولوية القائم (متأخّر ← مستحقّ ←
  منجز — فالظاهر دوماً الأكثر إلحاحاً) وتحتهما زرّ «▼ عرض N مهام أخرى» يفتح البقية في
  مكانها ويطويها («▲ طيّ»). **مصدر واحد** `_cappedTaskListHTML(b, list)` تستعمله الشاشتان
  (`boardHTML` و`_byBuildingGrid`) — لا قصّ متفرّق. حالة التوسيع لكل مبنى (`_expandedBlds`)
  تنجو من إعادة الرسم بعد التنفيذ وتُصفَّر مع تبديل المشروع، والمفتاح يُمرَّر بالـ
  URI-encoding فلا يكسر `onclick` مهما كانت محارف اسم المبنى، والتبديل يعيد رسم الشاشتين
  معاً. اختيار المستخدم من ثلاثة حلول معروضة (توسيع في المكان لا عدّاد ولا صفّين).
  فحوص +11. **783/783** (+ المتصفح 58/58 والسيناريوهات 58/58).

- **v18.9vw — «مبنى مرجعي» في التوليد الكامل: مبنى جديد على منوال القائم (`cleaning-operations.js`):**
  توليدُ جدول مبنى جديد كان عامّاً (٨–١٢ مهمة نمطية) فلا يرث تعديلات المستخدم على جداول
  المباني القائمة. أُضيف منتقي **«مبنى مرجعي»** لنموذج التوليد (الوضع الكامل فقط، ويظهر
  فقط بوجود مبانٍ ذات مهام): **تلقائي** = المبنى الأكثر مهامّاً عدا المستهدف، أو اختيارٌ
  يدوي، أو **بلا مرجع** = التوليد العام القديم حرفياً. مع المرجع يُمرَّر جدولُه الفعلي
  للنموذج (الاسم | نوع العمل | التكرار | بنود الفحص — حتى 30 مهمة، والبنود حتى 6) مع أمرٍ
  بمحاكاة نمطه وعدد مهامّه وتكييف ما يلزم لوصف المبنى الجديد — فيرث الجديدُ ما ضبطه
  المستخدم من قبل. المرجع داخل `_genPrompt` النقية نفسها (نفس شكل JSON — مسار تحليل
  واحد)، ويُتجاهَل في وضع «مهام محددة»، وسجلُّ التدقيق يذكر «على منوال …». فحوص +11.
  **772/772** (+ المتصفح 58/58 والسيناريوهات 58/58).

- **v18.9vv — وضع «إضافة مهام محددة» في توليد مهام النظافة بالذكاء الاصطناعي (`cleaning-operations.js`):**
  المستخدم ولّد جدول مبنى كاملاً ثم ظهرت مهمة جديدة يلزم إضافتها — وإعادة التوليد الكامل
  تكرّر 8–12 مهمة قائمة (الموجّه لا يعرف الموجود). صار نموذج التوليد **بوضعين**: «جدول كامل»
  (السلوك القائم حرفياً) و«إضافة مهام محددة لمبنى قائم» — يصف المستخدم المهمة (مثلاً «تنظيف
  خزانات المياه شهرياً») فيولَّد الموصوف **وحده** ببنود فحص مناسبة، مع تمرير أسماء مهامّ
  المبنى القائمة (النشطة، من كل المشروع لا نطاق المستخدم، حتى 60) للنموذج مع أمرٍ صريح
  بعدم التكرار. بناء الموجّه في دالة نقية مكشوفة **`_genPrompt`** (مصدر واحد للوضعين، نفس
  شكل JSON فمسار التحليل/الإنقاذ واحد)، حقل الوصف يظهر/يخفى بلا إعادة render (`genModeChanged`
  — لا تضيع القيم المكتوبة)، الوصف إلزامي في الوضع المحدد، وسقفه 10 مهام. المُولَّد يبقى
  اقتراحاً يُضاف للقائم ولا يستبدله. فحوص +13. **761/761** (+ المتصفح 58/58 والسيناريوهات 58/58).

- **v18.9vu — دفعة إصلاحات من الفحص العميق (أغسطس 2026):** تنفيذ توصيات `docs/deep-review-2026-08.md`
  عدا قواعد Firestore (مؤجَّلة كـ«المرحلة ٣»). **ذرّية حركات المخزون:** (C3) حذفُ طلب الشراء يعكس
  المخزون **ويحذف الطلب في معاملة واحدة** — فلا نافذة يُعكَس فيها المخزون ثم يفشل الحذف فيُضاعَف عند
  إعادة المحاولة. (C4) `doInventoryReview` صار **ينتظر تأكيد حفظ الطلب** (`savePurchaseAwait` بإعادة
  محاولة) — فلا يبقى الرصيد مخصوماً والطلب غير محفوظ فتعيد جلسةٌ أخرى الخصم. (H8) النقلُ بين المستودعين،
  و(M4) أمرُ الصرف وصرفُ العهدة، و(M5) إرجاعُ/حذفُ العهدة — كلها في **معاملات ذرّية** (`_atomicStockMove`
  + معاملات مركّبة عبر المجموعات) تمنع فقدان/مضاعفة الكميات، والرصيدَ السالب (قصرٌ عند الصفر مع تسجيل
  الفعليّ)، وقراءةُ `returnedQty` الطازجة داخل معاملة الإرجاع تمنع ضياع إرجاعٍ متزامن. **واتساب:** (C5)
  حجزُ الرسالة ذرّياً (`queued`→`sending`) قبل الإرسال + استعادةُ العالق في `sending` — exactly-once فلا
  رسائل مكرّرة؛ (M15) تطبيعُ الأرقام `05…`→`966…`. **أمان (عميلي، بلا قواعد):** (H4) تهريبُ اسم الفني/نوع
  العمل في تقرير الصيانة، (H5) استبدالُ `esc`→`_jsq` (عبر `JQ`) في زرّي حذف كتالوجَي الأسعار/المصنعيات
  (كان XSS مخزَّناً في مجموعةٍ مشتركة). **اتساق:** (H6) إصلاحُ ترقيم الكتالوجَين (جسور `render*Table`
  عامة)، (M2) عمودُ «التكلفة الفعلية» في التقرير/Excel/PDF يعرض الفعليّ **للمغلق فقط** لا التقدير، (H10)
  `rejected_final` ضمن الحالات الميتة في رصيد الاستعاضة. حرّاسُ اختبار +٢٤ لكل صنف. **748/748**.
  **مؤجَّل صراحةً:** قواعد Firestore/مصادقة الفنيين (C1/C2/H1/H2/H3/M10…)، ودلالةُ `delivered`/`executed`
  في إدارة المشاريع (H9/M1 — قرار منتج)، وسباقُ تدقيق طلبٍ واحد من جلستين (M3 — يحتاج معاملة على وثيقة الطلب).

- **v18.9vt — السبب الجذريّ لانهيار لوحة المعلومات: إزالة الاعتماد على مُعدِّلات `Date`:**
  خاتمةُ سلسلةِ تعقّبٍ (vq→vs) لانهيار «Aw, Snap» عند دخول **مشروعٍ بعينه**. بعد أن كسر
  كاشفُ vs الدوّامةَ وأتاح الوصول، ظهر الخطأ الفعليّ بمكدّسٍ كامل:
  `Method Date.prototype.setHours called on incompatible receiver [object Date]` من
  `workingMinutesBetween` (عبر `_elapsedHByTier` ← `_closeWorkH` ← `Array.reduce` ←
  `renderKPIData` ← `renderDashboard`). **السبب:** كائن `Date` العام قد يكون ملفوفاً/معدَّلاً
  في بيئة المستخدم (إضافات المتصفّح تفعل ذلك)، فتفشل **مُعدِّلات** Date (`setHours`/`setDate`)
  بينما البناء والقراءة يعملان. **والأخطر:** تقدّمُ الحلقة كان يعتمد على
  `cur.setDate(cur.getDate()+1)` نفسِه — فإن لم يُقدِّم صارت **حلقةً لا نهائية** تستنزف
  الصفحة حتى ينهار المتصفّح (بل وChrome بأكمله مرّة). ويفسّر ذلك اقتصارَ العطل على مشروعٍ
  واحد: لا يُبلَغ هذا المسار إلا عبر **البلاغات المغلقة** (`reduce` على المغلقات) فالمشاريع
  بلا مغلقات لا تصله — كما يفسّر سلامةَ الآيباد (بيئةٌ بلا تلك الإضافة). **الإصلاح** في
  `workingMinutesBetween` و`addWorkingMinutes` معاً: **صفر مُعدِّلات Date** (بناءٌ بالمُنشئ
  وحسابٌ بالطوابع الزمنية)، والتقدّم يوماً بيوم ببناء تاريخٍ جديد لا بتعديل القائم، و**حارسٌ
  صلب** (20000 دورة) يجعل الحلقة اللانهائية مستحيلةً مهما فسدت البيئة، وحمايةٌ من التواريخ
  غير الصالحة. **الحساب مطابقٌ للأصل**: قورن على 4004 حالة ⇒ صفر اختلاف، وعلى مرجعٍ مستقلٍّ
  يعدّ دقيقةً بدقيقة (600 حالة) ⇒ صفر انحراف؛ ومحاكاةُ بيئةٍ مكسورة أثبتت أن القديم يرمي
  الخطأ نفسه بينما الجديد يعمل. **درس عام: لا تعتمد على مُعدِّلات `Date` في منطقٍ حرج —
  ابنِ تواريخ جديدة بالمُنشئ، ولا تجعل تقدّم حلقةٍ يعتمد على مُعدِّل قد يفشل.**
- **v18.9vs — كاشف انهيار لوحة المعلومات الذاتي (شبكة أمان دائمة):** انهيار الـ renderer/GPU
  **لا يلتقطه JS إطلاقاً** (تموت العملية قبل أي معالِج) فلا يُصلَح بـ `try/catch`. الحل:
  **سنتينل في `localStorage` لكل مشروع** (`hail_dash_render:{projectId}`) يُرفَع *قبل* بدء
  عرض اللوحة ويُخفَض *بعد* اكتماله؛ فبقاؤه مرفوعاً عند الدخول التالي = **العرضُ السابق
  انهار** → تُتخطّى لوحة ذلك المشروع تلقائياً مع بطاقةٍ توضيحية وزرّ «محاولة عرض اللوحة
  مرّةً واحدة» (علَم `hail_dash_force` في `sessionStorage`). تنكسر الدوّامة بلا أي تدخّل
  ويصل المستخدم لبقيّة النظام. **لا أثر على السليم**: لا انهيار ⇒ السنتينل يُخفَض دائماً ⇒
  اللوحة تُعرَض طبيعياً. **عامٌّ** — يحمي أي مشروع ولأي سببِ انهيارٍ مستقبليّ، ويبقى قائماً
  بعد إصلاح الجذر في vt.
- **v18.9vq/vr — وضع التشخيص الآمن (`?diag=1` ثم `?diag=2`):** أداةُ التعقّب التي حسمت
  الموضع حين تعذّر أي تسجيل. `_diagMark` يحفظ مراحل الدخول والذاكرة في `localStorage`
  (تبقى بعد موت التبويب — تُقرأ بـ `hailDiagLog()`) ويعرضها على شريطٍ حيّ؛ `?diag=1` يتخطّى
  اللوحة و`?diag=2` يُشغّلها بعلاماتٍ دقيقة لكل جزء. أثبت `diag=1` أن الانهيار داخل
  `renderDashboard` وأن الذاكرة 88MB على 387 بلاغاً — **فليس نفاد ذاكرة ولا حجم بيانات**.
  التفاصيل التشغيلية في §5.
- **v18.9vp — ترقية Firebase SDK 12.15.0 → 12.17.0:** النسخة 12.15.0 هي المطروح عليها خلل
  `INTERNAL ASSERTION FAILED` (ca9/b815/da08) في تيّار Watch، وأصدرت Google إصلاحاتٍ لهذا
  الصنف في 12.16/12.17. رُفعت وسوم الـ CDN **الخمسة** على gstatic
  (app/auth/firestore/storage/app-check) دفعةً واحدة — الترقية ضمن نفس الإصدار الرئيسي فواجهة
  الـ compat مستقرّة (صفر تغييرٍ في أي استدعاء). فحصٌ حارسٌ يثبّت أن الوسوم الخمسة على
  **نسخةٍ واحدة موحّدة** (ترقيةٌ مجزّأة تُدخل النظام في حالةٍ غير مُختبَرة).
  ملاحظة: `tech-app.html` (تطبيق الفنيّين) ما زال على 10.12.0 — منفصلٌ وغير متأثّر.
- **v18.9vo — كسر حلقة انهيار Firestore عند المنبع:** خلل الـ SDK يُفسد تيّار Watch فيبقى
  يرمي `INTERNAL ASSERTION FAILED` **كل ~ثانية بلا توقّف**؛ ومعالج الأخطاء كان يسجّل ويحاول
  التعافي مع كل رمية، فيتراكم استهلاك الموارد حتى ينهار التبويب (`STATUS_ACCESS_VIOLATION`).
  والتعافي القائم (`_fsAutoRecover`) يعيد التحميل لكنه **لا يوقف التيّار المكسور**. الآن عند
  **أول** assertion يُستدعى `_fsHardHalt` فيُنهي عميل Firestore فوراً (`db.terminate()`)
  فيتوقّف التدفّق من جذره، ثم يتكفّل `_fsAutoRecover` بإعادة التحميل؛ وأيُّ assertion لاحق
  يُبتلَع صامتاً (علَم `_fsHalted`) فلا يعيد الحلقة ولا يلوّث سجلّ الأخطاء.
- **v18.9vn — المقارنة الذكية: عرض ملفات العروض الأصلية في نتيجة المقارنة:** كانت ملفات
  عروض الأسعار التي يرفعها مسؤول المشتريات (`g.aiFiles`) **محفوظةً فعلاً** لكنها لا تظهر في
  نتيجة المقارنة المعروضة في تفاصيل الطلب — فيتعذّر على المراجِع الرجوع للعرض الأصلي
  للتحقّق. الآن تظهر روابطُ العروض (اسم المورّد إن وُجد + اسم الملف، بأيقونة PDF/صورة تُفتح
  في تبويب جديد) أعلى جدول المقارنة الذكية **وفي طباعة الـ PDF**. دالةُ عرضٍ جديدة
  `_pcRenderAIQuoteFiles` وخيار `opts.files` على `_pcRenderAIExtractTable`؛ الملفات تُقرأ من
  المجموعة المُعاد بناؤها (`_pcGroupsFromPO`) التي تحفظ `aiFiles` أصلاً — بلا مساسٍ بالتخزين
  ولا بمنطق التحليل.
- **v18.9vl — مراجعة شاملة: أربعة إصلاحات (`index.html` + `cleaning-operations.js` + CI):**
  حصيلةُ فحصٍ شاملٍ للنظام (أكواد/معمارية/تداخلات/حرجية). **(#3 — حرج، صحّة حسابية)**
  الاستلامُ الجزئيُّ لنفس البند عبر جلستين كان يخزّن في `p.items` **كميةَ وتكلفةَ الجلسة
  الأخيرة وحدها** (`rcvQty: a.rcvQty` في إعادة بناء بنود `doWarehouseAudit`) دون التراكم —
  فبندٌ استُلم 5 ثم 5 يُخزَّن 5، وتنقص `poReceivedQty`/`poActualCost` (المشتقّتان من
  `p.items` منذ v18.9tk) إلى النصف، بينما `grnDocs` يبقى صحيحاً عبر `_waPrevRcv` فيُغلق الطلب.
  حارسُ v18.9tn (C1) عالج البنودَ **المختلفة** لا نفسَ البند المُقسَّم. الآن يُعاد بناء البند
  على `cumRcv` التراكمي (المحسوب أصلاً)، وتُعاد التكلفة (`itemCost/vat/lineTotal`) على
  `cumRcv` بنفس اصطلاح الوحدة (H3) و`_poItemLine` فيتّسق المخزَّن مع ما يشفيه `_poHealItems`.
  `stockQty`/`directQty` تبقيان لكل جلسة: عكسُ المخزون عند الحذف يقرأ `grnDocs` لا `p.items`
  (v18.9rv) فلا أثر. **(#2 — H1، XSS مخزّن)** تقريرُ الطلبات كان يُدرج `p.id`/`itemName`/
  `supervisor` بلا `esc()` بينما الخلايا المجاورة في نفس الصف تُهرَّب — أُغلق. **(#5a)**
  مراجعةُ المخزون الفاشلةُ **قبل** تطبيق الخصم تُطلق الحارسَ الدائم `_reviewedPOIds` (عبر علم
  `_stockApplied`) فتُمكن إعادةُ المحاولة بلا خصمٍ مزدوج — كان الطلب يُحبَس «مُراجَعاً» بلا
  مراجعةٍ رغم أن التوست يدعو لإعادة المحاولة. **(#5b/#5c)** كاشفُ الوحدات القديمة صار يسجّل
  `cleaning-operations.js` (لا `purchase-kpi` المدموجةَ وحدها التي لا تتقادم أبداً) وبصمتُها
  `MODULE_BUILD` تُطابق `APP_VERSION` — حارسٌ في hail-tests يمنع الانحرافَ الصامت الذي بلغ
  `vi≠vk`؛ ومُرشّحُ مسارات CI صار يشمل `cleaning-operations.js`/`project-management.js`
  (كانت الاختباراتُ تفحصهما لكن دفعاً مباشراً يمسّهما وحدهما يتخطّى CI). فحوص +١٣. **700/700**.

  **بنودٌ مرصودةٌ ومؤجَّلة (من المراجعة، لم تُعالَج هنا):** أضعفُ حلقةٍ أمنية = `firestore.rules`
  الفضفاضة (القاعدة العامة: أي دورٍ يكتب كل شيء؛ وإضافاتُ الفنيين المجهولة تقرأ/تكتب
  `technicians`/`*_tickets`) ⇐ تحتاج «المرحلة ٣» (قواعد مُقيَّدة بالدور/الحقل + مصادقة فنيين
  خادمية)؛ ووسيطُ الذكاء يُستدعى بلا `Authorization` (مُرحِّل مفتوح) ⇐ يحتاج إرفاق ID token
  والتحقّق في الـ Worker. وهيكلياً: شبكتا مراقبةٍ على `document.body` بالكامل (S3)، وتغليفُ
  `showPage` معتمِدٌ على ترتيب التحميل (S4).

- **v18.9vk — إتمام دمج المشروع اليدوي: حذف الاسم اليدوي المتبقّي (`index.html`):**
  بعد دمج طلبات مشروعٍ يدويٍّ في مشروعٍ رسميٍّ (v18.9vj) كان الاسمُ اليدويُّ يبقى ظاهراً
  «(يدوي)» في قوائم «طلب شراء جديد» — لأن `_manualProjectNamesAll` يقرأ من مصدرين: الطلبات،
  **وقائمةً محفوظةً** في `meta/manual_projects` (يكتبها `_saveManualProjectName` عند أول إدخالٍ
  يدوي) مستقلّةً عن الطلبات؛ فنقلُ الطلبات وحده لا يمسّها. **الإصلاح:** (١) `doMergeProject`
  بعد نقل كل الطلبات يحذف الاسم من القائمة المحفوظة (`_mpRemoveMetaName` عبر `arrayRemove`)
  متى لم يبقَ طلبٌ يدويٌّ بالاسم، فيكتمل الدمج تلقائياً. (٢) زرُّ **«حذف الاسم اليدوي»**
  (`doDeleteManualName`) في نافذة الدمج يزيل أيَّ اسمٍ يدويٍّ متبقٍّ بلا طلبات (للحالات المدموجة
  سابقاً) — بلا مشروعٍ هدفٍ ولا سبب، ويرفض الحذف إن بقيت طلباتٌ مرتبطةٌ (يدعو للدمج أولاً).
  مقصورٌ على المسؤول، ويعيد استخدام `showConfirm`/`logAudit`. فحوص +٥. **687/687**.

- **v18.9vj — دمج مشروع يدوي في مشروع رسمي: إعادة ربط طلبات الشراء (`index.html`):**
  عند تحوّل مشروع (كأعمال النظافة) من الإدخال اليدوي (`projectId:"__OTHER__"`/`isCustomProject`
  + اسمٌ نصّي) إلى مشروعٍ رسميٍّ في النظام، تبقى طلباتُه القديمة على الشكل اليدوي: تظهر مكرّرةً
  في قوائم «طلب شراء جديد» عبر `_manualProjectNamesAll` (ويتعذّر انطواؤها لاختلافٍ إملائيٍّ
  كالهمزة: «اعمال» ≠ «أعمال»)، **وتُنسب تكاليفها لدلوٍ يدويٍّ منفصل** (`__CUSTOM__:الاسم` في
  مؤشّرات الأداء) لا لمعرّف المشروع الرسمي — فيبدأ المشروع الرسمي بلا تاريخه الشرائي. **الحل:**
  أداةُ أدمن في المشتريات المركزية (زرُّ «دمج مشروع يدوي» ← `modal-po-merge-project`) تختار
  الاسمَ اليدويَّ (مع عدد طلباته) والمشروعَ الرسميَّ الهدف، فتُعيد كتابة كل الطلبات المطابقة
  دفعةً واحدة (Firestore batch) إلى `projectId` الرسمي مع `isCustomProject:false` وقيدٍ زمنيٍّ
  لكل طلبٍ وسجلٍّ للتدقيق. بذلك يختفي الاسمُ اليدويُّ من القوائم وتنضمّ كلُّ التكاليف السابقة
  لمؤشّرات وموازنة المشروع الرسمي. مقصورةٌ على المسؤول في الوضع المركزي، وتعيد استخدام
  `_manualProjectNamesAll`/`PURCHASES_COLLECTION`/`showConfirm`/`logAudit`. فحوص +٦. **682/682**.

- **v18.9vi — طباعة تقرير جولة التفتيش لكل جولةٍ على حدة (`cleaning-operations.js`):**
  زرُّ «طباعة تقرير الجولة» في تفاصيل الجولة يُصدّر مستنداً رسميّاً A4 لتلك الجولة: ترويسةٌ
  بالمشروع والتاريخ والمفتِّش · بلاطاتُ الدرجة · المخالفات · **تقييمٌ تفصيليٌّ بالنجوم مجمَّعٌ
  حسب المبنى** (نجومٌ ملوَّنة تُطبَع عبر `print-color-adjust`) · صورُ الجولة · سطرا الاعتماد.
  يعيد استخدام نافذة الطباعة القائمة `_openPrintWindow` (بلا مكتبات)، مكمّلاً تقريرَ العميل
  الشهري (v18.9vg) بمستندٍ لكل جولة. `_buildRoundReportHTML` مكشوفةٌ ومُختبَرة. فحوص +٤. **676/676**.

- **v18.9vh — إصلاح «عدم تحميل البيانات مباشرة» في «خريطة المباني» و«لوحة الإدارة» (`index.html`، نواة):**
  `loadSettings` (تحميل المباني/المشرفين من Firestore) غيرُ متزامنة وتُطلَق بلا انتظار في
  `loadData`، و`repopulateAllSelects` تملأ القوائم المنسدلة فقط لا الصفحة — فإن كان المستخدم
  على صفحةٍ تعتمد على `BUILDINGS`/`SUPERVISORS` وقتَ إطلاقها رُسِمت فارغةً («إجمالي المباني=0»)
  وبقيت كذلك بلا تحديثٍ يدوي (يبرز بعد إعادة التهيئة عقب تصدير PDF على iOS الذي يُبحر بـblob
  ويعود). **الإصلاح:** بعد `repopulateAllSelects` تُعاد رسمُ الصفحة النشطة إن كانت `buildings`
  أو `admin-panel`، وأُضيفت لوحةُ الإدارة إلى توزيع `renderCurrentPage`. تغييرٌ نواةٌ يفيد كلَّ
  المشاريع بلا مساسٍ بمنطقٍ آخر. فحوص +٢. **672/672**.

- **v18.9vg — تقرير العميل (PDF) لمشاريع النظافة (`cleaning-operations.js`):**
  مستندٌ رسميٌّ يُثبت الأداء لتجديد العقد، يجمع في صفحةٍ واحدة: ترويسةً بالمشروع والفترة ·
  ملخّصاً تنفيذيّاً (جودةُ الشهر · التغطية · تنفيذاتُ الشهر · انقضاءُ العقد) · مؤشّرات
  الالتزام بالجدول (CLN-01…06) · جودةَ النتيجة (الاتّجاه الشهري + أضعفُ أنواع العمل) ·
  التغطيةَ حسب المنطقة · جولاتِ التفتيش والمخالفات · سطورَ الاعتماد. **يعيد استخدام نافذة
  الطباعة القائمة** (`_openPrintWindow`) فالعربيةُ سليمةٌ RTL بلا مكتباتٍ جديدة (لا jsPDF)،
  والمستندُ ذاتيُّ الأنماط للطباعة على A4. يُبنى بالكامل من الدوال القائمة
  (`cleaningKPIs`/`coverageByBuilding`/`qualityTrend`/`contractProgress`) — إعادةُ استخدامٍ لا
  حساباتٍ جديدة. **الفترة:** الشهر الحالي؛ و**أداءُ المشرفين داخليٌّ لا يظهر للعميل** (قرار).
  `_buildClientReportHTML`/`_monthName` مكشوفتان ومُختبَرتان. مُقيَّدٌ بمشاريع النظافة، صفر
  مساس بالصيانة/المشتريات. فحوص +٦. **670/670**.

- **v18.9vf — جولات الجودة بالتقييم (§٣-٣ — البند المؤجَّل الأخير، `cleaning-operations.js`):**
  تفتيشٌ دوريٌّ يقيس **جودةَ النتيجة** (لا الالتزام بالجدول ولا زمن الشكوى): المفتِّش
  (أدمن/مدير المشروع) يُقيّم كلَّ نوع عملٍ في كل مبنًى **بالنجوم ١–٥** + مخالفاتٍ وصور،
  فتُنتج درجةً عامةً (متوسّطُ النجوم ونسبتُه المئوية) و**اتّجاهَ جودةٍ شهريّاً** يُعرَض للعميل.
  **البنية:** مجموعةٌ معزولةٌ بالمشروع `{id}_quality_rounds`؛ شاشةُ «جولات الجودة» view داخل
  «تشغيل النظافة» (قائمةُ الجولات + نموذجٌ بشبكة تقييمٍ مبنى×نوع + تفصيلُ الجولة)؛ وقسمُ
  «اتّجاه الجودة» أسفلَ مؤشّرات النظافة (متوسّطُ الشهر + أشرطةٌ شهريةٌ لآخر ٦ أشهر + أضعفُ
  أنواع العمل). يعيد استخدامَ رفع الصور (ضغط + Storage) والحفظِ المعزول بالمشروع، ويُصفَّر مع
  تبديل المشروع. الدوالُّ النقيّة `_roundScore`/`_qualityTrend` مكشوفةٌ ومُختبَرة. مُقيَّدٌ
  بـ`isCleaningProject`، صفر مساس بالصيانة/المشتريات. فحوص +١٣. **664/664**.

- **v18.9ve — الجمعة والسبت إجازةٌ في مشاريع النظافة (`cleaning-operations.js`):**
  قاعدةٌ ثابتة (مُقيَّدة بـ`isCleaningProject`) بثلاثة آثار: **(١) ترحيلُ الاستحقاق** —
  أيُّ `nextDueDate` يقع على جمعة/سبت يُرحَّل لأوّل يوم عمل (الأحد) عبر `_nextWorkingDay`
  و`_advanceDue`، ويُطبَّق في كل مواضع الضبط: التنفيذ (`executeTask`)، الإنشاء (`addTask`)،
  التوليد بالـAI، والتحرير اليدوي. فمهمةٌ يوميةٌ نُفِّذت الخميس تُستحقّ **الأحد** لا الجمعة.
  **(٢) لوحةُ اليوم في العطلة** تُظهر «إجازة اليوم» بلا مهامّ مستحقّة/متأخّرة (`isDue`/`isOverdue`
  تُرجعان `false` عبر `_isTodayHoliday`)، والتغطيةُ تُعرَض «إجازة» لا 0% عقابيّاً (اللوحة/
  المتابعة اليومية/CLN-01) — و`boardStats` يبثّ `holiday`. **(٣) التأخّر بأيام العمل فقط**
  (`_overdueWorkingDays`) فلا تُحتسب الجمعة/السبت. حسابُ العطلة نقيٌّ ومُختبَر بتواريخ صريحة
  (`_isWeekend`/`_nextWorkingDay`/`_advanceDue` مكشوفة). لا مساسَ بالصيانة/المشتريات.
  فحوص +١٢. **651/651**.

- **v18.9vd — إخفاءُ مجموعات القائمة غير المتعلّقة بعقد النظافة (`cleaning-operations.js`):**
  في مشروع النظافة لا معنى لأربع مجموعاتٍ في القائمة الجانبية: **إدارة الأصول**
  (`grp-assets`) · **المخزون** (`grp-inventory`) · **كتالوج البنود والأسعار** (`grp-catalog`)
  · **طلبات التسعير** (`grp-rfq`). أُضيفت `applyNavGroupVisibility()` تُخفي الترويسةَ والجسمَ
  معاً حين `isCleaningProject()`. **إخفاءُ عرضٍ فقط**: لا حذفَ بياناتٍ ولا لمسَ منطقٍ ولا
  تعديلَ `index.html`؛ ويُستعاد **ما أخفيناه وحده** (وسم `coHidden`) فلا يمسّ مجموعةً
  مخفيّةً أصلاً كالعُهد. يُعاد التطبيق عند التركيب وإعادة بناء القائمة وتبديل المشروع (نفس
  آلية إعادة حقن زرّ «تشغيل النظافة») فتعود المجموعاتُ كاملةً في مشاريع الصيانة/المشتريات.
  **طلباتُ الشراء وإدارةُ المشاريع تبقيان.** فحوص +٦. **639/639**.

- **v18.9vc — أداءُ المشرفين يقرأ كشفَ الربط لا تيّارَ المهامّ وحده (`cleaning-operations.js`):**
  المستخدم أبلغ تناقضاً: شاشةُ «المشرفون والمناطق» تُظهر سارة مربوطةً بثلاثة مبانٍ، بينما قسمُ
  «أداء المشرفين» في المؤشّرات يقول **«لا مشرفين مربوطين»** و**«لا مبنى مرتبطٌ بمشرف»**، والعدُّ
  يختلف (**١** بلا مشرف في القياس مقابل **١٤** في الربط). الجذر: `supervisorPerf` كانت
  **تستنبط المشرفين عكسيّاً من تيّار المهامّ/التنفيذات فقط** — فالمشرفُ المربوطُ بمبانٍ بلا مهامٍّ
  فيها بعد لا يحصل على صفٍّ فيختفي. **الإصلاح ثلاثة أجزاء:** (أ) **بذرُ الكشف من الخريطة**
  (`supervisorBuildings`): كلُّ مشرفٍ مربوطٍ يظهر بنطاقه (`zones` من الخريطة) ودرجةِ «—» (لا صفراً)
  حتى يُسجَّل عمل — مطابقاً فلسفةَ «ما لم يُقَس يظهر «—»»؛ وتوزيعُ المهمة/التنفيذ صار يقرأ **نفس**
  الخريطة المعتمَدة فلا يتباين عن البذر. (ب) **توحيد عدِّ «بلا مشرف»** عبر مصدرٍ واحد
  `unassignedBuildings` (= كلُّ `BUILDINGS` غير المسنَدة ضمن نطاق المستخدم) تقرؤه شاشتا الربط
  والقياس فيتطابق الرقمان. (ج) **تصحيح النصّ المُضلِّل**: «لا مبنى مرتبطٌ بمشرف» لا تظهر إلا حين
  لا ربطَ فعلاً؛ والمشرفُ المربوطُ يظهر بطاقةً بـ«—». النطاقُ محترَم، ولا مساسَ بالصيانة/المشتريات.
  فحوص +٦. **633/633**.

- **v18.9ue — الاستلام على مراحل: فاتورة السند السابق تُختار تلقائياً في التدقيق التالي:**
  امتداد لـ v18.9ud بطلب المستخدم — لا يكفي أن تكون الفاتورة السابقة **متاحة** كمرشّح، بل
  يجب أن تظهر **مختارةً بالفعل**. الآن إذا كانت فاتورةُ السندات السابقة **واحدةً** (الحالة
  الشائعة: مورّدٌ واحدٌ على مراحل)، تُختار تلقائياً في بطاقة الفاتورة عند فتح التدقيق:
  المورّد ورقم الفاتورة يُملآن من السند، والملف مُختارٌ من المنتقي، والحالة تقول **«✅ الفاتورة
  الأولى مرفوعة بالفعل ومختارة لهذا السند»** — فلا إجراء ولا إعادة رفع، وكل البنود المتبقّية
  تنتمي إليها (فاتورةٌ واحدة = لا عمود اختيار أمام البنود). لو تعدّدت الفواتير السابقة
  (موردون مختلفون) لا تُختار واحدةٌ تلقائياً (التباس) — تبقى مرشّحاتٍ للاختيار اليدوي. فحوص 451/451.
- **v18.9ud — الاستلام على مراحل بنفس الفاتورة: فاتورة السند السابق تبقى متاحة:**
  اشتكى المستخدم: عند تدقيق استلامٍ على مراحل (٣ بنود من ٥ ثم البندان المتبقّيان)، تختفي
  فاتورة السند الأول من نافذة التدقيق الثاني فيُضطر لإعادة رفعها. الجذر: منتقي «إعادة استخدام
  الفاتورة» (v18.9ua–ub) كان يقرأ **مرفقات الطلب فقط** — والفاتورة المُسجَّلة في التدقيق الأول
  تعيش في **سند الاستلام** (`grnDocs`) لا في المرفقات، فلم تظهر. الآن `openWarehouseAudit`
  يضيف **فواتير السندات السابقة** (‏`grnDocs[].invoicePhotoUrl` مع رقمها ومورّدها) كمرشّحات
  موسومة **«🧾 فاتورة سند سابق — المورّد — رقم …»**، فيختارها المدقّق للسند الجديد بلا إعادة
  رفع (نفس رابط Storage) ويُعبَّأ رقمها تلقائياً. لا تظهر في التدقيق الأول (لا سندات بعد).
  فحوص 450/450.

- **v18.9uc — منتقي إعادة استخدام الفاتورة يلتقطها مهما كان مسار رفعها:**
  بعد v18.9ua اشتكى مسؤول المستودع أنه لم يجد فاتورة المشتريات في التدقيق. الجذر: كان
  `_waProcInvoices` يقتصر على مرفقات `proc_invoice` — وهذه تُرفع من نافذة **«تم الشراء —
  إشعار المستودع»** حصراً؛ فإن رفع المشتريات الفاتورة من زرّ **«إضافة مرفق للطلب»** العام
  فنوعها `attachment` فلا تظهر في المنتقي. الآن `openWarehouseAudit` يجمع `proc_invoice`
  **+ أيّ مرفق عام** (`attachment` أو بلا نوع)، ويُستبعَد فقط ما ليس فاتورةً بطبعه (بيانات
  السداد `pay_info` وإيصال التحويل `transfer_receipt` — خاصّان بالمالية). كل خيار موسوم
  بوضوح (🧾 فاتورة المشتريات / 📎 وصف المرفق) مع اسم الملف ورقم الفاتورة إن وُجد. فحوص 449/449.
- **v18.9ub — خلل Firestore الداخلي (ca9/b815) على «جهازٍ محدّد ومستخدم الأدمن»:**
  المستخدم أبلغ أن أخطاء `INTERNAL ASSERTION FAILED (ID: ca9/b815)` تظهر في سجل الأخطاء
  **دائماً من كمبيوترٍ واحدٍ محدّد ولمستخدم الأدمن** — لا عشوائياً. الدليل الحاسم في اللقطة:
  `CONTEXT: {"Ce":-1,"targetId":1044}` — عدّاد أهداف المراقبة (`targetId`) بلغ **1044**.
  هذا العدّاد **تصاعديٌّ داخل عميل الـ SDK ولا يُصفَّر إلا بإعادة تحميل الصفحة** (SDK جديد).
  فالتشخيص: (أ) **الأدمن** أكثر الأدوار تركيباً/فكّاً للمستمعين وتنقّلاً بين المشاريع والوضع
  المركزي، (ب) **الجهاز المحدّد** = المحطّة التي **لا يُغلَق تبويبها أبداً** (تبقى مفتوحةً أياماً)،
  فيتراكم `targetId` حتى يبلغ منطقة الخلل — بينما بقيّة المستخدمين يُغلقون المتصفّح يومياً
  فيبدؤون بعدّادٍ منخفض. المخفّفات القائمة (§17: `forceLongPolling`، كاش ذاكرة، مستمعو
  `global_*` مرة واحدة، تعافٍ تلقائي `_fsAutoRecover`) تُقلّل الاحتكاك لكنها **لا تكفي مع عمرِ
  تشغيلٍ لا نهائيّ** (أيُّ احتكاكٍ ضئيل × زمنٌ لا نهائي ⇒ يبلغ الخلل). **الإصلاح جزءان:**
  • **إتمام قاعدة §17 لمستمعَي إشعارات سطح المكتب (`startHailNotifications`):** كانا يُفكّان
    ويُعاد تركيبهما مع **كل** استدعاءٍ (دخول مشروع + دخول المشتريات المركزية). مستمع **طلبات
    الشراء** يقرأ `global_purchases` (**مجموعة عامة** لا تتعلّق بالمشروع) فصار **يُركَّب مرة
    واحدة** (حارس idempotent)؛ ومستمع **البلاغات** (‏`_pfx` مرتبط بالمشروع) يُعاد تركيبه **فقط
    عند تغيّر المشروع فعلاً** (‏`_hnTicketsProjKey`). يُفكّان عند الخروج (`logout`/`logoutToLogin`)
    فيُعاد تركيبهما نظيفَين للجلسة التالية.
  • **تحديثٌ وقائيٌّ للجلسات الطويلة (يعالج «الجهاز الدائم التشغيل» جذرياً):** مستمعٌ على
    `visibilitychange` — إن تجاوز عمرُ الجلسة **٦ ساعات** وغادر المستخدمُ التبويبَ
    (‏`document.hidden`) يُعاد التحميل **صامتاً** بعد تأكيدٍ ٨ ثوانٍ، فيبدأ SDK جديد بعدّاد
    `targetId` صفريّ. لا يُطلَق أبداً والتبويبُ ظاهر فلا يقطع عملاً؛ المسودّات في `localStorage`
    و`onSnapshot` يعيد المزامنة فور العودة. فحوص +٨. **449/449**.

- **v18.9ua — منع ازدحام الطلب بنسختين من الفاتورة نفسها (إعادة استخدام لا إعادة رفع):**
  الجذر: مسؤول المشتريات يُرفق فاتورة المورد عند إشعار المستودع (تُحفَظ في «مرفقات الطلب»
  كمرجع، v18.9sz)، ثم يعيد مسؤول المستودع **رفع نفس الفاتورة** في التدقيق فتصير «ملف الفاتورة»
  الرسمي — فتُخزَّن نسختان وتظهر مرّتين في تفاصيل الطلب، ويتفاقم مع تعدّد الموردين.
  الحل: **(١)** `openWarehouseAudit` يجمع الفواتير التي رفعتها المشتريات (مرفقات `proc_invoice`
  + فاتورة الطلب القديمة `invoicePhotoUrl` للطلب بلا سندات) في `_waProcInvoices`، وتظهر
  **منتقياً** (`wa-inv-reuse`) في كل بطاقة فاتورة بالتدقيق؛ فيختار المدقّق الملف المرفوع مسبقاً
  **بلا إعادة رفع** — فيُسجَّل «ملف الفاتورة» الرسمي في السند (`grnDocs[].invoicePhotoUrl`)
  من **نفس رابط Storage** (لا نسخة ثانية). التجميع يقرأ `reuseUrl`، والتحقّق يقبله كمصدرٍ
  للفاتورة بلا إلزام رفع، ورقم الفاتورة يُعبَّأ مسبقاً من رقم المشتريات (`p.invoice`) قابلاً
  للتعديل. الفاتورة تبقى تظهر في السند وطباعة الـ PDF (`_buildAllInvoicesHtmlForPrint` من
  `p.invoices` المشتقّة من `grnDocs`) — مرّةً واحدة نظيفة. تعدّد الموردين يُخدَم عبر منتقي
  البند القائم (`wa-inv-pick`): لكل مورد فاتورةٌ واحدة. **(٢)** توحيد كاتب «ملف الفاتورة» الرسمي:
  أُخفي مسار الرفع المستقل في نافذة «تحديث الحالة» (`pu-invoice-photo`) الذي كان يكتب
  `p.invoicePhotoUrl` مباشرةً **خارج** `grnDocs` فيتباين مع `p.invoices` — فصار **تدقيق الاستلام
  هو المصدر الوحيد**. فحوص +١٠. **441/441**.
- **v18.9tt — L8: بندان في الطلب لنفس صنف المخزون (يُتمّ تدقيق يوليو):**
  كان توافر المخزون في `doInventoryReview` يُحسب لكل بند **مقابل الرصيد الكامل** — فبندان لنفس
  الصنف: (أ) **وعدٌ زائد** بالتغطية (كلاهما «مغطّى» من رصيد 10)، (ب) **خصمٌ ناقص** (الـ dedup يُبقي
  أوّل بند فقط)، (ج) **ردٌّ زائد** عند الحذف (كلاهما مُوسَم `_fromStock`). الجذر واحد، والحل ثلاثة
  تغييرات منسّقة: **التوافر تراكمي** (`_remainByInv` — كل بند يأخذ من المتبقّي، فمجموع `fromStock`
  للوثيقة ≤ رصيدها)، **الخصم مُجمَّع بـ `invId`** (يُخصَم المجموع لا أوّل بند)، **الوسم لكل بند بـ
  `origIdx`** لا بالاسم (كلٌّ يحمل ما أخذه فعلاً غير متداخل، فالعكس يطابق المخصوم). فحوص +٥.
  **بهذا أُغلقت كل بنود تدقيق يوليو (١١/١١).**

- **v18.9ts — تنظيف منخفض (L7/L9/L10/L11):** (L7) تقريب `estCost`/`estVAT`/`estCostNet` عند إنشاء
  الطلب — يمنع تسرّب العائم (`0.30000…004`) لـ Firestore. (L9) `_waPrevRcv` يطابق **بالكود قبل
  الاسم** — فالبند الحرّ الذي تغيّر اسمه بين جلستين (بلا `itemId`) لا يُصفَّر مستلَمه السابق ولا
  يتضاعف رصيده. (L10) تكلفة بطاقة قائمة المشتريات بمنزلتين عشريتين (تطابق التفاصيل، لا 324 مقابل
  323.55). (L11 — `price-analysis.js`) تقريب موثوق (`Math.round` لا `toFixed`)، و`directCost` =
  مجموع الجزأين المقرّبين. **بقي L8 وحده** (بندان لنفس صنف المخزون — يحتاج إعادة هيكلة حساب
  التوافر). فحوص +٣.

- **v18.9tr — H6: مصالحة المالية مع فاتورة المورد الفعلية:**
  المالية تسدّد **قبل** الاستلام (على التقدير)، ولم يكن ثمّ تسويةٌ بعد ظهور الفاتورة. (١)
  **«المطلوب سداده»** (`_poFinanceTotal`) صار من `poActualCost` = الفعلي للطلب المُدقَّق (لا التقدير
  الذي كان `getPOTotal` يعطيه). (٢) الطلب القديم المسدَّد بالكامل بلا مصفوفة دفعات: `_poPaidSoFar`
  يُرجِع `estCost` المجمَّد (ما سُدِّد وقتها) لا الفعلي الحالي المتغيّر. (٣) مؤشّر مصالحة في تفاصيل
  الطلب: **«زيادة تُستردّ»** (مُسدَّد > فاتورة) عبر `_poOverpaid`، وتنبيه أنّ «المتبقّي» نقصٌ يُستكمل
  محسوبٌ على الفاتورة الفعلية بعد التدقيق. فحوص +٧.

- **v18.9tq — H5: سلامة هوية وثيقة المخزون عند عكس الحذف:**
  الكتابة كانت تذهب لوثيقةٍ **محلولة** (الاستلام: `_canonId` عبر `_waCanonItemId`؛ المراجعة:
  `invMatch.id` — قد تخالف `itemId` مع وثيقة مدموجة/قديمة/مطابقة بالكود أو الاسم)، بينما عكس
  المخزون عند الحذف (`deletePurchase`) يخصم مقابل `it.itemId` الخام — فيبقى **رصيدٌ يتيم** (استلام 8
  ثم حذف = 8 وهمية باقية). الآن يُخزَّن معرّف الوثيقة المكتوبة فعلاً على البند (`_stockDocId`
  للاستلام، `_fromStockDocId` للمراجعة)، ويعكس الحذف مقابله، ويرجع لـ `itemId` للطلبات القديمة
  (متوافق خلفياً، صفر خطر على البيانات الموجودة). **L8/L9 (بندان لنفس الصنف، مطابقة الاسم لبند حرّ)
  مؤجَّلان لـ PR أصغر** — L8 يتطلب إعادة هيكلة حساب التوافر. فحوص +٤.

- **v18.9tp — H2+H4: حارسان في تدقيق الاستلام (`doWarehouseAudit`):**
  (H2) **يُمنع إغلاق الطلب ببندٍ مستلَم (`rcvQty>0`) وسعر وحدة = 0** — كانت طلبات عرض التسعير
  (`lumpSumQuote`، تبدأ `unitCost:0`) تُغلق بتكلفة فعلية صفر فتختفي قيمتها من كل التقارير ومن خصم
  البند المستعاض بصمت. (H4) **يُمنع نقص التوزيع** (الكمية المستلمة > دخل المستودع + استخدام مباشر)
  كما يُمنع التجاوز — كانت وحداتٌ مدفوعة تُحتسب تكلفتها ولا تدخل أي وجهة فتتبخّر بتحذيرٍ غير مانع.
  فحوص +٢.

- **v18.9to — H3: توحيد اصطلاح ض.ق.م على «الوحدة» في تدقيق الاستلام:**
  كان `doWarehouseAudit` (و`waUpdateRow`) يخزّنان تكلفة البند بـ `net×0.15` (الصافي)، بينما
  `_poItemLine`/الإنشاء/تعديل المسؤول **وفاتورة المورد** تستعمل «ض.ق.م الوحدة × الكمية» — ففي نفس
  الطلب `Σ items.itemCost ≠ actualCost ≠ سند الاستلام` بفرق ١–٣ هللات، ومع «تعديل شبح» (فتح+حفظ
  تعديل المسؤول يغيّر الإجمالي بلا تعديل حقل). الآن يحسبان على الوحدة
  (`vatUnit=round(unit×0.15)` ثم `total=round((unit+vatUnit)×rcv)`) فيتطابق المخزَّن مع المشتقّ
  ومع الفاتورة (البخاخ `13.04×3 ⇒ 45.00` لا 44.99). + تقريب `net` (يمنع تسرّب العائم لـ Firestore
  — بند L7). عتبة الشفاء في `_poItemLine` تبقى لاتساق الطلبات القديمة داخلياً (سند قديم بـ net×0.15
  = تفاصيله بنفس الاصطلاح). فحوص +٤.

- **v18.9tn — C1: الاستلام على دفعات لبنود مختلفة لا يُنقِص التكلفة/الكمية:**
  بعد أن صارت `poActualCost`/`poReceivedQty` تُشتقّان من `p.items` (v18.9tk)، كان تدقيق جلسةٍ
  لاحقة **يدهس بنودَ الجلسات السابقة بأصفار**: البند المكتمل سابقاً يُرسَم صفُّه في نافذة التدقيق
  بقيمة مبدئية 0 (`remQty=0`)، فيُبنى له صفٌّ بأصفار في `allAudit`، فيُدهَس في `p.items` إلى
  `rcvQty:0, itemCost:0` — فتُفقَد مساهمة الجلسة السابقة (بند A في جلسة و B في أخرى ⇒ يظهر أحدهما
  فقط، أو استُلم 5+5 ⇒ يظهر 5). **الحارس** في إعادة بناء البنود: البند بلا استلام فعليّ هذه الجلسة
  (`a.rcvQty<=0`) وله استلام سابق (`it.rcvQty>0`) لا يُدهَس — يبقى بقيمه المتراكمة. البند المُستلَم
  فعلاً (`rcv>0`) يُحدَّث طبيعياً. **مثبَّتٌ ببيانات PO-0091 الخام:** `grnDocs` فوضويّ (فاتورة
  مقسّمة على سندات، `cumRcv` متضخّم للضعف، كميات صُحّحت) فلا يصلح مصدراً؛ ومجموع `p.items` النهائية
  = 487.40 صحيح (= فاتورتَي المورد) — والحارس لا يمسّه (كل بنوده استُلمت في الجلسة الأخيرة). فحوص +٣.

- **v18.9tm — تطبيع بنود الطلب المُدقَّق عند التحميل (`_poHealItems`) — إتمام tl:**
  بعد tl (تكلفة البند على المستلَم عبر `_poItemLine` في مسارات العرض) بقيت **بطاقة القائمة**
  وكلُّ ما يقرأ `it.itemCost` **مباشرةً** (`getPOTotal`، تعبئة حقل التكلفة، تلميح الصافي/الضريبة)
  يعرض مجموع `itemCost` المخزَّن الخاطئ — فظهر تناقض: تفاصيل الطلب 323.55 بينما البطاقة 352
  (PO-202607-0113). الجذر: القيمة المخزَّنة نفسها ما زالت خاطئة، و`_poItemLine` كان يشفيها في
  مواضع العرض المحدَّدة فقط لا في القراءات المباشرة المبعثرة. **الحل الجذري:** `_poHealItems(p)`
  يُستدعى في **مساري تحميل الطلبات** (الأولي `loadPurchases` والحيّ `onSnapshot`)، فيشفي
  `itemCost/vat/lineTotal` لكل بندٍ مُدقَّق **مرةً في الذاكرة** عبر `_poItemLine` — فتتّسق كل
  الشاشات دون مطاردة كل موضع. غير مدمِّر (يشفي ما اختلف بأكثر من قرش فقط، ولا يمسّ الطلب غير
  المُدقَّق ولا البند بلا `rcvQty`)، والتقدير المجمَّد (`estCost`/`poEstTotal`) يبقى كما هو.
  فحوص +٥ (الشفاء 57.5→28.75، عدم مسّ السليم/غير المُدقَّق، Σ المباشر = الفعلي، حارس الاستدعاء).

- **درعٌ وقائيّ لثوابت الحساب المالي (فحوص فقط، بلا تغيير منطق):**
  كل الأخطاء الحسابية (0091، 0113) جذرها واحد: الرقم يُحسب في أكثر من مسار بأساسٍ مختلف.
  أُضيفت فحوص «ثلاثيّة متطابقة» على **طلبات-اختبار من حوادث حقيقية**: لأي طلب مغلق
  `actualCost == Σ تكلفة البنود == Σ توزيع الموردين`، و`receivedQty == Σ rcvQty`، وتكلفة البند
  **من المستلَم لا المطلوب** دائماً. + حارس مصدري يمنع عودة «الحساب المبعثر»: يفشل CI إن لم
  تمرّ مسارات العرض (تفاصيل الطلب/PDF) والتجميع وتوزيع الموردين عبر `_poItemLine`/`poActualCost`،
  أو إن عاد جمع السندات المتضخّم لـ actualCost. **القاعدة:** لا يُحسب أيّ رقم مالي داخل شاشة —
  كله عبر دالةٍ واحدة معتمدة (§٤). +14 فحصاً.

- **v18.9tl — تكلفة البند الفعلية على «المستلَم» لا «المطلوب» (تكملة tk):**
  بعد إصلاح tk (الإجمالي من البنود لا السندات) ظهر بندٌ مستلَمٌ جزئياً يُحسب على الكمية
  **المطلوبة**. الجذر: مسار تعديل المسؤول (`paeCalcItem` و`openAdminEditPurchase`) يعيد حساب
  `itemCost = تكلفة الوحدة × qty` بـ**qty = المطلوب**، فحين يُفتح/يُحفظ طلبٌ مُدقَّق (أو يُغلق
  المسؤول طلباً استُلم جزئياً) يدهس تكلفةَ التدقيق المبنيّة على `rcvQty` (المستلَم). مثال
  `PO-202607-0113`: «ماسورة ميفا» مطلوب 10 مستلم 5 ⇒ 57.5 بدل **28.75**، والإجمالي 352.3 بدل
  **323.55** (باقي البنود مستلَمها = مطلوبها فلم يظهر فيها الفرق). **الإصلاح:** (١) دالة موحّدة
  `_poItemLine(it, audited)` تشتقّ تكلفة البند من `rcvQty × تكلفة الوحدة` (باصطلاح ض.ق.م الوحدة
  مطابقاً مسار الإنشاء/التعديل)، **وتشفي المخزَّن الخاطئ فقط إن اختلف بأكثر من قرش** فلا يتذبذب
  تقريبُ البنود السليمة؛ تستعملها كل مسارات العرض (تفاصيل الطلب، PDF)، والتجميع (`_poItemsActual`)،
  وتوزيع الموردين (`poVendorBreakdown`). (٢) مسار تعديل المسؤول يحسب على `rcvQty` للطلب المُدقَّق
  فلا يُعيد إفساد المخزَّن. يُصحَّح الطلب المغلق سابقاً عند العرض بلا كتابة. فحوص +٣.

- **v18.9tk — التكلفة الفعلية والكمية المستلمة = حالة البنود النهائية لا جمع السندات:**
  «الإجمالي الفعلي» و«الكمية المستلمة» كانا يُحسبان بـ `_waSumGrn(p,"invoicedTotal"/"totalRcv")` =
  **جمع كل سندات الاستلام (`grnDocs`)**. لكن `grnDocs` تُلحَق (append) بينما `p.items` تُدهس كل
  جلسة تدقيق بآخر جلسة — فالطلب الذي يَعلَق على بندٍ لم يكتمل (بضاعة لم تصل) يُعاد استلامه
  وتدقيقه مراراً، وكل جلسة تُلحِق سنداً **يعيد احتساب البضاعة المستلَمة سابقاً**، فتتضخّم التكلفة
  والكمية أضعافاً. مثال حقيقي (PO-202607-0091): بندٌ «دهانات كسر رخام» لم يصل ⇒ ٧ سندات على
  أسبوع ⇒ `actualCost` = **1,457.19** بدل **487.40** الحقيقية (فاتورتان: 55.00 + 432.40)،
  و`receivedQty` = **95** بدل **32**. **الإصلاح:** مصدرٌ موحّد يشتقّ من حالة البنود النهائية —
  `poActualCost(p)` = Σ(المستلم × تكلفة الوحدة + ض) للبنود المستلَمة (كلُّ بند مرة واحدة، يطابق
  عمود «إجمالي البند»)، و`poReceivedQty(p)` = Σ الكمية المستلمة؛ البند غير المستلَم (مغطى من
  المخزون/لم يصل) كميته صفر فلا تكلفة له؛ والطلب القديم بلا بنود مفصّلة يُشتقّ من السندات
  (`grnDocs`) مباشرةً، ثم المخزَّن. يُحسب مرة واحدة عند التدقيق (`doWarehouseAudit`، كاتبٌ واحد
  لـ `receivedQty` كما قبل)، **ويُصحّح الطلبات المغلقة سابقاً عند العرض** بلا إعادة تدقيق ولا
  كتابة (كل مسارات العرض والتصدير والـ KPI تقرأ المصدر الموحّد). `poVendorBreakdown` صار يوزّع من
  البنود بمورّدها الفعلي فيطابق مجموعُه التكلفةَ الفعلية. فحوص تنفيذية للاشتقاق والتوزيع (+٨).

- **v18.9te — كتم رفض App Check/reCAPTCHA العابر:** لوحة reCAPTCHA تؤكّد أن المفتاح سليم والنطاق
  مُسجَّل ويُصدر رموزاً باستمرار (Protected)، لكن يتأخّر أحياناً الوصول لخوادم Google فيَرفض وعدُ
  App Check الداخلي برسالة «reCAPTCHA Timeout» تظهر `Uncaught (in promise)`. App Check يعيد الطلب
  تلقائياً (auto-refresh) والبيانات تُحمَّل طبيعياً. أُضيف مستمع `unhandledrejection` يمنع ظهور هذا
  الرفض **حصراً** إن طابق `recaptcha|appcheck` (بلا ابتلاع أي خطأ آخر). ملاحظة: أخطاء WebChannel/400
  وتحذير الـ host تُركت كما هي (قرارٌ سابق حول عائلة ca9/b815).
- **v18.9td — صفحة المؤشرات تُعاد رسمها تلقائياً عند وصول بيانات الطلبات:** خطّاف `showPage` كان
  يرسم صفحة `purchase-kpi` مرّة واحدة فقط عند فتحها؛ فإن فُتحت **قبل اكتمال تحميل الطلبات من
  Firestore** (وارد مع بطء الشبكة/أخطاء النقل) تُرسَم فارغةً وتبقى كذلك حتى يغادرها المستخدم ويعود —
  وهو **السبب الحقيقي لظهور «مخطّط الموردين فارغاً»** (لا الكود ولا البيانات). الآن مستمع
  `onSnapshot` للطلبات يستدعي `window.purchaseKPI.render()` إن كانت صفحتها مفتوحة، أسوةً بصفحة
  المشتريات ولوحتها ورصيد المستعاض. (الفلاتر محفوظة عبر `currentFilters` فلا يُفقد اختيار المستخدم.)
- **v18.9tc — مخطّط «أعلى الموردين» = المغلقة فقط بالتكلفة الفعلية:** بعد أن تبيّن (بفحص بيانات
  حيّ) أن السبب الأصلي لظهور المخطّط فارغاً كان **كاش المتصفح** لا الكود — والكود يُنتج التوزيع
  الصحيح — بقيت مسألة اتساق: كان `byVendor` يشمل الطلبات المفتوحة بتكلفتها **التقديرية** فيضخّم
  الإجمالي (40,288) بينما الإنفاق المغلق الفعلي 6,700. الآن يقتصر على `_kpiClosed(p)` ويستخدم
  التكلفة الفعلية (`_kpiActual`/توزيع السندات)، فيتطابق إجمالي المخطّط مع «إجمالي المبالغ المغلقة»
  في اللوحة و«الإنفاق الفعلي» في المؤشرات. (تبقى `_kpiVendorOf` تقرأ المورّد من أي حقل.)
- **v18.9tb — مؤشّر «أعلى الموردين» لا يُسقط إنفاقاً باسمٍ خالٍ:** كان الفرع الاحتياطي في
  `purchase-kpi.js` يقرأ المورّد من `actualVendor||vendor` فقط ويُهمل السطر بشرط `if(v&&c>0)` —
  فأي طلبٍ مغلقٍ بتكلفةٍ لكن بلا اسم مورّدٍ مُدخَل كان يسقط من المؤشر، فيبدو المخطّط فارغاً رغم وجود
  إنفاق. أُضيفت `_kpiVendorOf(p)` تقرأ أول حقلٍ غير فارغ من
  `actualVendor → vendors[] → supplier → vendor → grnDocs[].vendor → items[].vendor` (فالمورّد قد
  يُخزَّن في أيٍّ منها بحسب مسار الإنشاء — لا سيّما `supplier` للطلبات المحوّلة من عرض أسعار)، مع
  «غير محدد» احتياطاً؛ و`_kpiSpendOf(p)` تُرجع الإنفاق الأفضل-جهداً (فعلي ثم تقديري ثم مجموع البنود).
  والآن **لا يُسقَط أي إنفاقٍ** — يُنسب لمورّده أو لـ«غير محدد» بدل الاختفاء.
- **v18.9ta — رفع الإصدار لإبطال كاش الوحدات (.js):** ظلّت النسخة `v18.9sz` عبر تعديلاتٍ كثيرة على
  `purchase-kpi.js` وغيره **دون تغيير `?v=`**، فكان المتصفّح يخدم الوحدات من **الكاش القديم** — فلم تصل
  إصلاحات المؤشرات (الموردين/الإنفاق) للمستخدم رغم دمجها (ظهرت مؤشرات الموردين خالية باستمرار). الآن
  `?v=18.9ta` يُجبر إعادة الجلب. **تذكير حرج:** أي تعديل على وحدة `.js` يجب أن يرفع `APP_VERSION`
  والـ cache-busters معاً — وإلا لا يصل للمستخدم.

- **v18.9sz — مؤشرات الموردين تقرأ المورّد الفعلي (لا المقترح) وتُظهر بيانات دائماً:** «الإنفاق حسب
  المورد» و«تركّز الموردين» و«أعلى الموردين» في `purchase-kpi.js` كانت تقرأ `p.vendor` (المقترح عند
  الإنشاء) وهو فارغٌ غالباً — فتظهر خالية. الآن بالمورّد **الفعلي**: الطلب المغلق بسندات يُوزَّع على
  مورّديه الفعليين بحصصهم (`poVendorBreakdown`، يدعم الطلب متعدّد الموردين)؛ وغير ذلك يُنسب إنفاقُه
  الأفضل-جهداً (الفعلي إن وُجد وإلا التقديري `getPOTotal`/`estCost`) للمورّد الفعلي (`actualVendor`)
  ثم المقترح (`vendor`). فهو مؤشّر **تركّز مخاطر توريد** يشمل الملتزَم لا المغلق وحده — فلا يبقى خالياً
  ولا بلا قيم. فحوص جديدة (القسم ٢٦).

- **v18.9sz — توحيد «إجمالي الإنفاق (فعلي)» في مؤشرات المشتريات مع اللوحة:** كان `totalAct` في
  `purchase-kpi.js` يجمع `actualCost` لِـ**كل الطلبات** بأي حالة، فيختلف عن «إجمالي المبالغ المغلقة»
  في اللوحة (الطلبات المغلقة فقط). الآن الوحدة تحسب المغلق فقط بالتكلفة الفعلية عبر المصدر الموحّد
  (`poIsClosed`/`poActualCost` بحارس `typeof`): `actClosed = مغلق ? poActualCost : 0`، و`totalAct`
  و«الإنفاق الشهري الفعلي» يجمعانه. وصُحِّح تعريف «مغلق» (`isClosed`) ليشمل `closed_after_receipt`
  (لا `status==="closed"` وحده) فيتّسق عدّ المغلقة والدورة والانحراف. فحوص جديدة (القسم ٢٦).

- **v18.9sz — أمر الصرف: قائمة المشروع تشمل المشاريع اليدوية المحفوظة:** كانت قائمة مشروع
  «أمر صرف من المخزون» تعرض الرسمية فقط + «إدخال يدوي». الآن تُبنى من المصدر الموحّد
  `_manualProjectNamesAll()` فتظهر المشاريع اليدوية المحفوظة كخيارات (بقيمة = الاسم، إذ يخزّن أمر
  الصرف المشروع بالاسم)، وتُعاد القائمة بعد `_loadManualProjectNames()` لتشمل أسماء meta. `_issProjectOptionsHTML`.
  فحوص جديدة (القسم ٢٥).

- **v18.9sz — أوامر الصرف: عمود «المستودع» يعرض المستودع الفعلي:** كان العمود المعنون «المستودع»
  في قائمة أوامر الصرف يعرض `issuedBy` (اسم من صرف) بالخطأ. الآن يعرض أسماء مستودعات أصناف الأمر
  (مميَّزة). ولأن أصناف الأمر لم تكن تحفظ المستودع، أُضيف `warehouseName` (من رصيد المخزون `src`)
  إلى بنود الأمر عند الصرف وفي وثيقة الأمر؛ وللأوامر القديمة بلا الحقل يُشتقّ من رصيد المخزون الحالي
  بمعرّف الصنف (أفضل جهد). فحوص جديدة (القسم ٢٤).

- **v18.9sz — إحالة الطلب «قيد تنفيذ المشتريات» للمالية للسداد:** بعد اعتماد المدير التنفيذي يصبح
  الطلب `proc_executing`؛ وكان مسؤول المشتريات في هذه المرحلة لا يملك سوى «تم الشراء — إشعار
  المستودع» و«رفض». أُضيف خيار **«💳 إحالة للمالية للسداد»** (`__SEND_TO_FINANCE__`) في
  `getAvailableStatuses` لدور المشتريات عند `proc_executing` — يفتح نافذة `openSendToFinanceModal`
  المعتادة (وبما أن `ceoApprovedAt` مُسجَّل من اعتماد التنفيذي فالإحالة مباشرةٌ للمالية بلا عودة له).
  فحوص جديدة (القسم ٢٣).

- **v18.9sz — تدقيق الاستلام: «بند إضافي» لم يعد يمحو كميات صفوف الطلب:** صفوف الطلب الأصلية
  في نافذة التدقيق تعيش في الـ DOM وحده (بلا حالة، بخلاف الإضافية `_waExtras`)، فإضافة «بند إضافي»
  والخروج من نافذته كانت تُفقِد الكميات المستلمة/المباشرة المكتوبة فيها. لم يُعثر على مسارٍ يهدم
  `wa-items-tbody` صراحةً (إعادة رسمٍ عابرة/سلوك متصفّح)، فالحلّ الآمن: `_waCaptureOrig()` تلتقط قيم
  الصفوف قبل فتح نافذة البند الإضافي، و`_waRestoreOrig()` تعيد تطبيقها بعد إغلاقها (بـ `src='stock'`
  فلا تُعاد حسبة خانة المستودع). الحفظ يقرأ الـ DOM الحيّ فلا يتأثّر. فحوص جديدة (القسم ٢٢).

- **v18.9sz — «تعديل كامل» للمسؤول لم يعد يُخرج طلب الشراء من مساره:** قائمة الحالة في نافذة
  تعديل المسؤول (`pae-status`) كانت **قائمةً ثابتةً قديمة** (draft/in_review/new_request...) لا تحوي
  حالات المسار الحالي (`pending_pm`/`pm_approved`/`wh_review`...) — مخالفةً لقاعدة «ممنوع قوائم الحالات
  اليدوية». فعند فتح التعديل على طلبٍ في حالةٍ حديثة يفشل `select.value=p.status` بصمت (لا خيار مطابق)،
  فتُحفظ حالةٌ فارغة عند الحفظ فيسقط الطلب من مرحلته. الإصلاح: تُبنى الخيارات ديناميكياً من المصدر
  الموحّد `PO_STATUS` في `openAdminEditPurchase`، والحالة الحالية (المطبَّعة بـ `normalizePOStatus`)
  تُضبط وتُحفظ كما هي. فحوص جديدة (القسم ٢١).

- **v18.9sz — بطاقة «بنود أُضيفت عند الاستلام» (`renderExtrasCard`): للمسؤول فقط وتختفي بعد البتّ:**
  البطاقة الإشرافية للبنود المُضافة عند الاستلام صارت (١) **لا تظهر إلا للأدمن** (مدير النظام)
  وحده؛ و(٢) **تختفي بعد البتّ** — الافتراضي
  يعرض المعلّق فقط، فإن لم يبقَ بندٌ معلّق تُخفى تماماً (كانت ترتدّ لعرض المبتوت فيه فتبقى ظاهرة بعد
  الاعتماد). «أعرض الكل» يبقى للسجل ما دام هناك معلّق. فحوص جديدة (القسم ٢٠).

- **v18.9sz — بطاقة «الطلبات المغلقة» في لوحة المشتريات:** بطاقة «معتمدة هذا الشهر»
  (`po-dash-approved-box`) صارت **«الطلبات المغلقة»** — تحسب **عدد الطلبات المغلقة فعلياً**
  (`poIsClosed`)، نفس تعريف «مغلق» في بطاقة «إجمالي المبالغ المغلقة» المجاورة (لا تعريفَين).
  بأيقونة القفل، ونقرُها يفلتر القائمة على المرحلة المغلقة (`_poStageFilter('closed')`).
  (على لوحة **المشتريات**، و«المغلق» = طلب شراء مغلق؛ بلاغات الصيانة غير محمّلة في الوضع
  المركزي أصلاً.) فحوص جديدة (القسم ١٩).

- **v18.9sz — تكاليف المشاريع المُدخَلة يدوياً تدخل كل الحسابات والمؤشّرات:**
  كان تصنيف «المشروع اليدوي» (المخصّص) متضارباً بين الشاشات: لوحة المعلومات وإدارة المشاريع تصنّفانه
  بالعلَم `isCustomProject`، بينما تقرير المشتريات وتصدير PDF ومؤشّرات الأداء (KPI) تصنّفه بسنتينل
  `projectId==="__OTHER__"` **وحده**. والأخطر: الطلب المحوَّل من **طلب تسعير** يدوي كان يُخزَّن
  `projectId:""` بلا علَم ولا `projectName`، فتُنسَب تكلفته لمشروع «هايل» الافتراضي وتختفي من مؤشّرات
  مشروعه (وإن بقيت ضمن الإجماليات الكبرى التي تمرّ على كل الطلبات بلا بوّابة مشروع). الإصلاح:
  • **مصدر واحد للتصنيف والتسمية** في `index.html`: `poIsCustomProject(p)` = `isCustomProject===true || projectId==="__OTHER__"`،
    و`poProjectDisplayName(p)` (يدوي بالاسم، رسمي بالمعرّف). تقرؤهما كل شاشات التقرير/التصدير.
  • **تطبيع عند المصدر:** `submitNewRFQ` يحمل `isCustomProject`، وتحويل التسعير لطلب شراء يطبّق الشكل
    القياسي (`projectId:"__OTHER__"` + `isCustomProject` + `projectName`) كطلب الشراء اليدوي المباشر تماماً.
  • **مؤشرات الأداء (`purchase-kpi.js`):** كل مشروع يدوي يُميَّز بمفتاح `"__CUSTOM__:"+الاسم` بدل انطوائها
    جميعاً في خيار `__OTHER__` واحد يتعذّر فرزه. فحوص جديدة (القسم ١٦).
  • **تقليل احتكاك مستمعي Firestore (تخفيف خلل SDK الداخلي `INTERNAL ASSERTION ca9/b815`):**
    الخلل خللٌ داخليٌّ معروفٌ في Firestore SDK يُطلقه تراكم تركيب/فكّ أهداف المراقبة (targetId
    يتصاعد). المخفّفات القياسية مطبَّقة أصلاً (`experimentalForceLongPolling`، كاش ذاكرة فقط بلا
    IndexedDB، لا تبويبات متعددة) + تعافٍ تلقائي (`_fsAutoRecover`). المتبقّي: **الاحتكاك**.
    مستمعو المجموعات **العامة** (`global_*`: المشتريات/المخزون/الكتالوج/أنواعه/التسعير/أوامر
    الصرف/العهد/المستودعات) لا يتعلّقون بالمشروع، فصاروا يُركَّبون **مرة واحدة** ويبقون حيّين حتى
    تسجيل الخروج (حراس idempotent في دوال التشغيل)؛ ولم يعُد `switchProject`/`exitGlobalPurchases`
    يفكّونهم أو يصفّرون بياناتهم. المستمعون **المرتبطون بالمشروع** (البلاغات/الأصول/الوقائية/
    الإشعارات — عبر `_pfx`/`_meta`) وحدهم يُعاد تركيبهم عند تبديل المشروع. سابقة قائمة:
    `_whUnsub`/`_catTypesUnsub` لم يكونا يُفكّان عند التبديل أصلاً. فحوص جديدة (القسم ١٧).
    **إضافة:** «حفظ التحديث» في نافذة «تحديث حالة الطلب» كان يقرأ الحالة الطازجة بـ `await get()`
    **بلا مهلة**؛ فإن تجمّد عميل Firestore (نفس خلل ca9/b815) أو تعثّرت الشبكة على الجوال، تعلّق
    النافذة أبداً بلا إجراء ولا خطأ (التجمّد المُبلَّغ). الآن `Promise.race` بمهلة ٦ ثوانٍ ثم المضيّ
    بالنسخة المحلية (مرآة `onSnapshot`)، وكتابة `merge:true` لا تدهس تعديل غيرنا.
  • **«ملف الفاتورة» مصدره المستودع فقط:** بطاقة «ملف الفاتورة» في تفاصيل الطلب لم تعد تعرض
    **اسم المورد** (التوريد الفعلي قد يكون من مورد آخر غير المقترح، فالاسم مُضلِّل). ورفع مسؤول
    **المشتريات** للفاتورة (`doNotifyWarehouse`) صار يُحفَظ في **«مرفقات الطلب» فقط** كمرجع مُوسَّم،
    بلا كتابة `p.invoicePhotoUrl`؛ فـ«ملف الفاتورة» الرسمي (‏`p.invoices` المشتقّة من `grnDocs`)
    يسجّله **مسؤول المستودع** عند تدقيق الاستلام حصراً. كذلك أُزيل حقل «المورد الفعلي» من نافذة
    إشعار المستودع فلا يُدخل المشتريات مورّداً مقترحاً إطلاقاً (المورّد يُسجَّل من السندات). فحوص
    جديدة (القسم ١٨). **290/290**.

- **v18.9sx — المقايسة: استخراج البنود بالذكاء الاصطناعي (المرحلة ٣):**
  زر «استخراج بالذكاء الاصطناعي» في تبويب المقايسة (عرض + تحرير). يرفع المستخدم صورة أو ملف PDF للمقايسة،
  فتُقرأ base64 وتُرسَل إلى Claude (vision للصور / document بترويسة beta `pdfs-2024-09-25` للـ PDF) عبر
  `_callAnthropicAPI` بميزة «استخراج المقايسة» — فيُطلب منه إعادة `{items:[{desc,category,unit,qty,unitPrice}]}`
  فقط. تُقرأ الاستجابة بـ `_extractJSON`، ويُربَط كل بند ببند الموازنة عبر `_catKeyFromName`، ثم تُضاف
  البنود إلى وضع التحرير (`_boqDraft`) للمراجعة قبل «حفظ». تُحتسب توكنز النداء في عدّاد الاستهلاك تلقائياً.
  حدود أمان: 400 بند، تقصير الوصف/الوحدة، أرقام مُنقّاة، ورسائل خطأ عربية واضحة. 230/230.
- **v18.9sw — المقايسة: قالب استيراد Excel:**
  زر «تحميل قالب» (`downloadBoqTemplate`) يولّد ملف Excel جاهزاً عبر `XLSX.writeFile` — ورقة «المقايسة»
  بالأعمدة الصحيحة وأمثلة، وورقة «البنود العامة» بأسماء بنود الموازنة المسموحة لعمود «البند العام».
  تحقّق round-trip: القالب المُولّد يُقرأ بمستورِد المقايسة (`_parseBoqRows`) بدقّة. 230/230.
- **v18.9sv — المقايسة: استيراد Excel (المرحلة ٢):**
  زر «استيراد Excel» في تبويب المقايسة يقرأ الملف عبر مكتبة `XLSX` المحمّلة، ويطابق الأعمدة بمرونة
  (`_parseBoqRows`): يُحدَّد عمودُ كلِّ حقلٍ **مرة واحدة** بأولوية للأدق (سعر الوحدة قبل الوحدة، والبند العام
  قبل البند) فلا يختطف حقلٌ عمودَ آخر؛ يقبل عناوين عربية متعدّدة، ويُنظّف الأرقام من النص («62 ر.س»→62).
  التصنيف يُربَط ببند الموازنة (`_catKeyFromName`: أسماء البنود + خريطة أنواع البنود). البنود المستوردة
  تدخل **وضع التحرير** للمراجعة قبل الحفظ (تُضاف للموجود لا تستبدله). المرحلة التالية: الاستخراج بالـ AI. 230/230.
- **v18.9su — المقايسة التفصيلية (BOQ) — المرحلة ١ (إدخال يدوي):**
  تبويب **«المقايسة»** على بطاقة المشروع: بنودٌ تفصيلية `{desc, categoryKey, unit, qty, unitPrice}`،
  عرضٌ مجمّعٌ حسب البند العام مع مجاميع فرعية وإجمالي، وتحريرٌ كامل (إضافة/حذف/حفظ) بمسودّة
  (`_boqDraft`). التخزين `meta/{id}_boq`. **عند الحفظ تُشتَقّ «الموازنة المخطّطة» تلقائياً** —
  `saveBoq` يجمع بنود كل تصنيف ويكتبها في موازنة المشروع عبر `saveBudget` (بند بلا مقايسة يبقى
  بقيمته اليدوية). المرحلتان التاليتان: **استيراد Excel** و**استخراج بالذكاء الاصطناعي** (تملآن نفس
  الجدول). 230/230.
- **v18.9st — نقل «استهلاك الذكاء الاصطناعي» إلى قائمة «النظام»:**
  استهلاك الـ AI شأنٌ على مستوى النظام لا المشاريع — نُقل زرّه من قائمة إدارة المشاريع إلى مجموعة
  «النظام» (`grp-system`) في الشريط الجانبي (`injectSystemButton`، للأدمن). النقر يستدعي
  `openUsageFromNav` الذي يُظهر صفحة المشاريع ثم شاشة الاستهلاك. الشاشة نفسها بلا تغيير. 230/230.
- **v18.9ss — عدّاد استهلاك الذكاء الاصطناعي:**
  كان النظام يقرأ نص رد الـ AI ويتجاهل `data.usage`. الآن `_callAnthropicAPI` يسجّل توكنز كل نداء
  (من نسخة `res.clone()` بلا مسّ المستدعي) في `meta/ai_usage` عبر `_recordAIUsage` (`FieldValue.increment`
  بتجميع شهري + حسب الميزة؛ الميزة تُمرَّر رابعَ وسيطٍ لـ `_callAnthropicAPI` ومن `opts.feature` في
  `_aiText`). شاشة **«استهلاك الذكاء الاصطناعي»** في إدارة المشاريع (للأدمن، زر بقائمة المشاريع):
  إجمالي النداءات/التوكنز + **تكلفة تقديرية** (أسعار Sonnet) + جدول شهري وجدول حسب الميزة. الفوترة
  الرسمية تبقى من `console.anthropic.com`. 230/230.
- **v18.9sr — تمتين توليد الجدول الزمني بالـ AI + مخرج يدوي عند الفشل:**
  عند فشل التوليد كان يظهر توست عام «تعذّر — حاول مرة أخرى». الآن: **محاولتان** تلقائياً، و`_extractJSON`
  أمتن (يتحمّل الفواصل الزائدة و```markdown``` ويلتقط مصفوفة المراحل مباشرةً)، ورفع `max_tokens` إلى 2500،
  وتسجيل الرد الخام في console للتشخيص. وعند الفشل النهائي تظهر **رسالة سبب واضحة** (تشمل سبب تعذّر
  الاتصال إن كان AI غير مُفعّل) + زر **«أضف المراحل يدوياً»** في نفس النموذج (`_genErr`). 230/230.
- **v18.9sq — توليد الجدول الزمني بالـ AI يقرأ بيانات المشروع تلقائياً:**
  بدل الاعتماد على نطاقٍ نصّيٍّ يدوي فقط، صار `doAiGen` يُغذّي النموذج **بموازنة المشروع** (البنود
  المرصودة: التخصص + المبلغ) فتخرج المراحل **مطابقة للتخصصات وبأوزان زمنية قريبة من الأوزان المالية**،
  ومدةُ العقد وتاريخُ بدئه (`contractMonths`/`contractStart`) يُعبّآن الحقلين تلقائياً. النطاق النصّي
  صار **اختيارياً**. (أُضيف `contractStart/contractMonths/desc` إلى مخرجات `allProjects`.) 230/230.
- **v18.9sp — بنود موازنة: «مواد ميكانيكية» و«مواد مختلفة» بنودٌ مستقلة:**
  أُضيف بندَا `mechanical` («مواد ميكانيكية») و`misc` («مواد مختلفة») مع تعديل الخريطة الافتراضية:
  «مواد ميكانيكية»→`mechanical` (بدل `equipment`) و«مواد مختلفة»→`misc` (بدل `materials`). 230/230.
- **v18.9so — بنود موازنة: «خامات عامة»→«مواد بناء» + بند «مواد نظافة» + محاذاة الجدول:**
  (١) أُعيدت تسمية بند `materials` إلى **«مواد بناء»** (مطابقةً لاسم نوع البند). (٢) أُضيف بند
  **«مواد نظافة»** (`cleaning`) مع ربط نوع البند «مواد نظافة»→`cleaning` في الخريطة الافتراضية.
  (٣) **إصلاح محاذاة:** أرقام جدول الموازنة كانت محاذاةً لليسار بينما الرؤوس لليمين، فلا تقع تحتها
  على الشاشات العريضة — صارت `.pm-num` بمحاذاة يمين فتحت رؤوس الأعمدة. الأسماء تُعرَض من
  `BUDGET_CATEGORIES` بالمفتاح فالمستندات القديمة تُحدَّث اسمها تلقائياً. 230/230.
- **v18.9sn — توزيع المصروف على بنود الموازنة حسب نوع كل بند (بدل الاختيار اليدوي):**
  الاختيار اليدوي لبند واحد للطلب كله (v18.9sm) غير عملي — الطلب الواحد قد يجمع أنواعاً متعدّدة.
  الآن `rollupByCategory` يوزّع لكل طلب حسب **نوع كل صنف** (`item.itemType`): يُجمَع وزن كل بند
  موازنة من تكاليف أصنافه، ثم يُوزَّع الفعلي (الفاتورة) بنسبة الأوزان، والجاري بتكلفة الأصناف مباشرةً.
  الربط عبر **خريطة نوع البند→بند الموازنة** (افتراض ذكي للأنواع المدمجة + تجاوزات الأدمن في
  `meta/pm_category_map`، محرّرها زر «ربط أنواع البنود» بقائمة المشاريع). **يشمل الطلبات الحالية**
  (لها أنواع بنود) فتظهر لها قيمٌ في البنود فوراً بلا إدخال. أُضيف بند **«تكييف»** (`hvac`)، وأُلغي
  حقل `np-budget-category` اليدوي من نموذج الطلب. طلبٌ بلا أصناف يرجع لـ budgetCategoryKey ثم «غير
  مصنّف». 230/230.
- **v18.9sm — حقل «بند الموازنة» على نموذج طلب الشراء:**
  أُضيف مُنتقي **«بند الموازنة»** (اختياري) في نموذج طلب الشراء داخل صندوق المشروع، يُملأ من
  `projectMgmt._BUDGET_CATEGORIES` (مع بديل احتياطي مطابق عبر `_fillBudgetCategorySelect`).
  `submitPurchase` تخزّن `budgetCategoryKey` على الطلب، فتُوزِّع إدارةُ المشاريع (`rollupByCategory`)
  **المصروفَ والمرتبط على بنود الموازنة الحقيقية** بدل تجميعها كلها تحت «غير مصنّف». الطلبات القديمة
  أو بلا بند تبقى تحت «غير مصنّف» (توافق رجعي). لا يمسّ منطق الحفظ الأساسي. 230/230.
- **v18.9sl — «نوع المشروع» عند الإضافة/التعديل من القائمة الرئيسية:**
  أُضيف مُنتقي «نوع المشروع» (مقاولات/ترميم/صيانة، افتراضه صيانة) في نافذتَي إضافة/تعديل المشروع
  (`openAddProjectModal`/`openEditProjectModal`)، ويُخزَّن على **سجل المشروع** في `meta/projects`
  (`type`). إدارة المشاريع تقرأه عبر `effType` (سجل المشروع أو مستند الموازنة)، و`needsSchedule`
  صار يعتمد `effType` — فيظهر «الجدول الزمني» لمقاولات/ترميم **فور الإنشاء** بلا خطوة تصنيف إضافية.
- **v18.9sk — إدارة المشاريع: تصنيف نوع المشروع + الجدول الزمني (إنشاءات/ترميم فقط):**
  (١) **نوع المشروع** (مقاولات/ترميم/صيانة) يُحدَّد من بطاقة المشروع (تبويب نظرة عامة، للأدمن/مدير
  المشاريع)، يُخزَّن في مستند الموازنة (`type`). **غير المصنّف = صيانة (بلا جدول)** — القرار المعتمد.
  (٢) **تبويب «الجدول الزمني»** يظهر **لمقاولات/ترميم فقط** (`needsSchedule`): **توليد مبدئي بالذكاء
  الاصطناعي** (نموذج إدخال نطاق/تاريخ بدء/مدة → `_aiText` يرجع JSON مراحل → تُطبَّع تواريخها متتابعةً)،
  ثم **تحرير المراحل يدوياً** (إضافة/حذف/اسم/تاريخ/مدة) و**متابعة نسبة الإنجاز** لكل مرحلة مع **مؤشر
  حالة** (مكتملة/على المسار/متأخّرة/لم تبدأ حسب المتوقّع مقابل الفعلي زمنياً) و**Gantt مبسّط** (أشرطة
  HTML/CSS بلا مكتبات). التخزين `meta/{key}_schedule`. (٣) **إصلاح**: حفظ الموازنة كان يقرأ `.pm-inp`
  القديم بعد إعادة تسمية الحقل لـ `.pm-inp-w` (كان يحفظ أصفاراً) — صار يقرأ `input[data-cat]`. 230/230.
- **v18.9sj — إدارة المشاريع: قراءة المشاريع المُدخَلة يدوياً في المشتريات:**
  كانت الوحدة تقرأ المشاريع المسجّلة (`meta/projects`) فقط، فالطلبات المربوطة بمشروعٍ **يدوي**
  (`projectId==="__OTHER__"` + الاسم في `projectName`) لا تظهر. الآن `allProjects()` تدمج المسجّلة
  مع اليدوية (من `_manualProjectNamesAll()` الجاهزة)، و`poForProject` تُطابق اليدوي **بالاسم**،
  وتظهر بشارة «يدوي». موازنة المشروع اليدوي تُحفَظ بمفتاح آمن `meta/mpn_<الاسم>_budget` (بلا بادئة
  `__` المحجوزة ولا `/`). الفتح صار **بالفهرس** (`openAt`) لا بالمعرّف (أسماء يدوية قد تكسر onclick).
  230/230.
- **v18.9si — إدارة المشاريع: زرّ دخول على الصفحة الخارجية (منتقي المشاريع):**
  «إدارة المشاريع» كيانٌ شاملٌ لكل المشاريع مثل «المشتريات المركزية»، فأُضيف له زرٌّ على شاشة
  منتقي المشاريع بجوار زر المشتريات المركزية وبنفس طرازه (`injectLandingButton` في
  `project-management.js`، مقفول على الأدمن). النقر يستدعي `openFromLanding` الذي يعيد استخدام
  `openGlobalPurchases` (ينقل من المنتقي لهيكل التطبيق ويشغّل مزامنة المشتريات = مصدر المصروف
  الفعلي) ثم `showPage("projects")`. لا يمسّ زر المشتريات ولا الاختبارات (230/230).
- **v18.9sh — إدارة المشاريع (المرحلة ١، أول حتة): بطاقة المشروع + الموازنة مربوطة بالمشتريات:**
  وحدة جديدة `project-management.js` (نمط `purchase-kpi.js` — تركيب ذاتي: صفحة `page-projects` +
  مجموعة «إدارة المشاريع» في القائمة الجانبية + لفّ `showPage`، فتعديل `index.html` = وسم `<script>`
  واحد). (١) **قائمة المشاريع:** بطاقة لكل مشروع بالموازنة/المصروف/المتبقّي وشريط استهلاك ملوّن
  (أخضر <٨٠٪، أصفر ٨٠–١٠٠٪، أحمر >١٠٠٪). (٢) **بطاقة المشروع:** تبويب «نظرة عامة» (٤ بطاقات:
  الموازنة/المصروف الفعلي/المرتبط/المتبقّي + شريط) وتبويب «الموازنة» (جدول البنود العامة الهجينة
  قابل للتحرير — تعديل للأدمن/مدير المشاريع، حفظ في `meta/{id}_budget`). (٣) **مصدر واحد للحقيقة:**
  المصروف الفعلي (المغلق) والمرتبط (الجاري) يُسحبان من `purchases` عبر `poIsClosed`/`poActualCost`/
  `poStageIsWip` — بلا تخزين مزدوج. الطلبات غير الموسومة ببند تُجمَّع تحت «غير مصنّف» (توافق رجعي).
  (٤) **تنبيه تجاوز الموازنة تحذيري فقط.** (٥) **الأيقونات SVG من نظام المنصة** (`_ic`/`_svgIcon`)
  لا إيموجي. (٦) **قفل مؤقّت على الأدمن** (`RESTRICT_TO=["admin"]` في `project-management.js`)
  للاختبار الحيّ قبل فتحه للفريق — **للفتح للكل: اجعلها `[]` (سطر واحد)**. **مؤجَّل (موثّق في `docs/project-management-plan.md`):**
  بنود المقايسة، حقل «بند الموازنة» على طلب الشراء، الإيراد/المستخلصات (٥ مراحل)، دفعات العمالة،
  الجدول الزمني بالـ AI، لوحة الشركة. لا يمسّ النظام الحالي ولا اختباراته (230/230).

- **v18.9sg — السداد على دفعات (المالية) + عرض كل أرقام الفواتير:**
  (١) **دفعات جزئية:** كانت المالية تسدّد **المبلغ كاملاً فقط** (`payment.paid=true`) فيَعبر الطلب
  فوراً للتنفيذ. الآن نافذة السداد تطلب **مبلغ الدفعة** (افتراضه المتبقّي)، وتُسجَّل الدفعات في
  `payment.installments[]` (`{amount, by, at, ref, attName}`) مع إيصال إلزامي لكلٍّ منها. المسدَّد
  الفعلي = `_poPaidSoFar(p)` (مجموع الدفعات؛ الطلبات القديمة المسدَّدة بالكامل بلا مصفوفة تُعامَل
  كأن كامل قيمتها مُسدَّد)، والمتبقّي = `_poRemaining(p)`. **دفعةٌ أقل من المتبقّي** تُبقي الطلب عند
  المالية (`pending_finance`) وتسجّل قيداً زمنياً «سداد دفعة جزئية»؛ وعند اكتمال المتبقّي يُوسَم
  `paid=true` ويُحال لتنفيذ المشتريات كالسابق. (٢) **تفاصيل الطلب** تعرض بطاقة (الإجمالي/المسدَّد/
  المتبقّي) وقائمة الدفعات، ووسمَ الحالة يصير «سُدِّد جزئياً — X من Y». (٣) **بطاقة المالية:**
  «بانتظار السداد» تجمع **المتبقّي** لا الإجمالي، و«سُدِّد (تراكمي)» تجمع **المسدَّد فعلياً** عبر كل
  الطلبات (لا كامل قيمة المكتملة)، وعمود القائمة صار «المتبقّي» مع سطر «سُدِّد X من Y» للجزئي.
  (٤) **إصلاح عرض الفواتير:** شبكة بيانات الطلب كانت تُظهر **رقم فاتورة واحد** (`p.invoice`) للطلب
  المورَّد بفاتورتين؛ صارت تُظهر **كل** أرقام الفواتير المتمايزة من `_poInvoiceList(p)`.
- **v18.9se — أربعة إصلاحات متوسطة من جولة التدقيق الثانية، مع اختباراتها (+10 → 230/230):**
  (١) **تصدير Excel للمخزون** كان يمرّ على `_inventoryItems` خاماً: يتجاهل فلاتر الشاشة
  ويُدرج الوثائق **المدموجة** (أشباح قد تُضاعف الكمية). أُضيف `_inventoryFiltered` مصدراً
  موحّداً (يستبعد المدموج + يطبّق الفلاتر) تستخدمه الشاشة والتصدير. (٢) **`doInventoryReview`**
  كان يسجّل في الحركة `qty=fromStock` الكامل بينما يُقصَر الرصيد عند صفر، فتُعيد
  `recalcInventoryFromLog` بناءه **أقلّ من الواقع**؛ صار يسجّل الخصم الفعلي (`prevQty−newQty`).
  (٣) **المقارنة الشهرية:** «المستلمة» (`ticketsInMonth`) كانت تستبعد المؤرشف بينما «المغلقة»
  (`closedInMonth`) تشمله، وكلاهما من `tickets` (نافذة 600) لا `allTickets` — فتظهر
  مستلمة<مغلقة للأشهر السابقة وتختفي الأشهر القديمة. صار كلاهما من `allTickets()` بلا استبعاد
  `archived`. (٤) **KPI-03:** مقام معدّل SLA كان `closed` (كل المغلقة) بينما البسط `closedInSLA`
  مُرشَّح من `closedTix` (ذات الطوابع)، فبلاغٌ مغلق بلا طوابع يبخس المعدّل؛ صار المقام
  `closedTix.length`. **بذلك عولِجت كل المتوسطة من الجولة الثانية.**
- **tech-app v5.22 — مُنتقي المشروع: فنيّو المشروع المختار ديناميكياً (تطبيق الفنيين):**
  كان `WORK_TYPES` **ثابتاً** = فريق مشروع الأمانة فقط، فيرفض فنيّي بقية المشاريع. الآن شاشة
  الدخول فيها **مُنتقي مشروع** يُحمَّل من `meta/projects`؛ وعند اختيار مشروعٍ يُحمَّل فريقه من
  `meta/{id}_settings.workTypes` (عبر `_applyWT` الذي يبدّل `WORK_TYPES`/`ALL_TECHS` في مكانهما)،
  وتُقسَّم البلاغات على `{id}_tickets`. **الأمانة (`hail`) تبقى حرفياً كما كانت** (فريق مضمّن +
  `hail_tickets` + `meta/settings`) فلا انحدار، وإن تعذّر تحميل المشاريع تظهر الأمانة وحدها.
  الجلسة تحفظ `projectId` فيُستعاد المشروع عند العودة (مسار البلاغات حتمي فوري، الفريق خلفياً).
  **⚠️ يتطلّب قواعد Firestore:** التطبيق مجهول المصادقة، فلا بدّ من السماح له بقراءة
  `meta/projects` و`meta/{id}_settings` (غير مسموحتين حالياً — القاعدة العامة تشترط دوراً)؛
  بدونها تظهر الأمانة فقط. **لم يُختبَر على مشروعٍ حقيقي غير الأمانة** (تطبيق منفصل خارج hail-tests).
- **tech-app v5.21 — تخفيف تسريب أرقام الدخول (تطبيق الفنيين، منفصل عن النظام الرئيسي):**
  كشفت جولة التدقيق الثانية أن مصادقة الفنيين تتحقّق في المتصفح من أرقامٍ **مخزّنة صريحةً**
  (`technicians/*.pin` و`meta/settings.adminPin`)، والاستضافة عامّة (GitHub Pages) بمصادقةٍ
  مجهولة — فأي شخص يفتح الرابط يقرؤها. **تخفيف** (لا حلّ جذري): تُخزَّن الآن **مُجزّأةً**
  (SHA-256 بملح `اسم+ثابت` عبر `_hashPin`/`_pinMatches`)، فالقراءة تُعطي تجزئةً لا رقماً؛
  توافق رجعي كامل (الصريح القديم والافتراضي يبقيان يعملان)، وكتابة `changeTechPin` تُخزّن
  التجزئة. **يبقى ضعيفاً** (رقم 4 خانات قابل للتخمين)؛ الحلّ الجذري = نقل التحقّق للخادم
  (Custom Token بدور `technician`) وقفل قراءة تلك المستندات بالقواعد. (رقم المدير يُكتب
  يدوياً خارج الكود؛ المقارنة صارت مُدركة للتجزئة، فتجزئته يدوياً عبر `_hashPin('__admin__',pin)`
  تُخفيه.) لا يمسّ النظام الرئيسي ولا اختباراته.
- **v18.9sd — معالجة جذر التواريخ: توحيد تجميع الشهور/الأيام على التوقيت المحلّي (+13 → 220/220):**
  نمطٌ منهجي: `toISOString().slice(0,7|10)` (UTC) مطبَّقٌ على قيمٍ هي تواريخ تقويمية محلّية،
  مخلوطاً بـ `getMonth()/getFullYear()` (محلّي) في مواضع أخرى — فيقع السجلّ في الشهر/اليوم
  الخطأ ضمن نافذة ±3س حول الحدود، ويتناقض جانبُ المشتريات (UTC) مع جانب الصيانة (محلّي).
  أُضيف **`_ym`** (مفتاح شهر محلّي) و**`_parseLocalDate`** (تاريخ-فقط → منتصف نهارٍ محلّي فلا
  ينزلق ليومٍ سابق كـ `new Date("YYYY-MM-DD")` التي تُفسَّر UTC-midnight) بجوار `_ymd` القائم.
  المواضع المُصلَحة: (١) **تاريخ «من» الافتراضي** في تقريرَي الشاشة/الصور كان يقع في الشهر
  السابق. (٢) **المتابعة اليومية** كانت تُطابق بشريحة UTC فتخطئ بيومٍ للبلاغات 00:00–02:59
  وتُظهر «الأمس» فجراً. (٣) **مخطّط إنفاق المشتريات** كان يبني مفاتيح الأشهر محلياً ويُسند
  الطلب بشريحة UTC (تسريب عبر حدود الشهر). (٤) **`poApprovedThisMonth`/لوحة `thisMonth`** كانا
  UTC بينما الصيانة محلّي. (٥) **`isPOOverdue`/`poOverdueDays`** كانا يحلّلان تاريخ-فقط كـ UTC
  (ينزلق يوماً في المناطق سالبة الإزاحة). (٦) **فلاتر مدى التاريخ** (قائمة الطلبات + التقارير)
  كانت تحلّل «من» UTC و«إلى» محلّي (مدى مائل) وتقارن شريحة UTC بتاريخ الفلتر المحلّي — وُحِّدت
  على اليوم المحلّي. اختبارات تنفيذية للمساعدات + حراسة، مؤكَّدة عبر UTC+3 و UTC-8.
- **v18.9sc — أربعة إصلاحات حرِجة من جولة التدقيق الثانية، مع اختباراتها (+19 → 207/207):**
  (١) **XSS مخزّن:** `esc` وحدها لا تكفي داخل `onclick="fn('...')"` — `'`→`&#39;` يفكّها مُحلّل
  HTML إلى `'` **قبل** أن يُصرَّف جسم المعالج كـ JS، فتُكسَر السلسلة وتُحقَن شفرة (اسمٌ مثل
  `x'-alert(1)-'` ينفّذ في متصفّح الأدمن ⇒ ترقية صلاحية). أُضيفت **`_jsq`** (تهرّب `\` و`'`
  بخطٍّ مائل وتُبقي `" & < >` كِياناتٍ) ووُجِّهت إليها **حقول النصّ الحرّة** في المعالجات
  (اسم بند/مشروع/أصل/مورّد/مبنى/مستودع/IBAN — 18 موضعاً؛ المعرّفات آمنة بطبيعتها). (٢) **نقل
  المخزون:** `recalcInventoryFromLog` كان يتجاهل حركات `transfer` (لا فرع لها)، فيُعيد بناء
  المصدر **بلا خصم النقل** (وحدات وهمية) والوجهة **بلا إضافته**. الآن النقل يسجّل `destItemId`،
  وإعادة الحساب **تخصم من المصدر وتضيف للوجهة**، مع حارس `NaN` على `qty`. (٣) **تصدير Excel
  للمشتريات:** كان يمرّ على `purchases` **كاملاً متجاهلاً فلاتر الشاشة** (تفلتر مشروعاً/شهراً
  فيُصدَّر كل الطلبات). أُضيف `_purchaseReportFiltered` مصدراً موحّداً تستخدمه الشاشة والتصدير.
  (٤) **تناقض SLA «عادي»:** قياس الالتزام كان يقارن ساعات العمل بـ `getSLA` (ساعات الأولوية
  **التقويمية**: 48 لعادي) بينما `isOverdue` يقيس بميزانية ساعات العمل (16) — بطاقتان متناقضتان
  على الشاشة نفسها. رُفعت **`_closedOnTime`** لدالة عليا موحّدة (`budgetMin`) ووُجِّهت إليها كل
  مواضع الالتزام (KPI-03، التجميع، المقارنة الشهرية، ملخّص الذكاء)؛ `getSLA` للعرض النصّي فقط.
  **باقي جولة التدقيق الثانية** (تصدير المخزون، نمط التواريخ UTC/محلي، `doInventoryReview`،
  المقارنة الشهرية received/archived، `tech-app.html`، قواعد Firestore) موثَّق للدفعات اللاحقة.
- **v18.9sb — ملخّص الموردين: نسبة التكلفة/العدّ للمورّد الفعلي لا المطلوب (`poVendorBreakdown`):**
  ملخّص الموردين في تقرير المشتريات (`byVendor`/`byVendorPDF`) كان يُفهرِس على `p.vendor`
  (**مورد الطلب**)، فطلبٌ ورّده مورّدٌ آخر يُنسَب خطأً لمورد الطلب لا الفعلي. أُضيفت
  `poVendorBreakdown(p)` (بجوار `poActualCost`) تُوزّع الطلب المغلق على مورّديه **الفعليين**
  بحصّة كلٍّ من الفواتير (`grnDocs[].invoicedTotal`)؛ وعند غياب السندات (طلب مغلق قديم) يُنسَب
  لـ `actualVendor` ثم `vendor` بكامل التكلفة الفعلية؛ وغير المغلق يُعَدّ بلا تكلفة. استُخدمت في
  **نسختَي التقرير** (الشاشة والطباعة). فحوص تنفيذية لـ `poVendorBreakdown` + حراسة. **بذلك
  اكتمل علاج كل ما رصده الفحص الشامل** (حرج + متوسط + منخفض).
- **v18.9sa — ثلاثة إصلاحات متوسطة مع اختباراتها (+13 → 177/177):**
  (١) **KPI (`purchase-kpi.js`):** `pending_finance` (بانتظار سداد المالية) كانت **غائبة عن
  `STAGE_ORDER`**، فيبتلع `pending_ceo` زمنَ انتظار المالية كلَّه (`next` يبحث في `STAGE_ORDER`
  وحدها فيقفز فوق الحدث) — فيتضخّم «زمن اعتماد المدير» ويختفي «زمن المالية» من مخطّط متوسط
  المراحل رغم أن بطاقة المالية تقيسه صحيحاً (تناقض العرضين). أُدرجت في موضعها الصحيح بين
  `pending_ceo` و`proc_executing`. (٢) **الميزانية البديلة (`substitute-budget.js`):** الطلبات
  **الملغاة/المرفوضة/المحذوفة** كانت تبقى «قيد التنفيذ» أبداً (`_calcStats` ثنائي: مغلق→مستهلك،
  وكلُّ ما عداه→WIP) فتضخّم `wipSell` وعدّ الطلبات. أُضيف كاشف `_isDead` (يمرّ عبر
  `normalizePOStatus`) يستبعد الميّت من WIP والعدّ (`count` = الحيّ فقط، +`deadCount`)، ويُميَّز
  الميّت في الجدول التفصيلي بلا سعر بيع/ربح وهمي. (٣) **الجرد (`stocktake.js` `_apply`):** فشلُ
  بندٍ وسط حلقة التطبيق كان يزيد `failed` فقط ثم يُوسَم الجردَ **«مطبَّق»** والواجهةُ تختفي —
  فتُترَك التسويات الفاشلة معلّقةً بصمت. الآن الفشل الجزئي **يعيد الجرد لحالته القابلة للإعادة**
  (`counting` للتلقائي، `pending_approval` للمعتمَد)، والتطبيق `idempotent` (يتخطّى المطبَّق مقابل
  الطازج ويُعيد الفاشل وحده)، فلا يُوسَم «مطبَّق» إلا عند نجاحٍ كامل.
- **v18.9rz — الجرد الشهري: منع تخطّي بندٍ انحرف رصيده الحيّ أثناء العدّ (`stocktake.js` `_apply`):**
  فلتر «الأهداف» كان يقارن المعدود بـ **لقطة النظام المجمّدة** (`systemQty`) وقت إنشاء الجرد.
  فلو تصادف أن معدود البند = اللقطة القديمة بينما انحرف الرصيد الحيّ بعدها (وصل طلب شراء على
  الصنف **أثناء** العدّ)، كان البند **يُسقَط** من الأهداف ولا يُصحَّح رصيده — رغم أن «العدّ
  حقيقة مطلقة». الآن يُطبَّق على **كل بندٍ معدود**، وتُحسَب الدلتا مقابل **الرصيد الطازج الحيّ**
  داخل المعاملة (`_adjustDelta(counted, fresh)`)، والكتابة تجري فقط إن اختلف فعلاً عن الطازج
  (`delta≠0`) فلا كتابةَ لبندٍ لم يتغيّر (تكلفةُ التحقّق قراءةٌ رخيصة). وملاحظة السجل صارت
  «الرصيد قبل = الطازج» اتساقاً مع الدلتا الفعلية، و`adjustedCount` صار **التسويات الفعلية**
  لا مجرّد البنود المفحوصة. حراسة + فحوص تنفيذية لـ `_adjustDelta`.
- **v18.9ry — الإغلاق اليدوي لطلب الشراء يتطلّب «تكلفة فعلية» صريحة > 0 (منع تسرّب التقدير):**
  الإغلاق اليدوي (تجاوز الأدمن بلا سند استلام، في `doUpdatePurchaseStatus`) كان يحفظ
  `actualCost` فقط `if(actualCost)`، وحقل «التكلفة الفعلية» **مُعبّأ تلقائياً بالتقدير**
  (`autoActual` = مجموع البنود أو `estCost`). فلو أغلق الأدمن الطلب دون تصحيح القيمة —
  أو مسحها إلى صفر — دخل **التقدير** في الملخّصات المالية (إجمالي التكاليف، ملخّص المورّد/
  المشروع/المبنى، اللوحة) كأنه **فاتورة فعلية**، مخالفاً §٣ («الفعلي لا المقدّر»؛ الرجوع
  للتقدير مسموح للطلبات المغلقة القديمة فقط). الآن **يُمنع** الإغلاق اليدوي بقيمة ≤ صفر
  (رسالة تطلب إدخال فاتورة المورد شامل الضريبة)، ويُظهر تأكيد الإغلاق **الرقم المُسجَّل**
  كتكلفة فعلية ويطلب تأكيد أنه فاتورة المورد الحقيقية لا التقدير — فيصير قراراً واعياً لا
  تسرّباً صامتاً. مسار الاستلام السليم (`doWarehouseAudit`) غير متأثّر (يسجّلها من الفاتورة).
- **v18.9rx — شبكة أمان الاختبارات: تغطية `stocktake.js` + إصلاح العدّ السالب + فلتر CI (+14 → 152/152):**
  (١) **تغطية `stocktake.js` لأول مرة** (٩٠٨ أسطر كانت بلا أي اختبار — أكبر فجوة تغطية):
  فحوص تنفيذية لتصنيف الفرق `_classify` (الفرق = المعدود − النظام؛ «الكبير» = ≥`ST_BIG_PCT`%
  مع حدٍّ مطلق ≥`ST_ABS_FLOOR`؛ رصيد صفر + مخزون ظاهر = مراجعة) وقصر العدّ `_countAt`.
  (٢) **إصلاح «العدّ السالب → رصيد سالب»:** `_apply` كان يُثبّت `currentQty = counted` (إسناد
  مطلق)، فلو أُدخل عددٌ سالب (كتابةً أو استيراداً؛ `min="0"` مجرّد تلميح HTML) كُتب رصيدٌ سالب
  يخرق ثابت «الرصيد ≥ 0» المفروض في كل النظام. أُضيف `_countAt(counts,id)=Math.max(0,_num(...))`
  يُستهلَك في **فلتر الأهداف والنتائج والتطبيق** معاً فيتّسق الرصيد والسجل والتقرير.
  (٣) **إصلاح فلتر CI** (`.github/workflows/hail-tests.yml`): كان فلتر `paths` لحدث `push`
  يُغفل `stocktake.js`/`substitute-budget.js`/`HailNotify.js` (وكلها يفحصها `hail-tests`)،
  فلا يُشغَّل CI عند push مباشر لها. صار يشمل كل ملفٍ مفحوص + ملف سير العمل نفسه.
- **v18.9rw — معالجة جذر سباقات الكتابة (لقطة محلية → كتابة غير ذرّية)، مع اختباراتها (+17 → 138/138):**
  نمطٌ متكرّر: وحداتٌ تكتب مجموعات مشتركة اعتماداً على لقطة في ذاكرة المتصفح بلا معاملة،
  فتحدث سباقات فقدان تحديث وأكواد مكرّرة. عُولج مركزياً على ثلاثة مواضع:
  (١) **الميزانية البديلة (`substitute-budget.js`):** `_save` كان `set({accounts:_accounts})`
  يكتب المصفوفة كاملةً من اللقطة المحلية، فتعديل مسؤولٍ على حساب **يدهس** تعديل مسؤولٍ آخر
  على حسابٍ مختلف. صار الحفظ **معاملةً** تقرأ مصفوفة الخادم الحيّة وتدمج تغيير الحساب الواحد
  فقط (`_txAccounts` + `_upsertAccount`/`_removeAccountTx`)، فلا تُفقَد تعديلات الآخرين.
  (٢+٣) **تحليل الأسعار والكتالوج (`price-analysis.js`/`labor-catalog.js`):** توليد الكود
  `PA-###`/`LAB-###` كان من `_items` المحلية بلا فرض خادمي، فمستخدمان يضيفان معاً يُنتجان
  الكود نفسه (والفحص المحلي لا يرى التزامن). صار التخصيص **ذرّياً عبر عدّاد في `meta`**
  (بنمط `newId`): `next = max(عدّاد الخادم, أكبر رقم محلي)+1`، ويجري **عند الحفظ** لا عند
  فتح المحرّر (`genCode` بقيت معاينةً فقط)؛ والكود المخصّص يدوياً بنمطٍ مختلف يُحترَم.
  **الاختبارات:** تنفيذية للمبدّلات النقية (`_applyUpsert`/`_applyRemove` — تُثبت أن دمج
  حسابٍ يُبقي البقية) ومنطق العدّاد (`_nextCodeNum` — يُثبت أن عدّاد الخادم المتقدّم يمنع
  التكرار)، + حراسة النمط المصدري في الوحدات الثلاث.
- **v18.9rv — إصلاح ثلاث ثغرات حرجة كشفها فحص شامل، مع اختباراتها (+21 فحصاً → 121/121):**
  (١) **عكس المخزون عند حذف طلب مُستلَم على عدة جلسات (`deletePurchase`):** كان العكس
  يقرأ `po.auditItems` — وهي تُعاد كتابتها بالكامل كل جلسة تدقيق (لا تُلحَق مثل `grnDocs`)،
  فتحمل آخر جلسة وحدها. فطلبٌ استُلم على جلستين (٥ ثم ٣) كان يُعكَس منه ٣ فقط وتبقى ٥ وحدات
  يتيمة في المخزون. صار مصدر العكس بنودَ **كل السندات** (`po.grnDocs[].items`) بالجمع، مع
  الرجوع إلى `auditItems` للطلبات القديمة التي سبقت وجود `grnDocs`.
  (٢) **حفظ الطلب بعد كتابة المخزون كان fire-and-forget (`doWarehouseAudit`):** المخزون
  يُكتب في Transaction ذرّية ثم `savePurchase` غير مُنتظَرة تبتلع الخطأ. فلو فشلت بقي الرصيد
  مرفوعاً بلا `grnDocs`، وعند إعادة التحميل يقرأ `_waPrevRcv` صفراً فيُعاد ملء الكمية —
  فإعادة الإرسال تُضاعف المخزون (حارس التكرار مهزوم لأن `grnDocs` لم يُحفظ). صار الحفظ
  **يُنتظَر ويُعاد ٣ مرات ويُتحقَّق**، ورسائل النجاح (`_afterSaveOK`) تُؤجَّل حتى تأكيده،
  مع تحذير صريح عند الفشل يمنع إعادة التدقيق.
  (٣) **حلقة إزاحة الإشعارات اللانهائية (`HailNotify.js`):** `dismiss()` لا يحذف العقدة
  فوراً (يعلّمها `__gone` ويؤجّل الحذف 260ms)، فحلقة `while(children.length > maxVisible)`
  تكرّر على نفس العنصر المُعلَّم دون أن ينقص العدد ⇒ **تجمّد التبويب** عند الإشعار الخامس
  (والعاجل لزج فيتراكم). صارت تطوي على **لقطة ثابتة للعناصر الحيّة** وتُزيح الأقدم منها.
  **وخارج الحرج:** `num()` في `price-analysis.js` و`labor-catalog.js` كانت `replace(/,/g,".")`
  تحوّل «1,250.00» إلى «1.250.00»→`parseFloat`=1.25 (بخس السعر/الكمية 1000×). صارت تحسم
  الفاصل العشري: الأرقام العربية→لاتينية، وعند «.»+«,» الأخير عشري والآخر آلاف، وعند «,»
  وحدها تُعامَل عشرية فقط إن تبعها ١-٢ خانة. **التغطية:** أُضيف قسم اختبار تنفيذي يغطّي
  `num()` في الملفين لأول مرة (١٧ حالة لكلٍّ)، وقسمٌ لـ `HailNotify` يثبت انتهاء حلقة الإزاحة،
  وفحوص حراسة لإصلاحَي المخزون في `index.html`.
- **v18.9ru — تدقيق الاستلام: منع فقدان فاتورة مملوءة بلا بند مخصّص لها (`doWarehouseAudit`):**
  الجذر: عند تعدّد الفواتير يُخصَّص كل بند لفاتورته عبر القائمة في العمود (`wa-inv-pick`،
  الافتراضي = الفاتورة الأولى). فلو ملأ المستخدم فاتورةً ثانية كاملةً (مورد + رقم + صورة)
  دون أن يخصّص لها أي بند، كانت تُستبعَد من `activeInvs` (تُبنى من `_liveUids` = الفواتير
  التي عليها بندٌ بكمية مستلمة) و**تُحذَف بلا أثر** — فلا يُنشأ لها سند ولا تدخل `p.invoices`،
  فيظهر سندٌ واحد في تفاصيل الطلب والـ PDF رغم إدخال فاتورتين. (كل مسارات العرض — تفاصيل
  الطلب `_poInvoiceList` وطباعة `_buildAllInvoicesHtmlForPrint` — كانت تقرأ `p.invoices`
  بصحة؛ العطل في الإنشاء لا العرض.) الإصلاح: بعد حساب `activeInvs`، إن وُجدت فاتورة مملوءة
  (`vendor || invoiceNo || file`) غير نشطة، يُمنع الحفظ برسالة تطلب تخصيص بنودها في العمود
  أو حذفها إن أُضيفت خطأً — فلا تُفقد فاتورة بصمت. (لا يسترجع الفواتير المفقودة سابقاً — لم
  تُحفظ أصلاً — بل يمنع تكرار الفقد.)
- **v18.9rt — طباعة طلب الشراء (PDF): إظهار شارة استبدال البنود (عرض فقط):**
  عند استبدال بندٍ ببندٍ آخر أثناء تدقيق الاستلام (`it.substitution`) كانت الشارة «⇄ استُبدل:
  الأصلي ← البديل» تظهر في تفاصيل الطلب **على الشاشة فقط** (خانة اسم المادة) وتغيب عن ملف
  الـ PDF المطبوع. أُضيفت الشارة الآن في خانة اسم المادة في **كلا مولّدَي الطباعة**:
  `printPurchaseDetail` (مع الأسعار) و`printPurchaseDetailNoPrices` (بدون أسعار)، وتُظهر
  الاسمين والسبب (`fromName ← toName — reason`). لا منطق، عرض فقط.
- **v18.9rs — لوحة المشتريات: مؤشّر تحميل بتصميم المنصة أثناء أول مزامنة (عرض فقط):**
  عند فتح لوحة المشتريات قبل وصول بيانات الطلبات من الخادم (لا كاش محلي — عُطّل IndexedDB في
  `pf`) كانت البطاقات تعرض أصفاراً والمخططات «لا توجد بيانات»، فتبدو الصفحة فارغةً لا قيد
  التحميل. الآن في `renderPurchasesDashboard`: ما دام `_fsLoaded.purchases` غير محقّق، تعرض
  بطاقات المؤشّرات الخمس (`po-dash-*`) حلقةَ تحميل مصغّرة `.st-spin` (ترث لون البطاقة عبر
  `currentColor`)، وتعرض حاويات المخططات وبطاقتا المسار/الملخّص (`po-stage-flow`/`po-stage-donut`/
  `po-open-summary`/`po-chart-*`) مؤشّرَ `fs-sync` القائم، وتُخفى بطاقة «بانتظار إجراءك». يُرفَع
  الحجب فور وصول أول لقطة (`_fsLoaded.purchases=true`) فيكتمل الرسم الطبيعي. قائمة الطلبات كانت
  تعرض المؤشّر أصلاً. لا منطق. (ملاحظة أداء: التأخير نفسه سببه غياب الكاش المحلي + استعلام 400
  طلباً + انطلاق ~12 مزامنة معاً عند الدخول — لم يُغيَّر هنا، هذا تحسين إدراكي للتحميل فقط.)
- **v18.9rr — إصلاح ظهور اسم المشروع رمزاً إنجليزياً لبعض المستخدمين (`_getProjName`):**
  في بطاقة «بانتظار إجراءك» (وغيرها) كان اسم المشروع يظهر لمستخدمٍ (مثل مسؤول المشتريات)
  رمزاً إنجليزياً خاماً (`hail` / `bathroom001`) بينما يظهر لمستخدم آخر (المسؤول) بالاسم
  العربي الصحيح. الجذر: `_getProjName(projId)` كان يُرجِع **المعرّف الخام** عند عدم إيجاد
  المشروع في `window._projectsList` (قائمة لم تُحمّل كاملةً لذلك المستخدم، أو مشروع غير
  مُسجّل في `meta/projects`) — والمعرّف قيمة «صادقة» فتحجب الرجوع إلى `p.projectName`
  المخزّن على الطلب وقت إنشائه. صار يُرجِع `""` عند عدم الإيجاد، فيظهر الاسم الصحيح عبر
  بديل المستدعين الموحّد (`_getProjName(id) || p.projectName || p.projectId`). كل مستدعي
  الدالة (١٦ موضعاً) لديه هذا البديل أصلاً، فالتغيير آمن ولا يكسر أي مسار. عرض فقط.
- **v18.9rq — عنوان الإشعار الأصلي (Notification): «نظام هيل» → «نظام ادارة الصيانة والمشتريات»:**
  في `HailNotify.js` (`fireNative`) كان عنوان إشعار المتصفح `"نظام هيل — " + KICKER`؛ صار
  `"نظام ادارة الصيانة والمشتريات — " + KICKER`. عرض فقط، لا منطق. (بقيت كلمة «هيل» في
  تعليقات المصدر وعناوين الملفات الداخلية فهي غير ظاهرة للمستخدم.)
- **v18.9rp — النوافذ (modal): إلغاء الإغلاق بالنقر خارج النافذة وتفعيله بمفتاح Esc:**
  كان النقر على خلفية أي نافذة (`.modal-overlay`) يُغلقها — فتُفقَد المُدخَلات عند نقرة
  عرضية. أُزيل مستمع النقر على الخلفية، وحلّ محلّه مستمع `keydown` عامّ يغلق **النافذة
  العليا فقط** (الأعلى `z-index`، وعند التساوي الأخيرة في DOM) عند ضغط `Esc`. نافذة
  التأكيد (`modal-confirm`) تُغلق عبر مسار الإلغاء (`_confirmReject(false)`) كي لا يبقى
  الوعد معلّقاً. وأُضيف `stopPropagation` لفرع `Esc` في `onAcKeydown` (قائمة اقتراحات
  الكتالوج) كي يغلق `Esc` القائمة المفتوحة وحدها لا النافذة تحتها. سلوك واجهة فقط.
- **v18.9ro — بطاقة طلب الشراء (`po-card`): اسم المشروع هو العنوان الكبير والمبنى شارة صغيرة (عرض فقط):**
  كانت بطاقة الطلب في قائمة المشتريات تعرض — للطلبات الرسمية — **المبنى** كبيراً (13px)
  واسم المشروع **شارةً صغيرة**، بينما المشاريع اليدوية (`isCustomProject`) تُظهر اسم المشروع
  كبيراً. صار اسم المشروع **دائماً** هو العنوان الكبير (أيقونة `folderOpen`) لكلا النوعين —
  عبر `_getProjName(p.projectId)||p.projectName` للرسمي و`p.projectName` لليدوي — والمبنى
  شارةً صغيرة (أيقونة `building2`) تظهر للطلبات الرسمية ذات المبنى فقط. اتساقاً بصرياً بين
  الطلبات الرسمية واليدوية. لا منطق، عرض فقط.
- **v18.9rn — تدقيق الاستلام: إلغاء التعبئة التلقائية لخانتي «المورد الفعلي» و«اسم المستودع»:**
  في `openWarehouseAudit` كانت خانتان تُملآن بقيمةٍ منقولة من سياق سابق فتُحفَظ خطأً إن قبلها
  المدقّق كما هي: (١) **المورد الفعلي** كان يُعبّأ من `p.actualVendor || p.vendor` (مورد الطلب)
  فيُقبل بلا مراجعة رغم أن المورّد الفعلي قد يختلف — صار يُفتح فارغاً، ومورد الطلب يبقى ظاهراً
  في التلميح (`wa-ordered-vendor-hint`) للاسترشاد لا للتعبئة. (٢) **اسم المستودع** كان يقترح
  مستودع أحد أصناف الطلب، وإلا يرجع إلى **آخر مستودع مُستخدم** (`localStorage hail_last_warehouse`)
  فيتسرّب اسم مستودع عملية مخزون سابقة ويدخل المخزون تحته صامتاً — أُلغي كل الاقتراح وصار يُفتح
  فارغاً؛ وقائمة الإكمال التلقائي (`datalist` من `getWarehouseNames`) باقية. لا تغيير في منطق
  الحفظ نفسه — القيمتان تُقرآن من الحقلين عند الإرسال كما كان.
- **v18.9rm — المقارنة الشهرية (`renderMonthlyCompare`) + أزرار الذكاء: إيموجي → SVG (عرض فقط):**
  الترويسة 📈→`trendingUp`. بطاقات المقارنة: بدلاً من 📊 عامّة + إيموجي المقياس، صار لكل مقياس
  أيقونته الخاصّة عبر حقل `icon` (📋→`clipboardList`، ✅→`checkCircle`، ⚠→`alertTriangle`، 🎯→`target`،
  ⏱→`timer`، 🔁→`repeat`) وحُذفت 📊 المكرّرة. عنوانا المخططين 📊/📈→`barChart`/`trendingUp`. وشارة
  ✨ في **كل أزرار «تحليل ذكي/ملخص تنفيذي»** (`.ai-spark` — ٩ مواضع) صارت أيقونة `sparkles` (+قاعدة
  `.ai-spark svg`). أسهم الاتجاه (↑/↓/=) نصّية تبقى. عرض فقط، لا منطق.
- **v18.9rl — خريطة المباني (`page-buildings` + `renderBuildings`): إيموجي → SVG (عرض فقط):**
  الترويسة 🗺️→`map`. بطاقات الملخّص (`stat-tile`) 🏢/✅/🔴/⚠️→`building2`/`checkCircle`/`alertCircle`/
  `alertTriangle` (عبر `_svgIcon`؛ `.st-ico svg` مُحجَّم 19px أصلاً). بطاقة المبنى: 🏢 الاسم→`building2`،
  و⚠ «متأخر عن الاستجابة»→`alertTriangle`. زرّ القائمة الجانبية 🗺️ مُحوَّل أصلاً عبر `NAV_ICON_BY_PAGE`
  (`buildings:'map'`). سهم «اضغط للتفاصيل ←» بقي (نصّي). عرض فقط، لا منطق.
- **v18.9rk — بطاقات مؤشرات الأداء التفصيلية + صفحة «مؤشرات الأداء»: إيموجي → SVG (عرض فقط):**
  في `renderKPIData` (مشتركة بين الداشبورد وصفحة الأداء): بطاقة الدونات 🎯 هدف→`target` (كان سطر
  «الهدف» في الشريط الخطّي قد حُوِّل في rf؛ هذا يكمل بطاقة الدونات). في صفحة «مؤشرات الأداء»:
  بطاقات القمة (`kpi-trend-card`) 📅/✅/⏱/📊→`calendar`/`checkCircle`/`timer`/`barChart`، عنوان مخطط
  «نشاط البلاغات» 📅→`calendar`، وتلميح 💡→`lightbulb`. عناوين الأقسام (`.kpi-section-title`: 📊/📈/🔍)
  تُحوَّل أصلاً عبر `applyGlyphIcons`. عرض فقط، لا منطق.
- **v18.9rj — صفحة «بلاغ جديد» (`page-new`): كل الإيموجي أيقونات SVG (عرض فقط):**
  أُضيفت أيقونة `clock` جديدة إلى `_ICON` (+قاعدة `#ticket-success-card .tsc-icon svg` بحجم 56px).
  بطاقة النجاح ✅→`checkCircle`، أزرارها 📋/➕/👁→`clipboardList`/`plus`/`eye`. الترويسة ➕→`plus`.
  الحقول 📅→`calendar`، 🕐→`clock`، 🔧 الأصل المرتبط→`wrench`، 📸/📷 الصورة→`camera`؛ أيقونة البحث
  داخل حقل الأصل 🔍→`search` مضمّنة. الأزرار 🗑 مسح→`trash`، ✅ تسجيل→`checkCircle`. وحُذفت نقاط
  الأولوية 🔴🟡🟢 و🔍 من `placeholder` (لا يقبل SVG). ملاحظة: نافذة تعديل البلاغ تشارك حقول
  التاريخ/الوقت والأولوية فطُبّق عليها الاستبدال نفسه لاتساقها. عرض فقط، لا منطق.
- **v18.9ri — المتابعة اليومية (`renderDaily`): إيموجي → أيقونات SVG — يكتمل جانب الصيانة (عرض فقط):**
  ترويسة اللوحة 📅→`calendar`. بطاقات الإحصاء 📥/✅/🔧/⏰→`download`/`checkCircle`/`wrench`/`alarmClock`
  (+قاعدة `.daily-card-ico svg` بحجم 22px). بطاقات البلاغ المصغّرة (`miniTicket` + قائمة المتأخّر):
  ⚠→`alertTriangle`، 👷→`hardHat`، ⏱→`timer`، 📍→`pin`، الأزرار 👁/📸/🔄/👷/🗑→`eye`/`camera`/`repeat`/
  `hardHat`/`trash`. الحالات الفارغة 🎉→`sparkles`/✅→`checkCircle`. وشريط تنبيه SLA الطافي ⚠→
  `alertTriangle`. عناوين أعمدة اللوحة (`.card-title`: 🔴/✅/⚠) تُحوَّل أصلاً عبر `applyGlyphIcons`.
  بذلك اكتملت الشاشات الخمس لجانب الصيانة (الداشبورد، البلاغات، الأرشيف، المتابعة، والمؤشرات). عرض فقط.
- **v18.9rh — أرشيف البلاغات (`page-tickets-archive` + `renderArchiveMonthTickets`): إيموجي → SVG (عرض فقط):**
  الترويسة 🗄️→`archive`، زر «تصدير Excel» 📗→`sheet` (٣ مواضع: الأرشيف + تقريرا الشاشة/المشتريات)،
  زر «أرشفة شهر يدوياً» 📦→`archive`. الحالات الفارغة 🗄️/⏳→`archive`/`hourglass`، `📭`→`archive`.
  شريحة الشهر 📅→`calendar`، وعنوان القائمة 📋→`clipboardList`. بطاقة البلاغ المؤرشف: شارة «مؤرشف»
  🗄→`archive`، ⚠ تجاوز SLA→`alertTriangle`، 🏢→`building2`، 👷→`hardHat`، 📅→`calendar`، ✅ أُغلق→
  `checkCircle`، 🔧 نوع العمل→`wrench`، زر 👁 التفاصيل→`eye`. وحُذف 🔍 من `placeholder` البحث، و🔧/🛡
  من خيارات فلتر نوع الصيانة الأصيلة (`<option>` — في كل القوائم لاتساقها). عرض فقط، لا منطق.
- **v18.9rg — قائمة البلاغات (`renderTickets`): كل الإيموجي أيقونات SVG (عرض فقط):**
  أُضيفت أيقونتا `eye`/`camera` إلى `_ICON` (+قاعدة `.te-ico svg` بحجم 34px). حُوِّل الإيموجي في
  بطاقتَي البلاغ (سطح المكتب والجوال): شريط الشهر 📅→`calendar`/↩️ المرحّل→`rotateCcw`/🗄️ الأرشيف→
  `archive`؛ حالات فارغة 🔍/📭→`search`/`archive` و➕→`plus`؛ ⚠ متأخر→`alertTriangle`، نوع الصيانة
  🛡/🔧→`shield`/`wrench`، 👷 الفني→`hardHat`، 👤 المشرف→`user`، ⏱→`timer`، 📅→`calendar`، ✅ أُغلق→
  `checkCircle`، 📍→`pin`، 🏢→`building2`؛ الأزرار 👁 التفاصيل→`eye`، 📸 الإغلاق→`camera`، 🔄 تغيير/
  👷 تعيين الفني→`repeat`/`hardHat`، 🗑 الحذف→`trash`. (`typeIcon`/`statusBadge`/`priBadge` كانت SVG/CSS
  أصلاً.) وحُذف إيموجي 🔍 من `placeholder` البحث (لا يقبل SVG). عرض فقط، لا منطق.
- **v18.9rf — اللوحة التنفيذية (الداشبورد): الإيموجي الديناميكي أيقونات SVG (عرض فقط):**
  عناوين أقسام اللوحة (`.kpi-section-title`/`.card-title`) كانت تُحوَّل أصلاً عبر `applyGlyphIcons`.
  الآن حُوِّل الإيموجي الديناميكي في `renderDashboard`: بطاقات مؤشّرات الأداء 🎯 الهدف→`target`
  (أُضيفت أيقونة `target` جديدة إلى `_ICON`)، ✅ تحقق الهدف→`checkCircle`، ⚠ دون الهدف→`alertTriangle`؛
  وبطاقات البلاغات في اللوحة: شارة نوع الصيانة 🛡/🔧→`shield`/`wrench`، ⚠ متأخر→`alertTriangle`،
  ⏱ الزمن المنقضي→`timer`؛ وبطاقة «أحدث الطلبات المعلقة» ⏳→`hourglass`/🏢→`building2`/🙋→`user`/
  📅→`calendar`. (الأيقونات تُحلّ وقت العرض فلا مشكلة TDZ.) بقية شاشات جانب الصيانة (قائمة
  البلاغات، الأرشيف، المتابعة اليومية) في تعديلات لاحقة. عرض فقط، لا منطق.
- **v18.9re — أربع شاشات (مؤشرات الأداء، تقرير المشتريات، سجل التدقيق، كتالوج البنود): إيموجي → أيقونات SVG:**
  (١) **مؤشرات الأداء** (`purchase-kpi.js`): أُضيف مساعدا `ICN`(=`_ic`)/`ICG`(=`_svgIcon`)؛
  العنوان 📈→`trendingUp`، الحالة الفارغة 📭→`archive`، «أداء المالية» 💳→`banknote`، وعناوين
  المخططات ⏱→`timer`/📊→`pieChart`/💰→`receipt`/⛔→`ban`/🏪→`store`/🚦→`alertTriangle`، وزرّ القائمة
  الجانبية 📈→`trendingUp`. (٢) **تقرير المشتريات:** بقية عناوين الأقسام 🏭 ملخص حسب المورد→`store`،
  📋 تفاصيل جميع الطلبات→`clipboardList` (نسختا التقرير). (٣) **سجل التدقيق:** الترويسة 🧾→`scrollText`،
  زر «تحديث» 🔄→`rotateCcw`، وشارات الأقسام في الجدول (`_PO_AUDIT_CATS.label`) 🛒→`cart`/📦→`package`/
  🏪→`warehouse`/💰→`receipt`؛ وخيارات فلتر القسم الأصيلة `<option>` حُذفت إيموجيها. (٤) **كتالوج
  البنود** (بطاقات الإحصاء كانت SVG أصلاً): علامة «أُضيف عند الاستلام» 🆕→`sparkles`، وأزرار الصفّ
  «السعر» 💰→`receipt` و«الدمج» ⇄→`repeat`. ملاحظة: إيموجي `placeholder` (🔍/🏢) لا يقبل SVG — حُذف
  واكتُفي بالنصّ. لا منطق، عرض فقط.
- **v18.9rd — ثلاث شاشات (تقرير المشتريات، الجرد الشهري، طلب تسعير جديد): الإيموجي أيقونات SVG (عرض فقط):**
  (١) **تقرير المشتريات:** عنوانا القسمين في نسختَي التقرير (الشاشة والطباعة) 📊 توزيع الطلبات→
  `barChart`، 🗂 ملخص حسب المشروع→`folderOpen` (اتساقاً مع «المشروع»=folderOpen). شارات الحالة
  كانت أصلاً أيقونات SVG (`poStatusBadge`) فلم تُمَسّ. (٢) **الجرد الشهري** (`stocktake.js`):
  أُضيف مساعد `_icn`؛ ترويسة الصفحة وترويسة تفاصيل الجرد 📊→`barChart`، زر «بدء جرد جديد» وعنوان
  نافذته ➕→`plus`، وشارة حالة «جاري العدّ» ✏️→`edit`. وأُضيف `'stocktake':'barChart'` إلى
  `NAV_ICON_BY_PAGE` فصار زرّ القائمة الجانبية أيقونةً. (٣) **طلب تسعير جديد** (`#page-new-rfq`):
  العنوان ➕→`plus`، 📦 البنود المطلوب تسعيرها→`clipboardList`، 🧮 من تحليل الأسعار→`sheet`،
  ➕ إضافة بند→`plus`، 🗑 حذف صفّ البند→`trash`، 📤 إرسال طلب التسعير→`send` (زرّ + إعادة ضبطه).
  لا منطق، عرض فقط.
- **v18.9rc — نافذة «تعديل طلب الشراء» (`modal-po-admin-edit`): كل الإيموجي أيقونات SVG (عرض فقط):**
  إتماماً لتوأم rb، استُبدلت إيموجيات نافذة تعديل الطلب بأيقونات `_ICON` مضمّنة: 📅→`calendar`،
  🚚→`truck`، 📑 تصنيف→`tag`، 📄→`fileText`، 📊→`barChart`، 💰 التكاليف→`receipt`، 📦 بنود→
  `clipboardList` (نفس أيقونة «بنود الطلب» في صفحة الطلب الجديد للاتساق)، ➕→`plus`، 💾 حفظ→`save`
  (أُضيفت أيقونة `save` جديدة إلى `_ICON`). خيارات الأولوية في `<select>` حُذفت نقاطها 🔴🟡🟢.
  الاستبدال محصورٌ بمدى النافذة (بين `modal-po-admin-edit` و`modal-inventory-review`) فلم تُمَسّ
  نافذة مراجعة المخزون المجاورة ولا غيرها. لا منطق، عرض فقط.
- **v18.9rb — صفحة «طلب شراء جديد»: كل الإيموجي أيقونات SVG من الطقم (عرض فقط):**
  استُبدلت إيموجيات صفحة `#page-new-purchase` كلّها بأيقونات `_ICON` مضمّنة (`<span class="ic">`):
  العنوان 🛒→`cart`، شريط المسودة 📥→`download`/↩️→`rotateCcw`/🗑→`trash`، 📁→`folderOpen`،
  📅→`calendar`، 🚚→`truck`، 📑 تصنيف→`tag`، 📄→`fileText`، 📊→`barChart`، 📋 بنود→`clipboardList`،
  🔒→`lock`، ➕→`plus`، 📎→`paperclip`، 📤 إرسال→`send`. الاستبدال محصورٌ بمدى الصفحة
  (٣٢٠٢–٣٤١٤) فلم تُمَسّ نافذة تعديل الطلب التوأم ولا صفحات أخرى. خيارات الأولوية في
  `<select>` الأصيل (لا تقبل SVG) حُذفت نقاطها الملوّنة 🔴🟡🟢 واكتُفي بالنصّ (عاجل/متوسط/عادي).
  سهم الرجوع «←» بقي (مؤشّر تنقّل نصّي لا إيموجي). لا منطق، عرض فقط.
- **v18.9ra — كل أزرار «تعديل» وعناوين نوافذ التعديل أيقونة `edit` بدل ✏️ (عرض فقط):**
  بطلب المالك، حُوِّل إيموجي القلم ✏️ على **إجراء «تعديل»** في كل النظام إلى أيقونة الطقم
  `edit`: أزرار «تعديل» (البلاغ، المشروع، الأصل، المخزون، البند المستعاض، إعادة تسمية
  المستودع، تفعيل تعديل التقرير)، وعناوين نوافذ التعديل (تعديل البلاغ/المشروع/الأصل/خطة
  PPM/رصيد المخزون/الصنف/نوع البند/البند/المصنعية/تحليل البند/حساب البند المستعاض). حيث
  كان العنوان يُضبط بـ `textContent` (لا يعرض SVG) حُوِّل إلى `innerHTML` بنصٍّ ثابت آمن.
  في `substitute-budget.js` عبر `_icn`، وفي `labor-catalog.js`/`price-analysis.js` عبر
  `_ic` المحروس. **لم تُمَسّ** أيقونات غير الأزرار: شارة حالة «جاري العدّ» في الجرد
  (`stocktake.js`)، ورموز `icon:"✏️"` المخزّنة في الـ timeline، ونصّ الـ toast — فهي
  ليست أزرار تعديل. عرضي فقط، لا منطق.
- **v18.9qz — نافذة «حساب البند المستعاض»: علامتا «مربوط/مستقلّ» أيقونات SVG بدل الإيموجي:**
  استكمالاً لـ qy، بقيت في `substitute-budget.js` علامات إيموجي لنوع الحساب (🔗 مربوط /
  ✏️ مستقلّ) لم تُحوَّل. الآن: أُضيف مساعد `_icn(name,cls)` (غلاف آمن حول `_ic`)، وحُوِّلت
  كل علامات النوع في سياق HTML إلى أيقونات الطقم: شارة النوع في صفّ الحساب، وترويسة تفاصيل
  الحساب، وزرّا الاختيار (radio) في النافذة — `🔗 ← link` و`✏️ ← edit`. وفي عناصر `<option>`
  الأصيلة (لا تقبل SVG): قائمة ربط المشروع تُلحِق «(يدوي)» بالمشاريع اليدوية بدل ✏️، وقائمة
  حساب البند المستعاض في نموذج الطلب تُلحِق «(مستقلّ)» بالحساب المستقلّ بدل ✏️/🔗. لم تُمَسّ
  أزرار «✏️ تعديل» ولا عنوان نافذة التعديل (إجراء «تعديل» — لغة موحّدة في كل النظام). عرضي فقط.
- **v18.9qy — أيقونتا «البند المستعاض» و«اليدوي» أيقونات SVG من طقم المنصة (تَخلُف إيموجي qx):**
  بطلب المالك «أيقونات مثل المستخدمة في النظام لا إيموجي» — فحلّت محلّ إيموجي qx (🪙/✍️)
  أيقوناتُ الطقم الموحّد (`_ICON`/`_svgIcon`/`_ic`). (١) أُضيفت أيقونة `landmark` إلى
  `_ICON` وحلّت محلّ أيقونة «البند المستعاض» في كل مواضعه: زرّ القائمة الجانبية (بإدخال
  `'substitute-budget':'landmark'` في `NAV_ICON_BY_PAGE` — كان مفقوداً فلم يكن يُستبدَل)،
  ورأس صفحة `substitute-budget.js` (`_ic("landmark","ic-lg")`)، وقسم «طلب من البند المستعاض»
  في نموذج الطلب، وسطر «البند المستعاض» في تفاصيل الطلب (`_ic("landmark")`). (٢) علامة
  الإدخال اليدوي: عناصر `<option>` الأصيلة لا تقبل SVG، فحُذف الإيموجي واعتُمد على نصّ
  «(يدوي)» القائم (خيارات «مشروع آخر (يدوي)»، أسماء المشاريع اليدوية `nm + " (يدوي)"`،
  خيارات «إدخال يدوي»)؛ وحيث يكون العنصر HTML حقيقياً (حقل «اسم المشروع اليدوي») استُعملت
  أيقونة النظام `edit`. القيم (`__OTHER__`/`__MPN__:`/`__MANUAL__`) لم تتغيّر — عرضي فقط.
  لم تُمَسّ أزرار «تعديل» (إجراء مختلف). يَخلُف هذا تعديلَ qx الإيموجي أدناه.
- **v18.9qx — توحيد أيقونات الواجهة (عرض فقط):** أيقونة «رصيد البند المستعاض» تغيّرت من
  🏦 (مصرف) إلى 🪙 (عملة/رصيد) في كل مواضعها — زر القائمة الجانبية، خانة «طلب من البند
  المستعاض» في نموذج الطلب الجديد، سطر «البند المستعاض» في تفاصيل الطلب، وترويسة صفحة
  الرصيد في `substitute-budget.js` — لتنفصل بصرياً عن مرحلة **المالية** (💳) وطلبات
  **التسعير** (💰) اللتين تشغلان أيقونتين ماليتين أخريين. وعلامة «الإدخال اليدوي» في
  قوائم اختيار المشاريع/البنود (مشروع آخر يدوي، الأسماء اليدوية، «إدخال يدوي…») تغيّرت من
  ✏️ إلى ✍️ لتنفصل عن أيقونة زرّ **«تعديل»** (✏️) الشائعة — فصار القلم للتحرير واليد
  الكاتبة للإدخال اليدوي. لا مساس بأي منطق عمل. **استبدلها qy بأيقونات SVG لاحقاً.**
- **v18.9qw — استبدال بند عند الاستلام: سعر البند البديل من الفاتورة يدخل الكتالوج
  وتفاصيل الطلب:** الجذر — عند استبدال بند طلب الشراء بالبند الفعلي المورَّد، كان صفّ
  الاستلام يبقى محمّلاً بـ`it.unitCost` (تقدير الطلب للبند **الأصلي**). هذا التقدير كان
  يتسرّب سعراً للبند الجديد من مسارين: نافذة «بند كتالوج جديد» تُعبّأ به (فيُحفَظ في
  الكتالوج)، وتكلفة بند الطلب `a.unitPrice` تُقرأ من الخانة نفسها — فلا الكتالوج ولا
  تفاصيل الطلب يحملان السعر الصحيح من الفاتورة. الإصلاح: (١) `waPickCat` — عند الاستبدال
  إلى بندٍ موجود يُسحب سعر الكتالوج للبند الصحيح إلى `wa-unit-price` (أو تُفرَّغ إن لا سعر
  له)، مع إبراز الخانة وتنبيه بالتأكد من الفاتورة. (٢) `waOpenNewCatItem` — في وضع
  الاستبدال (الصفّ يحمل `data-orig`) لا تُعبّأ نافذة البند الجديد بتقدير البند القديم،
  فيُدخِل أمين المستودع سعر الفاتورة الفعلي. (٣) `waSaveNewCatItem` تمرّر `unitPrice`
  المُدخَل إلى `onSaved`، فيُسحب إلى صفّ الاستلام — فيتطابق تفصيل الطلب مع الكتالوج ولا
  يبقى تقدير البند القديم. (تصحيح البيانات القديمة يدويّ: عدّل سعر البند في الكتالوج
  وأعد إدخال سعر الوحدة الصحيح للطلب المتأثّر.)
- **v18.9qr — إصلاح ٤ أخطاء كشفتها مراجعة شاملة لـ index:**
  (١) **مراجعة المستودع — خصم مخزون ذرّي:** `doInventoryReview` كان يخصم كل صنف في
  transaction منفصلة مع `.catch` يبتلع الفشل ويخصم الذاكرة المحلية دائماً — فلو فشل صنف
  يُعلَّم الطلب «مُراجَع» بـ `_fromStock` بلا خصم فعلي (تضخّم وهمي يعيده الحذف) أو خصم مزدوج
  عند إعادة التحميل. صار الخصم كله في Transaction واحدة (قراءات ثم كتابات) بلا ابتلاع أخطاء،
  فإن فشلت تُرمى للـ catch الخارجي ولا يُحدَّث الطلب ولا الرصيد المحلي — مثل `doWarehouseAudit`.
  (٢) **حارس التزامن:** `doUpdatePurchaseStatus` كان يقارن حالة Firestore الخام بـ `p.status`
  المطبّع، فيرفض تحديث كل طلب قديم غير مطبّع بحلقة «غيّر مستخدم آخر الحالة». صار يطبّع
  `freshStatus` قبل المقارنة. (٣) **طباعة المقارنة:** `_buildPCGroupPrintTable` كان يختار
  «الأفضل»/التوفير بين كل الموردين بلا تصفية التغطية (عكس الشاشة) فيناقض خانة القرار في نفس
  الجدول — صار من كاملي التسعير فقط (`_covFull`). (٤) **فهرس الترسية:** `_pcBuildGroupRecord`
  كان يفهرس `g._sel` (فهرس المصفوفة الكاملة) على مصفوفة الموردين المصفّاة، فيُفقد/يُحرّف القرار
  عند ترك مورّد وسطي بلا اسم — صار يحوّل فهرس الكامل → المصفّاة صراحةً.
- **v18.9qq — تصغير مسار الطلبات + بطاقة ملخّص المفتوحة/المتأخرة بجانبه:** وُضِعت بطاقة
  المسار في صفٍّ `#po-flow-row` (شبكة `1.7fr : 1fr`، تنطوي لعمود واحد ≤1024px) فصغُرت،
  وصُغِّرت الحلقة (128px). بجانبها بطاقة `#po-open-summary` (دالة `renderPOOpenSummary`):
  رقمان كبيران — «مفتوحة (جارية)» = `poStageIsWip` و«متأخرة عن الاستلام» = `isPOOverdue` —
  ينقران لتصفية القائمة (`_poStageFilter('wip','')` / `_poOverdueFilter`)، وتوزيع عمر
  التوقّف بشريط ملوّن من `PO_AGE_BUCKETS` نفسها (كل شريحة تنقر لتصفية wip+عمر)، وأقدم
  توقّف بأيام العمل. تقرأ `dashData` نفسها فتحترم فلتر المشروع.
- **v18.9qp — إخفاء المقارنة اليدوية غير المستعملة + ضبط عرض جدول المقارنة الذكية للطباعة:**
  (١) مصدر موحّد `_pcManualUsed(g)` (سعّر مورّدٌ بنداً واحداً على الأقل — لا مجرّد خانات
  موردين فارغة) و`_pcAIUsed(g)` يقرأهما العرض والطباعة معاً. المقارنة اليدوية الفارغة
  (`0/5 بند`) لم تعد تظهر — لا في تفاصيل الطلب (`renderPCSection`/`_renderPCGroupSection`
  تعرض المقارنة الذكية وحدها حينها) ولا في الطباعة (`_buildPCHtmlForPrint`). (٢) جدول
  المقارنة الذكية في الطباعة صار `table-layout:fixed` بأعمدة نسبية (`colgroup`) وخطوط
  أصغر ولفّ النص (`word-break`) فلا تتجاوز أعمدته عرض الصفحة ويظهر كاملاً.
- **v18.9qo — المقارنة الذكية تظهر في طباعة PDF:** كانت طباعة الطلب تعرض المقارنة
  اليدوية (شبكة الموردين) فقط. أُضيفت دالة `_buildPCAIPrintTable(g,multi)` تبني جدول
  المقارنة الذكية (`g.aiExtract`) بأنماط مضمّنة مطبوعة (لا تعتمد على CSS التطبيق) —
  نفس شكل الجدول اليدوي: أخضر للأرخص، برتقالي للأغلى، وسم «🔗 بند موحّد»، وإجماليات
  المورّدين + ملاحظات التحليل عبر `_aiPrintBlock`. عُدِّلت `_buildPCHtmlForPrint` لتطبع
  المجموعة إن كان فيها مقارنة يدوية **أو** ذكية (فالمجموعة ذات المقارنة الذكية فقط لم
  تكن تُطبع إطلاقاً)، وتُلحِق الجدول الذكي بعد اليدوي لكل مجموعة. يظهر في «طباعة PDF»
  (مع الأسعار) فقط لا في «طباعة بدون أسعار».
- **v18.9qn — حذف ملف الفاتورة (للمسؤول فقط):** زر «🗑 حذف الفاتورة» بجوار كل فاتورة
  في تفاصيل الطلب، يظهر لـ `admin` فقط (`isAdmin()`). الدالة `adminDeleteInvoiceFile`
  تمسح الملف من مصدره الحقيقي — سند `grnDoc` المطابق (بمطابقة `invoicePhotoUrl`) أو
  الحقل المفرد `p.invoicePhotoUrl`+`p.invoice` للفاتورة المرفقة من المشتريات — ثم تعيد
  اشتقاق الإسقاطات `p.invoices`/`p.invoicePhotoUrl` بنفس منطق `waSubmit` (idempotent).
  تكتب قيداً زمنياً + `logAudit`، وتحذف الملف من Storage best-effort فقط إن لم يعد
  مرجعاً في أي مكان، مع استرجاعٍ كامل عند فشل الكتابة. مصدر قائمة الفواتير موحّد في
  `_poInvoiceList(p)` يقرأه العرض والحذف معاً فلا يفترقان.
- **v18.9qm — بطاقة «بانتظار إجراءك»: عمود المشروع بدل الجهة:** استُبدل عمود «الجهة»
  (كان يعرض `p.building`) بعمود **«المشروع»** يعرض اسم المشروع من المصدر القياسي
  الموحّد `_poMyTaskProjName(p)`: المسجّل عبر `_getProjName(p.projectId)`، أو المُدخل
  يدوياً `p.projectName` للمشاريع المخصّصة (`isCustomProject`/`__OTHER__`).
- **v18.9ql — إصلاح حجم أيقونات بطاقة «بانتظار إجراءك»:** كانت أيقونة كل مرحلة في
  رؤوس المجموعات تتمدّد لتملأ عرض البطاقة، لأن `_svgIcon` يُنتج `<svg>` بلا أبعاد
  وقاعدة تحجيمها `.pof-ic svg` مُقيَّدة بـ `.pof-lbl` (خارجه بلا عرض فتتمدّد). استُبدلت
  بـ `_ic(g.icon,"ic-sm")` المُحجَّم عالمياً (12px) — نفس نمط بطاقة المالية.
- **v18.9qk — بطاقة «بانتظار إجراءك» أعلى صفحة المشتريات:** بطاقة جديدة
  (`#po-my-tasks-card`) بجانب مسار الطلبات تعرض لكل مستخدم الطلبات **الجارية**
  المتوقّفة على **اعتماده أو تنفيذه حسب صلاحيته**، مجمّعةً حسب المرحلة ومرتّبةً
  بالأقدم توقّفاً أولاً (مع عمر التوقّف بأيام العمل وإبراز المتأخّر). **المصدر
  الموحّد للحقيقة** هو `getAvailableStatuses(status, po)` نفسها التي تبني أزرار
  الإجراء في تفاصيل الطلب — إن أعادت إجراءً حقيقياً لهذا المستخدم على طلبٍ جارٍ
  (`poStageIsWip`) فالطلب متوقّف عليه — فلا تنحرف البطاقة عن الأزرار. الدوال:
  `poNeedsMyAction(p)` / `renderPOMyTasks(dashData)` (تُنادى من
  `renderPurchasesDashboard` فتحترم فلتر المشروع). لا تظهر للأدوار العارضة
  (viewer/observer) ولا حين لا يوجد ما ينتظر المستخدم.
- **v18.9qj — إزالة علامة استبدال + مرساة تلقائية عند إنشاء الطلب:** (١) بطاقة «⇄ بند
  استُبدل عند الاستلام» في رصيد المخزون صار لكل صفٍّ زر **«✗ أزِل العلامة»**
  (`removeSubstitution`) يمسح `it.substitution` **فقط** — لا يمسّ المرساة `itemId` ولا رصيد
  المخزون (البضاعة دخلت تحت مرساتها الفعلية؛ تحريكها يكون بنقل مستقلّ) — مع قيد `timeline`
  وسجل تدقيق. للحالات التي سُجِّل فيها استبدال خطأً. يظهر للمسؤول/مدير المستودع/مسؤول
  المشتريات فقط (`canFixUnanchored`). (٢) **الجذر:** كان البند في طلب الشراء يُحفَظ غير
  مربوط (`itemId=null`) إن لم يُنقَر اقتراح الكتالوج صراحةً — وإن طابق اسمه صنفاً موجوداً —
  فيصل غير مربوط للاستلام فيربطه أمين المستودع بصنفٍ قريب ويُسجَّل استبدالاً. أُضيفت **مرساة
  تلقائية** في `addPurchaseItem`: إن طابق اسم/كود البند تماماً صنفاً واحداً في الكتالوج (بعد
  التطبيع `_catSearchNorm` وحلّ الدمج `_catResolveId`) يُربط آلياً؛ لا يربط عند اللبس
  (تطابق متعدّد) ولا يدهس اختياراً صريحاً.
- **v18.9qi — مقارنة أسعار بالذكاء الاصطناعي (استخراج بنود العروض):** تبويب جديد
  «🤖 مقارنة ذكية» داخل مودال مقارنة الأسعار (`modal-price-comparison`). يرفع مسؤول
  المشتريات ملفات عروض الأسعار (PDF/صور، عرض لكل مورّد) فيقرأها النموذج
  (`claude-sonnet-4-6` عبر الـ proxy مع دعم PDF) ويستخرج البنود وأسعارها ويبني **جدول
  مقارنة بندي موحّد**. الميزة الجوهرية: **توحيد البنود المتشابهة** — إذا وصف موردان بنداً
  بتسميات مختلفة لكنه نفس البند فعلياً، يدمجهما النموذج في صفٍّ واحد (`unified`/`unifyNote`/
  `rawName`) ويبرز ذلك، مع تمييز الأرخص/الأغلى لكل بند وأقل إجمالي مورّد. النموذج على
  المجموعة: `g.aiFiles[]` + `g.aiExtract`، يُحفَظ ضمن `priceComparisons2` ويُعاد تحميله
  ويُعرَض في تفاصيل الطلب للقراءة. دوال المحرّك: `runPCAIExtract` / `_pcRenderAIExtractTable`
  / `_pcParseAIJson` / `_pcRenderAIExtractPanel`. مستقلّ تماماً عن شبكة الموردين اليدوية
  وعن بنود `p.items` — تكميلي لزر «تحليل ذكي — ملخص تنفيذي» (`runPCAIAnalysis`) القائم.


- **PR #1 — توحيد الحالات والتكاليف:** إدخال المصدر الموحّد (`PO_CLOSED_STATUSES` /
  `poIsClosed` / `poActualCost`)؛ جعل ملخصات الموردين/المشاريع/إجمالي التكاليف تُحسب من
  المغلق فعلياً فقط وبالتكلفة الفعلية؛ توحيد التصنيف عبر لوحة المعلومات والتقريرين. (v18.9qf)
- **PR #2 — إصلاح `_poCostCounted` + تثبيت اختبار cache-busters:** إزالة استدعاءين قديمين
  لمتغيّر محلي حُذف أثناء التوحيد كانا يرميان `ReferenceError` عند بناء لوحة المعلومات
  (مخطط مبالغ المشاريع + المخطط الشهري)، وتحويلهما للمصدر الموحّد؛ وجعل اختبار «cache-busters
  تطابق الإصدار» يتحقّق من الثابت الحقيقي بلا عدد ثابت هشّ. (v18.9qg)
- **إصلاح مخططات لوحة المشتريات على الجوال:** كان `#po-charts-row` يحمل `style` مضمّناً
  (`grid-template-columns:repeat(4,1fr)`) يتغلّب بأولويته على media queries المسؤولة عن الطي
  إلى عمودين (≤1100px) ثم عمود واحد (≤640px)، فتبقى الأعمدة الأربعة محشورة على شاشة الجوال
  وتلتفّ أسماء المشاريع العربية حرفاً في كل سطر. نُقل `align-items:stretch` إلى قاعدة CSS
  وحُذف الـ `style` المضمّن، فعادت الـ media queries للعمل. (v18.9qh)
